# Bevy Baselines for P0 Visual Gates

Place fixed-camera Bevy baseline images in this directory:

- `3d_scene.png`
- `3d_shapes.png`

These files are consumed by:

- `/Users/zhengyu/Documents/projects/moon-game-studio/mgstudio-engine/scripts/p0_visual_gate.sh`

Recommended capture constraints:

- Same logical window size as mgstudio capture run.
- No overlapping windows (fullscreen snapshot mode captures the full screen).
- Stable camera transform, projection, and scene setup matching parity acceptance definition.
