# Render Parity Issue Tracker

Last updated: 2026-04-18

## Status Legend

- `TODO`: not started
- `IN_PROGRESS`: currently being fixed
- `BLOCKED`: blocked by platform/public API limitations or external dependency constraints
- `DONE`: fixed and verified locally

## Issues

| ID | Source | Problem | Status | Notes |
| --- | --- | --- | --- | --- |
| RENDER-001 | bevy_pbr/render/skin.rs + mesh.rs | `render3d.prepare` still does skin extraction work instead of extract/prepare split. | DONE | 2026-04-07: `pbr/render3d.mbt` now does skin matrix extraction in extract stage and upload-only in prepare stage; trace check (`/tmp/mgstudio_stress_profile_render_parity/traces/many_foxes.trace.json`) shows `mgstudio.render3d.prepare#3` avg ~`4.863ms` (was ~`56.7ms`). |
| RENDER-002 | bevy_pbr/render/skin.rs | Skin extraction is full-scan every frame; missing changed-joint driven dirty skin extraction path. | DONE | 2026-04-07: added `joint -> skins` reverse mapping + per-skin registration/removal + `Changed<GlobalTransform>` dirty extraction with Bevy-style threshold fallback in `pbr/render3d.mbt`. |
| RENDER-003 | bevy_pbr/render/skin.rs | Skin uniforms use per-skin slot cache, not Bevy-style global allocator + current/prev double buffers. | IN_PROGRESS | 2026-04-12: switched skin binding keys from entity-id binding to global slot allocator with reuse/free-list in `pbr/render/skin.mbt`; 2026-04-14: fixed upload traversal to skip invalid skin keys instead of aborting the whole upload pass (locked by `pbr/render/skin_wbtest.mbt`); 2026-04-18: skin presence tracking now snapshots `current_skins -> previous_skins` per extract frame, so newly-registered skins no longer immediately report previous-frame skin data, renderer main-pass skin binding now consumes `skinning_key_lo` to select current/prev skin bind groups, and motion-vector mesh uniform packing now writes `current_skin_index` from `skinning_key_hi` instead of a fixed sentinel; 2026-04-18: mesh extraction now carries explicit `has_previous_skin` into motion-vector draw payload wiring so motion-vector passes stop forcing all skinned meshes down “has previous skin” call-site semantics, and main-pass draw payload generation now hard-pins skinned draws to current-skin binding selection instead of inheriting motion-vector routing flags; 2026-04-18: renderer motion-vector prepass now specializes three Bevy-shaped shader/pipeline variants (`base`, `SKINNED`, `SKINNED+HAS_PREVIOUS_SKIN`), selects them by draw-time skin keys, and binds mesh+current-skin+previous-skin buffers on `@group(2)` with skin-revision-aware bind-group refresh in both preprocess and runtime paths; remaining work is deeper morph/advanced prepass specialization parity. |
| RENDER-004 | bevy_pbr/render/mesh.rs | Mesh extract path is not changed/removed incremental at Bevy granularity. | DONE | 2026-04-18: `pbr/render_execute_mesh_collect.mbt` now keeps a render-world extracted-mesh cache and updates it by `added/changed/removed` component deltas (including required + optional mesh render components), with Bevy-shaped reextract-next-frame behavior for material-not-ready entities; full-world mesh scan now runs only on first frame/bootstrap. |
| RENDER-005 | bevy_pbr/render/mesh.rs | Missing dedicated `set_mesh_motion_vector_flags` stage after skin/morph extraction. | DONE | 2026-04-12: extracted motion-vector requirement detection into `render3d_set_mesh_motion_vector_flags` in PrepareMeshes; 2026-04-14: converged mesh collection to per-mesh motion-vector gating (`previous_global_transform` / `previous_skin` / `previous_morph`) and removed global bool-only enable behavior. |
| RENDER-006 | bevy_pbr/render/mesh.rs + mesh_bindings.rs | Mesh bind-group prep still partially deferred to draw/execute paths. | IN_PROGRESS | 2026-04-13: `motion_vector_mesh_bind_group` creation moved out of draw path (`mesh3d_draw_motion_vector_pass`) into preprocess payload upload (`upload_mesh3d_preprocess_camera_payload`); 2026-04-14: main-pass view bind-group creation now uses renderer-side cache entries (`Mesh3dViewBindGroupCacheEntry`) instead of unconditional per-draw allocation; 2026-04-14: `PrepareBindGroups` now prebuilds mesh material bind-group cache entries from queued extracted meshes (via `host_gpu_prepare_mesh3d_material_bind_group`), so draw path no longer pays first-hit material bind-group construction for those entries; 2026-04-18: material bind-group prebuild ownership is now prepare-stage only (execute main-pass payload-bucket prebuild loop removed), and prepare-stage mesh material prebuild now deduplicates by full texture-binding key to match Bevy-style per-material caching boundaries more closely; remaining work is full Bevy-equivalent prepare-stage ownership migration. |
| RENDER-007 | bevy_pbr/render/mesh_view_bindings.rs | View bind-group preparation order and ownership are not fully equivalent. | IN_PROGRESS | 2026-04-14: renderer now persists/reuses mesh3d view bind groups via explicit cache + texture-change invalidation path; fog uniform preparation also moved from `PrepareBindGroups` to `PrepareResources` to match Bevy stage ownership; `PrepareBindGroups` now prebuilds per-camera view bind-group cache entries (`host_gpu_prepare_mesh3d_view_bind_group`) from queued extracted cameras (environment map aware, fallback transmission/shadow slots), reducing draw-path first-hit construction. 2026-04-18: execute main-pass path now prebuilds pass-accurate view bind groups at pass entry (`scene/transmission` variants with real point-shadow and transmission-source texture ids) before draw submission, so draw loop avoids creating those dynamic view bind groups on first hit. 2026-04-18: removed redundant source-world `prepass_update_pipeline_state` call from `PrepareBindGroups`; prepass pipeline-state refresh remains plugin-owned `PreUpdate` work, avoiding cross-stage ownership leakage. Remaining gap is full Bevy-equivalent view/material bind-group ownership for all pass variants. |
| RENDER-008 | bevy_pbr/render/gpu_preprocess.rs + mesh.rs | GPU preprocess pipeline stages and flush ordering differ from Bevy. | IN_PROGRESS | 2026-04-18: added Bevy-shaped stale-view cleanup for preprocess work-item state (`delete_old_work_item_buffers` equivalent): render-side camera preprocess upload states are now pruned each frame against current queued extracted cameras, so deleted views no longer retain preprocess/indirect/depth-pyramid buffers indefinitely; 2026-04-18: moved this cleanup system from `PrepareResourcesFlush` to `PrepareResources`, aligning the set ownership with Bevy `mesh.rs` (`delete_old_work_item_buffers.in_set(RenderSystems::PrepareResources)`); 2026-04-18: moved per-camera preprocess payload upload from `QueueMeshes` to `PrepareResourcesFlush`, matching Bevy-style buffer-write ownership for flush stage (`write_*_buffer.in_set(RenderSystems::PrepareResourcesFlush)`); remaining work is deeper stage-boundary ordering alignment (`PrepareResourcesFlush/PrepareResources/PrepareMeshes`). |
| RENDER-009 | bevy_pbr/render/morph.rs | Morph extraction/preparation lifecycle is not yet split and tracked like Bevy. | DONE | 2026-04-12: split to `render3d_extract_morphs` + `render3d_prepare_morphs`; `Render3dPhaseState` now keeps current/prev morph indices and uniform-like weight buffers; render systems call extract in render-extract set and apply in render-prepare set. |
| RENDER-010 | bevy_pbr/render/mesh.rs | Queue/execute still carries non-queue responsibilities (build-time material readiness retries). | IN_PROGRESS | 2026-04-12: moved camera/mesh collection and frame assembly from `render3d_queue_system` into `render3d_prepare_meshes_system` (PrepareMeshes set), and moved camera/mesh motion-blur history ownership from global statics into render queue state resource; 2026-04-18: GPU preprocess/indirect dispatch moved to execute-camera stage (`render3d_execute_camera`) so queue no longer performs per-camera compute dispatch; 2026-04-18: per-camera preprocess payload upload moved from queue to `PrepareResourcesFlush`; 2026-04-18: prepared camera queue-entry construction (draw-item/indirect-index assembly) moved from `PrepareMeshes` into `QueueMeshes`, so queue regains queue-entry ownership while prepare-flush remains upload-only. Remaining work is Bevy-equivalent schedule-boundary ownership for remaining queue/extract tails. |
| RENDER-011 | bevy_pbr/render/mesh.rs + occlusion_culling.wgsl | Occlusion/depth-pyramid preprocess integration is not fully staged like Bevy. | IN_PROGRESS | 2026-04-18: renderer `build_indirect_params` now has dedicated late/main(occlusion) pipeline variants (indexed + non-indexed) instead of reusing only default/early; preprocess dispatch is split into explicit execute-stage boundaries (`early preprocess/build` before passes, `downsample+late preprocess/build` after shadow pass, `main build` before main pass) instead of one queue-stage monolith; 2026-04-18: preprocess runtime now clears indexed/non-indexed indirect-parameter GPU metadata buffers at early/main preprocess entry, mirroring Bevy `clear_indirect_parameters_metadata` semantics before indirect parameter build passes. Remaining work is full schedule-boundary convergence with Bevy Core3d set topology. |
| RENDER-012 | bevy_render schedule + bevy_pbr/render/mod.rs | Render schedule set topology differs from Bevy (`Extract/Prepare*/Queue/Render/Cleanup`). | DONE | 2026-04-14: staged topology is now concretely wired (`prepare meshes -> prepare bind groups -> queue -> execute`) with queue-frame snapshot flow, single-pass prepared-camera queue assembly, queue-sweep mesh-runtime-cache cleanup, extracted topology/static preprocess payload reuse, prepare-stage mesh pipeline prewarm (deduped by specialization key), and `PrepareResources` now runs as explicit `prepare_morphs` + `prepare_skin_bindings` systems instead of one merged prepare function. |

## Current work queue

- RENDER-003
- RENDER-006
- RENDER-007
- RENDER-008
- RENDER-010
- RENDER-011
