This file must not exceed 200 lines. Excessive done issues should be moved to ARCHIVED_CHECKLIST.md.

The table below is the current authoritative score. Criteria are source-level 1:1 parity: directory/API topology, runtime semantics, upstream test/example coverage, and visual/performance parity. Bands: 95%+ near source-level parity; 80-94% strong but incomplete; 60-79% substantial partial port; 40-59% partial; <40% major gap.

Terminology note: `erased render asset` refers to Bevy's typed `ErasedRenderAsset` source owner. It does not allow JSON-backed runtime payloads, which are forbidden by ADR 0002.

| Scope | Structure | Runtime | Overall | Status | Strict Reassessment |
|---|---:|---:|---:|---|---|
| `app` | 75% | 70% | 70% | 🟡 In Progress | Plugin/schedule surface is useful, but full Bevy scheduler/build-pass/source topology parity is not proven. |
| `ecs` | 72% | 72% | 72% | 🟡 In Progress | Core concepts exist, but storage/query/system internals remain MoonBit-specific and not source-equivalent. |
| `transform` | 78% | 74% | 74% | 🟡 In Progress | Bevy-shaped propagation exists, but dirty-tree/performance/edge-case parity is not complete. |
| `hierarchy` | 76% | 75% | 75% | 🟡 In Progress | Relationship semantics are close enough for main paths, but source/test parity remains incomplete. |
| `render` | 60% | 50% | 50% | 🔴 Major Gap | Render graph/stage names align in places, generic pipeline descriptor ownership has moved down to `render/render_resource`, render asset preparation now has Bevy-shaped next-frame retry/remove/update state-machine helpers plus bytes-per-frame upload throttling for typed and erased assets, and RenderPlugin wires the matching main-world budget resource, render-world limiter, extract sync, and cleanup reset. GPU preprocessing batching now owns Bevy-shaped input-buffer free-list reuse plus direct/indirect/occlusion work-item buffers, and mesh3d consumes material descriptor layout data at pipeline/bind-group creation; renderer ownership, pass layout, and visual parity still diverge substantially. |
| `render/renderer` | 56% | 52% | 52% | 🔴 Major Gap | Too much local renderer architecture remains, but mesh3d allocator-backed uploads now preserve Bevy-style indexed versus non-indexed mesh buffer identity, have a real deletion path that frees renderer mesh records and unused slab buffers, and mesh3d draw groups can now bind Bevy-style morph uniform buffers plus morph target 3D textures with dynamic offsets across main, shadow depth, and motion-vector prepass material descriptor paths. Renderer ownership, render-graph integration, broader pass layout, and visual parity still diverge substantially. |
| `core_pipeline` | 58% | 53% | 53% | 🟠 Partial | Shader assets are closer, tonemapping now includes Bevy's `PbrNeutral` mode/define path, prepass node planning mirrors Bevy's early/late prepass gating and skybox prerequisites, deferred prepass node planning mirrors Bevy's early/late gating, and the deferred lighting-id copy path now has Bevy-shaped pipeline/pass/depth-texture plans. Postprocess/pass scheduling/runtime behavior remain only partially aligned. |
| `pbr` | 64% | 58% | 58% | 🔴 Major Gap | Material pipeline descriptor specialization now mirrors Bevy's mesh-layout + material bind-group path at the pbr boundary, including morphed/skinned/lightmapped/motion mesh draw layouts and `MORPH_TARGETS`/`SKINNED`/lightmap shader defs. `StandardMaterial` extraction carries the Bevy `AsBindGroup`-derived layout shape into `MaterialProperties`, standard material properties install base and prepass specialize hooks, PBR material draw-function labels now follow Bevy's prepared-material order through a registry surface, StandardMaterial plugin state now records the Bevy `MaterialPlugin` main-world/render-startup/extract schedule plan, the material bind-group allocator install path follows Bevy's bindless support + slot-count rule, render material instances now model Bevy's dummy-material lookup plus early/late sweep change-tick behavior with ECS `Tick` semantics and are populated by mesh extraction, entity specialization ticks plus per-view material pipeline caches now mirror Bevy's stale-entity removal rules, `MaterialsPlugin` initializes these Bevy material render resources through ECS instead of leaving them as detached helper types, and PBR material prewarm now consumes the per-view specialization cache before registering material pipeline descriptors. PBR deferred exposes the shared core deferred-prepass run-plan surface and deferred lighting now has Bevy-shaped prepare/specialize/fullscreen-pass planning. Renderer pipeline cache identity now preserves the cached material pipeline id, but lighting/shadows/material pass runtime consumption and examples still show visible divergence. |
| `pbr/render` | 77% | 71% | 71% | 🔴 Major Gap | Bind layouts and extracted data improved, pbr owns Bevy-shaped material and prepass pipeline specializer descriptor paths, `PrepareMeshes` now derives real `RenderMesh` layout/topology/morph key data from mesh assets before warming `SpecializedMeshPipelines`, and the resulting cached pipeline id now flows through extracted mesh payloads, GPU preprocess work items, phase items, binned batch keys, draw payloads, and the renderer mesh3d pipeline cache key. Mesh draw bind layouts now select Bevy-equivalent model-only/lightmapped/skinned/morphed/motion variants from mesh key bits plus vertex layout skinning attributes, mesh bind-group preparation now owns Bevy-shaped CPU/GPU preprocessing resource variants, skinned pairs, per-mesh morph-target pairs, lightmap slab groups, and `SetMeshBindGroup` dynamic-offset ordering, and pbr now uploads morph weights as renderer uniform buffers, packs morph target position/normal/tangent deltas from glTF into Bevy-style R32Float 3D textures, removes the CPU mesh-deformation compatibility path, and passes morph offsets/textures into main, shadow depth, and motion-vector draw paths. Lightmaps now travel through mesh-owned draw bindings plus Bevy-style `MeshUniform` UV/slot packing instead of mutating material base color, and bindless lightmap descriptor entries now preserve Bevy's array counts. GPU preprocess mesh input now consumes renderer mesh allocation metadata for first vertex/index offsets, stale mesh cache entries now delete renderer mesh allocations, PBR cached upload ids are revalidated against renderer state after asset modification, indexed/non-indexed phase routing now receives real renderer mesh buffer identity instead of synthetic sequential indices, transparent/transmissive phase ordering now applies Bevy material depth bias, transmissive materials now occupy a separate sorted phase instead of being folded into opaque, and the main pass now skips deferred opaque materials like Bevy's `queue_material_meshes` path. Motion-vector prepass now registers PBR prepass material descriptors and renderer execution consumes descriptor view/draw/material layouts. Full visual parity and broader Bevy prepass scheduling remain incomplete. |
| `pbr/prepass` | 65% | 59% | 59% | 🟠 Partial | Bevy-shaped `PrepassPipeline` descriptor specialization now exists at the PBR material boundary, including prepass view/no-motion layouts, empty group handling, Bevy shader defs, prepass target descriptors, morphed/skinned/motion draw-layout selection, StandardMaterial `pbr_prepass.wgsl` fragment selection, depth-only opaque empty material layout, a separate prepass material pipeline cache, alpha-material shadow prepass descriptor registration, renderer-side consumption of descriptor shader paths/defs/vertex layout/depth state, motion-vector descriptor view/draw/material bind groups, shared core prepass run-plan semantics for early/late pass gating and attachments, and Bevy-equivalent material eligibility/key/draw-function decisions for prepass versus deferred-prepass queuing. Runtime pass scheduling and visual parity are still not fully Bevy-equivalent. |
| `pbr/meshlet` | 63% | 50% | 50% | 🔴 Major Gap | Asset loading, meshlet storage buffers, queue buffer planning, descriptor modeling, material-shade pass planning, native meshopt/METIS `from_mesh` processing, and Bevy-shaped meshlet visibility-buffer shader assets/descriptors now exist. Material ids now follow Bevy's prepare-before-queue flow across material families: extraction preserves material family identity, material preparation assigns ids from scene material assets, queueing writes per-instance material ids for material-depth resolve, and the meshlet example's debug row uses a distinct empty-bind-group `MeshletDebugMaterial` family instead of a `StandardMaterial` shader override. Runtime meshlet visibility is gated unavailable while exact Bevy `R64Uint`/atomic storage texture support is externally blocked; material-shade consumption must be completed after that ABI exists. |
| `material` | 74% | 66% | 66% | 🟠 Partial | Material specialization and descriptor ownership are closer to Bevy, including shared mesh vertex layouts, richer bind-group layout entries, and a Bevy-shaped draw-function registry. Forward, deferred, and decal integration are still not source-equivalent. |
| `camera` | 65% | 60% | 60% | 🟠 Partial | Camera components/plugins align in parts, but view/projection/render integration is incomplete. |
| `sprite` + `sprite_render` | 61% | 56% | 56% | 🟠 Partial | Basic 2D render path exists, and `ColorMaterial` now uses Bevy-shaped `Affine2` UV transform storage/upload plus distinct Bevy `Default` versus `From<Color>` alpha-mode semantics. Batching/extraction/render parity still needs a dedicated audit. |
| `ui` + `ui_render` + `ui_widgets` | 60% | 50% | 50% | 🟠 Partial | Data model is improving and UI text access is now a thin Bevy-style wrapper over the text owner, but layout/render/interaction parity is blocked by incomplete deeper taffy/render integration. |
| `feathers` | 45% | 40% | 40% | 🔴 Major Gap | Only a subset of controls/tokens is ported; source-level widget ecosystem parity is not close. |
| `text` | 55% | 45% | 45% | 🔴 Major Gap | Font/layout surfaces exist and `TextReader`/`TextWriter` now own Bevy-style flattened root/span access, but text shaping/visual parity depends on incomplete upstream and local integration. |
| `gltf` + scene import path | 55% | 45% | 45% | 🔴 Major Gap | Loader path works for selected cases, but full asset/animation/material scene parity is not established. |
| `animation` | 60% | 50% | 50% | 🟠 Partial | Graph/runtime work exists, but example behavior and full Bevy test parity are not complete. |
| `scene` static path | 65% | 55% | 55% | 🟠 Partial | Static scene spawning is useful, but dynamic/runtime corner cases and reload parity remain open. |
| `gizmos` + `gizmos_render` | 55% | 45% | 45% | 🔴 Major Gap | API subset exists; real render asset/pipeline and builder coverage are incomplete. |
| `picking` | 60% | 50% | 50% | 🟠 Partial | Main components exist, but pointer-state/event-generation/backend ordering still diverge. |
| `input` | 75% | 70% | 70% | 🟡 In Progress | Event resources are close for common paths, and numeric key coverage now includes Bevy's `Digit0` through `Digit9` surface; platform/event-order long tail still needs upstream-test parity. |
| `window` + `winit` | 65% | 55% | 55% | 🟠 Partial | Window lifecycle/cursor work progressed, but backend/platform parity is not broad enough for high score. |
| `asset` | 55% | 45% | 45% | 🔴 Major Gap | Asset events and loader slices exist, but source/processor/reload/ready-checker parity remains shallow. |
| `log` + `diagnostic` + `dev_tools` | 60% | 55% | 55% | 🟠 Partial | Useful diagnostics exist; generic deferred diagnostics/material UI paths are not Bevy-equivalent. |
| `anti_alias` | 55% | 40% | 40% | 🔴 Major Gap | Shader assets and components are present, but SMAA/TAA/DLSS runtime pass parity is incomplete. |
| `light` | 55% | 45% | 45% | 🔴 Major Gap | Component ownership moved closer, but clustering/shadow/probe behavior is still materially divergent. |
| `mesh` | 83% | 77% | 77% | 🟡 In Progress | Primitive, attribute, vertex-layout, allocator-state, upload-descriptor surfaces, render-mesh allocation planning, renderer mesh3d slab upload execution, Bevy-shaped indexed/non-indexed buffer preservation, RenderMesh key/layout/index-format/morph-target ownership, free-before-reallocate deletion, and PBR preprocess offset consumption are closer to Bevy. Remaining runtime work is broader indirect/preprocess draw parity and complete extracted-asset scheduling equivalence. |
| `image` | 65% | 55% | 55% | 🟠 Partial | Codec table is broader, but exact loader/settings/error behavior parity is not complete. |
| `color` | 75% | 65% | 65% | 🟡 In Progress | Conversion math is comparatively close; remaining work is upstream vector/test coverage breadth. |
| `math` | 80% | 75% | 75% | 🟡 In Progress | Core math and primitives are one of the stronger areas, but generic trait limits still create source-shape gaps. |
| `a11y` | 55% | 45% | 45% | 🔴 Major Gap | Basic forwarding exists; full Bevy/accesskit behavior parity is not established. |
| Rapier integration | 0% | 0% | 0% | ⏸ Out of Bevy Scope | Not present in pinned local `./bevy`; should not count as Bevy parity without a separate third-party reference. |
| stress tests | 45% | 35% | 35% | 🔴 Major Gap | Tooling exists, but heavy examples are not source/behavior/performance equivalent. |
| visual screenshot parity | 45% | 30% | 30% | 🔴 Major Gap | Captures exist, but broad screenshot equivalence is still poor. |
| workspace native validation | 65% | 60% | 60% | 🟠 Partial | `moon check` is stable, but full native test/runtime parity is not yet a reliable gate; native framework linking still blocks some targeted meshlet tests. |


### Active Checklist

- [ ] `pbr/meshlet/upstream wgpu-native C ABI`: Bevy meshlet visibility-buffer resources require Rust `wgpu` native-extension equivalents for `TextureFormat::R64Uint` plus `StorageTextureAccess::Atomic` / WGSL `texture_storage_2d<r64uint, read/write/atomic>`. Runtime meshlet visibility remains unavailable until upstream exposes the exact ABI and `wgpu_mbt` can create that exact texture format/access pair.
- [ ] `pbr/meshlet/wgpu_mbt pipeline-layout API` ([moonbit-community/wgpu-mbt#22](https://github.com/moonbit-community/wgpu-mbt/issues/22)): Exact Bevy meshlet pipelines require bind-group layouts and `IMMEDIATES`/push-constant ranges in the same `PipelineLayoutDescriptor`. mgstudio now models Bevy immediate sizes in meshlet pipeline descriptors and gates meshlet runtime on `IMMEDIATES`; the remaining gap is renderer-side consumption through the exact combined descriptor shape once `wgpu_mbt` exposes it.
- [ ] `pbr/render/mesh-bind-groups`: renderer draw bind-group creation follows descriptor entries for model, skin, morph-target, and non-bindless lightmap bindings with Bevy-style mesh uniform packing plus Bevy-shaped `PrepareBindGroups` ownership. Mesh draw layouts use Bevy labels and binding order, descriptor layouts preserve binding-array counts, and PBR/prepass lightmap/probe array shader defs are gated by pipeline capability. Main, shadow depth, and motion-vector morph weights and morph target textures now reach group(2) with dynamic offsets, glTF morph position/normal/tangent deltas are packed in Bevy's 9-float target layout, motion-vector execution consumes PBR prepass material pipeline descriptors including group(0), group(1), group(2), and group(3), and prepass material eligibility/key/draw-function decisions match Bevy's specialize/queue rules. Remaining gap: wire those decisions into full Bevy-equivalent runtime prepass phase scheduling/commands and verify full visual parity.
- [ ] `render/wgpu_mbt macOS NSView surface` ([moonbit-community/wgpu-mbt#23](https://github.com/moonbit-community/wgpu-mbt/issues/23)): Real `Milky2018/window` AppKit windows passed through `Instance::create_surface_macos_ns_view_u64` can remain at native `Occluded` acquire status while visibly frontmost, blocking Bevy-equivalent 2D screenshot gates until the upstream NSView/CAMetalLayer integration path can acquire drawable surface textures reliably.
- [ ] `winit/window monitor enumeration` ([moonbit-community/window#16](https://github.com/moonbit-community/window/issues/16)): Bevy-style monitor sync requires cheap monitor handle enumeration, but `Milky2018/window@0.5.1` eagerly loads every macOS display mode inside `available_monitors()` through `SLDisplayCopyAllDisplayModes`, making 2D screenshot gates CPU-bound before visual parity can be assessed. Mgstudio avoids rewriting existing `Monitor` ECS data, but exact parity still needs upstream lazy video modes or a cheap monitor enumeration API.
- [ ] `render/wgpu_mbt` (deferred, upstream): mesh-shader capability surface is still not exposed in `wgpu_mbt`, so `meshlet_adapter_supports_mesh_shader()` remains conservative `false` instead of Bevy-equivalent adapter capability reporting.
- [ ] `text`: finish visual and script-coverage parity for text layout/rendering.
- [ ] `stress_tests`: keep source-level convergence coverage and representative performance/visual regression gates for heavy examples.
