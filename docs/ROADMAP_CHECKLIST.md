This file must not exceed 200 lines.
| Bevy Side | mgstudio Side | Structure | Runtime | Overall | Status | Main Gaps |
|---|---|---:|---:|---:|---|---|
| `bevy_app` | `mgstudio-engine/app` | 98% | 97% | 97% | 🟡 In Progress | Plugin API is unified to Bevy-style single `add_plugins` entry with typed plugin/group lifecycle semantics (`build/ready/finish/cleanup/name/is_unique`); `PluginGroupBuilder` now mirrors Bevy's add/readd, try-add, add-before/after, add-group, disabled-set, and missing/duplicate error semantics within MoonBit's explicit target-plugin call shape, nested plugin registration now uses Bevy's placeholder-registry ordering so parent finish/cleanup run before child, plugin build-depth guards prevent `run_startup/update/run` reentry while a plugin is building, post-`finish/cleanup` plugin insertion now follows Bevy's hard failure while `finish` callbacks can still append plugins, `is_plugin_added` is now available during finish/cleanup, and `plugins_state()` now reports readiness without transitioning stored state like Bevy. Remaining gaps are scheduling ergonomics tails. |
| `bevy_ecs` (core surface) | `mgstudio-engine/ecs` | 99% | 99% | 99% | 🟡 In Progress | ECS now converges on Bevy-shaped runtime semantics end-to-end: static metadata + world-local component ids, deterministic builtin bootstrap registration (`ADD/INSERT/REPLACE/REMOVE/DESPAWN/IS_RESOURCE`), queued registration/flush stability, pre-use-only hook/required mutation gates, required-components reverse graph (`required_by`) with deterministic cycle checks, relationship parent accessors frozen in world-local registration state, observer parent-lookup cache tied to component/archetype/relationship generations, and lifecycle observers covering `Add/Insert/Replace/Remove/Despawn`. Storage now has Table/SparseSet split routing with world-local storage-type authority, table-row/swap-remove/move-to-superset helper semantics, archetype↔table topology with add/remove edge caching, bundle migration path classification (`SameArchetype/NewArchetypeSameTable/NewArchetypeNewTable`), and deterministic archetype membership updates across `spawn/set/remove/despawn`. Query now has multi-anchor candidate selection by membership cardinality, matched-entity/table/archetype cache tracking with dependency-signature invalidation, sequence-dependent filter invalidation, archetype-set prefiltering by component-set metadata, and app-side persistent query cache slots (no per-run JSON cache roundtrip). Remaining gap is deep storage backend internals still not byte-for-byte equivalent to Bevy's Rust memory/unsafe primitives implementation. |
| `bevy_transform` | `mgstudio-engine/transform` | 98% | 95% | 95% | 🟡 In Progress | `Transform` required-components are metadata-driven and now installed through explicit plugin/usage paths (no package `init` side-effect dependency), with transform/hierarchy runtime resources and propagation ordering preserved; remaining work is heavy-scene throughput parity closure. |
| `bevy_hierarchy` | `mgstudio-engine/hierarchy` | 98% | 96% | 96% | 🟡 In Progress | Parent/Children relationship hooks now use explicit metadata installation paths (not implicit package init side effects), parent lookup remains world-local, and recursive despawn/parent maintenance semantics are preserved; remaining work is edge-case visual parity in large scenes. |
| `bevy_reflect` | N/A (explicit non-goal) | 0% | 0% | 0% | ⏸ Excluded | Reflection remains explicitly out of scope. |
| `bevy_tasks` | N/A (explicit non-goal) | 0% | 0% | 0% | ⏸ Excluded | Task runtime parity remains explicitly out of scope. |
| `bevy_render` (topology) | `mgstudio-engine/render` | 98% | 96% | 96% | 🟡 In Progress | `sync_world` + `sync_component` + `extract_component` + `extract_resource` now run on Bevy-shaped sync/extract flow, render visibility-range extraction/writeback is wired into `Extract` + `PrepareResourcesFlush` with deduped range-buffer packing, `lod_index_for_entity`/crossfade query surface, device-driven uniform-fallback selection, and typed `RenderAssets<A>`/`ErasedRenderAssets<A>` resources now use Bevy-equivalent per-type registration instead of sharing one generic key; `RenderResources` now carries real Bevy-shaped `RenderDevice/RenderQueue/RenderAdapterInfo/RenderAdapter/RenderInstance` resources and unpacks them into main/render worlds with a render-world `PipelineCache`; tonemapping fullscreen bind-group semantics align with Bevy (`hdr` non-filtering sampler slot + LUT texture-owned filtering sampler slot). Remaining gaps are deeper stage-boundary equivalence and renderer internals. |
| `bevy_render::renderer` | `mgstudio-engine/render/renderer` | 98% | 95% | 95% | ✅ Mostly Done | Draw/prepare responsibilities are still partially mixed in hot paths, but queue-frame churn and per-frame mesh pipeline cache misses have been reduced by prepare-stage snapshot/prewarm paths with topology-ownerized and deduped prewarm inputs plus extracted static preprocess metadata reuse, stale mesh runtime caches are now swept in queue-sweep stage, queue state no longer requires post-store prepared-queue mutation (`render3d_queue_update_prepared_camera_queues` removed), `PrepareMeshes` now writes only pending frame snapshots and `QueueMeshes` promotes pending->queued once per frame (instead of double queued-frame store with placeholder prepared queues), per-frame camera-loop bounds are precomputed in queue state, camera iteration logic is shared between prepare/queue consumers, mesh3d view bind groups are now cache-reused instead of unconditional per-draw creation, both mesh3d material/view bind groups now have `PrepareBindGroups` prebuild paths instead of relying on draw-path first-hit construction, motion-vector mesh uniform packing now carries runtime `current_skin_index` from extracted skin keys (instead of fixed sentinel payload), motion-vector prepass now has Bevy-shaped `base/SKINNED/SKINNED+HAS_PREVIOUS_SKIN` pipeline specialization with `@group(2)` mesh+current-skin+previous-skin binding and skin-revision-aware bind-group refresh, stale preprocess camera upload states are now pruned against current extracted cameras with Bevy-matching `PrepareResources` ownership (`delete_old_work_item_buffers` equivalent lifecycle cleanup), mesh extraction now follows changed/removed incremental cache updates with a reextract queue for material-not-ready entities (instead of unconditional per-frame full mesh scans), occlusion preprocess build-indirect staging now includes dedicated `early/late/main` indirect-parameter pipeline variants, preprocess dispatch is split by execute-stage boundaries (`early` before passes, `late` after shadow pass, `main` before main pass), indirect-parameter GPU metadata buffers are now cleared at early/main preprocess entry (Bevy `clear_indirect_parameters_metadata` parity), per-camera preprocess payload upload now runs in `PrepareResourcesFlush`, and `QueueMeshes` now owns prepared camera queue-entry construction while execute main-pass prebuilds pass-accurate view bind groups (`scene/transmission`) with redundant `-1` fallback prebuild calls skipped; mesh material bind-group prebuild ownership is prepare-stage only and deduplicated by texture-binding key, redundant source-world prepass pipeline-state refresh has been removed from `PrepareBindGroups`, mesh3d custom pipeline cache now follows Bevy `SpecializedMeshPipelines` key-map shape by storing `RenderPipelineCacheState` under a full specialization key instead of linearly scanning an array of duplicated key fields, the renderer package now exposes Bevy-shaped `WgpuWrapper`/`RenderDevice`/`RenderQueue`/`RenderAdapter`/`RenderInstance`/`PendingCommandBuffers`/`RenderContextState` surfaces over `wgpu_mbt` handles, `RenderPlugin` now initializes the render-world `PendingCommandBuffers` resource like Bevy, `RenderGraph`/`CurrentView` now expose Bevy-shaped schedule/resource surfaces, render-state readiness now mirrors Bevy `RenderState::{Initializing,Ready,Errored,Reinitializing}` at the resource level, and render startup/recovery schedule labels are now registered on the render sub-app. |
| `bevy_core_pipeline` | `mgstudio-engine/core_pipeline` | 95% | 95% | 95% | 🟡 In Progress | Core camera-driver scheduling is now on unified dedicated render-stage entry, dead `CameraDriverState/CameraDriverSortedCamera` placeholder state has been removed from core-pipeline API/export surface, `camera_driver` now executes core3d/core2d camera schedules through the current render-subapp schedule registry (instead of no-op), core2d/core3d pass-set chains are wired in render sub-app with Bevy-shaped core2d `main_pass/post_process` ordering, core2d/core3d per-camera schedule labels are now explicitly registered in render sub-app (`core_2d`/`core_3d`) with internal set ordering (`prepass->main->post` / `main->post`) configured on those schedules, core2d queue follows queue-meshes/sweep subset ordering, core2d phase resources now initialize in the render sub-app instead of the main world (matching Bevy render-app ownership), tonemapping LUT ownership is now centralized in `core_pipeline::TonemappingLuts` (shared by both sprite 2D and pbr 3D paths) with build-time plugin initialization, tonemapping fullscreen pass bind-group binding semantics are aligned to Bevy slot behavior (`hdr_sampler` non-filtering + LUT uses texture-owned filtering sampler), core-pipeline plugin installation now uses typed plugin entries (`add_plugin_typed`) so plugin lifecycle hooks are honored consistently, `mip_generation` job IDs now use typed `AssetId<Image>`/`Handle<Image>` API (instead of raw ints), `MipGenerationPlugin` resource ownership now follows Bevy-side render-world semantics via render-subapp build-stage init + cleanup-set `MipGenerationJobs` reset + finish-stage `MipGenerationResources` initialization, `mip_generation::TEXTURE_FORMATS` has been mirrored into MoonBit list form (40 entries) and is now the source of default general-shader registration count, `MipGenerationPhaseId` and phase-index keys are now `UInt` (Bevy `u32`-shape), and Bevy core-pipeline WGSL shader tree has been synced into mgstudio assets; postprocess/mip ordering still needs deeper convergence. |
| `bevy_pbr` (overall) | `mgstudio-engine/pbr` | 96% | 96% | 96% | 🟡 In Progress | Base render-stage ownership is aligned to shared RenderSystems sets, fog setup now installs the render-side fog plugin directly (removed main-world `FogPluginState` placeholder), default light-resource bootstrap and directional-shadow-map validation have been removed from `mesh_render` and moved to `LightPlugin` ownership, and Bevy PBR shader/static assets (including meshlet preview + blue-noise table) are synced; decal/contact-shadows plugin chain now installs in `PbrPlugin` with render-world clustered-decal extraction (scene + light textures, including inverse global-transform `local_from_world`), `DecalsBuffer` upload lifecycle, per-view contact-shadows uniform preparation now uses render-world entities with extracted `ContactShadows` data, and camera/mesh extracted phase entities plus sorted-camera ordering now preserve render-entity IDs instead of assuming main-world IDs; clustered decal component ownership now follows Bevy (`ClusteredDecal`/component key live in `light`, pbr only consumes). `DistanceFog` is now a standalone component key (`ecs_key_distance_fog`) rather than `Camera3d`-embedded state, camera decal/deferred config state is now likewise carried by independent components (`ecs_key_camera_decal_config3d` / `ecs_key_camera_deferred_config3d`) consumed via camera payload queries in execute/queue paths, camera auto-exposure/depth-of-field settings are now standalone components (`ecs_key_auto_exposure` / `ecs_key_depth_of_field`), and volumetric/atmosphere camera settings are now separated from `Camera3d` via light-owned component keys (`ecs_key_volumetric_fog` / `ecs_key_atmosphere` / `ecs_key_atmosphere_settings`). Remaining gaps concentrate in meshlet/deferred/advanced-pass depth. |
| `bevy_pbr::render` | `mgstudio-engine/pbr/render` | 97% | 97% | 97% | 🟡 In Progress | Core stage split is landed; `render/top` is `mod.rs`-equivalent aggregation, `render/fog` carries Bevy-shaped `FogPlugin` + `FogMeta` render-subapp install path with `GpuFog/GPU_FOG_MODE_*/prepare_fog`, fog uniform generation now runs in `PrepareResources` (not `PrepareBindGroups`) and now consumes render-world sorted camera entities, `PrepareResources` morph/skin work is now split into explicit systems, skin upload traversal skips invalid bindings instead of aborting the pass, skin presence now snapshots `current_skins -> previous_skins` per extract frame, renderer main-pass skin bind-group selection now consumes `skinning_key_lo` (current/prev), motion-vector extraction now carries explicit `has_previous_skin` semantics instead of forcing all skinned draws into previous-skin call-site routing, main-pass draw payload generation now hard-pins skinned draws to current-skin selection, mesh visibility-range checks now use canonical `VisibilityRange::is_visible_at_all`, mesh motion-vector gating now follows per-mesh previous-data checks (`previous_transform/previous_skin/previous_morph`) instead of global camera bool-only enable, and Bevy PBR WGSL tree (`render/prepass/atmosphere/deferred/light_probe/...`) is now copied into mgstudio assets; `mesh_view_bindings` layout metadata now mirrors Bevy slot indices (including contact-shadows/deferred/atmosphere/OIT/decal-binding-array entries), `GpuLights.cluster_factors.xy` now follows Bevy viewport scaling shape (`cluster_dimensions.xy / viewport_size`) instead of fixed zeros with camera-viewport priority over window fallback, `GpuLights.cluster_factors.zw` now consumes orthographic-vs-perspective runtime projection state (instead of fixed perspective path), cluster helper selection is unified on a single preferred cluster camera entity across viewport/config/runtime/visible/projection-runtime paths, non-runtime orthographic cluster near now reads projection-runtime first-slice depth, single-z perspective clusters now pin near to far before factor generation (Bevy `z == 1` semantics), non-runtime cluster z-range fallback now consumes `ClusterConfig` (`first_slice_depth` + `far_z_mode`) with `VisibleClusterableObjects` camera-list priority plus Bevy-shaped view-depth contribution for `MaxClusterableObjectRange` (instead of range-only fallback), and main-pass/material-phase routing now consumes Bevy-shaped `standard_material_render_phase_type` semantics (transparent precedence over transmissive + shared transmissive branch) while reusing extracted material-phase/method metadata when available; mesh shader selection/prewarm now resolves from extracted `MaterialProperties` shader slots with extension-path fallback. Remaining gaps are meshlet and advanced-pass specialization depth. |
| `bevy_pbr::prepass` | `mgstudio-engine/pbr/prepass` | 95% | 95% | 95% | ✅ Mostly Done | Remaining parity work is maintenance-level drift monitoring. |
| `bevy_pbr::meshlet` | `mgstudio-engine/pbr/meshlet` | 95% | 95% | 95% | 🟡 In Progress | Plugin API shape is slot-only, runtime state semantics + render/core3d stage ownership are aligned, queue ordering follows Queue→QueueMeshes boundaries, per-view prepass/deferred bin wiring follows Bevy query flags, non-Off camera MSAA fails fast like Bevy, `InstanceManager` tracks per-instance payload with queue-time material ID writeback, meshlet view resources + bind-groups are prepared/stale-cleaned per view in `ResourceManager` and pass execution requires both `material_depth` + `material_shade` readiness, meshlet material preparation filters by view-key + Forward/Deferred + Opaque + transmission semantics (with strict `AlphaMode::Opaque` gate matching Bevy), deduplicates by material ID (HashSet-like pass semantics), normalizes prepass pipeline keys to ignore `depth_prepass` and strips main-pass-only bits (matching Bevy prepass key shape), meshlet camera/projection reads now route through `@camera` component keys (instead of `@pbr` aliases), main-pass pipeline keys now include Bevy-shaped camera dimensions (`hdr/temporal_jitter/projection/shadow_filter/tonemap/deband/ssao/fog/light-probe`), and pipeline identity derives from pass+view-key instead of per-material asset id; shade-pass dispatch keeps Bevy-shaped per-material fullscreen triangle draw-call semantics (`material_id * 3`) on top of valid-draw counting, visibility-raster state tracks Bevy-shaped first/second/resolve counters under pipeline-ready gating, and pipeline readiness goes through `MeshletPipelines::get` (Bevy-shaped all-slot retrieval surface) with material shader modeled separately from pipeline slot ids; remaining gap is shader/pipeline-specialization depth. |
| `bevy_material` | `mgstudio-engine/material` | 99% | 95% | 95% | ✅ Mostly Done | `phase.rs` surface now includes Bevy-shaped `RenderPhaseType` (`Opaque/AlphaMask/Transmissive/Transparent`) with default semantics and material-side type aliasing; `descriptor.rs/key.rs/specialize.rs` now expose Bevy-shaped core data-models (`BindGroupLayoutDescriptor`/pipeline descriptors, erased material/mesh keys, typed specialize callbacks), descriptor structs expose Bevy-derived `Default` trait entries, erased key structs expose Bevy-like equality/debug identity plus accessor helpers, and `BindGroupLayoutEntries` now includes Bevy's dynamic sequential/indexed extension builder surface. `MaterialProperties` API now matches Bevy crate surface (`get/add shader`, `get/add draw function`, plus runtime `with_*` builders), and `MaterialProperties` now also carries Bevy-aligned specialize callback slots (`base_specialize` / `prepass_specialize` / `user_specialize`) with white-box invocation coverage and explicit builder coverage for material layout/bindless state (`with_material_layout` / `with_bindless`). `MaterialProperties::default` now mirrors Bevy's derived default semantics (`shadows_enabled=false`, `prepass_enabled=false`) while the pbr `StandardMaterial` extraction path explicitly sets both runtime properties like Bevy; `AlphaMode`, `OpaqueRendererMethod`, `DefaultOpaqueRendererMethod`, and `RenderPhaseType` now implement `Default` with Bevy-equivalent variants and Bevy variant order. `pbr/material` extraction now carries Bevy-style `MaterialProperties` payload (including render method / transmission / phase routing / depth bias / material key) into main-pass consumption, material `shadows_enabled` now gates point-shadow sampling in the main pass, and `prepass_enabled` now follows `StandardMaterialPluginState.prepass_enabled` instead of fixed `true`; extended material shader-path metadata is now reflected into extracted properties shader slots with dedicated white-box coverage. `OpaqueRendererMethod::default` and `DefaultOpaqueRendererMethod::default` now match Bevy (`Forward`), `StandardMaterial.depth_bias` is now present and encoded into `StandardMaterialKey` like Bevy key packing, `StandardMaterial::default().clearcoat_perceptual_roughness` now matches Bevy `0.5`, `StandardMaterial::from_color` alpha-mode threshold now follows Bevy `< 1.0` semantics, and Bevy `flip/flipped` UV-transform helpers are now ported with left-multiply order semantics (MoonBit immutable-record shape: `flip` returns updated material). Remaining gaps are deeper deferred/forward/decal behavior convergence. |
| `bevy_camera` | `mgstudio-engine/camera` + `pbr/render` | 96% | 96% | 96% | 🟡 In Progress | `CameraPlugin`/`CameraProjectionPlugin` typed plugin surface is now explicit in `camera` (Bevy-style plugin entry), `CameraPlugin` installation shape now mirrors Bevy by wiring `CameraProjectionPlugin` + `camera::visibility::VisibilityPlugin` + `VisibilityRangePlugin` as independent typed plugins, projection-frusta update scheduling is now anchored in camera-side plugin wiring (`after transform_set_propagate`), and duplicate `update_frusta` scheduling in `pbr` has been removed so frusta updates are camera-plugin-owned like Bevy. Scene/glTF camera payload and spawn wiring now consume `@camera.Camera3d/@camera.Projection` instead of `@pbr` aliases on the static scene path, stress gate `many_foxes` camera call sites are switched to `@camera.Camera3d`, picking-focused examples (`picking3`/`ray_casting3`/`mesh_ray_cast`) now use `@camera.Projection`, and current example-side camera/projection call sites (`examples/**/main.mbt` plus helper wrappers like `examples/helpers/rapier_examples`) now use `@camera.Camera3d*` + `@camera.Projection*` instead of `@pbr`/`@engine_pbr` aliases. Picking backend camera/projection ownership is now routed through `@camera` (`ecs_key_camera3d`/`ecs_key_projection`) and camera package no longer has reverse dependency on `picking/mesh_picking/ray_cast`, removing that cross-package coupling. Visibility component API shape has moved closer to Bevy (`Visibility` toggle trio plus `InheritedVisibility::visible/hidden/get` and `ViewVisibility::hidden/get` helpers); `NoCpuCulling` marker semantics are now wired into mesh CPU frustum-culling gating with Bevy-shaped precedence (`NoFrustumCulling` + camera/entity `NoCpuCulling` opt-out checks), viewport clamping semantics are now unified to Bevy edge behavior via `Viewport::clamp_to_size` (2D/3D both share the same 1px edge-clamp logic), camera package export surface now directly includes `Viewport/RenderTarget/CameraTargetInfo` plus a camera-level clamp helper entry, and `Camera3d` data model now includes Bevy-shaped depth fields (`depth_load_op` / `depth_texture_usages`) while fog/decal/deferred/atmosphere/auto-exposure/DoF state remains split into standalone components. `VisibilityRange` API/runtime path (`abrupt/is_visible_at_all/is_culled`, `VisibleEntityRanges + check_visibility_ranges`, `use_aabb` with shared camera primitives) remains aligned, and camera visibility now wires Bevy's `calculate_bounds -> update_skinned_mesh_bounds` chain through `PostUpdate` using `Mesh3d`, `Aabb`, and `DynamicSkinnedMeshBounds`. Remaining divergence is deeper camera/view/projection integration depth and full ownership split completion for non-scene call sites. |
| `bevy_sprite` | `mgstudio-engine/sprite` + `sprite_render` | 97% | 95% | 95% | 🟡 In Progress | Render-subapp scheduling is unified and anchored to shared render/core2d set chains, queue systems are aligned to queue-meshes/sweep subset ordering, Bevy sprite-render WGSL shader tree has been synced into mgstudio assets, `Mesh2d` component ownership now follows Bevy-side `mesh` package ownership (`sprite` uses `@mesh.Mesh2d` + shared component key), and Bevy `calculate_bounds_2d` behavior is now mirrored by sprite-plugin-owned post-update AABB generation for Mesh2d/Sprite/SpriteMesh with `NoAutoAabb` opt-out semantics and visibility-range ordering. `SpriteMesh` now has the Bevy-shaped component surface, `SpriteAlphaMode`, required-component registration for `Transform`/`Visibility`/`Anchor`, `Sprite::from_color`, shared `compute_pixel_space_point`, and `SpriteMeshPlugin` now mirrors Bevy's chained `add_mesh`/`add_material` path by lazily inserting a shared quad `Mesh2d` plus cached `SpriteMaterial`; `SpriteMaterial` uniform packing now follows Bevy atlas/rect/custom-size scale/fit/fill/tile/slice CPU transform math with white-box atlas UV coverage. `Sprite`, `SpriteImageMode`, `SpriteScalingMode`, `Anchor`, and `ClearColor` now expose Bevy-shaped `Default` trait semantics in addition to their constructor helpers. Remaining gaps are final stress-scale/picking behavior and broader visual parity after the upstream WGSL entrypoint blocker is resolved. |
| `bevy_ui` | `mgstudio-engine/ui` + `ui_render` + `ui_widgets` | 96% | 95% | 95% | ✅ Mostly Done | Render-subapp extraction scheduling is unified and bridged to base render/core2d set chains, Bevy ui-render WGSL shader tree has been synced into mgstudio assets, geometry primitives now follow Bevy's `Val`/`UiRect`/`BorderRadius` model, `Node` owns Bevy-shaped `box_sizing`/`border_radius`/`scrollbar_width`, layout now stores Bevy-style computed border/padding/border-radius/outline geometry consumed by ui-render/clip, `UiStack` carries Bevy-style per-root partitions while `UiLayout` stores the computed stack index updated by `ui_stack_system`, and interaction-state/a11y observer behavior now mirrors `interaction_states.rs` (`InteractionDisabled`, `Pressed`, `Checkable`, `Checked`, and disabled/toggled AccessKit updates); remaining work is screenshot-level drift plus `BoxSizing::ContentBox` layout pass-through once `moon_taffy#9` lands. |
| `bevy_feathers` | `mgstudio-engine/feathers` | 96% | 95% | 95% | ✅ Mostly Done | Feathers plugin-group wiring now follows Bevy dependency shape (`UiWidgetsPlugins` + `InputDispatchPlugin` + `TabNavigationPlugin` + `FeathersPlugin`), controls runtime no longer re-registers `UiWidgetsPlugins` (duplicate-unique-plugin abort removed), `ControlsPlugin` now has explicit typed plugin entry (`name/build`) and is installed via `add_plugins` from `FeathersPlugin`, fonts now use mgstudio asset paths with Bevy Feathers source font files copied into `mgstudio-engine/assets/fonts`, the design-token/dark-theme state matrix now covers Bevy's hover/pressed/disabled/checked variants for button, slider, checkbox, radio, switch, and color-plane tokens, button props / rounded-corner data now mirror Bevy's `Normal/Primary` + `RoundedCorners` API shape, checkbox/radio/toggle-switch construction no longer carries state props that Bevy models as components, and color slider/plane/swatch data models now use Bevy-shaped Color/Vec3 component payloads instead of local hex/HSV placeholders; remaining work is deeper template spawning/styling behavior parity. |
| `bevy_text` | `mgstudio-engine/text` | 95% | 95% | 95% | ✅ Mostly Done | Render-subapp text prepare/queue scheduling is unified, bridged to base render sets, and queue-subset ordered with sprite queue; text ECS bridge now mirrors Bevy-style culling opt-out semantics by treating both `NoFrustumCulling` and `NoCpuCulling` markers as frustum-cull bypass flags for runtime text instances. Default font path now points to Bevy-matching `FiraMono-subset.ttf`, and the source font asset has been copied from `bevy_text` into mgstudio assets for closer startup/render parity. Font asset event handling now mirrors current Bevy `load_font_assets_into_font_collection`: only `Added` preloads font data into the runtime font system and marks all text instances dirty for rerender; other asset event variants are ignored. `ComputedTextBlock::needs_rerender` now matches Bevy's viewport/rem change-flag semantics; `Last`-stage shaping cache trim now mirrors Bevy `trim_source_cache` intent via `moon_cosmic` `ShapeRunCache::trim(2)` lifecycle. `FontHinting` now mirrors Bevy text semantics across required components, computed text blocks, text pipeline hinting, and font-atlas keys; `Justify::Start/End` are now exposed in the Bevy-facing API and `End` maps to the runtime text backend, while true direction-aware `Start` remains blocked by `moon_cosmic#4`; remaining work is visual/script coverage drift monitoring. |
| `bevy_gltf` | `mgstudio-engine/gltf` + `scene` | 95% | 95% | 95% | 🟡 In Progress | Loader/runtime edge cases and extension semantics are not fully closed yet; skin inverse-bindposes are now loader-owned assets cached by glTF `skin index` (instead of per-node duplicate allocation), glTF loader registration lifecycle now follows Bevy-style plugin `finish` semantics (registration no longer runs during `build`), asset-path loader settings are now persisted and consumed during scene build (`load_meshes/load_materials/load_animations`, plus sampler override shape), glTF loader settings now key by canonical base path so root `load_with_settings(...)` applies to labeled sub-assets (`#Scene/#Animation/#Mesh`), node/camera/light parse now includes Bevy-shaped camera projection mapping and `KHR_lights_punctual` light decoding, scene node payload now carries camera/light components into scene spawner runtime, root `Assets<Gltf>` now honor `include_source` and retain source payload when enabled, root `Assets<Gltf>` now publish Bevy-shaped `named_scenes` and `materials/named_materials` entries (material loading gated by `load_materials`), loader texture-sampler fallback now consumes plugin-configured or per-load default sampler state, glTF sampler mapping now follows Bevy semantics (`wrap_s/wrap_t`, `mag/min/mipmap` filter decode, anisotropic-default filter override guard, and default-texture-sampler `Repeat` wrap behavior when no explicit sampler is bound), loader settings now also drive Bevy-shaped coordinate conversion (`rotate_scene_entity` / `rotate_meshes`) plus per-load skinned-mesh bounds policy resolution, and material cull-mode selection now follows Bevy's per-node negative-scale rule (`double_sided` + inverted-scale front/back switch). |
| `bevy_animation` | `mgstudio-engine/animation` | 95% | 95% | 95% | ✅ Mostly Done | Triggered-event hit-window logic is now Bevy-shaped (`partition_point` + forward/reverse/looping iterator order), targeted event dispatch now happens inside `animate_targets` during threaded-graph clip evaluation (matching Bevy's target-entity trigger point; the old extra target-event pass was removed), clip events are unified under target-keyed storage (Root/Node), runtime event dispatch is event-owned trigger-closure flow (removed global typed/callback registries), legacy free-function clip-event constructors were removed, zero-duration transition behavior now matches Bevy’s system-chain semantics (`play` no longer directly stops old animation; stop occurs through `advance_transitions` + `expire_completed_transitions`), transition blending now matches Bevy’s non-clamped remaining-weight semantics (main animation weight may go negative in edge cases), `ActiveAnimation::update` now matches Bevy’s completion gate (`speed > 0` for end-of-clip completion, so `speed == 0` no longer spuriously completes), and the non-Bevy zero-duration early-return guard was removed so update flow follows Bevy’s raw completion/modulo path. `AnimationGraph` now has Bevy-shaped `add_edge/remove_edge` APIs, stores multiple parents internally instead of a single tree parent, preserves multi-parent edges in the `.animgraph.ron` loader, and tests verify DAG edge removal changes mask propagation. `ThreadedAnimationGraph` now matches Bevy's stored acceleration fields by dropping the non-Bevy `effective_weights` cache, and `animate_targets` now evaluates the threaded graph in postorder with Bevy-style blend/add/register/push/commit semantics instead of flattening clips into weighted sample lists. `AnimationTargetId` hashing now follows Bevy's namespace UUID + full BLAKE3 chunk-tree + SHA1-version UUID byte shaping instead of mgstudio's previous custom hash, with white-box BLAKE3 vector coverage. Asset-event handling now explicitly ignores Bevy-shaped `Unused` events like upstream `thread_animation_graphs`. Remaining gaps are MoonBit-specific typed-event/evaluator extensibility tails, not core runtime blockers. |
| `bevy_scene` (static scene path) | `mgstudio-engine/scene` | 98% | 97% | 97% | 🟡 In Progress | `SceneRoot`/`SceneInstance` lifecycle hooks and required components now install via explicit scene metadata installer paths (no package `init` side-effect dependency), scene asset loader/reload hooks are plugin-installed, and spawner scheduling/runtime remains Bevy-shaped for root respawn/readiness flow. Remaining gaps are deep spawn/runtime corner-case parity. |
| `bevy_scene` (`dynamic_scene*`) | N/A (explicit non-goal: dynamic) | 0% | 0% | 0% | ⏸ Excluded | Dynamic-scene path remains explicitly out of scope. |
| `bevy_gizmos` | `mgstudio-engine/gizmos` + `gizmos_render` | 97% | 95% | 95% | 🟡 In Progress | GizmoPlugin/GizmoRenderPlugin ownership is now split like Bevy default plugins, `Gizmos` system params queue into world-scoped `GizmoStorage` before render drain, `GlobalGizmosPlugin` installs a Bevy-shaped `Last` flush for `gizmo()` calls outside systems, retained `GizmoAsset`/`Gizmo` ownership now follows Bevy asset + required-`Transform` semantics, retained extraction now emits Bevy-shaped render entities, retained `GizmoBuffer` covers the Bevy line/ray/loop/rect/cube/cross/2d/gradient helper slice, and retained circle/ellipse/arc/short-long arc/sphere/arrow/axes now use ordered pending builders so builder mutations change geometry before flush while preserving call order. Remaining gaps are real line-gizmo render asset/pipeline parity plus remaining builder-backed primitive helpers (`grid`, `text`). |
| `bevy_picking` | `mgstudio-engine/picking` | 96% | 96% | 96% | 🟡 In Progress | Camera/projection dependency in picking backend and mesh-ray-cast camera entry now route through `@camera` package ownership (instead of direct `@pbr` camera keys), mesh ray cast now follows Bevy's `Mesh3d`/`Mesh2d` filter shape by reading world-owned `Assets<Mesh>` and including Mesh2d front/back-face hits, sprite picking now has Bevy-shaped `SpritePickingSettings` / `SpritePickingMode` / `SpritePickingCamera` surface plus alpha-threshold and bounding-box modes over world-owned `Assets<Image>`/`TextureAtlasLayout`, and sprite hit testing now uses active `Camera2d` + `Camera` + `Projection` ray generation/intersection instead of the deleted UI-context world-coordinate fallback. `Pickable` component semantics (`should_block_lower` / `is_hoverable` + ignore preset), `PickingSettings` resource gating (`is_enabled` / input / hover / window), window-level fallback hit backend (`window::update_window_hits`-shape), primary-hit ordering now include `ui > sprite2d > mesh3d > window`, and `DefaultPickingPlugins` plugin-group API surface is now present. Pointer input collection now has Bevy-shaped `PointerInputSettings` (`is_touch_enabled` / `is_mouse_enabled`), merges touch pointers from `@input.Touches` into the same stream, registers `PointerInput` and `PointerHits` as real ECS messages, writes default pointer collection and backend hit output only into those message streams, and adds Bevy-shaped pointer entity components/resources (`PointerId`, `PointerLocation`, `PointerPress`, `PointerInteraction`, `PointerMap`). Backends now consume `PointerLocation`/`PointerPress` components instead of the deleted per-frame `PointerInputs` resource bridge, `HoverMap` / `PreviousHoverMap` coalesce by pick layer and depth from `PointerHits`, pointer interaction components are updated from the hover map, and high-level pointer event target selection uses `HoverMap` rather than the removed `PickingHits` resource bridge. Picking system sets now mirror Bevy's `Input/PostInput` (`First`) plus `ProcessInput/Backend/Hover/PostHover/Last` (`PreUpdate`) chain while keeping plugin responsibilities split to Bevy-style `PointerInputPlugin` / `PickingPlugin` / `InteractionPlugin`; remaining work is moving high-level event generation fully to Bevy's `PointerState`/button event model and unifying sprite/mesh backend output ordering exactly with camera order. |
| `bevy_input` | `mgstudio-engine/input` | 95% | 95% | 95% | ✅ Mostly Done | Package tests are green; mouse accumulation now follows Bevy’s split update shape by running dedicated motion/scroll accumulation systems from `MouseMotion`/`MouseWheel` message streams (instead of direct poll-time resource writes), with matching “sum all events per frame + last wheel unit wins” semantics, and keyboard focus-loss handling now follows Bevy behavior by suppressing loss when a focus-gained event appears in the same frame, emitting `KeyboardFocusLost`, and synthesizing `KeyboardInput::Released` events for currently-pressed keys before clearing host key state; `winit` focus-loss lifecycle tests keep this release synthesis behavior covered. Touch runtime state now mirrors Bevy’s start/previous/current position tracking semantics (`Touch::start_position/previous_position/delta/distance`), multi-move same-frame handling now follows Bevy’s frame-boundary previous-position snapshot semantics (matching `touch_screen_input_system` / issue #12442 intent), and touch helper API surface now includes the full just/iter/get/clear/release wrappers. Remaining work is maintenance-level platform/event-order drift monitoring. |
| `bevy_window` + `bevy_winit` | `mgstudio-engine/window` + `winit` | 96% | 95% | 95% | 🟡 In Progress | Package tests are green; monitor-aware sizing parity moved forward by adding `WindowPosition::Centered(MonitorSelection::Entity(_))` placement in window runtime (using monitor components directly), moving monitor synchronization before window creation in the winit PreUpdate chain, aligning centered placement math to Bevy `winit_window_position` semantics (`logical_size -> target_scale_factor -> saturating_sub` centering), consuming/applying initial window mode requests during create pass so new fullscreen/borderless windows no longer wait for a later stage, aligning close lifecycle to Bevy two-frame semantics (`close_when_requested`: despawn existing `ClosingWindow`, mark new close requests; winit `Last` stage emits `WindowClosing` without same-frame despawn), emitting `WindowClosing` for all open windows when `AppExit` is received (Bevy `despawn_windows` parity path), wiring cursor `hit_test` create/update propagation into runtime (`CursorOptions.hit_test` now reaches host window state), aligning cursor-grab behavior with Bevy `attempt_grab` semantics (Confined/Locked cross-fallback + update-time component rollback on failure), restoring Bevy-shaped keyboard focus-loss flow in `winit` lifecycle (`KeyboardFocusLost` + synthesized released key events for pressed keys), aligning `WindowScaleFactorChanged` emission semantics to Bevy (`relative_eq`-style epsilon compare + no emission while scale-factor override is active), and removing both `WindowPlugin` and `WinitPlugin` marker-based fallback registration so plugin build follows Bevy-style direct resource/system/message installation. Remaining tails are broader platform semantics and long-tail monitor behavior. |
| `bevy_asset` | `mgstudio-engine/asset` | 96% | 95% | 95% | 🟡 In Progress | Native asset runtime is stable with capability-gated HTTP fetch; `AssetEvent` now includes Bevy-shaped `Unused` plus id helper methods (`is_added/is_modified/is_removed/is_unused/is_loaded_with_dependencies`); image extension routing now derives from `bevy_image`-shaped loader tables and image meta settings (including sampler compare/border/anisotropy) are wired; remaining gaps focus on broader codec parity depth. |
| `bevy_log` + diagnostics | `mgstudio-engine/log` + `diagnostic` + `dev_tools` | 95% | 95% | 95% | 🟡 In Progress | `LogPlugin` now follows Bevy's `level + filter` EnvFilter-style shape with target-specific longest-match directives and mgstudio-branded default filters; once-log wrappers cover trace/debug/info/warn/error. `Diagnostics` now uses a Bevy-style deferred per-system buffer that keeps the last measurement per path and applies it through the existing `ApplyDeferred` command-batch boundary, and `Diagnostic` history now uses a deque-backed timeline to match Bevy's `VecDeque` pop-front semantics instead of copying `Array` history on retention. FPS overlay now mirrors Bevy's static `Text(\"FPS: \")` + dynamic child `TextSpan`, `FpsText`/`FrameTimeGraph` marker components, and `Pickable::IGNORE` overlay entities; `FrameTimeGraphPlugin` is now a root dev-tools plugin that `FpsOverlayPlugin` installs like Bevy, while the `dev_tools/frame_time_graph` package re-exports the Bevy-shaped path, `FrameTimeGraphConfigUniform`, `update_frame_time_values`-style seconds buffer flow, embedded Bevy `frame_time_graph.wgsl` registration, Bevy-shaped `FrametimeGraphMaterial` + `StorageBuffer<Float>` asset initialization, and material-buffer updates via `Assets::iter_mut`. Remaining gaps are the generic `Deferred<T: SystemBuffer>` SystemParam abstraction and the actual `UiMaterialPlugin<FrametimeGraphMaterial>` GPU bind-group/render-phase path. |
| `bevy_anti_alias` | `mgstudio-engine/anti_alias` | 95% | 95% | 95% | ✅ Mostly Done | CAS now mirrors Bevy's embedded WGSL/pipeline-specialization path and runs after FXAA in Core3d plus the Core2d postprocess path; FXAA/CAS/SMAA/TAA WGSL files are byte-identical with Bevy sources and the owning plugins now register their embedded shader assets. SMAA LUT assets are copied byte-for-byte from Bevy, plugin build now registers both lookup textures as embedded assets, `SmaaLuts` owns render-world image handles with Bevy-shaped non-sRGB/render-world loader settings, `SmaaPreset::shader_def` plus `SmaaInfoUniform` mirror Bevy's shader-def and `rt_metrics` data model, and SMAA now has Bevy-shaped `init_smaa_pipelines` plus edge/blend/neighborhood pipeline specialization descriptor surface. TAA now installs Bevy-shaped required component metadata for `TemporalJitter`, `MipBias`, `DepthPrepass`, and `MotionVectorPrepass`; `MipBias` mirrors Bevy's `bevy_render::camera::MipBias` default of `-1.0`, `prepare_taa_jitter` now follows Bevy's FrameCount-driven Halton sequence with the same Camera3d/TemporalAntiAliasing/prepass filters, and TAA reset flags are cleared after the update/extract-equivalent pass like Bevy. DLSS now mirrors Bevy's component defaults plus required camera components (`TemporalJitter`, `MipBias`, `DepthPrepass`, `MotionVectorPrepass`, `Hdr`) while keeping runtime support disabled until the native DLSS backend exists, and DLSS reset flags are likewise cleared after the extract-equivalent pass. Remaining gaps are SMAA three-pass GPU execution, TAA history texture/pipeline execution integration depth, and DLSS native capability/runtime integration. |
| `bevy_light` | `mgstudio-engine/light` | 96% | 95% | 95% | 🟡 In Progress | `LightPlugin` typed plugin entry is now present and wired into `DefaultPlugins` after `CameraPlugin`, default light-resource initialization and directional-shadow-map validation are extracted from `pbr/mesh_render`, Bevy-matching `init_asset::<ScatteringMedium>()` ownership is now mirrored via light-plugin asset init, `automatically_add_parallax_correction_components` now runs in `Update` with Bevy-matching reflection-probe filters (`EnvironmentMapLight + LightProbe + Without<ParallaxCorrection>`), and probe required-components semantics are now registered on plugin build (`LightProbe -> Transform/Visibility/ViewVisibility`, `IrradianceVolume -> LightProbe`) to mirror Bevy `probe.rs`; clustered decal component ownership is now light-owned (`ClusteredDecal` + ECS key) to match `bevy_light::cluster`, `VisibleClusterableObjects` no longer stores decals (matching Bevy add-path semantics), cluster assignment now filters clusterables by `ViewVisibility` before visible-list/far-range aggregation, and `ClusterLightPlugin` is now part of the default plugin chain, with remaining gaps concentrated in deeper clustering/visibility/update-system parity. |
| `bevy_mesh` | `mgstudio-engine/mesh` | 99% | 96% | 96% | 🟡 In Progress | `MeshPlugin` typed plugin entry now owns both `Assets<Mesh>` and `Assets<SkinnedMeshInverseBindposes>` initialization, `SkinnedMesh` now stores inverse-bindpose asset handles (Bevy-shape), scene/glTF spawn reuses loader-created inverse-bindposes handles, `Mesh3d` + `Mesh2d` component ownership (including shared component keys and mesh-handle serialization surface) is moved into `mesh` (instead of `pbr`/`sprite`), Bevy `MeshTag` component shape is mirrored in `mesh/components`, `bevy_mesh::BaseMeshPipelineKey` / `InheritWeightSystems` API surface now has a MoonBit counterpart in `mesh/lib`, and mesh plugin now marks `Mesh2d`/`Mesh3d` changed on `AssetEvent<Mesh>::Modified` to support Bevy-shaped asset-change propagation. `skinning.rs` runtime data now includes Bevy-shaped `JointIndex`, `Influence`, `InfluenceIterator`, `JointAabb`, `SkinnedMeshBounds::from_mesh`, and `entity_aabb_from_skinned_mesh_bounds`, while `Mesh` now stores `skinned_mesh_bounds` and exposes Bevy-shaped `skinned_mesh_bounds` / `set_skinned_mesh_bounds` / `with_skinned_mesh_bounds` / `generate_skinned_mesh_bounds` / `with_generated_skinned_mesh_bounds`; non-zero-weight influence iteration, per-joint model-space bounds, generated/stored mesh bounds, and dynamic skinned entity AABB updates are covered by white-box tests. Remaining gaps are deeper GPU mesh upload parity details. |
| `bevy_image` | `mgstudio-engine/image` | 96% | 95% | 95% | ✅ Mostly Done | `ImageLoader` shape, extension routing, supported-format table, magic-byte guessing, and host decode/dimension coverage now include BMP, TGA(RLE), PNM(P1..P7, 8/16-bit), farbfeld, QOI, ICO(PNG+DIB32), GIF/WEBP/TIFF/JPEG/TGA dimensions; remaining work is broader long-tail codec behavior depth. |
| `bevy_color` | `mgstudio-engine/color` | 97% | 95% | 95% | ✅ Mostly Done | `Xyza/Laba/Lcha/Oklaba/Oklcha` are no longer `Color` aliases: Bevy conversion formulas and sequential-dispersed hue generation are now ported (`LinearRgba <-> Xyza <-> Laba <-> Lcha`, `LinearRgba <-> Oklaba <-> Oklcha`) with round-trip tests; remaining work is maintenance-level parity drift monitoring. |
| `bevy_math` | `mgstudio-engine/math` | 96% | 96% | 96% | ✅ Mostly Done | `Rect` now exposes the small Bevy-style read/offset helper surface needed by sprite pixel-space parity (`min/max/translate`) on top of the existing primitive/vector coverage; glam/Bevy vector and quaternion helpers needed by gizmos are now present (`Vec2::angle_to/perp_dot/distance`, `Vec3::normalize/distance/any_orthonormal_vector`, `Quat::from_rotation_arc/inverse/mul_vec3/to_axis_angle`, `Isometry3d::from_rotation/mul`). Remaining work is maintenance-level parity drift monitoring. |
| `bevy_a11y` | `mgstudio-engine/a11y` | 95% | 95% | 95% | ✅ Mostly Done | Only maintenance-level parity drift monitoring remains. |
| Rapier integration (third-party; not in local `./bevy`) | `mgstudio-engine/physics2d` + `physics3d` | 95% | 95% | 95% | ✅ Mostly Done | This is not a Bevy core package in the pinned local source tree, so source-level Bevy 1:1 is not available. 2D/3D plugins now use explicit fixed-schedule system sets, ordering, and profiler names; package tests are green. Future work requires adding a pinned third-party `bevy_rapier` reference if strict wrapper parity is desired. |
| Stress test parity | `examples/stress_tests/*` + scripts | 95% | 70% | 70% | 🟡 In Progress | Full-suite profile baseline now runs end-to-end; remaining work is source-first convergence for low-FPS heavy cases. |
| Visual screenshot parity | `/tmp` captures + parity gates | 96% | 75% | 75% | 🟡 In Progress | Coverage expanded to heavy stress + physics3d cases; remaining work is full-suite visual equivalence closure. |
| Workspace-wide native validation | `moon check/test` integration | 90% | 66% | 66% | 🟡 In Progress | Package-level native reliability improved and stress diagnostics are stable; full-suite runtime parity is still open. |

| Rollup | Value |
|---|---:|
| Bevy→mgstudio path parity (considered scope) | 100% (`1028/1028`, `missing=0`, 2026-04-13) |
| Migration completion scoring rule | `Overall = min(Structure, Runtime)` |
| Current weighted migration completion (included scope) | 95% |
| Last updated | 2026-04-30 |

### WGSL naga_oil 过渡层清理 (2026-04-15)
| 已删除过渡文件 | 对齐后的 Bevy 路径 |
|---|---|
| `assets/shaders/custom_postprocess_3d.wgsl` | `assets/shaders/post_processing.wgsl` |
| `assets/shaders/custom_render_phase_3d.wgsl` | `assets/shaders/custom_stencil.wgsl` |
| `assets/shaders/custom_shader_instancing_3d.wgsl` | `assets/shaders/instancing.wgsl` |
| `assets/shaders/fallback_image_3d.wgsl` | `assets/shaders/fallback_image_test.wgsl` |
| `assets/shaders/render_depth_to_texture_3d.wgsl` | `assets/shaders/show_depth_texture_material.wgsl` |
| `assets/shaders/shader_material_3d.wgsl` | `assets/shaders/custom_material.wgsl` |
| `assets/shaders/shader_material_bindless_3d.wgsl` | `assets/shaders/bindless_material.wgsl` |
| `assets/shaders/shader_material_glsl_3d.wgsl` | `assets/shaders/custom_material.wgsl` |
| `assets/shaders/shader_material_screenspace_texture_3d.wgsl` | `assets/shaders/custom_material_screenspace_texture.wgsl` |
| `assets/shaders/shader_material_wesl_3d.wgsl` | `assets/shaders/custom_material.wgsl` |
| `assets/shaders/shader_prepass_3d.wgsl` | `assets/shaders/custom_material.wgsl` |

### External shader toolchain split (2026-04-15)
- `naga_oil` 已从 `mgstudio-engine` 拆出为外部模块 `Milky2018/moon_wgsl`；`mgstudio` 现在直接依赖该模块，不再保留本地 `mgstudio-engine/naga_oil` 包。
- `mgstudio` 侧继续保留 `@naga_oil` import alias，仅作为调用点别名，不代表本地包仍然存在。
- `moon_wgsl` 0.4.0 历史验证：0.4.0 删除了临时 `preserve_imported_entry_points` 选项，并恢复 naga_oil/Bevy 语义：unused item-imported entrypoint 会被 tree-shake，`pbr/render/wgsl_entrypoint_wbtest.mbt` 已改为覆盖该语义。Bevy PBR alias-qualified global/module variable 解析（`#import bevy_pbr::mesh_view_bindings as view_bindings` 后访问 `view_bindings::view`）仍由 `pbr render: module alias global binding access composes` 覆盖；上游 `moonbit-community/moon_wgsl#4` 已验证关闭。`moon_wgsl#5` 中 0.2.1 的 `apply_wgsl_template_consts -> String::sub` abort 已不再复现；0.2.2 的 `pbr_types__StandardMaterial` 重复定义已由 0.2.3 修复；0.2.5 的真实 Bevy PBR shader module validation 阻塞（`mesh[in_wgsl.instance_index]` 全局绑定未定义）在 0.2.6 下不再复现。`F0_` 来自本地 `./bevy` 原版 `crates/bevy_pbr/src/render/pbr_lighting.wgsl`，不是 mgstudio shader rewrite；0.2.6 报 `invalid WGSL struct member identifier: F0_`，0.3.0 最小复现改为报 `composable module identifier requires naga writeback substitution: F0_`，0.3.1 最小 `F0_` 成员名复现已通过。0.4.0 能通过 `moon check`、`moon test pbr/render`、`moon test render/renderer`、`moon test pbr`，但真实 `3d_shapes` capture 仍显示 `moon_wgsl` 组合出的 Bevy PBR WGSL 缺 imported global 的 `View` 类型定义（`clustering__bindings__view: View` unknown type，10 次 mesh3d pipeline failure），并新增 `tonemapping pipeline failed: not enough #endif directives`（1 次）；本次日志为 `/tmp/mgstudio_3d_shapes_moon_wgsl_040.log`，截图 blob 为 `/tmp/mgstudio_3d_shapes_moon_wgsl_040.rgba`，结果已追加到 `moonbit-community/moon_wgsl#6` comment `4343051619`。目前不在 mgstudio 中修改 Bevy WGSL 或做本地 composer 绕过，继续由上游追踪；mgstudio 继续按 Bevy 的 layout/pipeline/shader-cache 分层重构。
- `moon_wgsl` 已升级到 `0.4.1`；0.4.1 修复了真实 PBR path 的 `unknown type: View`，`3d_shapes` 日志中该错误计数为 0。mgstudio 同步删除 tonemapping 的本地手动 `#ifdef` 重写，`core_pipeline/tonemapping/tonemapping.wgsl` 改回正常 source graph compose；renderer postprocess pipeline 创建也补齐 shader module / pipeline error-scope validation，非法 WGSL 不再延迟到 `QueueSubmit` panic。剩余 blocker 已追加到 `moonbit-community/moon_wgsl#6` comment `4343482315`：PBR fragment 仍生成重复 `@group(0) @binding(0)` imported global（`clustering__bindings__view: pbr_functions__view_bindings__View`，10 次 `Bindings for [34] conflict with other resource`），tonemapping root-local `#define TONEMAPPING_PASS` 仍保留在最终 WGSL 第 1 行并被 wgpu 拒绝。验证日志 `/tmp/mgstudio_3d_shapes_moon_wgsl_041_postprocess_scope.log`，截图 blob `/tmp/mgstudio_3d_shapes_moon_wgsl_041_postprocess_scope.rgba`。
- 2026-04-29：mgstudio 侧继续收敛到 Bevy 分层：renderer 管线缓存现在记录 `Ok/Err` 状态，shader validation 失败不再每帧重复重建；mesh3d shared pipeline-layout 初始化已和 forward pipeline specialization 拆开；manual fullscreen postprocess loader 改为通过 renderer runtime WGSL registry 读取 startup shader，`core_pipeline/fullscreen_vertex_shader/fullscreen.wgsl` 由无 asset/app 依赖的 source package 注册，避免 `renderer -> asset -> render -> renderer` 循环。默认 3D forward pipeline 已删除本地 `mesh3d_forward` / `mesh3d_bevy_forward` wrapper，改为 Bevy 同构的分离 shader module：vertex 使用 `pbr/render/mesh.wgsl`，fragment 使用 `pbr/render/pbr.wgsl`；descriptor 层补齐了独立 fragment shader module 写入路径，`forward_decal` 也纳入默认 pipeline cache key。Material 扩展元数据也已从单一 `shader_path` 拆成 Bevy 同构的 `MaterialVertexShader` / `MaterialFragmentShader` / prepass / deferred shader label，mesh3d custom pipeline cache 和 draw payload 同步以 vertex+fragment shader pair 为 key，并进一步改为 `Map<specialization-key, RenderPipelineCacheState>`，不再线性扫描重复 key 字段。升级 0.4.0 前的 `3d_shapes` 截图门禁产物 `/tmp/mgstudio_3d_shapes_custom_pipeline_map.rgba` 已生成，日志中 `tonemapping pipeline failed` 为 0；剩余 `mesh3d pipeline failed` 均进入 `pbr.wgsl` fragment module validation，仍为上游 `moon_wgsl#6` 的 `View` 类型缺失 blocker。
- 2026-04-29 shadow/WGSL cleanup：renderer shadow path 已删除本地 `mesh3d_shadow_depth*.wgsl` wrapper，改用 Bevy 原版 `pbr/prepass/prepass.wgsl` + `pbr/render/pbr_prepass.wgsl`，shadow render pass 改为 depth-only、Bevy empty bind group slot 语义；tonemapping 也改成 Bevy 同构的 fullscreen vertex module + tonemapping fragment module。6 个剩余自造 shader 文件已从树中移除（shader-loader 测试改为虚拟内嵌源，frame-time graph 移到 Bevy 形目录，mesh2d custom material 改用 Bevy `custom_material_2d.wgsl`）。当前 3d_shapes 仍空屏，根因不是 mgstudio shadow shader：`moon_wgsl` 仍无法把 Bevy PBR nested alias globals 归并，并保留 root-local `#define TONEMAPPING_PASS`；完整复现已提交 `moonbit-community/moon_wgsl#7`。`wgpu_mbt#16` 已随 0.12.6 验证解除，mgstudio 已删除 zero-entry bind group C descriptor 桥接并改用 public `BindGroupBuilder::new(max_entries=0UL).finish(...)`。
- 2026-04-29 window cursor parity gate：`moon run examples/3d/3d_shapes --release` 仍 exit 0 并写出 `/tmp/mgstudio_3d_shapes.rgba`，但截图仍受 `moon_wgsl#7` 阻塞；本次日志 `/tmp/mgstudio_3d_shapes_window_cursor.log` 仍只显示 PBR imported global binding conflict 与 tonemapping root-local `#define TONEMAPPING_PASS`。
- 2026-04-29 window resolution parity gate：`moon run examples/3d/3d_shapes --release` 仍 exit 0 并写出 `/tmp/mgstudio_3d_shapes.rgba`；转换图 `/tmp/mgstudio_3d_shapes_window_resolution.png` 仍为空背景，日志 `/tmp/mgstudio_3d_shapes_window_resolution.log` 继续只显示 `moon_wgsl#7` 已记录的 PBR imported global binding conflict 与 tonemapping root-local `#define TONEMAPPING_PASS`，无 window scale-factor 回归迹象。
- 2026-04-29 text FontHinting parity：对照 `bevy_text::FontHinting` / `ComputedTextBlock::font_hinting` / `TextPipeline::update_buffer` / `Text2d` and UI `Text` required-components，mgstudio 已补齐 `FontHinting` 组件、`should_hint`、text2d/ui text required-components、pipeline 到 `moon_cosmic` hinting 转换、font-atlas key 分流与 ECS dirty sync。`moon test text`、`moon test ui`、`moon check`、`moon fmt`、`moon info` 已通过；3d_shapes 截图门禁 exit 0 并写出 `/tmp/mgstudio_3d_shapes.rgba`，转换图 `/tmp/mgstudio_3d_shapes_text_font_hinting.png` 仍为空背景，日志 `/tmp/mgstudio_3d_shapes_text_font_hinting.log` 继续只显示 `moon_wgsl#7` 已记录的 PBR imported global binding conflict 与 tonemapping root-local `#define TONEMAPPING_PASS`。
- 2026-04-30 text Justify parity：对照 `bevy_text::Justify` / `impl From<Justify> for parley::Alignment` / `layout_with_bounds`，mgstudio 已补齐 `Justify::Start` / `Justify::End` 公开枚举、atlas/ECS key equality、pipeline alignment 映射与白盒覆盖；`Justify::End` 直接映射到 `moon_cosmic::Align::End`，`Justify::Start` 暂以 LTR-compatible `Align::Left` 桥接，真实 direction-aware 语义继续由 `moonbit-community/moon_cosmic#4` 阻塞。`moon check --target native`、`moon test text --target native`、`moon fmt`、`moon info`、`scripts/sync_bevy_wgsl.py --check` 均通过；3d_shapes 截图门禁 exit 0 并写出 `/tmp/mgstudio_3d_shapes_text_justify_start_end.png`，图像仍为空背景，日志 `/tmp/mgstudio_3d_shapes_text_justify_start_end.log` 继续只显示已记录的 `moon_wgsl#7` PBR binding conflict 与 tonemapping root-local `#define TONEMAPPING_PASS`。
- 2026-04-30 text font-event / ComputedTextBlock parity：对照 `bevy_text/src/font.rs::load_font_assets_into_font_collection` 与 `ComputedTextBlock::needs_rerender`，mgstudio 字体资产事件同步改为 Bevy 当前源码的 Added-only 语义，`LoadedWithDependencies/Modified/Removed/Unused` 不再触发字体运行时重建或文本 rerender；`ComputedTextBlock::needs_rerender` 改为接收 viewport/rem changed flags 并按 Bevy 公式返回。`moon check --target native`、`moon test text --target native`、`moon test ui --target native`、`moon fmt`、`moon info`、`scripts/sync_bevy_wgsl.py --check` 均通过；3d_shapes 截图门禁 exit 0 并写出 `/tmp/mgstudio_3d_shapes_text_font_event.rgba`，转换图 `/tmp/mgstudio_3d_shapes_text_font_event.png` 仍是已记录的 `moon_wgsl#7` 背景输出。
- 2026-04-30 ui stack partition parity：对照 `bevy_ui::stack::UiStack` / `ui_stack_system` / `ComputedNode.stack_index`，mgstudio `UiStack` 已补齐 per-root `partition`，`ui_stack_system` 在 root traversal 后写入分区并把每个节点的 stack index 写入 `UiLayout`，供后续 render/interaction 分层继续对齐；`moon check --target native`、`moon test ui --target native`、`moon fmt`、`moon info`、`scripts/sync_bevy_wgsl.py --check` 均通过。3d_shapes 截图门禁 exit 0 并写出 `/tmp/mgstudio_3d_shapes_ui_stack_partition.png`，图像仍为空背景，日志 `/tmp/mgstudio_3d_shapes_ui_stack_partition.log` 继续只显示已记录的 `moon_wgsl#7` PBR binding conflict 与 tonemapping root-local `#define TONEMAPPING_PASS`。
- 2026-04-30 ui interaction-states parity：对照 `bevy_ui/src/interaction_states.rs` 与 `lib.rs::build_text_interop` observer 注册顺序，mgstudio 补齐 `InteractionDisabled`、`Pressed`、`Checkable`、`Checked` 组件与 disabled/toggled AccessKit 更新 observer，并在 `a11y` 包补齐相应 `AccessibilityNode` 转发 API。`moon check --target native`、`moon test ui --target native`、`moon test a11y --target native`、`moon fmt`、`moon info`、`scripts/sync_bevy_wgsl.py --check` 均通过；3d_shapes 截图门禁 exit 0 并写出 `/tmp/mgstudio_3d_shapes_ui_interaction_states.rgba`，转换图 `/tmp/mgstudio_3d_shapes_ui_interaction_states.png` 仍是已记录的 `moon_wgsl#7` 背景输出。
- 2026-04-30 Feathers token/dark-theme parity：对照 `bevy_feathers::tokens` / `dark_theme::create_dark_theme`，mgstudio 已补齐 checkbox/radio/switch 的 hover/disabled/checked token 常量，并把 dark theme 表扩展到 Bevy 的完整状态矩阵；hover/pressed/alpha 色值按 Bevy OKLCHA palette + `lighter/with_alpha` 结果固定为 hex/alpha 字符串。`moon check --target native`、`moon test feathers --target native`、`moon fmt`、`moon info`、`scripts/sync_bevy_wgsl.py --check` 均通过。3d_shapes 截图门禁 exit 0 并写出 `/tmp/mgstudio_3d_shapes_feathers_tokens.png`，图像仍为空背景，日志 `/tmp/mgstudio_3d_shapes_feathers_tokens.log` 继续只显示已记录的 `moon_wgsl#7` PBR binding conflict 与 tonemapping root-local `#define TONEMAPPING_PASS`。
- 2026-04-30 Feathers button props / rounded-corners parity: compared with `bevy_feathers/src/controls/button.rs` and `rounded_corners.rs`; `ButtonVariant::Default` / `ButtonProps.disabled` were removed, `ButtonVariant::Normal`, `ButtonProps.corners`, and the full Bevy `RoundedCorners` matrix (`TopLeft/TopRight/BottomRight/BottomLeft`) were added through a leaf package to avoid the MoonBit `feathers -> controls -> feathers` package cycle. `moon check --target native`, `moon fmt`, `moon info`, `moon test feathers --target native`, `moon test feathers/rounded_corners --target native`, `moon test feathers/controls --target native`, `moon test render/renderer --target native`, `moon test anti_alias/smaa --target native`, `moon test material --target native`, `moon test ui --target native`, `moon test window --target native`, `moon test winit --target native`, and `scripts/sync_bevy_wgsl.py --check` pass with no warnings. 3d_shapes exits 0 and writes `/tmp/mgstudio_3d_shapes_feathers_button_props.rgba`; converted `/tmp/mgstudio_3d_shapes_feathers_button_props.png` is still the known single-color background because `moon_wgsl#7` continues to emit the PBR imported global binding conflict and tonemapping root-local `#define TONEMAPPING_PASS`.
- 2026-04-30 Feathers selection controls state-props removal: compared with `bevy_feathers/src/controls/{checkbox,radio,toggle_switch}.rs`; mgstudio removed local `CheckboxProps`, `RadioProps`, and `ToggleSwitchProps` because Bevy expresses checked/disabled state as `Checked` and `InteractionDisabled` components, not construction props. `moon check --target native`, `moon test feathers --target native`, `moon test feathers/controls --target native`, `moon fmt`, `moon info`, and `scripts/sync_bevy_wgsl.py --check` pass with no warnings. 3d_shapes exits 0 and writes `/tmp/mgstudio_3d_shapes_feathers_selection_props.rgba`; converted `/tmp/mgstudio_3d_shapes_feathers_selection_props.png` remains the known single-color `moon_wgsl#7` blocker output.
- 2026-04-30 Feathers color controls data parity: compared with `bevy_feathers/src/controls/{color_slider,color_plane,color_swatch}.rs`; mgstudio replaced local hex/HSV placeholders with Bevy-shaped `ColorChannel` variants, `SliderBaseColor(Color)`, `ColorSlider { channel }`, `ColorPlane` variants/default, `ColorPlaneValue(Vec3)`, and `ColorSwatchValue(Color)`, plus source-equivalent `range` and gradient endpoint helpers. `moon check --target native`, `moon test feathers --target native`, `moon test feathers/controls --target native`, `moon fmt`, `moon info`, and `scripts/sync_bevy_wgsl.py --check` pass with no warnings. 3d_shapes exits 0 and writes `/tmp/mgstudio_3d_shapes_feathers_color_controls.rgba`; converted `/tmp/mgstudio_3d_shapes_feathers_color_controls.png` remains the known single-color `moon_wgsl#7` blocker output.
- 2026-04-29 fullscreen postprocess cleanup：Blit/FXAA/CAS/Bloom/MotionBlur/EffectStack/CustomPostprocess3d 均改为 Bevy 同构的 `FullscreenShader` vertex module + fragment shader module，不再把 fullscreen WGSL 手工拼进 fragment source；EffectStack 也改回 `post_process.wgsl` 原始 import graph。Blit layout/sampler 修正为 Bevy 的 unfilterable texture + non-filtering nearest sampler，MotionBlur 改为显式 bind-group layout + pipeline-owned sampler，不再依赖 pipeline 反射 layout 或 scene texture sampler。
- 2026-04-29 MotionBlur component parity：对照 `bevy_post_process/src/motion_blur/mod.rs` 的 `#[require(DepthPrepass, MotionVectorPrepass)]`，mgstudio 在 `register_render3d_required_components` 中为 `MotionBlur` 补齐 required-components；`examples/3d/motion_blur` 已恢复通过 `pbr` public API 插入真实 `MotionBlur` 组件，不再保持 native no-op guard。
- 2026-04-29 PostProcess root shader library：对照 `bevy_post_process/src/lib.rs` 的 `load_shader_library!(..., "gaussian_blur.wgsl")`，mgstudio `post_process` 根包已注册原版 `post_process/gaussian_blur.wgsl`，render plugin startup 会统一加载，供 DOF/custom postprocess import graph 使用。
- 2026-04-29 MsaaWriteback data model：对照 `bevy_camera/src/clear_color.rs` + `bevy_post_process/src/msaa_writeback.rs`，`MsaaWriteback` 已从本地 `enabled: Bool` 结构体改为 `Off/Auto/Always` 枚举，默认 `Auto`，`msaa_writeback_required` 使用 Bevy 的 `samples > 1 && (Always || Auto && sorted_camera_index > 0)` 判定。

### WESL 对齐 (2026-04-15)
- `.wesl` 现已走 Bevy 语义链路：`Shader::from_wesl` + `Source::Wesl` + shader asset loader + `ShaderCache` resolver-backed compile-to-WGSL。
- runtime `.wesl` 路径不再回退到 `.wgsl` 文件；当前在 source-world startup/prepare 边界完成 asset-backed compile 并把结果注册回 runtime shader registry。
- 上述“前移到 source-world”属于 render/shader 包拆分下的接线重排，不是语义 blocker；`examples/shader/shader_material_wesl` 已恢复为 `custom_material.wesl` 并显式加载 `util.wesl`。
- `wesl` 现已拆分为外部模块 `Milky2018/moon_wesl`，并继续承载 Bevy 当前依赖到的关键编译语义：lazy import resolution、entrypoint-rooted stripping、`@publish` re-export 解析、以及 alias/global-const lowering；剩余差距集中在 upstream `wesl` 的更深 AST/validation/eval 能力，而不再是当前 mgstudio runtime 所需主链路。

### Remaining 非 naga_oil 定制 WGSL (显式跟踪)
| 路径 | 状态 | 备注 |
|---|---|---|
| N/A | 已清零 | 2026-04-29 已删除本轮追踪的 6 个自造 shader；剩余 WGSL 应通过 `scripts/sync_bevy_wgsl.py --check` 保持与 `./bevy` 原文一致。 |

- [x] `render/wgpu_mbt`: upgraded to `0.12.5`; Bevy meshlet required feature-name constants (`TEXTURE_INT64_ATOMIC`, `TEXTURE_ATOMIC`, `SHADER_INT64`, `SUBGROUP`, `IMMEDIATES`) are now exposed, so the meshlet required-feature gate no longer needs local parity workarounds (verified against `moonbit-community/wgpu-mbt#11`, which is closed).
- [x] `render/wgpu_mbt`: upgraded to `0.12.5`; visibility-range read-only binding fallback now uses `Device::get_supported_read_only_binding_type_u32(...)` instead of backend-type heuristics, matching Bevy's capability-query shape (upstream tracker `moonbit-community/wgpu-mbt#12` is still open, but the API is present and mgstudio now consumes it).
- [x] `render/wgpu_mbt`: upgraded to `0.12.5`; `Device::pop_error_scope_sync_result(...)` now exposes validation messages, so mesh3d shader/pipeline creation errors no longer require local `.mooncakes` C-stub debugging patches (verified against `moonbit-community/wgpu-mbt#14`).
- [x] `render/wgpu_mbt`: upgraded to `0.12.6`; zero-entry bind groups are now created through public `BindGroupBuilder::new(max_entries=0UL).finish(...)`, so the temporary descriptor FFI bridge from `moonbit-community/wgpu-mbt#16` has been removed.
- [ ] `render/wgpu_mbt` (deferred, upstream): mesh-shader capability surface is still not exposed in `wgpu_mbt`, so `meshlet_adapter_supports_mesh_shader()` remains conservative `false` instead of Bevy-equivalent adapter capability reporting.
- [x] `render/pbr/sprite`: embedded WGSL registration no longer relies on generated top-level side effects; runtime-owning shaders now register from public plugin install paths (`render_plugin`, `pbr_plugin_with`, `render2d_plugin`, `ui_render_plugin`, `gpu_mesh_preprocess_plugin_install_with`), and mismatched child-package embeds (`post_process/auto_exposure`, `sprite_render/sprite_mesh`, `sprite_render/tilemap_chunk`) were folded back into their owning plugin packages.
- [x] `scene/sprite` runtime blocker resolved: `moon test scene --target native` now passes after moving `render2d` shader startup registration to sprite-owned embedded WGSL sources (no cross-package fallback list).
- [x] `window`: `Window` now mirrors Bevy cursor-position state (`size/physical_size/cursor_position/physical_cursor_position/set_*`) and cursor moved/left events write back window-local physical cursor state before emission, matching `bevy_winit::state`.
- [x] `window`: `WindowResolution` now mirrors Bevy scale-factor semantics for `scale_factor/base_scale_factor/set_scale_factor/set_scale_factor_and_apply_to_physical_size`; scale-factor accessors preserve raw values and creation-time scale application updates physical size exactly like `bevy_window::WindowResolution`.
- [x] `text`: `FontHinting` now follows Bevy component/default/pipeline/font-atlas semantics for `Text2d` and UI `Text`; bare UI tests install hierarchy relationship metadata in test init so test worlds match plugin-maintained relationship assumptions without production fallback logic.
- [x] `text`: `Justify::Start/End` are now present in the public text API and ECS equality surface; `End` maps to `moon_cosmic::Align::End`, while `Start` keeps an explicit LTR-compatible bridge until upstream exposes direction-aware alignment.
- [x] `gizmos`: retained `GizmoAsset`/`Gizmo` component ownership now mirrors Bevy's asset-backed retained gizmo model at the ECS/plugin layer, including plugin-owned `Assets<GizmoAsset>`, `Gizmo -> Transform` required-components, Bevy-shaped `GizmoBuffer` line-list/line-strip positions/colors, render extraction into `LineGizmoUniform` + `GizmoMeshConfig` + `MainEntity` + `TemporaryRenderEntity` via collect-then-spawn_batch semantics, dashed-line invalid scale handling that warns once while preserving the original values, retained asset/buffer delegates for Bevy line/ray/loop/rect/cube/cross/2d/gradient helper paths, retained circle/ellipse/arc/short-long arc/sphere/arrow/axes pending-builder semantics with mixed primitive call-order preservation, and the Bevy/glam math helpers required by those builders; `moon test math`, `moon test gizmos`, `moon test gizmos_render`, `moon check`, `moon fmt`, `moon info`, and WGSL sync pass, while 3d_shapes remains the known `moon_wgsl#7` blank-screen blocker (`/tmp/mgstudio_3d_shapes_gizmo_arrow_builder.png`).
- [x] `ui`: `UiRect::new`, `px`, `percent`, `horizontal`, `vertical`, `axes`, and side helpers now mirror Bevy `bevy_ui::geometry::UiRect` ordering and default semantics (`left/right/top/bottom`), with updated call sites and white-box coverage; `moon test ui`, `moon check`, `moon fmt`, `moon info`, and WGSL sync pass, while the 3d_shapes screenshot gate still captures only the known `moon_wgsl#7` blank-screen blocker (`/tmp/mgstudio_3d_shapes_uirect.log`).
- [x] `ui`: `Val` and `BorderRadius` geometry helpers now mirror Bevy semantics for viewport units, zero-unit equality, parse/resolve arithmetic, `Val -> UiRect` side constructors, `UiRect::with_*`, and `BorderRadius` corner/edge/with/resolve helpers; `moon test ui`, `moon check`, `moon fmt`, `moon info`, and WGSL sync pass, while 3d_shapes remains the same `moon_wgsl#7` blank-screen blocker (`/tmp/mgstudio_3d_shapes_val_geometry.log`).
- [x] `ui`: `Node` now mirrors Bevy-owned `box_sizing`/`border_radius`/`scrollbar_width` data shape, ui-render consumes `node.border_radius`, and example call sites no longer use a standalone `BorderRadius` ECS component; tests/check/info/WGSL sync pass, while 3d_shapes remains the known blank `moon_wgsl#7` blocker (`/tmp/mgstudio_3d_shapes_node_fields_final.log`); `BoxSizing::ContentBox` runtime layout remains blocked on `moonbit-community/moon_taffy#9`.
- [x] `ui`: `UiLayout` now carries Bevy-style computed border, padding, resolved border radius, and outline geometry from `ui_layout_system`; `ui_render` consumes those computed fields instead of re-resolving `Node.border_radius`/outline on extraction. Targeted `moon test ui` and `moon test ui_render` pass; 3d_shapes gate remains the known `moon_wgsl#7` shader-composition blocker (`/tmp/mgstudio_3d_shapes_ui_computed_layout.log`).
- [x] `ui`: `UiLayout` now exposes Bevy `ComputedNode`-style box helpers (`is_empty`, `outlined_node_size`, `content_inset`, `border_box`, `padding_box`, `content_box`, `resolve_clip_rect`), and `ui_clip_system` consumes computed layout border/padding instead of reparsing `Node` style; 3d_shapes remains the known `moon_wgsl#7` blocker (`/tmp/mgstudio_3d_shapes_ui_layout_clip.log`).
- [x] `ui`: `UiStack` now mirrors Bevy's per-root partition list and `ui_stack_system` writes computed stack indices into `UiLayout`, matching `ComputedNode.stack_index` update semantics at the data-model level.
- [x] `feathers`: design tokens and dark-theme color table now cover Bevy's hover/pressed/disabled/checked state matrix for the shipped controls.
- [x] `feathers`: `ButtonVariant`, `ButtonProps`, and `RoundedCorners` now mirror Bevy's public data model; the implementation is split into a cycle-free leaf package while preserving the parent package API boundary.
- [x] `feathers`: checkbox/radio/toggle-switch template state now follows Bevy's component-owned model instead of local construction props.
- [x] `feathers`: color slider/plane/swatch control data now uses Bevy-shaped `Color`, `Vec3`, and HSL/RGB channel models instead of local string/HSV placeholders.
- [ ] `ui/moon_taffy` (upstream): `moon_taffy@0.2.0` lacks `Style.box_sizing` / `Style::with_box_sizing`, so mgstudio cannot pass Bevy `Node.box_sizing` into layout without local workaround; upstream issue `moonbit-community/moon_taffy#9` tracks this.
- [ ] `ui/moon_taffy` (upstream): `moon_taffy@0.2.0` exposes only `Layout.location/size`, while Bevy UI consumes computed `content_size`, `border`, `padding`, and `scrollbar_size` from layout; upstream issue `moonbit-community/moon_taffy#10` tracks exposing these fields/accessors.
- [ ] `text`: keep behavior parity deltas explicit and minimized (archived: `moon_cosmic` bidi linearization starvation no longer reproducible on `0.3.0`; remaining work is visual/script-coverage parity, not startup blocker).
- [ ] `text/moon_cosmic` (upstream): Bevy `Justify::Start` cannot be completed 1:1 yet because `moon_cosmic@0.3.0` exposes `Align::End` but not `Align::Start`; upstream issue `moonbit-community/moon_cosmic#4` tracks adding direction-aware `Start` alignment.
- [x] `physics2d/physics3d`: local `./bevy` does not contain `bevy_rapier`; treat physics as third-party Rapier integration rather than a Bevy core package. 2D/3D plugins now expose explicit fixed-schedule system sets, set ordering, and stable profiler names; `moon test physics2d --target native` and `moon test physics3d --target native` pass.
- [ ] `stress_tests`: keep source-level convergence tracker from full-suite baseline (`/tmp/mgstudio_stress_full_20260414/results.tsv`) and per-case traces (e.g. `/tmp/mgstudio_trace_many_foxes_20260414`).
- [x] Screenshot parity runner lifecycle issue (resolved 2026-04-19): parity capture now exposes a runtime completion flag and `winit` runner closes all windows once `MGSTUDIO_PARITY_CAPTURE_RGBA8_BLOB` is written, so `MGSTUDIO_SCREENSHOT_DEBUG=1 ... moon -C mgstudio-engine run examples/3d/3d_shapes --release` exits automatically without manual `pkill`.
- [ ] `window` (upstream, Milky2018/window): `window/core` surface does not yet expose a backend-agnostic cursor hit-test setter/getter (Bevy/winit-equivalent `set_cursor_hittest` style API), so mgstudio currently uses backend-specific `window/macos` calls inside `windowing_native` as a workaround.
- [ ] `window` (upstream, Milky2018/window): `RequestError` only exposes coarse variants (`NotSupported`/`Ignored`/`Os`) without structured OS error payload, so mgstudio cannot provide Bevy/winit-equivalent detailed error diagnostics for cursor-grab / cursor-hit-test failures.
