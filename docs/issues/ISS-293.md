# ISS-293: Fix volumetric_fog visual reference hull dominance group

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, visual-parity, render, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-06
- External ref: none

## Description
After the `volumetric_fog` example's Bevy-shaped scene, controls, and
`VolumetricLight` toggles were restored, the final visual gate still fails.
The fresh mgstudio capture
`/tmp/mgstudio_iss292_volumetric_fog_toggle.png` is nonblank and has an empty
run log, but it is visually dominated by a large camera-adjacent surface or fog
hull. It does not match the existing checked-in
`mgstudio-engine/examples/3d/volumetric_fog/reference.png` and cannot be
accepted as the final reference.

This is a local render/PBR/example parity gap, not an external blocker and not
a reason to refresh `reference.png`.

## Design
Use this issue as a grouping issue only. The executable work is split into
smaller source-owner issues:

- ISS-294 records the Bevy visual baseline and current mgstudio failure packet.
- ISS-295 audits and fixes `VolumetricFog`/`FogVolume` defaults and scene input
  parity.
- ISS-296 audits and fixes volumetric fog pipeline variant selection and draw
  routing.
- ISS-297 audits and fixes camera, clip, depth, and uniform packing parity.
- ISS-298 captures the repaired mgstudio output and proves the oversized
  hull/surface symptom is gone.

Do not hide the issue by changing capture timing, disabling fog, changing the
example camera, or accepting a plausible but wrong screenshot.

## Acceptance Criteria
- [x] ISS-294, ISS-295, ISS-296, ISS-297, and ISS-298 are closed.
- [x] The root cause is assigned to a concrete source-owner boundary instead of
      being described only as a screenshot delta.
- [x] ISS-224 remained open until the fixed screenshot was accepted and
      `reference.png` was refreshed.

## Relationships
- Depends on: ISS-294, ISS-295, ISS-296, ISS-297, ISS-298
- Parent: ISS-224
- Related: ISS-104, ISS-108, ISS-191, ISS-294, ISS-295, ISS-296, ISS-297, ISS-298
- Discovered from: ISS-224

## Notes
- 2026-07-05: Initial visual review packet:
  - Scope: `examples/3d/volumetric_fog`.
  - Bevy source owner: `bevy/examples/3d/volumetric_fog.rs` plus
    `bevy/crates/bevy_pbr/src/volumetric_fog/{mod.rs,render.rs}`.
  - Mgstudio artifact: `/tmp/mgstudio_iss292_volumetric_fog_toggle.png`.
  - Bevy reference artifact: pending; current checked-in mgstudio reference is
    `mgstudio-engine/examples/3d/volumetric_fog/reference.png`.
  - Human judgment: `FAIL` for refreshing the reference from the current
    capture.
  - Visible symptom: the current capture is mostly a flat gray/white
    camera-adjacent surface or fog hull with only small ceiling-slot detail,
    unlike the checked-in street-scene reference.
  - Severity: `MAJOR`.
  - Logs: `/tmp/mgstudio_iss292_volumetric_fog_toggle.run.log`; summary:
    empty, no fatal or missing-resource text.
  - Comparison artifacts:
    `/tmp/mgstudio_iss224_volumetric_fog_side_by_side.png` and
    `/tmp/mgstudio_iss224_volumetric_fog_diff.png`.
  - Diff metrics: same size `2560x1440`; changed pixels `3686400/3686400`;
    diff mean RGBA `[111.78585259331597, 120.69678629557292,
    114.29501925998264, 0.0]`.
- 2026-07-05: Split into ISS-294 through ISS-298 so visual evidence, source
  input parity, pipeline routing, uniform math, and final repaired capture can
  be worked and verified independently.
- 2026-07-06: ISS-294 through ISS-298 are closed. ISS-393 records the exact
  source-owner root cause and repair, and ISS-224 records the accepted
  `reference.png` refresh.

## Close Notes
Closed on 2026-07-06.

The visual reference hull-dominance group is complete. The final accepted
output comes from the Bevy-shaped tangent-generation repair in ISS-393, not a
camera change, asset edit, fog bypass, or screenshot workaround. ISS-224 now
owns and closes the refreshed checked-in reference.
