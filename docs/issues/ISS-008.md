# ISS-008: Make previous transform sync source-equivalent and bounded

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: performance, many_foxes, transform, pbr, render
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-04
- External ref: none

## Description
Previous transform sync currently costs about `0.998 ms/frame`. It is smaller
than the top systems, but it is an always-on per-frame write path and may
amplify change tracking pressure after animation and transform propagation.

## Design
Compare the current previous-transform ownership model with Bevy's mesh/PBR
source and port the update conditions directly. The path should not update every
existing `PreviousGlobalTransform` unless the source semantics require it.

## Acceptance Criteria
- [x] The system reports how many `PreviousGlobalTransform` components are scanned and written each frame.
- [x] The update condition matches Bevy's prepass or motion-vector ownership model.
- [x] Unchanged previous-transform data does not create unnecessary ECS changed-state pressure.
- [x] At `1000` foxes, previous transform sync is reduced below `0.3 ms/frame` or the remaining cost is justified by source-equivalent semantics.
- [x] Relevant PBR and transform tests pass with `--release`.

## Relationships
- Depends on: ISS-002
- Parent: ISS-001
- Related: ISS-003, ISS-004, ISS-007
- Discovered from: none

## Notes
- Current evidence: `pbr/mesh_material.mbt:101=0.998 ms/frame`.
- 2026-06-04: Ported the Bevy `update_mesh_previous_global_transforms` update condition: missing previous transforms are inserted, and existing `PreviousGlobalTransform` components are only rewritten when the source `GlobalTransform` changed after the previous component's last change tick.
- 2026-06-04: Added trace counters for `pbr.previous_global_transform.existing_scanned`, `existing_written`, `missing_scanned`, and `missing_inserted`.
- 2026-06-04: Reliable many_foxes profile at `/tmp/mgstudio_many_foxes_iss008_previous_transform_sync/frametime_breakdown.md`: `pbr/mesh_material.mbt:101=0.680 ms/frame`, with `existing_scanned=1001/frame`, `existing_written=1000/frame`, `missing_scanned=0/frame`, and `missing_inserted=0/frame`.
- 2026-06-04: The remaining cost is source-equivalent for this example because the 1000 animated fox mesh entities receive changed `GlobalTransform` values every frame; the unchanged non-fox mesh is scanned but not rewritten.

## Close Notes
- Closed after `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine test pbr --release`, `moon -C mgstudio-engine test transform --release`, and the many_foxes profile listed above.
