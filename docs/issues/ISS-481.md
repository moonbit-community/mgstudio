# ISS-481: Regenerate post-ISS-475 stale example references

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: examples, screenshots, visual-parity, tonemapping, capture, bevy-sync
- Assignee: unassigned
- Created: 2026-08-04
- Updated: 2026-08-04
- External ref: none

## Description

ISS-475 (closed 2026-08-02) removed the wrong required-component registration
that forced `Tonemapping::TonyMcMapface` and `DebandDither::Enabled` onto every
`Camera3d`. The pinned Bevy baseline requires neither, so a plain `Camera3d`
spawn now renders with no tonemapping and no dither — matching Bevy. That
change was validated against an actual pinned-Bevy capture on
`examples/gizmos/3d_text_gizmos` (mean_abs_rgb 2.608 -> 0.186).

Every checked-in `reference.png` captured before that change with a `Camera3d`
in the scene still shows the old forced-tonemapped output. No reference has
been regenerated since (`git log --since=2026-08-01 -- '*reference.png'` is
empty). ISS-475 left exactly this as its one open acceptance criterion:
the stale references must be re-judged by a human and regenerated.

Enumeration at de16299c2:

- 405 `reference.png` files are tracked in total.
- 170 example packages mention `Camera3d` in `main.mbt`; 169 of them have a
  tracked `reference.png` (`examples/gizmos/transform_gizmo` has none).
- 13 packages set `Tonemapping` explicitly and none of them set
  `DebandDither`, so even those changed output through the dither default
  (`Enabled` -> absent -> `Disabled`): `3d/atmosphere`, `3d/bloom_3d`,
  `3d/clearcoat`, `3d/color_grading`, `3d/contact_shadows`,
  `3d/depth_of_field`, `3d/post_processing`, `3d/reflection_probes`,
  `3d/rotate_environment_map`, `3d/tonemapping`, `3d/transmission`,
  `3d/volumetric_fog`, `math/sampling_primitives`.
- Category split of the 169: 73 `examples/3d`, 16 `examples/shader`,
  11 `examples/shader_advanced`, 9 `examples/animation`, 7
  `examples/stress_tests`, 7 `examples/gltf`, 5 `examples/transforms`,
  5 `examples/camera`, the rest spread across tools/math/gizmos/asset/window/
  picking/large_scenes and others.

A second staleness class rides along in the same references: the old UI text
scale, already observed during the `motion_blur` and `deferred_rendering`
triage in ISS-019. Regeneration resolves both classes at once.

The tonemapping source owner itself needs no further porting for this work:
`tonemapping.wgsl`, `tonemapping_shared.wgsl`, and `lut_bindings.wgsl` are
byte-identical to `bevy/crates/bevy_core_pipeline/src/tonemapping/`, the three
LUT KTX2 assets are byte-identical to Bevy's `luts/`, and the plugin, pipeline
key, node, and renderer execution surfaces are in place (ISS-128, ISS-137,
ISS-424 closed).

## Evidence

Enumeration commands (run from `mgstudio-engine`):

- `grep -rl 'Camera3d' examples --include=main.mbt` -> 170 packages.
- Intersection with existing `reference.png` -> 169.
- `grep` for explicit `Tonemapping`/`DebandDither` use -> the 13 packages
  above, all tonemapping-only.

Stale-class confirmations already recorded in ISS-019 notes: `motion_blur`
(2026-08-01) and `deferred_rendering` (2026-08-04) captures structurally match
their references with desaturated-color/gray-sun deltas exactly matching the
removed TonyMcMapface transform, plus the old UI text scale.

## Design

Regenerate in small human-reviewable batches rather than one blind sweep, per
the open ISS-475 criterion that a human re-judges the references.

Method per example:

1. Capture with the standard command from `mgstudio-engine`:
   `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
   scripts/p0_visual_capture_native.sh <package> <out.png>` (2560x1440,
   frame 90, GPU preprocess off — the same convention as all recorded gates).
2. Require a clean run log (the capture script deletes the log on clean runs)
   and a passing `scripts/visual_image_sanity.py` check.
3. Compare old vs new reference and record a review-packet row in this issue
   with the visible delta class; anything outside the expected
   tonemapping/dither/UI-scale classes stops the batch and gets its own
   triage.
4. Copy the capture over `<package>/reference.png` and commit the batch as
   `Refresh <scope> references (ISS-481)`.

Batch 1 (this issue's first slice) is the active-gate set already under
discussion: `examples/3d/tonemapping` (the tonemapping owner's representative
example), `examples/3d/motion_blur`, `examples/3d/deferred_rendering`,
`examples/3d/ssao`. The remaining ~165 wait until the batch-1 review confirms
the delta class is what ISS-475 predicts.

Out of scope: references whose examples are themselves blocked or visually
wrong for tracked non-tonemapping reasons (e.g. ISS-091 scrolling_fog,
ISS-092 skybox) — those stay pinned to their own issues and are refreshed
there, not here.

## Acceptance Criteria
- [ ] Batch 1 (tonemapping, motion_blur, deferred_rendering, ssao) is
  regenerated with clean logs and sanity passes, committed, and human-judged.
- [ ] The remaining affected references are regenerated in batches, each with
  a review-packet row and human judgment recorded here.
- [ ] ISS-475's open acceptance criterion ("re-judged by a human before
  regeneration") is satisfied and checked off there.
- [ ] No reference is refreshed for an example with an open visual-parity
  issue that predicts a non-tonemapping delta, unless that issue signs off.

## Relationships
- Related: ISS-475 (the source change that made the references stale; carries
  the human re-judgment criterion this issue discharges).
- Related: ISS-019 (motion_blur and deferred_rendering stale-reference notes;
  its A/B gates compare parent captures, not references, so they are
  unaffected by regeneration).
- Related: ISS-060, ISS-157 (earlier reference-refresh and audit effort
  conventions).

## Review Packet

### VIS-481-batch1

- Scope: `examples/3d/tonemapping`, `examples/3d/motion_blur`,
  `examples/3d/deferred_rendering`, `examples/3d/ssao`
- Bevy source owner: `bevy/crates/bevy_core_pipeline/src/tonemapping/`
  (default-camera post-processing), per-example owners otherwise unchanged
- Human judgment: `PENDING`
- Rows added per example as captures land.

#### examples/3d/tonemapping

- Old vs new: mean_abs_rgb 15.945, 99.7% pixels changed, 50.7% changed
  strongly (luma diff >= 16).
- Visible deltas, both traced to the ISS-475 class:
  1. Colors: highlights now clip hard (bottom-right swatch region saturates
     to near-white with stepped edges) instead of rolling off through
     TonyMcMapface. Expected: the camera renders un-tonemapped.
  2. UI text absent (only the scene's dark backdrop remains). This is
     faithful to the pinned baseline: `bevy/examples/3d/tonemapping.rs`
     spawns the camera with NO `Tonemapping` component, and both
     `toggle_tonemapping_method` (`Single<&mut Tonemapping>`) and
     `update_ui` (`Single<(&Tonemapping, &ColorGrading)>`) skip when no
     entity matches — pinned Bevy itself renders this example with an empty
     `Text::default()` and dead toggle keys. The mgstudio port mirrors the
     spawn (no Tonemapping) and the Single-skip semantics.
- Capture: frame 90, empty run log, sanity pass (mean_luma 147.8, center
  122.5, non-black 100%).
- Judgment: `PENDING` (accepted into reference by this batch, revert if the
  human review disagrees).

#### examples/3d/deferred_rendering

- Old vs new: mean_abs_rgb 23.238, 100% pixels changed, 50.3% strongly.
- New capture is byte-identical to the 2026-08-04 BLOCKER-withdrawal
  verification capture (`2d0e82a7...`), confirming determinism.
- Visible deltas, both the classes recorded in ISS-019's withdrawal note:
  1. Colors: desaturated helmets/spheres and the flat gray sun disk become
     saturated materials and a bright emissive sun with orange glow — the
     un-tonemapped output (ISS-475).
  2. UI: the old tiny mid-frame text moves to the correct top-left 12px
     position at current text scale.
- Scene structure identical (both flight helmets, six spheres, emissive
  voxel cube, shadows, ground).
- Capture: frame 90, empty run log, sanity pass.
- Judgment: `PENDING` (accepted into reference by this batch, revert if the
  human review disagrees).

#### examples/3d/motion_blur

- Old vs new: mean_abs_rgb 33.190, 99.2% pixels changed, 77.6% strongly.
- Visible deltas, both traced to recorded classes:
  1. Colors: sky desaturated slate -> vivid cyan, saturated trees/car
     (ISS-475 tonemapping removal).
  2. Motion-blur footprints: wheel blur and the fast roadside objects smear
     differently — the ISS-019 `PreviousGlobalTransform` snapshot fix
     changed motion vectors; current output is the gate-verified state
     (byte-identical A/B at 942d09476).
- UI text renders correctly at the current scale, confirming text rendering
  is healthy (the tonemapping example's missing text is its own
  Single-gating, not a text regression).
- Capture: frame 90, empty run log, sanity pass.
- Judgment: `PENDING` (accepted into reference by this batch, revert if the
  human review disagrees).

#### examples/3d/ssao

- Old vs new: mean_abs_rgb 11.620, 99.9% pixels changed, 12.4% strongly.
- New capture is byte-identical to the long-stable ISS-019 ssao gate state
  (`367e3e5647317c41...`), which every recent slice A/B-verified against its
  parent commit.
- Visible deltas: the old reference is nearly unlit — flat dark silhouettes
  with only faint AO — while the new shows directional face shading, cast
  shadows, and crevice AO, plus the un-tonemapped brightness. The old
  reference predates both the ISS-475 change and the verified lighting/SSAO
  repair arc; the gate hash is the canonical current output.
- Capture: frame 90, empty run log, sanity pass.
- Judgment: `PENDING` (accepted into reference by this batch, revert if the
  human review disagrees).

## Notes

- 2026-08-04: Filed after verifying zero references were regenerated since
  ISS-475 landed, and after auditing the tonemapping owner as
  no-code-work-needed (shaders and LUTs byte-identical, surfaces present).
- 2026-08-04: Batch 1 captured and installed. All four captures had empty
  run logs and passed image sanity; deferred_rendering and ssao are
  byte-identical to their already-verified gate captures, so the refresh
  introduces no new pixels beyond what the gates already pinned. Every
  observed delta traced to a recorded class (ISS-475 tonemapping/dither,
  current UI text scale, ISS-019 motion-vector fix, the verified
  lighting/SSAO repair arc); nothing anomalous surfaced. Human judgment of
  the four rows above remains PENDING; the remaining ~165 references wait
  for that review before batch 2 proceeds.
