# ISS-081: Fix ssr scene and screen-space reflection parity before reference refresh

## Metadata
- Type: bug
- Status: in_progress
- Priority: 1
- Labels: examples, pbr, ssr, deferred, screen-space-reflections, visual-parity, source-parity
- Assignee: unassigned
- Created: 2026-06-13
- Updated: 2026-06-14
- External ref: none

## Description
`examples/3d/ssr/reference.png` is still an unusable transparent baseline. A
standard native capture of the current example produced a numerically nonblank
image, but manual review rejected it: the frame is effectively a dark scene with
only a sparse diagonal line, not the visible Bevy SSR scene.

This is not just a screenshot artifact. Bevy's `examples/3d/ssr.rs` is a rich
deferred-rendering example built around `ScreenSpaceReflections`,
`DefaultOpaqueRendererMethod`, `ExtendedMaterial<StandardMaterial, Water>`, a
custom water WGSL extension, sky/environment lighting, model/base selection, and
UI controls. Mgstudio's current `examples/3d/ssr/main.mbt` explicitly states
that Bevy's custom deferred water material extension is represented by closest
available standard-material behavior. That is a local approximation and must not
be counted as source or visual parity.

## Design
Audit and port Bevy `examples/3d/ssr.rs` and the required PBR/render owners as
the migration unit. Preserve Bevy's source shape as closely as MoonBit allows:

- `ScreenSpaceReflections` component setup and runtime updates;
- `DefaultOpaqueRendererMethod::deferred()` behavior for the camera and
  materials;
- `ExtendedMaterial` / `MaterialExtension` support needed by the custom
  `Water` material;
- `WaterSettings` uniform data and `shaders/water_material.wgsl` binding shape;
- skybox/environment lighting and displayed model/base setup;
- Bevy-shaped UI control state and input flow.

Do not accept a dark capture, standard-material water approximation, or
SSR-disabled render path as parity. If a MoonBit language or dependency
capability truly prevents a direct port, record that blocker precisely before
leaving the source shape partial.

## Acceptance Criteria
- [ ] Every source-shape anchor in Bevy `examples/3d/ssr.rs` is classified as
      complete, partial, facade, blocked, or intentionally excluded.
- [x] The custom deferred water material extension is ported, or a precise
      language/dependency blocker records why it cannot be expressed yet.
- [x] The rendered scene visibly contains the Bevy-equivalent base, model, and
      screen-space reflection signal instead of a dark sparse-line frame.
- [ ] `examples/3d/ssr/reference.png` is refreshed only after the visual signal
      is present and the approximation note is removed or replaced by a precise
      blocker.
- [x] The standard native capture command succeeds without validation, fatal,
      or missing-resource output.

## Relationships
- Depends on: none
- Parent: ISS-060
- Related: ISS-012, ISS-019, ISS-021
- Discovered from: ISS-060

## Notes
- 2026-06-13: Standard capture command completed and produced
  `alpha_mean=1`, `gray_mean=0.387916`, `size=2560x1440`,
  `non_black_ratio=1.0`, and `mean_luma=46.18836278955987`. Manual review
  rejected the output because it lacked the expected Bevy SSR scene and
  reflection signal. The generated reference image was discarded.
- 2026-06-14: Current standard capture now shows visible model/base/water and
  screen-space reflection signal at `/tmp/mgstudio_iss081_ssr_current.png`, so
  the old dark sparse-line symptom is stale. The remaining parity blocker is
  local source/runtime shape, not a screenshot wrapper issue: the example still
  uses `StandardMaterial` normal-map behavior for water instead of Bevy's
  `ExtendedMaterial<StandardMaterial, Water>` with `WaterSettings` and bindings
  100/101/102. The shader asset is already identical to Bevy's
  `assets/shaders/water_material.wgsl`; the missing local owner is a
  material-extension bind group data path that can carry extra layout entries,
  texture/sampler bindings, and uniform bytes from `MeshMaterial3d` through
  `MeshGpuDrawPayload3d` into `mesh3d_material_cache_get_or_create`.
- 2026-06-14: Removed another local approximation from the SSR example:
  `spawn_capsules` now uses Bevy's `Capsule3d::new(0.4, 0.5)` shape with a
  hidden parent entity and five child capsule meshes instead of a handwritten UV
  sphere helper. Re-ran the standard 90-frame native capture to
  `/tmp/mgstudio_iss081_ssr_capsules.png`; the visible model/base/water SSR
  scene still renders. The checked-in reference is still not refreshed because
  the Water `ExtendedMaterial` bind-group path remains partial.
- 2026-06-14: Ported the missing Water `ExtendedMaterial` bind-group path rather
  than keeping the standard-material water approximation. `MaterialExtension`
  metadata now carries Bevy-shaped extension layout entries and typed
  per-material binding resources; PBR resolves those into texture/sampler ids
  and uniform bytes in `MeshGpuDrawPayload3d`; the renderer includes that data in
  material bind-group cache keys and creates the extra bindings in group 3.
  `examples/3d/ssr` now defines `Water`, `WaterSettings`, the deferred fragment
  shader path, and bindings 100/101/102 matching Bevy's `ssr.rs`; the old CPU
  water-material animation system was removed because Bevy animates through
  `globals.time` in `water_material.wgsl`. Re-ran
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/ssr /tmp/mgstudio_iss081_ssr_water_payload.png`;
  the capture succeeds and shows the expected reflected water surface, model,
  and base. The reference image is still not refreshed because the full
  source-shape anchor classification remains open.
- 2026-06-14: Replaced the SSR example's local keyboard/text-menu settings path
  with the Bevy-shaped helper widget flow: `ExampleSetting`,
  `WidgetClickEvent<ExampleSetting>`, option buttons, range adjustment rows,
  radio-button state refresh, and range-value text refresh now drive the
  `AppSettings` resource. Camera movement was also moved back toward Bevy's
  source shape by reading keyboard/mouse-wheel input into camera `Transform`
  updates instead of maintaining a separate radius/theta state. Native check
  passed and the standard screenshot command produced
  `/tmp/mgstudio_iss081_ssr_widget_flow_root.png`, showing the SSR scene plus
  visible widget controls. This anchor remains `partial`, not `complete`,
  because mgstudio currently needs an explicit 2D UI camera plus
  `UiTargetCamera`, and the example still keeps local entity handles where
  Bevy uses typed `Query` groups.
- 2026-06-14: Removed the remaining SSR example entity-handle cache. Cube,
  helmet, capsule, base, and water visibility now use marker components plus
  ECS queries, and model rotation uses marker queries rather than stored entity
  ids. The extra SSR thickness/step/secant fields were dropped from the example
  because Bevy's `ssr.rs` uses `ScreenSpaceReflections { ranges, ..default() }`
  and the mgstudio component defaults already carry those values. A first
  attempt to express cube and helmet rotation as two `Write<Transform>` system
  params exposed a current ECS validation limitation, so the final version uses
  a world system with marker queries to preserve source ownership without
  reintroducing cached entities. Verified with native check and
  `/tmp/mgstudio_iss081_ssr_marker_queries_fixed.png`.
- 2026-06-14: Fixed the hidden FlightHelmet leak in the SSR default Cube view by
  aligning scene/visibility ownership with Bevy instead of adding an example
  workaround. Bevy glTF scene nodes are spawned with `Visibility::default()`;
  mgstudio scene nodes now insert `Visibility::Inherited` for every spawned
  `SceneNode3d`, including empty intermediate glTF nodes, so hidden `SceneRoot`
  state can propagate through the full hierarchy. The visibility plugin now also
  follows Bevy's `reset_view_visibility -> check_visibility ->
  mark_newly_hidden_entities_invisible` ViewVisibility pipeline, and incremental
  propagation accounts for added `Visibility`/`Parent` components plus parent
  local visibility. Verified with targeted scene/visibility tests and
  `/tmp/mgstudio_iss081_ssr_scene_node_visibility.png`, where the default Cube
  view no longer renders the hidden FlightHelmet.
- 2026-06-14: Removed SSR example source-shape drift in model/base setup rather
  than treating the rendered scene as sufficient parity. The cube material no
  longer overrides Bevy-default roughness/metallic values; capsule roughness now
  follows Bevy's `roughness.max(0.08)` expression; metallic, red, and water
  bases use unit planes scaled by `Transform::scale(Vec3::splat(100.0))`; the
  red plane now uses CSS red instead of a local muted red; duplicate MSAA/TAA
  camera inserts and non-Bevy directional/point/ambient fill lights were
  removed. Verified with native check and the standard 90-frame capture at
  `/tmp/mgstudio_iss081_ssr_source_shape_mesh_material.png`.
- 2026-06-14: Aligned the SSR settings visibility update with Bevy's
  `adjust_app_settings` semantics: selected model/base entities are now written
  as `Visibility::Visible`, while unselected ones remain `Visibility::Hidden`.
  The previous `Inherited` write rendered the same in the default unparented
  cases, but it was not source-equivalent and would differ under hidden parents.
  Verified with native check and
  `/tmp/mgstudio_iss081_ssr_visibility_visible.png`.
- 2026-06-14: Aligned the range-value UI node shape with Bevy's
  `range_controls`: the `RangeValueText` marker now lives on the value
  container node with its displayed `Text` as a child, rather than marker and
  text sharing the same entity. `update_range_value_text` now follows the
  container's `Children` and updates child text/color, matching Bevy's
  `TextUiWriter` ownership shape as closely as the current mgstudio text API
  allows. Verified with native check and
  `/tmp/mgstudio_iss081_ssr_range_value_children.png`.

## Close Notes
