# ISS-074: Restore decal example and forward decal material parity

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: examples, screenshots, visual-parity, pbr, decal, bevy-source-parity
- Assignee: unassigned
- Created: 2026-06-12
- Updated: 2026-06-16
- External ref: none

## Description
`examples/3d/decal/reference.png` is fully transparent, but a standard 90-frame native capture currently produces only a dark gray frame with no visible decal, floor, cube grid, or lighted scene content.

The mgstudio example also diverges from Bevy's `bevy/examples/3d/decal.rs` source shape. Bevy uses `ForwardDecalMaterial<StandardMaterial>` as a render asset containing both the base `StandardMaterial` and `ForwardDecalMaterialExt`, while mgstudio currently spawns a normal `StandardMaterial` plus separate `ForwardDecal` and `ForwardDecalMaterialExt` components. This may bypass the intended forward-decal material pipeline.

## Design
Audit and port the Bevy decal example and PBR decal material owner source-level shape before refreshing the reference. The MoonBit implementation should preserve Bevy's ownership model as closely as the language allows:

- the decal entity should use a decal material asset equivalent to `ForwardDecalMaterial<StandardMaterial>`,
- the example setup should match Bevy's camera, floor, obstacle grid, material, and light semantics,
- any real MoonBit or dependency limitation should be recorded as a blocker instead of hidden behind a fallback path.

## Acceptance Criteria
- [x] `examples/3d/decal` renders visible floor, obstacle grid, and decal content.
- [x] The decal material path is audited against Bevy's `ForwardDecalMaterial<StandardMaterial>` source owner.
- [x] `examples/3d/decal/reference.png` is refreshed only after the visual output is useful.
- [x] The refreshed reference is produced by `mgstudio-engine/scripts/p0_visual_capture_native.sh`.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-060
- Discovered from: ISS-060

## Notes
- 2026-06-12: Standard command `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=600 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/decal mgstudio-engine/examples/3d/decal/reference.png` produced `alpha_mean=1`, `gray_mean=0.387874`, `size=2560x1440`, but the captured image was visually empty except for the dark background. The generated image was discarded.
- 2026-06-12: Bevy source checked: `bevy/examples/3d/decal.rs`.
- 2026-06-12: Fixed a concrete PBR material extraction bug: `ForwardDecalMaterialExtension::alpha_mode` now maps to Bevy's `Some(AlphaMode::Blend)`, and `render3d_material_properties_for_standard_material` now applies extension alpha-mode and opaque-render-method overrides before deriving render phase, material key, and render method.
- 2026-06-12: Updated the example to attach forward-decal extended material metadata to `MeshMaterial3d`, instead of relying only on a detached `ForwardDecalMaterialExt` component. A follow-up 90-frame capture still rendered only the dark background, so the reference image was discarded and the remaining runtime visibility gap is still open.
- 2026-06-12: Aligned the floor source shape with Bevy's `Rectangle::from_length(10.0)` plus `Quat::from_rotation_x(-FRAC_PI_2)`. A follow-up 90-frame capture still rendered only the dark background, so the remaining gap is now in runtime visibility, material extraction, or main-pass execution rather than just floor mesh orientation.
- 2026-06-12: Fixed the render queue phase owner bug: CPU queue and GPU preprocess now classify meshes from `MaterialProperties.render_phase_type`, matching Bevy's material queue logic instead of recomputing phase from the base `StandardMaterial`. This prevents extended forward-decal materials from being treated as opaque/prepass candidates when their extension-owned properties make them transparent. A clean 90-frame native capture now writes a visible reference to `mgstudio-engine/examples/3d/decal/reference.png`.
- 2026-06-12: Replaced the example-side manual extended-material metadata path with a concrete `ForwardDecalMaterial` asset family that maps Bevy's `ForwardDecalMaterial<StandardMaterial>` owner into MoonBit. The render extractor now resolves `Material3dFamily::ForwardDecal` into its base `StandardMaterial`, extension metadata, and forward-decal uniform payload.

## Close Notes
- 2026-06-12: Closed after verifying `moon -C mgstudio-engine check --target native pbr/decal`, `moon -C mgstudio-engine check --target native pbr`, `moon -C mgstudio-engine check --target native examples/3d/decal`, `moon -C mgstudio-engine test pbr/decal --release`, `moon -C mgstudio-engine test pbr --release`, and `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=600 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/decal mgstudio-engine/examples/3d/decal/reference.png`.
- 2026-06-16: Kept closed as a visual/runtime repair issue, but the stricter ISS-105 symbol audit classifies `bevy/examples/3d/decal.rs` as `partial`. Remaining source-fidelity gaps include missing `Name` components, local world/helper system-param shape, local `AssetServer::new()`, no explicit `FreeCameraPlugin`, extra `Msaa::Off`/`Fxaa`, and a deterministic sine/cosine obstacle rotation formula instead of Bevy's seeded `ChaCha8Rng`.
