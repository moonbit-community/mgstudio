# ISS-268: Port fog_volumes camera and directional light inputs

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, fog-volumes, light, camera, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-06
- External ref: none

## Description
Bevy's `fog_volumes` visual result depends on the camera `Hdr` and
`VolumetricFog` setup plus a shadowed directional light with
`VolumetricLight` and high illuminance.

## Design
Port the camera and light entities directly from Bevy after ISS-225 restores
the bunny fog volume. Verify that both the light and camera fog data reach the
volumetric fog prepare path.

## Acceptance Criteria
- [ ] Directional light transform, shadow flag, illuminance, and
      `VolumetricLight` match Bevy.
- [ ] Camera transform, `Hdr`, `VolumetricFog.step_count`, and
      `ambient_intensity` match Bevy.
- [ ] Focused evidence proves camera fog and directional volumetric light data
      reach render preparation.

## Relationships
- Depends on: ISS-225
- Parent: ISS-226
- Related: ISS-104, ISS-109
- Discovered from: ISS-226

## Notes
- 2026-07-05: Split from ISS-226 so static camera/light render inputs are
  independent from runtime camera rotation evidence.

## Close Notes
- 2026-07-06: Verified the `fog_volumes` camera and directional light authoring
  matches Bevy's source: shadowed directional light at `(1.0, 1.0, -0.3)`
  looking at the fog center with illuminance `32000.0` and `VolumetricLight`,
  plus a `Camera3d` at `(-0.75, 1.0, 2.0)` with `Hdr` and `VolumetricFog {
  step_count: 64, ambient_intensity: 0.0, .. }`. Strengthened the example test
  to prove those inputs reach the PBR render owners: direct volumetric-fog
  uniform preparation produces one density-textured view volume, RenderApp
  storage contains one volumetric-fog view, one fog volume, and one volumetric
  light, directional light extraction returns one light, and GPU light
  collection sees one directional light. Validation: `MOONBIT_NEW_NATIVE=1 moon
  -C mgstudio-engine test examples/3d/fog_volumes --target native --release`.
