# ISS-384: Prove volumetric_fog exposure tonemapping and final copy path

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: pbr, volumetric-fog, exposure, tonemapping, color-space, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Prove that the real `examples/3d/volumetric_fog` frame applies exposure,
tonemapping, color-space conversion, and final surface copy in the same way as
Bevy after the volumetric fog pass.

## Design
Use the owner map from ISS-381 and pass-order evidence from ISS-383. Compare
the Bevy postprocess source owners against mgstudio for:

- exposure resource and default value;
- tonemapping mode and shader variant;
- HDR-to-surface format conversion;
- final copy or presentation path;
- any example-specific postprocess settings that affect the fog result.

Do not tune constants or insert local color correction.

## Acceptance Criteria
- [x] Bevy exposure, tonemapping, color-space, and final-copy source anchors
      are cited in close notes.
- [x] The corresponding mgstudio runtime values are asserted or recorded for
      the real example.
- [x] Any divergence is assigned to ISS-375 with exact owner and field name.
- [x] If no divergence is found, close with evidence that rules out color and
      final-copy composition.

## Relationships
- Depends on: ISS-381, ISS-383
- Parent: ISS-374
- Related: ISS-089, ISS-348, ISS-349
- Discovered from: ISS-374

## Notes
- 2026-07-05: Split from ISS-374 so color-space and final-copy behavior can be
  audited independently from pass target and ordering.

## Close Notes
- 2026-07-05: Bevy source anchors: `bevy/examples/3d/volumetric_fog.rs:67-74`
  spawns `Camera3d::default()`, `Tonemapping::TonyMcMapface`, and
  `Bloom::default()`. `bevy/crates/bevy_post_process/src/bloom/settings.rs:32-33`
  requires `Hdr` for `Bloom`. `bevy/crates/bevy_camera/src/camera.rs:232-280`
  defines the default `Exposure::BLENDER` value and `Exposure::exposure()`.
  `bevy/crates/bevy_core_pipeline/src/tonemapping/node.rs:26-130` returns
  before drawing when `Tonemapping::None` or the camera is not HDR, otherwise
  uses `ViewTarget::post_process_write()` and draws a fullscreen triangle into
  the destination attachment. `bevy/crates/bevy_core_pipeline/src/upscaling/node.rs:17-88`
  reads `ViewTarget::main_texture_view()` and writes the final output
  attachment returned by `ViewTarget::out_texture_color_attachment`.
- 2026-07-05: mgstudio camera execution evidence:
  `mgstudio-engine/pbr/render_execute_camera.mbt:376-435` records the real
  postprocess flags and default-resolved camera exposure before calling
  `render3d_execute_camera_postprocess`. For the real `volumetric_fog` example,
  wbtest asserts HDR enabled, Bloom enabled, tonemapping postprocess enabled,
  tonemapping stage enabled, auto exposure disabled, default exposure value,
  texture-backed scene target, and surface-backed camera target.
- 2026-07-05: mgstudio postprocess evidence:
  `mgstudio-engine/pbr/render_execute_camera_postprocess.mbt:232-462` records
  the actual source texture, Bloom stage, postprocess stage count, tonemapping
  source texture, tonemapping target, tonemapping mode, HDR flag, and
  default-resolved exposure value around the real draw calls. The test asserts
  the example uses one postprocess stage, draws Bloom, draws TonyMcMapface
  tonemapping mode `6`, keeps HDR enabled for tonemapping, and writes the final
  tonemapping stage directly to the camera target.
- 2026-07-05: Runtime assertions:
  `mgstudio-engine/examples/3d/volumetric_fog/main_wbtest.mbt:640-879`
  captures the postprocess counters, and `main_wbtest.mbt:975-994` asserts the
  expected Bevy-shaped path. It also asserts that the fallback final-copy paths
  (`final_copy_tonemapping_resolve`, `final_copy_sprite_uv`, and
  `final_source_present`) are not entered for this example.
- 2026-07-05: Verification: `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine check --target native`, and
  `moon -C mgstudio-engine test examples/3d/volumetric_fog --release` passed.
- 2026-07-05: No exposure, tonemapping, color-space, or final-copy divergence
  was found for `examples/3d/volumetric_fog`; no new external blocker is
  required.
