# ISS-135: Restore Bevy implicit UI target and WorldAssetRoot source shape

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: ui, scene, examples, source-parity, bevy-parity
- Assignee: unassigned
- Created: 2026-06-19
- Updated: 2026-06-19
- External ref: none

## Description
While closing ISS-122, `examples/3d/anti_aliasing` exposed two cross-owner
source-shape gaps that should not be hidden inside the anti-alias example:

- Bevy's `examples/3d/anti_aliasing.rs` spawns UI as `Text::default()` plus an
  absolute `Node`; mgstudio currently needs an explicit `Camera2d`,
  `IsDefaultUiCamera`, `UiRoot`, and `UiTargetCamera` for the UI text to render
  in the 3D example.
- Bevy now uses `WorldAssetRoot(asset_server.load(...))` for the FlightHelmet
  scene. mgstudio's scene runtime currently exposes `SceneRoot` for this path.

These are source-owner parity gaps in UI/scene infrastructure, not local
anti-aliasing state-machine behavior.

## Design
Audit the current Bevy owners for implicit UI camera/root targeting and
`WorldAssetRoot`, then map the exact mgstudio owners that should provide the
same authoring surface. Do not keep example-local compatibility code as the
final solution. If MoonBit or dependency limits prevent exact ownership, record
the blocker with evidence; otherwise implement the missing UI/scene source
shape and simplify examples back toward the Bevy source.

## Acceptance Criteria
- [x] The Bevy owner for implicit UI camera/root target selection is audited and
      each source-shape anchor is classified.
- [x] 3D examples can use Bevy-shaped `Text` plus `Node` UI without adding
      example-local UI camera/root scaffolding solely to make text visible.
- [x] The Bevy `WorldAssetRoot` owner is audited and mapped to mgstudio scene
      runtime without pretending `SceneRoot` is source-identical.
- [x] `examples/3d/anti_aliasing` is simplified after the underlying UI/scene
      owners are restored.
- [x] A representative screenshot gate proves the simplified example still
      renders UI and the FlightHelmet scene.

## Relationships
- Depends on: none
- Parent: ISS-105
- Related: ISS-122
- Discovered from: ISS-122

## Notes
- 2026-06-19: Discovered while restoring anti-aliasing example parity. The
  current anti-aliasing example keeps explicit UI target scaffolding and
  `SceneRoot` only because the underlying UI/scene owners do not yet expose the
  Bevy source shape.
- 2026-06-19: Bevy source anchors audited:
  `bevy_ui/src/ui_node.rs` (`UiTargetCamera`, `ComputedUiTargetCamera`),
  `bevy_ui/src/update.rs` (`propagate_ui_target_cameras`),
  `bevy_ui/src/experimental/ghost_hierarchy.rs` (`UiRootNodes`), and
  `bevy_world_serialization/src/components.rs` / `world_asset_spawner.rs`
  (`WorldAssetRoot`). The mgstudio UI owner already had implicit root and
  target propagation; the remaining mismatch was the PBR overlay pass using a
  physical viewport extent as the 2D logical pass extent. `WorldAssetRoot` is
  mapped as a scene authoring component that shares the `SceneRoot` runtime
  queue and lifecycle instead of pretending the old `SceneRoot` name is source
  identical.

## Close Notes
- Restored the Bevy-shaped authoring surface for `WorldAssetRoot` and allowed
  3D examples to spawn UI as `Text` plus `Node` without local `Camera2d`,
  `IsDefaultUiCamera`, `UiRoot`, or `UiTargetCamera` scaffolding.
- Fixed the 3D UI overlay pass boundary so the pass receives a logical extent
  derived from the physical viewport and target scale factor; the 3D scene
  render and postprocess paths continue to use physical dimensions.
- Simplified `examples/3d/anti_aliasing` to use `WorldAssetRoot` and Bevy-shaped
  text UI. Verification: `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine test --release scene`, `moon -C mgstudio-engine test
  --release ui`, `moon -C mgstudio-engine test --release pbr`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/anti_aliasing
  /tmp/mgstudio_iss135_anti_aliasing.png`. The captured screenshot matches
  `mgstudio-engine/examples/3d/anti_aliasing/reference.png` byte-for-byte.
