# ISS-222: Restore volumetric_fog authored scene and assets

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, assets, source-parity
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
`examples/3d/volumetric_fog` still must be checked against Bevy's
`bevy/examples/3d/volumetric_fog.rs` at the scene and asset boundary. Bevy uses
the authored `models/VolumetricFogExample/VolumetricFogExample.glb` scene and
example-owned entities; local proxy geometry is not source parity.

## Design
Use this issue as the parent for the static Bevy scene migration. The
executable work is split into:

- ISS-262 ports the glTF scene root and asset references.
- ISS-263 ports the camera, tonemapping, bloom, skybox, and camera
  `VolumetricFog` setup.
- ISS-264 ports the static point/spot/fog-volume entities.

This issue does not accept the final screenshot; it only proves the source
scene and asset inputs are Bevy-shaped.

## Acceptance Criteria
- [x] ISS-262, ISS-263, and ISS-264 are closed.
- [x] Proxy distance-fog/corridor geometry is removed from the parity path.
- [x] A focused check or capture proves the authored scene loads without
      missing-resource or renderer fatal output.

## Relationships
- Depends on: ISS-264
- Parent: ISS-191
- Related: ISS-104, ISS-108
- Discovered from: ISS-191

## Notes
- 2026-07-04: Split from ISS-191 so authored-scene migration is not mixed with
  controls or final reference acceptance.
- 2026-07-05: Split further into ISS-262 through ISS-264 so asset root,
  camera/render components, and static light/fog-volume entities are migrated
  independently.

## Close Notes
Closed on 2026-07-05.

The authored-scene migration is complete through child issues:

- ISS-262 restored the Bevy glTF scene root
  `models/VolumetricFogExample/VolumetricFogExample.glb` and removed local
  corridor/proxy geometry from the parity path.
- ISS-263 restored the Bevy camera transform, TonyMcMapface tonemapping,
  Bloom, Skybox, and camera `VolumetricFog`.
- ISS-264 restored the Bevy static point light, spot light, and `FogVolume`.

Verification:

- `moon -C mgstudio-engine test examples/3d/volumetric_fog --target native --release`
  passed.
- The native capture after ISS-285 completed with an empty run log:
  `/tmp/mgstudio_iss285_volumetric_fog_msaa.png`.

Final controls, UI text, and final visual-reference acceptance remain tracked
by ISS-223, ISS-224, ISS-265, and ISS-266.
