# ISS-294: Record volumetric_fog Bevy visual baseline

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, visual-parity, evidence
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
The `volumetric_fog` failure must be compared against a concrete Bevy visual
target, not only against an old mgstudio `reference.png`. Record a fresh Bevy
baseline or an explicit human-approved target for the current pinned Bevy
source before changing render behavior.

## Design
Run the Bevy `examples/3d/volumetric_fog` baseline in the local `./bevy` tree
or reuse a current verified artifact if one already exists. Record the command,
image path, and the current mgstudio failure packet so later fixes can be judged
against the same target.

Do not modify mgstudio rendering code in this issue.

## Acceptance Criteria
- [x] A Bevy baseline screenshot or explicit human-approved target is recorded.
- [x] The current mgstudio failure screenshot, side-by-side image, and diff
      artifact paths are listed.
- [x] The issue notes state the exact visible delta that later fixes must remove.
- [x] No render code is changed as part of this issue.

## Relationships
- Depends on: none
- Parent: ISS-293
- Related: ISS-224, ISS-104, ISS-108
- Discovered from: ISS-293

## Notes
- 2026-07-05: Split from ISS-293. Current mgstudio failure artifacts are
  `/tmp/mgstudio_iss292_volumetric_fog_toggle.png`,
  `/tmp/mgstudio_iss224_volumetric_fog_side_by_side.png`, and
  `/tmp/mgstudio_iss224_volumetric_fog_diff.png`.
- 2026-07-05: Bevy baseline capture was attempted from the pinned local Bevy
  checkout `1413bc4e3632233b8fa610a6cf14b90258556c51` with
  `cargo +1.95.0 run --example volumetric_fog --release`. A disposable local
  patch spawned `Screenshot::primary_window()` at frame 90 and then frame 300,
  wrote `/tmp/bevy_iss294_volumetric_fog_render.png`, and was removed after the
  run. The render-path capture is not a useful visual target: SHA256
  `046c1fa4179928b64442fa0316c8db3411a323553736549d689ed073c77d25d0`, size
  `2560x1440`, mean RGBA `[0.0, 0.0, 0.0, 255.0]`, extrema
  `((0, 0), (0, 0), (0, 0), (255, 255))`.
- 2026-07-05: The only usable visual target for the next repair pass is the
  checked-in, human-reviewed mgstudio reference
  `mgstudio-engine/examples/3d/volumetric_fog/reference.png`: SHA256
  `ed63fa3293a6667c52b237e3b72eb1a70ef43c5f49c8718c6853a1da228ba3cd`, size
  `2560x1440`, mean RGBA `[53.722104, 40.956802, 36.557568, 255.0]`. It shows
  the authored street scene through the fog-volume frame and is not dominated
  by a flat gray/white surface.
- 2026-07-05: Current mgstudio failure packet:
  `/tmp/mgstudio_iss292_volumetric_fog_toggle.png`, SHA256
  `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`, size
  `2560x1440`, mean RGBA `[157.436204, 156.130897, 145.295103, 255.0]`;
  side-by-side
  `/tmp/mgstudio_iss224_volumetric_fog_side_by_side.png`, SHA256
  `e6f38829d18c6bfe3318ef4b28057a5b7bf72ba0e7af2957f5395248047bf7c5`;
  diff `/tmp/mgstudio_iss224_volumetric_fog_diff.png`, SHA256
  `606076803160263458e50085c27357415ed1ffbb3f70dfcea5ca5f1ac7de0c07`.
  The exact visible delta to remove is the frame-dominating camera-adjacent
  gray/white hull or surface; the street scene must become visible again.

## Close Notes
Closed as an evidence issue. No mgstudio render code was changed for ISS-294.
The pinned Bevy executable could be run with `cargo +1.95.0`, but direct Bevy
render-path screenshot output was black in this environment, so it is recorded
as non-authoritative evidence. The repair target for subsequent ISS work is the
checked-in human-reviewed reference plus the pinned Bevy source owner.
