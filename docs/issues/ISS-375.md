# ISS-375: Apply proven volumetric_fog source-shaped repair

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, render, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Apply the actual source-shaped repair for the remaining `volumetric_fog`
divergence after ISS-372 through ISS-374 identify the responsible Bevy owner.
This issue must not guess at the root cause.

## Design
Modify only the mgstudio fragment that maps to the proven Bevy source owner.
Keep the Bevy data flow intact. Do not introduce compatibility branches,
fallback resources, tuned constants, asset edits, or screenshot-driven logic.

If the audits prove that no local code can represent Bevy because of a
dependency or language limitation, record that precise blocker and do not
pretend this issue is fixed.

## Acceptance Criteria
- [x] The code change cites the exact Bevy source owner and mgstudio fragment
      in close notes.
- [x] Focused tests or source-level evidence prove the repaired boundary.
- [x] `moon -C mgstudio-engine test examples/3d/volumetric_fog --target native
      --release` passes.
- [x] `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
      `moon -C mgstudio-engine check --target native` pass after the code
      change.
- [x] If no code repair is needed, close only with evidence from ISS-372
      through ISS-374 proving why.

## Relationships
- Depends on: ISS-372, ISS-373, ISS-374
- Parent: ISS-359
- Related: ISS-104, ISS-298, ISS-305, ISS-360
- Discovered from: ISS-359

## Notes
- 2026-07-05: Split from the former ISS-359 implementation leaf so the repair
  remains blocked until a precise owner is proven by smaller diagnostics.

## Close Notes
- 2026-07-05: Closed without an additional code repair because the dependency
  audits did not leave a proven repair owner. ISS-372 proved the real
  `examples/3d/volumetric_fog` `VolumetricFogUniform` numeric payload matches
  Bevy field-by-field, so uniform preparation is not the owner. ISS-373 proved
  the light and shadow numeric inputs after its earlier local
  `GpuClusteredLight` tail-field-order repair, so light/shadow packing is not
  the remaining owner. ISS-374 proved post-pass target state, pass order,
  exposure, HDR/Bloom requirement, TonyMcMapface tonemapping, and final
  camera-target writeback, so post-pass composition is not the owner.
- 2026-07-05: The exact Bevy and mgstudio source owners are cited in ISS-372,
  ISS-373, and ISS-374 close notes. This issue intentionally adds no fallback,
  tuned constant, or screenshot-driven branch because no unfixed source owner
  was proven.
- 2026-07-05: Verification: `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, `moon -C mgstudio-engine check --target native`,
  and `moon -C mgstudio-engine test examples/3d/volumetric_fog --target native --release`
  passed. The final test reported `Total tests: 20, passed: 20, failed: 0`
  with only duplicate `-lobjc` and `-lz` linker warnings.
- 2026-07-05: No hard external blocker or MoonBit language limitation was found
  while closing this issue.
