# ISS-033: Port screenshot capture to Bevy render-app ownership

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, render, screenshot, render-app, render-graph, window
- Assignee: unassigned
- Created: 2026-06-04
- Updated: 2026-06-20
- External ref: none

## Description
Motion blur's screenshot gate now exits successfully, but screenshot capture is
not source-level equivalent to `bevy_render/src/view/window/screenshot.rs`.
Bevy owns screenshots through `ScreenshotPlugin`, `CapturedScreenshots`,
`RenderScreenshotTargets`, `RenderScreenshotsPrepared`,
`RenderScreenshotsSender`, `trigger_screenshots`, `extract_screenshots`,
`prepare_screenshots`, and `submit_screenshot_commands` in the render app.
`ScreenshotCaptured` is an entity event triggered after readback.

Mgstudio currently splits the path across `render/screenshot.mbt`,
`render/view/window/screenshot.mbt`, and renderer-local global state in
`render/renderer/screenshot.mbt`. Readback queues, capture IDs, parity capture
environment variables, target preparation, copy-to-buffer, and completion live
inside `GpuBackend`/global runtime state rather than Bevy-shaped render-app
resources and systems.

## Design
Use Bevy's screenshot owner as the source unit. Rehome screenshot target
extraction, preparation, readback, completion, and trigger/save behavior into
Bevy-shaped main-world/render-world resources and systems. Preserve MoonBit
package splits only where they reflect owner boundaries. Do not keep the
renderer-local global runtime as the normal screenshot path once the source
owner is ported.

## Acceptance Criteria
- [ ] `ScreenshotCaptured`, `Screenshot`, `Capturing`, `Captured`, screenshot target resources, prepared state, sender/receiver state, and screenshot-to-screen pipeline all have explicit Bevy-owner mappings.
- [ ] Screenshot target extraction and preparation run in Bevy-equivalent extract/render schedule positions, not hidden inside `GpuBackend::end_frame_binding`.
- [x] Screenshot completion triggers observer/event behavior equivalent to Bevy's `commands.trigger(ScreenshotCaptured { ... })`; component-observer modeling is removed or documented as a language-level blocker.
- [ ] Surface, image, and manual texture-view screenshot targets are supported through the same owner path.
- [ ] `examples/window/screenshot` and `examples/3d/motion_blur` screenshot gates save nonblank images and exit successfully.

## Relationships
- Depends on: ISS-013, ISS-015
- Parent: ISS-012
- Related: ISS-021, ISS-030
- Discovered from: ISS-030

## Notes
- 2026-06-04: Audit finding. The current screenshot gate is behaviorally fixed, but it remains a partial/local renderer implementation. This also overlaps the open `ISS-013` acceptance item that screenshot, readback, command submission, and present ownership must stop being hidden inside broad renderer-local execution.
- 2026-06-20: Completed the first owner rehome step. The main-world
  screenshot API and systems moved out of `window` into `render`, and
  `RenderPlugin` now installs `screenshot_clear_system` plus
  `screenshot_queue_capture_requests` in `screenshot_trigger_system_set`.
  `window` no longer imports `render`; dev_tools, winit runner, and the
  screenshot example now call `@render.Screenshot` / `@render.save_to_disk`.
  Remaining gap: the render-world side is still renderer-global capture IDs and
  backend readback state, not Bevy's `RenderScreenshotTargets`,
  `RenderScreenshotsPrepared`, `RenderScreenshotsSender`, `CapturedScreenshots`,
  `prepare_screenshots`, `submit_screenshot_commands`, and
  `collect_screenshots` resource/system chain.
- 2026-06-20: Removed winit runner ownership of the ECS screenshot
  queue/finalize path. The render plugin now owns both the request queue system
  and the completion/finalize system in the main-world `Update` schedule, with
  named systems for regression inspection. This improves source ownership but
  does not close the issue because extraction/preparation/readback are still
  not Bevy render-world resources.
- 2026-06-20: Moved `ScreenshotCaptured` from component-insert observer
  semantics to Bevy-shaped trigger-event semantics. `ScreenshotCaptured` is now
  a public trigger event with entity targeting, `save_to_disk` accepts
  `@ecs.On[ScreenshotCaptured]`, screenshot completion inserts the `Captured`
  marker and triggers `ScreenshotCaptured`, and callers now register entity
  trigger observers rather than component-insert observers. Remaining gap:
  screenshot target extraction/preparation/readback still uses local
  renderer-global capture IDs rather than Bevy render-world resources.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine check --target native --package-path
  dev_tools`, `moon -C mgstudio-engine check --target native
  examples/window/screenshot`, `moon -C mgstudio-engine test render
  --release`, `moon -C mgstudio-engine test dev_tools --release`, `moon -C
  mgstudio-engine test examples/window/screenshot --release`, `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`, and
  `moon -C mgstudio-engine info`. The fixed native screenshot gate
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss033_screenshot_captured_trigger_event_custom_shader_instancing.png`
  completed, and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Removed the file-level
  `pending_screenshot_entity_captures` global `Ref` from the main-world
  screenshot bridge. Pending capture IDs are now stored in the ECS world as
  `PendingScreenshotEntityCaptures`, initialized by `RenderPlugin` and
  self-initialized for direct system calls; queue/collect writes go through
  `ResourceMut` so resource ownership and change ticks stay inside the world.
  This is still a local bridge over the renderer capture ID path, not complete
  Bevy parity: the remaining target/prepared/readback path must still move to
  `RenderScreenshotTargets`, `RenderScreenshotsPrepared`, and
  `RenderScreenshotsSender`-shaped render-world resources. Validation:
  `moon -C mgstudio-engine check --target native --package-path render`,
  `moon -C mgstudio-engine test render --release`, `moon -C mgstudio-engine
  check --target native`, `moon -C mgstudio-engine fmt`, and `moon -C
  mgstudio-engine info`. The fixed native screenshot gate
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss033_pending_capture_resource_custom_shader_instancing.png`
  completed, and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Split screenshot completion into a Bevy-shaped
  `CapturedScreenshots` world resource plus public `trigger_screenshots`
  system. The local capture-ID polling bridge now collects completed readbacks
  into `CapturedScreenshots`; `trigger_screenshots` drains that resource,
  inserts the `Captured` marker, and triggers `ScreenshotCaptured` for the
  screenshot entity. The old public `screenshot_finalize_capture_requests`
  surface is gone, and the remaining local collect bridge stays private. This
  still does not complete render-world parity because the actual GPU target
  preparation/readback still uses renderer capture IDs instead of
  `RenderScreenshotTargets` / `RenderScreenshotsPrepared` /
  `RenderScreenshotsSender`. Validation: `moon -C mgstudio-engine check
  --target native --package-path render`, `moon -C mgstudio-engine test render
  --release`, `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, and `moon -C mgstudio-engine info`. The fixed native
  screenshot gate `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss033_captured_screenshots_resource_custom_shader_instancing.png`
  completed, and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Added the Bevy source-owner `ScreenshotPlugin` anchor. Screenshot
  resource initialization and main-world screenshot systems now live under
  `ScreenshotPlugin`; `RenderPlugin` installs that plugin instead of directly
  owning screenshot systems. This moves the owner shape closer to
  `bevy_render/src/view/window/screenshot.rs` while leaving the render-app
  target/prepared/readback resources as the remaining parity gap. Validation:
  `moon -C mgstudio-engine check --target native --package-path render`,
  `moon -C mgstudio-engine test render --release`, `moon -C mgstudio-engine
  check --target native`, `moon -C mgstudio-engine fmt`, and `moon -C
  mgstudio-engine info`. The fixed native screenshot gate
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss033_screenshot_plugin_owner_custom_shader_instancing.png`
  completed, and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Added render-world `RenderScreenshotTargets` and
  `RenderScreenshotsPrepared` resource anchors under `ScreenshotPlugin`, and
  verified that they are initialized in the render sub-app when `RenderPlugin`
  is installed while `ScreenshotPlugin` alone still tolerates a missing
  `RenderApp` like Bevy's `let Some(render_app) = ... else { return; }`.
  These resources currently hold the local bridge target key and prepared
  capture-id shape, so the owner is present but the remaining implementation
  gap is still the real `extract_screenshots` normalization,
  `prepare_screenshots`, `submit_screenshot_commands`, and
  `collect_screenshots` render-world data flow. Validation: `moon -C
  mgstudio-engine check --target native --package-path render`, `moon -C
  mgstudio-engine test render --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, and `moon -C mgstudio-engine
  info`. The fixed native screenshot gate
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss033_render_screenshot_resources_custom_shader_instancing.png`
  completed, and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Split Bevy's `extract_screenshots` responsibility out of the
  local capture request queue. `extract_screenshots` now clears
  `RenderScreenshotTargets`, normalizes screenshot targets through the current
  window/image/texture-view key bridge, deduplicates targets, despawns duplicate
  screenshot entities, inserts `Capturing`, and stores the extracted target map.
  `screenshot_queue_capture_requests` now consumes that extracted target map
  instead of owning extraction itself. This makes the local bridge use the
  Bevy-shaped target resource rather than leaving the resource as a facade.
  Remaining gap: this extraction still runs in the main-world screenshot system
  chain to feed the current renderer capture-ID bridge; the Bevy-equivalent
  render-app `ExtractSchedule`, `prepare_screenshots`,
  `submit_screenshot_commands`, `RenderScreenshotsSender`, and GPU buffer
  readback path are not complete. Validation: `moon -C mgstudio-engine check
  --target native --package-path render`, `moon -C mgstudio-engine test render
  --release`, `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, the fixed native
  screenshot gate `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss033_extract_screenshots_custom_shader_instancing.png`, and
  `python3 mgstudio-engine/scripts/visual_image_sanity.py --image
  /tmp/mgstudio_iss033_extract_screenshots_custom_shader_instancing.png
  --report
  /tmp/mgstudio_iss033_extract_screenshots_custom_shader_instancing_sanity.json`.

## Close Notes
