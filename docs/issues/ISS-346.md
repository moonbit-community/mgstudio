# ISS-346: Record volumetric_fog post-phase visual delta

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, screenshots, visual-parity, diagnosis
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
`ISS-324` and `ISS-325` ruled out the GLB Cube being absent from extraction,
phase queue, or material depth-state selection, but the mgstudio
`examples/3d/volumetric_fog` capture still renders as a near-grey wall/ground
frame instead of the Bevy-shaped reference with the Pisa skybox/street signal.

Create a compact evidence packet that records the current visual delta before
the next source-owner repair. This issue should not change renderer behavior.

## Design
Use the standard native screenshot capture for `examples/3d/volumetric_fog`.
Compare the produced image against the checked-in `reference.png` and against
the earlier failed capture when available. Record image paths, hashes, run-log
status, and pixel-delta summary in this issue.

Also record the already-proven boundaries so later issues do not repeat them:
GLB Cube import, material/normal texture readiness, skybox asset readiness,
PBR mesh extraction, phase queue, and material pipeline depth-state selection.

## Acceptance Criteria
- [x] A fresh mgstudio capture path, hash, and run-log status are recorded.
- [x] The capture is compared against the checked-in reference and the earlier
      failed capture when available.
- [x] The issue states which boundaries have already been proven and which
      boundaries remain candidates.
- [x] No renderer or example behavior is changed for this diagnostic issue.

## Relationships
- Depends on: none
- Parent: ISS-305
- Related: ISS-298, ISS-324, ISS-325
- Discovered from: ISS-305

## Notes
- 2026-07-05: Split from ISS-305 to keep visual evidence separate from repair.

## Close Notes
Closed with a diagnostic-only evidence packet. No renderer, example, shader,
asset, or test behavior was changed for this issue.

Fresh mgstudio capture:

- Command:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/volumetric_fog /tmp/mgstudio_iss305_volumetric_fog_after_phase_proof.png`
- Artifact:
  `/tmp/mgstudio_iss305_volumetric_fog_after_phase_proof.png`
- SHA-256:
  `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`
- Run log:
  `/tmp/mgstudio_iss305_volumetric_fog_after_phase_proof.run.log`, size `0`.

Comparison artifacts:

- Checked-in reference:
  `mgstudio-engine/examples/3d/volumetric_fog/reference.png`, SHA-256
  `ed63fa3293a6667c52b237e3b72eb1a70ef43c5f49c8718c6853a1da228ba3cd`.
- Earlier failed capture:
  `/tmp/mgstudio_iss298_volumetric_fog_after_input_audit.png`, SHA-256
  `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`.

Pixel summary:

- New capture vs checked-in reference:
  - size: `2560x1440` vs `2560x1440`
  - new mean RGBA: `[157.436204, 156.130897, 145.295103, 255.0]`
  - reference mean RGBA: `[53.722104, 40.956802, 36.557568, 255.0]`
  - absolute diff mean RGBA:
    `[111.785853, 120.696786, 114.295019, 0.0]`
  - absolute diff extrema:
    `[(0, 249), (0, 254), (0, 253), (0, 0)]`
- New capture vs earlier failed capture:
  - absolute diff mean RGBA: `[0.0, 0.0, 0.0, 0.0]`
  - absolute diff extrema: `[(0, 0), (0, 0), (0, 0), (0, 0)]`

Already ruled out by ISS-324, ISS-325, and the current volumetric_fog
white-box evidence:

- The GLB Cube is not missing from scene import.
- The GLB Cube reaches PBR render mesh extraction with the expected transform
  and extraction identity.
- The GLB material and normal texture are loaded.
- The skybox image asset is loaded into asset storage and render texture
  storage.
- The Cube material queues into the opaque phase through both CPU and GPU
  queue-builder paths.
- The material specialization selects the expected opaque/depth-state path.

Remaining candidate boundaries:

- The camera render entity may still miss a prepared skybox pipeline, bind
  group, or actual skybox draw execution. Tracked by ISS-347.
- The main opaque pass may still differ from Bevy in skybox/opaque/atmosphere/
  volumetric-fog ordering or attachment/depth state. Tracked by ISS-348.
- The skybox shader, cubemap view, sampler, or exposure/brightness path may
  still differ from Bevy even if the draw path is reached. Tracked by ISS-349.
- The final source-shaped repair is tracked by ISS-350.
