# ISS-205: Resolve scrolling_fog final shader-input visual parity group

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, screenshots, visual-parity
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
After shader source, uniforms, density textures, and depth binding are audited,
`scrolling_fog` still needs the final Bevy-shaped visual signal: density fog,
foreground depth occlusion, and the directional shadow/light-shaft structure.
This issue groups the remaining smaller audits needed before refreshing the
checked-in reference in ISS-200.

## Design
Do not treat one visually plausible capture as sufficient. Split the remaining
gap by source owner: example scene authoring, directional shadow-map content,
volumetric-fog light/shadow shader inputs, shader shadow integration, and final
capture evidence. Each child issue must compare against Bevy source and either
fix local divergence source-faithfully or record a precise blocker.

## Acceptance Criteria
- [x] ISS-211 verifies the `scrolling_fog` example authoring inputs.
- [x] ISS-212 verifies directional shadow-map content for the scene.
- [x] ISS-213 verifies volumetric-fog light and shadow shader input packing.
- [x] ISS-214 verifies directional shadow sampling and scattering math.
- [x] ISS-215 records the final capture evidence before ISS-200 refreshes the
      checked-in reference.

## Relationships
- Depends on: ISS-215
- Parent: ISS-199
- Related: ISS-091, ISS-104, ISS-190, ISS-200
- Discovered from: ISS-199

## Notes
- 2026-07-04: Split from ISS-199.
- 2026-07-04: Standard capture after the view-depth fixes completed with an
  empty run log:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/scrolling_fog /tmp/mgstudio_iss210_scrolling_fog.png`.
  The image proves foreground depth reconstruction, but it is not acceptable
  as final shader-input parity evidence because it still lacks the checked-in
  reference's diagonal directional shadow/light-shaft structure. The comparison
  artifact is `/tmp/mgstudio_iss205_scrolling_fog_vs_reference_diff.png`; diff
  stats were mean RGB `[6.339, 6.336, 6.325]`, RMS RGB
  `[14.808, 14.809, 14.8]`, nonzero RGB ratio `0.636986`.
- 2026-07-04: Split the remaining visual gap into ISS-211 through ISS-215 so
  the work can proceed by Bevy source owner instead of a single broad screenshot
  mismatch.

## Close Notes
- 2026-07-05: Closed after ISS-211, ISS-212, ISS-213, ISS-214, and ISS-215
  were closed. The `scrolling_fog` final shader-input visual parity group now
  has source-owner evidence for example authoring, shadow-map content,
  volumetric fog light/shadow input packing, shader sampling math, and final
  native visual evidence. Reference acceptance remains tracked by ISS-200.
