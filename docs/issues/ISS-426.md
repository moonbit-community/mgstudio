# ISS-426: Fix Moon test generated-driver ambiguous-braces warnings

## Metadata
- Type: bug
- Status: blocked
- Priority: 1
- Labels: moonbit, tooling, tests, warnings, external-blocker
- Assignee: unassigned
- Created: 2026-07-12
- Updated: 2026-07-12
- External ref: none

## Description
`MOONBIT_NEW_NATIVE=1 moon test --target native --release --deny-warn` fails after project sources pass strict checking because Moon generates `__generated_driver_for_blackbox_test.mbt` files containing ambiguous `{}` expressions. The same generated warning appears across unrelated packages such as `app`, `color`, `image`, `math`, and `utils`.

## Design
Do not edit `_build` outputs or weaken project warning policy. Reproduce against the pinned Moon toolchain and upgrade or patch Moon's black-box test driver generator when an upstream fix is available.

## Acceptance Criteria
- [ ] Moon-generated black-box test drivers compile without `ambiguous_braces` warnings.
- [ ] `MOONBIT_NEW_NATIVE=1 moon test --target native --release --deny-warn` reaches and runs the project test suite.
- [ ] No generated files are checked into the repository as a workaround.

## Relationships
- Depends on: none
- Parent: none
- Related: none
- Discovered from: none

## Notes
- 2026-07-12: Project source passes `MOONBIT_NEW_NATIVE=1 moon check --target native --deny-warn`; failure paths point only to generated `__generated_driver_for_blackbox_test.mbt` files.

## Close Notes
