# ISS-034: Rehome motion blur pipeline ownership from renderer-local lazy state

## Metadata
- Type: task
- Status: open
- Priority: 0
- Labels: parity, pbr, post-process, motion-blur, pipeline, render-app
- Assignee: unassigned
- Created: 2026-06-04
- Updated: 2026-06-20
- External ref: none

## Description
`ISS-030` fixed the concrete runtime bug where motion blur skipped because no
settings buffer existed, and the example now captures a nonblank screenshot.
However, the source owner is still not complete. Bevy's
`bevy_post_process/src/motion_blur/{mod.rs,pipeline.rs}` owns
`MotionBlurPlugin`, `MotionBlur`, `MotionBlurUniform`, `MotionBlurPipeline`,
`MotionBlurPipelineKey`, `MotionBlurPipelineId`,
`init_motion_blur_pipeline`, `prepare_motion_blur_pipelines`,
`prepare_view_depth_texture_usages_for_motion_blur`, and the `motion_blur`
render system in render-app schedules.

Mgstudio's `post_process/motion_blur/top.mbt` is only a facade, while
`post_process/motion_blur/pipeline.mbt` owns the public `MotionBlur` component
and helper sample generation. The actual layouts, buffers, pipeline creation,
uniform writes, bind group creation, and draw call live in
`render/renderer/gpu_motion_blur.mbt` as `GpuBackend` lazy state. That is a
working local path, but not source-level owner parity.

## Design
Move motion blur pipeline/resource ownership into the Bevy-shaped post-process
owner and render-app schedule. Renderer APIs should execute prepared render
commands, not own the plugin resource lifecycle or specialize pipelines lazily
as a substitute for `RenderStartup`/`RenderSystems::Prepare`. If MoonBit lacks a
needed render-app resource or specialization abstraction, record the exact
language/engine blocker and avoid adding another fallback.

## Acceptance Criteria
- [ ] `MotionBlurPlugin` installs embedded WGSL, extraction/uniform handling, pipeline resource initialization, prepare systems, depth usage preparation, and Core3d postprocess ordering with explicit Bevy mappings.
- [ ] `MotionBlurPipeline`, `MotionBlurPipelineKey`, and `MotionBlurPipelineId` are represented in the post-process owner rather than hidden in `GpuBackend` fields.
- [ ] Pipeline specialization and bind group layout selection are driven by view target format and MSAA samples in the Bevy-shaped prepare path.
- [ ] Renderer-local motion blur code no longer creates missing pipeline/layout/resource state as a lazy fallback.
- [ ] `examples/3d/motion_blur` still passes the screenshot-exit gate and has a fresh visual parity note after the rehome.

## Relationships
- Depends on: ISS-014, ISS-016, ISS-018, ISS-019
- Parent: ISS-019
- Related: ISS-030, ISS-033
- Discovered from: ISS-030

## Notes
- 2026-06-04: Audit finding. The current runtime is useful evidence that the feature works, but `post_process/motion_blur` remains `partial/facade`: a Bevy source owner file exists, yet important structs, systems, resources, and plugin hooks are implemented as renderer-local lazy state.
- 2026-06-20: While validating the PBR plugin debug-flag propagation slice,
  `examples/3d/motion_blur` regressed to a hard wgpu validation failure during
  queue submit: the motion-vector prepass pipeline uses sparse color targets
  `[None, Rg16Float]`, but the active render pass reports no color attachments.
  This is concrete evidence for the same ownership gap tracked here: pipeline
  specialization and render-pass attachment construction are still split across
  renderer-local lazy state instead of a Bevy-shaped motion blur/prepass owner.
- 2026-06-27: Started the source-owner rehome by moving the public
  `MotionBlur` component and default constructor surface to the
  `bevy_post_process/src/motion_blur/mod.rs` fragment instead of the local
  `pipeline.rs` fragment. Added the Bevy-shaped `MotionBlurUniform` component
  and explicit `MotionBlur -> MotionBlurUniform` extract helper so later render
  scheduling can consume a source-owned uniform target. This does not close the
  issue: `MotionBlurPipeline`, specialized pipeline IDs, bind groups, depth
  texture usage preparation, and the actual `motion_blur` Core3d node are still
  renderer-local or absent. Validation: `moon -C mgstudio-engine test
  --release post_process/motion_blur --deny-warn`, `moon -C mgstudio-engine
  test --release pbr --deny-warn`, `moon -C mgstudio-engine check --target
  native --package-path examples/3d/motion_blur --deny-warn`, full `moon -C
  mgstudio-engine check --target native --deny-warn`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and the
  `examples/3d/motion_blur` native screenshot sanity gate at
  `/tmp/mgstudio_iss034_motion_blur_owner_uniform.png`.
- 2026-06-27: Added the Bevy-shaped `MotionBlurPlugin` install path for the
  currently available owner slice: embedded WGSL registration now lives in the
  motion-blur plugin, source `MotionBlur` components extract into
  `MotionBlurUniform` only for camera views, sync target removal clears the
  uniform target, and prepare-view depth texture usage adds texture binding for
  views with motion blur. The slice also removed renderer-owned registration of
  root post-process shader assets and moved `ColorGrading*` ownership from the
  post-process root facade to the render owner, matching Bevy's
  `bevy_render::view` boundary and breaking the render/post-process import
  cycle. `Bloom` ownership was also flipped from the root post-process facade
  into `post_process/bloom`, with a Bevy-shaped `BloomPlugin` owning
  `bloom.wgsl` registration so bloom shader loading did not regress after
  removing render-owned post-process registration. Remaining work is still
  substantial: `MotionBlurPipeline`,
  `MotionBlurPipelineId`, pipeline specialization, bind group creation, and the
  actual Core3d `motion_blur` render node remain renderer-local or absent.
  Validation: `moon -C mgstudio-engine test --release
  post_process/motion_blur --deny-warn`, `moon -C mgstudio-engine test
  --release post_process --deny-warn`, `moon -C mgstudio-engine test
  --release pbr --deny-warn`, `moon -C mgstudio-engine check --target native
  --package-path examples/3d/motion_blur --deny-warn`, full `moon -C
  mgstudio-engine check --target native --deny-warn`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and the
  `examples/3d/motion_blur` native screenshot sanity gate at
  `/tmp/mgstudio_iss034_motion_blur_postprocess_owned.png`.

## Close Notes
