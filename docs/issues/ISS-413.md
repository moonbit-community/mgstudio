# ISS-413: Establish complete GPU backend teardown ownership

## Metadata
- Type: task
- Status: open
- Priority: 0
- Labels: render, native, wgpu, resource-lifecycle, shutdown, tests
- Assignee: unassigned
- Created: 2026-07-12
- Updated: 2026-07-12
- External ref: none

## Description
`GpuBackend` owns the native instance, adapter, device, queue, surfaces, resource registries, and many cached render resources, but currently has no complete teardown operation. Replacing the global backend in tests or recreating the renderer in one process discards all of those manual handles.

## Design
Use hierarchical explicit ownership: each aggregate and cache entry releases the native handles it owns, while one idempotent backend shutdown boundary invokes those owners in dependency order. Drain pending and retired frame resources first, release feature caches and registries next, then release surfaces, queue, device, adapter, and instance. Route global backend replacement through that boundary. Use `defer` to roll back partially initialized instance/adapter/device stacks. Do not add a second heuristic registry of every created handle.

## Acceptance Criteria
- [ ] `GpuBackend` exposes one idempotent internal shutdown operation covering every owned native handle.
- [ ] Global backend replacement releases the previous backend before storing the next backend.
- [ ] Partial backend initialization releases already-created handles on error.
- [ ] WGPU GlobalReport tests demonstrate backend create/use/shutdown returns live handle counts to baseline.
- [ ] Repository-native checks and targeted tests pass with `MOONBIT_NEW_NATIVE=1`.

## Relationships
- Depends on: ISS-414, ISS-415, ISS-416, ISS-417, ISS-418, ISS-419
- Parent: ISS-408
- Related: ISS-014
- Discovered from: ISS-408

## Notes

Implementation is split by existing source-owner boundaries so each release surface can be tested and committed independently.

## Close Notes
