# ISS-370: Assign volumetric_fog shader audit outcome

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: pbr, volumetric-fog, source-parity, blocker-triage
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Consolidate ISS-366 through ISS-369 and assign the proven outcome. If the
shader/pipeline boundary is Bevy-shaped, record that conclusion. If it has a
local divergence, hand the exact owner to ISS-359. If it is blocked by an
upstream dependency or MoonBit language limitation, record a precise blocker.

## Design
Read the child issue close notes and produce a narrow decision. This issue
must not become a new broad investigation and must not contain implementation
work. It exists to prevent ISS-359 from starting from guesses.

## Acceptance Criteria
- [x] ISS-366 through ISS-369 are closed.
- [x] The shader/pipeline audit outcome is summarized with exact Bevy owner and
      mgstudio fragment references.
- [x] Any local repair owner is assigned to ISS-359.
- [x] Any external or language blocker is recorded as its own issue and linked
      here.

## Relationships
- Depends on: ISS-366, ISS-367, ISS-368, ISS-369
- Parent: ISS-358
- Related: ISS-104, ISS-199, ISS-254, ISS-255, ISS-305, ISS-359
- Discovered from: ISS-358

## Notes
- 2026-07-05: Split from ISS-358 as the final decision gate between audit work
  and ISS-359 implementation.

## Close Notes
Closed on 2026-07-05 after consolidating ISS-366 through ISS-369.

Outcome:

- The volumetric-fog shader composition and pipeline-specialization boundary is
  Bevy-shaped for the audited scope.
- No local shader, shader-def, layout-key, blend, color-target, resolve,
  depth-attachment, or real-example runtime variant divergence was proven.
- No external dependency blocker or MoonBit language blocker was found in this
  shader/pipeline audit group.
- Nothing from ISS-366 through ISS-369 needs assignment to ISS-359.

Bevy owners and mgstudio fragments:

- `bevy/crates/bevy_pbr/src/volumetric_fog/volumetric_fog.wgsl` maps to
  `mgstudio-engine/pbr/volumetric_fog/volumetric_fog.wgsl` and its registered
  import closure.
- `bevy/crates/bevy_pbr/src/volumetric_fog/render.rs::VolumetricFogPipelineKey`
  and `VolumetricFogPipeline::specialize` map to
  `mgstudio-engine/pbr/volumetric_fog/render.mbt`,
  `mgstudio-engine/pbr/volumetric_fog_execute.mbt`, and
  `mgstudio-engine/render/renderer/mesh3d_pipeline_build.mbt`.
- Bevy's volumetric-fog pass descriptor maps to
  `mgstudio-engine/render/renderer/mesh3d_volumetric_fog_pass.mbt` and the
  renderer pipeline build fragments.

Child conclusions:

- ISS-366: static WGSL source and import closure are complete for this
  boundary.
- ISS-367: shader definitions, pipeline keys, and textureless/textured layout
  variants are complete for this boundary.
- ISS-368: color target, additive blend, no-resolve, and no-depth pass
  semantics are complete for this boundary.
- ISS-369: the real `examples/3d/volumetric_fog` runtime variant is proven:
  both pipelines are prepared, MSAA is enabled, atmosphere is disabled, density
  texture differs by variant, and hardware 2x2 shadow filtering is enabled.

Validation evidence is recorded in the child close notes. No additional command
was needed for this decision gate.
