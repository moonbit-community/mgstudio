# ISS-383: Prove volumetric_fog pass ordering around postprocess

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: pbr, volumetric-fog, render-graph, postprocess, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Prove that the real `examples/3d/volumetric_fog` pass executes in the same
order as Bevy relative to the main 3D pass, skybox or atmosphere work,
transparent rendering if applicable, and early postprocess.

## Design
Use the owner map from ISS-381. Record or assert the concrete mgstudio pass
order for the real frame and compare it to Bevy's source order. Focus only on
ordering; target state belongs to ISS-382 and color transforms belong to
ISS-384.

## Acceptance Criteria
- [x] Bevy pass-order anchors are cited with paths and symbols.
- [x] Mgstudio pass-order evidence is recorded for the real example.
- [x] Any ordering divergence is assigned to ISS-375 with exact owner.
- [x] If no divergence is found, close with evidence that rules out pass order.

## Relationships
- Depends on: ISS-381, ISS-382
- Parent: ISS-374
- Related: ISS-089, ISS-092, ISS-346
- Discovered from: ISS-374

## Notes
- 2026-07-05: Split from ISS-374 because pass order is a separate failure mode
  from target state and color conversion.

## Close Notes
- 2026-07-05: Bevy source anchors: `bevy/crates/bevy_core_pipeline/src/core_3d/mod.rs:144-162`
  installs `Core3d::base_schedule()` and chains
  `(main_opaque_pass_3d, main_transparent_pass_3d)` in
  `Core3dSystems::MainPass`, with `tonemapping` in
  `Core3dSystems::PostProcess`. `bevy/crates/bevy_pbr/src/volumetric_fog/mod.rs:101-106`
  inserts `volumetric_fog.after(Core3dSystems::MainPass).before(Core3dSystems::EarlyPostProcess)`.
  `main_opaque_pass_3d_node.rs:83-110` draws opaque, alpha-mask, then skybox
  inside the opaque main pass; `main_transparent_pass_3d_node.rs:50-99`
  executes transparent rendering as the second chained MainPass node.
- 2026-07-05: mgstudio camera execution anchor:
  `mgstudio-engine/pbr/render_execute_camera.mbt:341-379` records and executes
  `main_pass_complete`, `volumetric_fog_begin`, `volumetric_fog_complete`, and
  `postprocess_begin` in that order around the actual camera calls. The
  corresponding postprocess completion counter is emitted after
  `render3d_execute_camera_postprocess`.
- 2026-07-05: mgstudio main-pass anchor:
  `mgstudio-engine/pbr/render_execute_camera_main_pass.mbt:1033-1050` draws
  the binned main phase, then the prepared skybox, then optional atmosphere sky,
  before leaving the main pass and before the later camera-level fog call.
- 2026-07-05: Runtime evidence:
  `mgstudio-engine/examples/3d/volumetric_fog/main_wbtest.mbt:632-779`
  records first-seen counter indices for skybox, main pass completion,
  volumetric fog begin/end, and postprocess begin/end. `main_wbtest.mbt:853-871`
  asserts the real example has no transparent draw items, does draw skybox
  before main pass completion, and then orders main pass completion before fog,
  fog before postprocess, and postprocess begin before completion.
- 2026-07-05: Verification: `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine check --target native`, and
  `moon -C mgstudio-engine test examples/3d/volumetric_fog --release` passed.
- 2026-07-05: No ordering divergence was found. No external blocker is required.
