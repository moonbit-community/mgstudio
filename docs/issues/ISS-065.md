# ISS-065: Port Bevy AtmospherePlugin render resource owner

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, pbr, atmosphere, render-app, bind-groups, luts
- Assignee: unassigned
- Created: 2026-06-10
- Updated: 2026-07-09
- External ref: none

## Description
`examples/3d/atmosphere` enables the PBR `ATMOSPHERE` mesh pipeline key, so the
mesh view layout includes bindings 31, 32, and 33. The renderer cannot bind
those entries because mgstudio has not ported Bevy's atmosphere render resource
owner. The local `pbr/atmosphere` package currently contains embedded WGSL,
component facades, and small state structs, but not the render-side systems that
create atmosphere LUT textures, prepare uniforms, build bind groups, write the
`AtmosphereBuffer`, run LUT compute passes, or render the sky pass.

## Design
Use Bevy source owners as the migration unit, not the current mgstudio file
shape. The primary owner is `bevy/crates/bevy_pbr/src/atmosphere/mod.rs`, with
required implementation fragments in `resources.rs`, `node.rs`,
`environment.rs`, and the WGSL files under `bevy_pbr/src/atmosphere`. Port the
logic directly into Bevy-shaped mgstudio owners. Do not use placeholder
textures, identity atmosphere buffers, fake LUTs, constant white transmittance,
or logic that silently drops the `ATMOSPHERE` layout bit while claiming
atmosphere support.

## Acceptance Criteria
- [ ] `AtmospherePlugin` installs the same render-side resource lifecycle as
      Bevy: shader registration, capability checks, render resources,
      extraction, prepare systems, bind group preparation, LUT passes, and
      Core3d sky pass ordering.
- [ ] `ExtractedAtmosphere`, `GpuAtmosphere`, `GpuAtmosphereSettings`,
      `AtmosphereTextures`, `AtmosphereSampler`, `AtmosphereBindGroupLayouts`,
      `RenderSkyBindGroupLayouts`, `AtmosphereLutPipelines`,
      `AtmosphereTransforms`, `AtmosphereBindGroups`, and `AtmosphereBuffer`
      have explicit mgstudio mappings or documented language/dependency
      blockers.
- [ ] The mesh view bind group receives real Bevy-equivalent atmosphere
      bindings: transmittance LUT texture at 31, filtering sampler at 32, and
      read-only `GpuAtmosphere` storage buffer at 33.
- [ ] `examples/3d/atmosphere` no longer logs `unsupported binding 31`, renders
      a non-black capture, and saves `examples/3d/atmosphere/reference.png`.
- [ ] `moon -C mgstudio-engine check --target native`, `moon -C
      mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and focused
      atmosphere/PBR tests or visual gates pass.

## Relationships
- Depends on: ISS-066, ISS-067, ISS-167
- Parent: ISS-012
- Related: ISS-061, ISS-016, ISS-018, ISS-019, ISS-066, ISS-067, ISS-107, ISS-167
- Discovered from: ISS-061

## Notes
- 2026-06-10: Created from the ISS-061 source audit. Bevy's
  `prepare_mesh_view_bind_groups` only enables `MeshPipelineViewLayoutKey::ATMOSPHERE`
  when `ExtractedAtmosphere`, `AtmosphereTextures`, `AtmosphereBuffer`,
  `AtmosphereSampler`, and a buffer binding are all available. Mgstudio lacks
  those resource owners, so adding cases 31/32/33 to the renderer bind-group
  switch alone would only move the failure to missing or fake resources.
- 2026-06-10: Added explicit source-owner mappings for the Bevy atmosphere
  resource symbols in `pbr/atmosphere/resources.mbt`, including
  `GpuAtmosphere`, LUT texture descriptors, sampler/layout/pipeline/bind-group
  owner structs, transforms, and `AtmosphereBuffer`. Added focused tests for
  LUT descriptor sizes and `GpuAtmosphere` field mapping.
- 2026-06-10: Added renderer-side recognition for mesh view bindings 31, 32,
  and 33, and set the `Atmosphere` storage binding minimum size to 96 bytes to
  match the WGSL struct layout. This intentionally fails fast with
  `wgpu: missing atmosphere transmittance texture` until the real Bevy
  `prepare_atmosphere_textures` and LUT compute resource owner is ported. It
  must not be treated as a completed visual fix.
- 2026-06-10: Capture
  `/tmp/mgstudio_atmosphere_iss065_binding_explicit.png` remains fully black
  (`non_black_ratio=0.0`, `mean_luma=0.0`). The log no longer reports
  `unsupported binding 31`; it now reports missing real atmosphere
  transmittance resources, confirming the next gap is the actual LUT resource
  lifecycle rather than the bind group switch.
- 2026-06-10: Added renderer-owned `GpuAtmosphereResources` with real
  Rgba16Float transmittance, multiscattering, sky-view, and aerial-view LUT
  textures, a filtering sampler, and a 96-byte `GpuAtmosphere` storage buffer.
  The camera main pass now writes `GpuAtmosphere` data and LUT sizes before
  preparing mesh view bind groups, and mesh view bind group cache keys include
  the atmosphere resource revision.
- 2026-06-10: Verified the binding-resource stage with
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=600
  MGSTUDIO_PARITY_CAPTURE_RETRY_TIMEOUT_SECONDS=600
  mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/atmosphere
  /tmp/mgstudio_atmosphere_iss065_resources.png`. The run log contains no
  `unsupported binding`, `missing atmosphere`, or validation fatal messages.
  The capture is no longer fully black (`non_black_ratio=0.2391289605034722`,
  mean RGBA `[28.3027, 28.2230, 26.1631, 255.0]`), but the sky remains black
  because the Bevy LUT compute passes and render-sky pass are still incomplete.
- 2026-06-10: While starting the transmittance LUT compute pass, found a real
  local prerequisite: Bevy's `transmittance_lut.wgsl` samples
  `GpuScatteringMedium` density/scattering LUT textures at bindings 5 and 6.
  Mgstudio does not yet have the Bevy-shaped `ScatteringMedium` render asset
  owner that uploads those Rgba32Float textures. This prerequisite is tracked
  as ISS-066 and must be solved before claiming atmosphere LUT parity.
- 2026-06-11: ISS-066 is closed. The renderer now owns real Rgba32Float
  scattering medium density/scattering LUTs and requests `Float32Filterable`
  when available so the Bevy-shaped filtering bind group layout is valid under
  the WebGPU C ABI. The capture
  `/tmp/mgstudio_atmosphere_iss065_float32_filterable.png` produced a
  nonblack frame (`nonblack_ratio = 0.20571451822916667`) and its run log had
  no validation, fatal, unsupported-binding, or missing-atmosphere output.
  Remaining work for this issue is the rest of Bevy's atmosphere resource
  owner: multiscattering, sky-view, aerial-view LUT compute passes and the
  render-sky pass.
- 2026-06-11: Added Bevy-shaped multiscattering LUT layout, pipeline, and bind
  group construction, then isolated a native backend blocker before enabling
  dispatch. The exact Bevy shader dispatch stalls capture even when reduced to
  `dispatch_workgroups(1, 1, 1)`, while omitting the dispatch allows the same
  1-frame capture to complete. This is tracked as ISS-067 with upstream
  `moonbit-community/wgpu-mbt#26`; ISS-065 is blocked until that subtarget is
  resolved.
- 2026-07-01: Fresh comparison against Bevy `examples/3d/atmosphere` shows the
  render owner is still incomplete. Mgstudio
  `/tmp/mgstudio_atmosphere_compare.png` is non-empty but mostly black
  (`mean_luma = 9.410760354327593`), while Bevy
  `/tmp/bevy_atmosphere_compare.png` is a bright atmospheric sky and water
  reflection (`mean_luma = 153.93989456975845`). This issue remains the render
  owner task: multiscattering dispatch must be restored through ISS-067, then
  the remaining Bevy sky-view, aerial-view, and render-sky pass ownership must
  be audited before the example can claim visual parity.
- 2026-06-11: Verified the blocked partial path still runs with the standard
  90-frame atmosphere capture command:
  `/tmp/mgstudio_atmosphere_iss065_blocked_multiscattering.png` is nonblack
  (`nonblack_ratio = 0.23825059678819444`, mean RGBA
  `[28.118436143663196, 28.03991021050347, 25.98101888020833, 255.0]`), and
  the run log contains no validation, fatal, unsupported-binding,
  missing-resource, or error output. This is not atmosphere parity because the
  multiscattering dispatch is intentionally left blocked by ISS-067.
- 2026-06-16: The ISS-105 symbol audit found a separate example-owner gap in
  `bevy/examples/3d/atmosphere.rs`. ISS-065 remains the render-resource
  blocker, while ISS-107 tracks local example divergence such as missing
  Earth/Mars presets, missing `Water` extended material, local probe spheres,
  and non-Bevy camera/sun/fog/control shape.
- 2026-07-01: ISS-067 is closed. The renderer now dispatches transmittance,
  multiscattering, sky-view, and aerial-view LUTs, writes a Bevy-shaped
  `AtmosphereTransform` buffer, and runs a render-sky fullscreen pass. The
  capture `/tmp/mgstudio_atmosphere_iss065_fullchain.png` is no longer black
  (`mean_luma = 134.19663178672081`, `non_black_ratio = 1.0`) and has no run-log
  validation or fatal output. This issue remains open because render-resource
  parity is not yet symbol-complete: MSAA render-sky specialization and exact
  render graph ownership/scheduling still need source-owner audit, and the
  current example cannot be used as final visual parity evidence until ISS-107
  is resolved.
- 2026-07-01: After removing the local probe/help overlay and restoring the
  Bevy water material shape in ISS-107, the 90-frame capture
  `/tmp/mgstudio_atmosphere_iss107_water.png` still shows a large vertical
  right-half color block and does not visually match
  `/tmp/bevy_atmosphere_compare.png`. The run log is empty and image sanity
  passes (`mean_luma = 135.4505941110167`), so the remaining gap is a real
  render/depth/scene-owner parity problem rather than the old black-sky
  dispatch blocker.
- 2026-07-01: Added the native deferred-lighting bridge required by the
  atmosphere example's deferred path: the renderer now builds the
  `copy_deferred_lighting_id` depth-only pipeline, the deferred-lighting
  fullscreen pipeline, and the matching view/depth-id bind groups. Validation
  exposed two real local integration bugs and both are fixed: the copy pipeline
  must declare zero color targets for Bevy's depth-only pass, and deferred
  lighting must compute mesh-view dynamic offsets from the same
  `MeshPipelineViewLayoutKey` used to create the view bind group. The standard
  90-frame capture now completes at
  `/tmp/mgstudio_atmosphere_deferred_lighting.png` without the previous
  `set_pipeline` color-target or `set_bind_group` dynamic-offset validation
  failures. The issue remains open because exact Bevy visual parity and the
  remaining source-owner audit are not complete.
- 2026-07-01: Ported the next atmosphere environment-map source slice: the
  main-world `AtmosphereEnvironmentMap` image creation path now creates a
  cubemap source for `GeneratedEnvironmentMapLight`, the generated light-probe
  owner creates diffuse/specular target cubemaps, and the renderer owns a
  Bevy-shaped `environment.wgsl` compute bind group/pipeline that writes an
  `rgba16float` 2D-array storage view. Exact visual parity is still blocked:
  the full Bevy dispatch shape for a 512x512 cubemap does not complete the
  standard native capture gate, while disabled, `1x1x1`, and small `8x8x6`
  diagnostic dispatches complete. The scaling/root-cause investigation is
  split to ISS-167 and must not be hidden by a smaller-dispatch workaround.
- 2026-07-01: Follow-up sampling corrected the immediate failure boundary for
  the current full atmosphere run. The delay-frame-1 capture now times out
  before reaching the environment compute pass because the generic
  `pbr_opaque_mesh_pipeline` is still composing `pbr/render/pbr.wgsl` through
  `moon_wgsl_naga_oil`. This is tracked by ISS-165. ISS-167 remains the
  follow-up task for measuring the environment cubemap compute dispatch after
  the PBR shader composition blocker is unblocked.
- 2026-07-04: ISS-167 is closed on the current stack. The standard 90-frame
  atmosphere capture now completes with the full Bevy-shaped environment
  cubemap dispatch path enabled, and the captured PNG matches
  `examples/3d/atmosphere/reference.png` byte-for-byte. This issue remains
  open for the remaining AtmospherePlugin render-resource source-owner audit,
  not because of the old environment dispatch scaling failure.
- 2026-07-04: Removed local facade symbols that made the atmosphere owner look
  more complete than it is: `pbr/atmosphere/node.mbt` and the unused bool-only
  resource placeholders for bind-group layouts, pipelines, transforms, bind
  groups, sampler, and buffer. The real runtime implementation remains in
  `render/renderer/gpu_atmosphere.mbt` and the camera main-pass bridge. This
  keeps the local gap visible: the remaining work is to move ownership toward
  Bevy's `AtmospherePlugin::finish` render/Core3d systems instead of counting
  unused facade structs as source parity.
- 2026-07-04: Tightened the extracted atmosphere payload to mirror Bevy's
  `ExtractedAtmosphere` shape: render extraction now carries
  `inner_radius`, `outer_radius`, `ground_albedo`, `medium`, and
  `world_to_atmosphere` instead of copying the whole `Atmosphere` component
  into the render payload. The old nested-component shape made later render
  resource preparation less source-shaped even though the numeric result was
  equivalent for Earth.
- 2026-07-09: Moved the Bevy `resources.rs` uniform ownership slice out of the
  renderer bridge and into `pbr/atmosphere/resources.mbt`. `GpuAtmosphere`,
  `GpuAtmosphereSettings`, and `AtmosphereTransform` now produce their own GPU
  uniform bytes, and the renderer only creates and writes GPU resources. This
  removes the duplicate local renderer-side packing path, including the old
  non-Bevy clamp that silently forced atmosphere sample counts from 0 to 1.
  Verified with `moon -C mgstudio-engine test --release pbr render
  examples/3d/atmosphere` and a `MOONBIT_NEW_NATIVE=1` 90-frame atmosphere
  capture at `/tmp/mgstudio_iss065_atmosphere_bytes_owner.png`.
- 2026-07-09: Ported the Bevy render-sky MSAA specialization shape in the
  renderer bridge. The atmosphere renderer now owns both non-MSAA and MSAA
  render-sky bind group layouts, lazily specializes the MSAA render pipeline by
  the current view sample count, selects the multisampled depth binding shape,
  and opens the render pass with a resolve target when the view is MSAA. This
  removes the previous fatal `atmosphere render-sky MSAA pipeline is not
  implemented`. Verified with `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine test --release pbr render examples/3d/atmosphere`,
  and a `MOONBIT_NEW_NATIVE=1` 90-frame atmosphere capture at
  `/tmp/mgstudio_iss065_atmosphere_render_sky_msaa.png`.

## Close Notes
