# ISS-492: meshlet example renders an empty frame (both reference vintages broken)

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: meshlet, render, examples, visual-parity, regression
- Assignee: unassigned
- Created: 2026-08-05
- Updated: 2026-08-05
- External ref: none

## Description

Human judgment on the ISS-481 chunk-2 capture: "meshlet is empty." The
fresh frame-90 capture of `examples/3d/meshlet` contains no meshlet
scene content — a blank light frame (mean_luma 178.9, 100% non-black),
which fooled the automated screening: `visual_image_sanity` passes on
brightness/non-blackness alone, and the old-vs-new delta (178.97
mean_abs_rgb) read as "improvement" only because the OLD committed
reference is a fully BLACK broken frame (mean_luma 0.0, sanity FAIL).
Both vintages are broken states: black then, empty-light now. The
capture is byte-stable across two runs, so the empty render is
deterministic, with a clean run log (nothing crashes — the content is
silently absent).

`bevy/examples/3d/meshlet.rs` exists at the pin, so a pinned-Bevy anchor
is available (note: the Bevy example may need the `meshlet` feature and
its preprocessed asset to run).

## Evidence

- New capture: `.private/parity/artifacts/iss481/c2_meshlet.png`
  (hash 8f0ba55eeae399c2..., byte-identical re-run).
- Old committed reference: 100% black, sanity FAIL.
- Human judgment: FAIL ("meshlet is empty"), VIS-481-b2c2.

## Acceptance Criteria
- [ ] The missing meshlet rendering is root-caused at the meshlet
  runtime owner (ISS-019 arc territory) with staged probes.
- [ ] A frame-90 capture shows the meshlet scene and is human-judged
  against a pinned-Bevy spot capture; the reference then refreshes
  through ISS-481.

## Relationships
- Related: ISS-019 (meshlet/prepass runtime arc owner).
- Related: ISS-481 (sweep row held; old broken reference retained until
  a real render exists).

## Notes
- 2026-08-05: Filed from the VIS-481-b2c2 human review. Method lesson
  recorded in ISS-481: brightness-based sanity cannot distinguish an
  empty-light frame from content; already-broken references make
  old-vs-new deltas meaningless for feature presence.
