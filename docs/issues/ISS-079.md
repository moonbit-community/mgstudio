# ISS-079: Fix rotate_environment_map skybox and environment lighting parity

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: examples, pbr, environment-map, skybox, visual-parity, source-parity
- Assignee: unassigned
- Created: 2026-06-12
- Updated: 2026-06-12
- External ref: none

## Description
`examples/3d/rotate_environment_map` still has a fully transparent checked-in `reference.png`. A standard native capture run succeeds and passes the generic image sanity threshold, but the generated image only shows a dark, featureless sphere on a near-black background.

The Bevy source owner demonstrates rotating `Skybox` and `EnvironmentMapLight` together. The mgstudio capture does not show the expected skybox/environment-map visual signal, so the capture must not be accepted as a refreshed reference.

## Design
Audit the example and the PBR skybox/environment-map render path against `bevy/examples/3d/rotate_environment_map.rs` and the relevant Bevy PBR owners. Fix local missing or approximate behavior rather than accepting the dark capture.

## Acceptance Criteria
- [ ] `examples/3d/rotate_environment_map` is source-shape audited against Bevy's `rotate_environment_map.rs`.
- [ ] The rendered output shows visible skybox/environment-map contribution and a metallic clearcoat sphere, not only a dark diffuse sphere.
- [ ] `mgstudio-engine/examples/3d/rotate_environment_map/reference.png` is refreshed with the standard native capture command after the visual signal is present.
- [ ] The refreshed reference passes automated image sanity and manual visual inspection.

## Relationships
- Depends on: none
- Parent: ISS-060
- Related: ISS-012
- Discovered from: ISS-060

## Notes
- 2026-06-12: Standard capture command succeeded with `alpha_mean=1`, `gray_mean=0.304705`, `size=2560x1440`, and the generic sanity script passed. Manual review rejected the output because it lacked the visible skybox/environment-map features expected from the Bevy source. The generated image was discarded.

## Close Notes
