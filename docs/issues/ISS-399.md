# ISS-399: Consolidate duplicated 2D and 3D material runtime plumbing

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: pbr, sprite-render, materials, architecture, source-parity
- Assignee: unassigned
- Created: 2026-07-06
- Updated: 2026-07-06
- External ref: none

## Description
The 3D PBR material path and 2D mesh material path maintain similar typed keys,
prepared-asset resources, runtime registries, plugin setup, and entity
extraction logic independently. Bevy has separate `Material` and `Material2d`
owners, but both are backed by common render-asset and render-resource
concepts. The duplicated local plumbing is an architecture debt.

## Design
After ISS-396 and ISS-398 settle the key and lookup shape, extract the shared
typed-material runtime pieces into a common render material support layer. Keep
domain-specific pipeline specialization and phase selection separate for PBR and
2D.

## Acceptance Criteria
- [x] Shared material-runtime responsibilities are factored out of PBR and 2D
      without changing public material authoring behavior.
- [x] PBR-specific material properties and 2D-specific phase/pipeline logic
      remain in their source-owner packages.
- [x] Existing 2D and 3D material tests pass.
- [x] `moon fmt`, `moon info`, and `moon check` pass.

## Relationships
- Depends on: ISS-396, ISS-398
- Parent: ISS-395
- Related: ISS-022, ISS-155
- Discovered from: ISS-395

## Notes
- 2026-07-06: This should not be attempted before the material key and lookup
  shapes are corrected; otherwise it would only consolidate the wrong
  abstraction.

## Close Notes
Closed 2026-07-06. Extracted shared entity-indexed render material slot
storage into `Milky2018/mgstudio/material` while keeping PBR and 2D material
owner-specific resource wrappers, pipeline specialization, and phase selection
in their source-owner packages. Verified with:
`MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine test material --target native --release`,
`MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine test pbr/material_plugin_wbtest.mbt --target native --release`,
`MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine test sprite_render/mesh2d/material --target native --release`,
`moon -C mgstudio-engine fmt`,
`moon -C mgstudio-engine info`,
`MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine check --target native`, and
`MOONBIT_NEW_NATIVE=1 MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/2d/mesh2d_custom_material /tmp/mgstudio_iss399_mesh2d_custom_material.png`.
The captured image matched `examples/2d/mesh2d_custom_material/reference.png`
with RMSE `0 (0)`.
