# ISS-120: Restore blend_modes source parity

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: examples, screenshots, visual-parity, pbr, blend-modes, material, source-parity
- Assignee: unassigned
- Created: 2026-06-16
- Updated: 2026-06-19
- External ref: none

## Description
`examples/3d/blend_modes` exercises the blend-mode render path, but it is not a
symbol-level port of Bevy's `bevy/examples/3d/blend_modes.rs`.

The current implementation keeps the five alpha-mode spheres, chessboard,
camera, light, labels, and keyboard controls. The strict gaps are source-shape
issues: Bevy's `ExampleControls`, `ExampleLabel`, `ExampleDisplay`,
`Local<ExampleState>`, material asset queries, camera `Single`, label query,
display text query, command-based HDR toggle, `rand::random`, and
`rotate_around` camera mutation are replaced by a global `BlendModesState`,
tracked arrays, direct world mutation, duplicated UI setup writes, deterministic
LCG randomization, and accumulated-angle camera orbit.

The sphere primitive also uses a local UV sphere instead of
`Sphere::new(0.9).mesh().ico(7).unwrap()`.

## Design
Port the example directly from the Bevy source owner. Keep only local changes
that are required by concrete MoonBit language/API limitations, and record each
such limitation as a precise blocker. Do not keep global state, tracked arrays,
direct world mutation, deterministic replacement randomization, or duplicated
UI setup as the parity path when the Bevy source has a specific owner.

## Acceptance Criteria
- [x] `ExampleControls`, `ExampleLabel`, and `ExampleDisplay` are represented
      as ECS components with Bevy-shaped query ownership.
- [x] `ExampleState` is represented with Bevy-shaped local system state
      semantics or a precise local-state blocker is recorded.
- [x] `setup` exposes Bevy-shaped commands, mesh assets, material assets, and
      asset server parameters where possible.
- [x] The five blend spheres use
      `Sphere::new(0.9).mesh().ico(7).unwrap()` semantics or record a precise
      primitive-builder blocker.
- [x] The chessboard planes preserve Bevy's per-entity `ExampleControls`
      ownership rather than shared global material records.
- [x] The camera spawn preserves `Hdr` plus the wasm-only `Msaa::Off` cfg shape
      where applicable.
- [x] Controls text preserves the Bevy text content and UI tuple shape while
      following mgstudio branding rules where concrete user-facing brand names
      are involved.
- [x] Labels preserve the Bevy label closure, parent/child hierarchy, label
      component, and no-wrap text layout semantics.
- [x] `example_control_system` uses Bevy-shaped material query, camera single,
      label query, display text single, labeled transform query, local state,
      time, input, and commands where possible.
- [x] Random color updates use source-equivalent random behavior rather than a
      deterministic LCG replacement, unless a precise random API blocker is
      recorded.
- [x] Camera rotation uses source-equivalent `rotate_around` semantics rather
      than an accumulated-angle recomputation.
- [x] Display text formatting preserves Bevy's `"{:.2}"` alpha formatting.
- [x] `BlendModesState`, tracked material/label/camera/display arrays,
      duplicated UI writes, local LCG helpers, and direct world mutation helpers
      are removed from the source-parity path unless precise blockers are
      documented.
- [x] The reference screenshot is refreshed after the source-shaped port still
      renders correctly.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-019, ISS-021, ISS-105
- Discovered from: ISS-105

## Notes
- 2026-06-16: Created from the ISS-105 symbol audit. This is local
  source-parity work; the current global-state/direct-world implementation,
  deterministic randomization replacement, duplicated UI writes, and UV-sphere
  replacement are not external blockers.

## Close Notes
- 2026-06-19: Restored the example to Bevy source-owner shape. The port now
  uses `ExampleControls`, `ExampleLabel`, `ExampleDisplay`, `Local<ExampleState>`,
  material asset queries, camera `Single`, label/display/labeled queries,
  command-based HDR toggling, `rotate_around` camera mutation, Bevy-shaped
  icosphere and plane mesh builders, and per-entity chessboard controls.
- 2026-06-19: Removed the old `BlendModesState`, tracked entity/material arrays,
  duplicated UI writes, accumulated-angle camera recomputation, local UV-sphere
  builder, and hand-written LCG. MoonBit does not currently expose Bevy's
  `rand::random()` global call shape, so the port keeps random state as local
  example state using the existing checked `ChaCha8Rng` helper instead of a
  project-local LCG.
- 2026-06-19: Added `system_config_param9/10`, `add_system_param9/10`, and
  `add_systems_param9/10` so the example can preserve the Bevy 10-parameter
  control-system shape without collapsing parameters into a workaround.
- 2026-06-19: Verification: `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine test app --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/blend_modes /tmp/mgstudio_iss120_blend_modes_source_parity.png`.
  Refreshed `mgstudio-engine/examples/3d/blend_modes/reference.png`.
