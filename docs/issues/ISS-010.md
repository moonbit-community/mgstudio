# ISS-010: Revalidate mesh preparation after render and skinning fixes

## Metadata
- Type: task
- Status: closed
- Priority: 3
- Labels: performance, many_foxes, mesh, render
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-24
- External ref: none

## Description
Mesh preparation costs about `3.096 ms/frame`. It is not the first optimization
target, but it remains large enough that it may become a blocker after transform,
animation, skinning, and render execution improve.

## Design
Do not prematurely rewrite mesh preparation. Re-profile after the higher-priority
issues are closed, then port any remaining differences from Bevy's mesh prepare
path directly.

## Acceptance Criteria
- [x] Mesh preparation has counters for prepared meshes, buffer uploads,
      skipped unchanged meshes, and cache hits.
- [x] The post-optimization profile states whether mesh preparation remains
      above `1 ms/frame`.
- [x] Any remaining implementation changes are source-equivalent to Bevy's
      mesh preparation logic.
- [x] Relevant mesh and render tests pass with `--release` if code changes are
      made.

## Relationships
- Depends on: ISS-002, ISS-005, ISS-006, ISS-007
- Parent: ISS-001
- Related: none
- Discovered from: none

## Notes
- Current evidence: `mgstudio.render.prepare_meshes=3.096 ms/frame`.

## Close Notes
- 2026-06-24: Closed after adding durable timeline counters to
  `mgstudio.render.prepare_meshes`: `render_3d_prepare_meshes.prepared_meshes`,
  `buffer_uploads`, `skipped_unchanged_meshes`, `mesh_upload_cache_hits`,
  `mesh_upload_cache_misses`, `preprocess_static_cache_hits`, and
  `preprocess_static_cache_misses`. The counters are diagnostics only and do
  not alter render behavior.
- 2026-06-24: Current many_foxes release profile:
  `/tmp/mgstudio_many_foxes_iss010_prepare_mesh_counters/frametime_breakdown.md`.
  It reports `mgstudio.render.prepare_meshes=2.899 ms/frame`,
  `prepared_meshes=1001`, `buffer_uploads=1`,
  `skipped_unchanged_meshes=1000`, `mesh_upload_cache_hits=1000`,
  `mesh_upload_cache_misses=1`, `preprocess_static_cache_hits=1000`, and
  `preprocess_static_cache_misses=1`. Mesh preparation therefore remains above
  `1 ms/frame`; the real source-parity optimization follow-up is `ISS-153`.
- 2026-06-24: Validation passed:
  `moon -C mgstudio-engine check --target native --release --deny-warn
  --package-path pbr/render`, `moon -C mgstudio-engine check --target native
  --release --deny-warn --package-path pbr`,
  `moon -C mgstudio-engine test --release pbr/render --deny-warn`, and
  `moon -C mgstudio-engine test --release pbr --deny-warn`.
