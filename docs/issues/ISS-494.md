# ISS-494: Distance-fog binding still flakes whole-frame off under startup perturbation

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: pbr, fog, render, determinism, regression-risk
- Assignee: unassigned
- Created: 2026-08-05
- Updated: 2026-08-05
- External ref: none

## Description

The ISS-483 fourth fix (resolving the per-camera fog binding through
`SortedCameras` by id) removed one source of whole-frame fog dropout, but
a residual startup-timing fragility remains somewhere in the fog chain.
Registering `MaterialPlugin[ForwardDecalMaterial]` (ISS-482's fix — new
systems + ecs key registrations, no fog-related code) was enough to
reproduce a full-frame fog-off atmospheric_fog capture again:

- Capture-script run with the ISS-482 slice: hash `8ed1ed8d7469748a...`,
  sky flat (129,129,129), terrain green-but-unfogged — everything except
  DistanceFog correct (tonemap applied, materials fine).
- Repeat runs with probes: fog EXACT at the ISS-483 parity values
  ((80,114,167) sky, (226,228,229) glow), including a frame-90 run —
  intermittent, roughly 1-in-4 on the day.
- Probes in the good runs show every checked link healthy:
  `render3d_prepare_camera_fog_uniforms` sees the render-world camera
  with the DistanceFog component (`has_fog=true` every frame), and the
  pass config computes `fog_enabled=true` every frame. The failing link
  therefore sits between those probes and the uploaded uniform (the
  binding/offset path or the pass-kind consumption) OR the probes'
  observed runs simply never hit the failure; a failing-run probe capture
  is still missing.

## Repro

Standard frame-90 capture of `examples/3d/atmospheric_fog`, repeated;
compare against canonical `aaacaca88b53a7e6...`. A failing run renders
the whole frame fog-less (mean_abs_rgb ~33 vs the reference).

## Next step (recorded)

Loop the capture with a FULL-chain probe set (prepare offsets ->
`render3d_apply_view_fog_uniform_offsets` writes ->
`render3d_prepare_camera_fog_binding` resolution incl. offset lookup and
FogMeta hit/miss -> `host_gpu_prepare_mesh3d_fog` values -> pass-kind
fog bit) until a failing run is caught with probes active; pty capture
per the buffering hazard. Fix the owner outright — the ISS-483 fourth
fix's SortedCameras resolution shows the pattern.

## Impact

- The atmospheric_fog byte gate (`aaacaca88b53a7e6...`) is UNSTABLE until
  fixed: gate mismatches on that example must be pixel-diffed against the
  installed reference before being read as regressions.
- Blocks trusting single captures of any DistanceFog scene in ISS-481.

## Relationships
- Related: ISS-483 (fourth fix covered the entity-generation symptom).
- Related: ISS-482 (whose fix's system registrations perturb startup
  enough to surface this).
- Related: ISS-484 (the other startup-timing capture hazard; different
  mechanism — that one shifts sim phase, this one drops a binding).

## Notes
- 2026-08-05: Filed while landing ISS-482's decal fix; the slice itself
  is exonerated by bisect (disabling only the MaterialPlugin
  registration returns the gate to canon byte-exactly, and re-enabling
  it produces correct fog in probed runs — the perturbation only changes
  timing).
