# ISS-073: Fix voxels3 reference framing before refresh

## Metadata
- Type: bug
- Status: open
- Priority: 2
- Labels: examples, screenshots, visual-parity, physics3d, camera
- Assignee: unassigned
- Created: 2026-06-12
- Updated: 2026-06-12
- External ref: none

## Description
`examples/3d/voxels3/reference.png` is currently fully transparent, but a standard 90-frame native capture produces an unusable replacement: the scene is nonblank, yet all visible content is reduced to a tiny point near the center of the frame.

The example sets its camera at `(-100, 100, -100)` looking at `(0, 0, 0)`, while the spawned dynamic bodies are distributed across a `0..64` X/Z area. The resulting capture is not a useful visual regression baseline even though it passes a naive nonblank image sanity check.

## Design
Do not refresh `voxels3/reference.png` until the example framing has been checked against the intended example behavior. If this example has a Bevy source owner, fix the camera and scene setup by porting that source shape. If it is mgstudio-local, choose a deterministic framing that actually captures the spawned voxel/physics content and record the local ownership decision.

## Acceptance Criteria
- [ ] `examples/3d/voxels3` captures visible scene content at normal reference scale.
- [ ] The refreshed reference is produced by `mgstudio-engine/scripts/p0_visual_capture_native.sh`.
- [ ] The fix is documented in ISS-060 or the relevant visual parity issue before `voxels3/reference.png` is refreshed.

## Relationships
- Depends on: none
- Parent: ISS-047
- Related: ISS-052, ISS-060
- Discovered from: ISS-060

## Notes
- 2026-06-12: Standard command `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=600 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/voxels3 mgstudio-engine/examples/3d/voxels3/reference.png` produced `alpha_mean=1`, `gray_mean=0.387914`, `size=2560x1440`, but the visual content was only a tiny central object. The generated image was discarded.

## Close Notes
