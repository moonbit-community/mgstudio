# ISS-462: Align many_animated_sprites with pinned Bevy source

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, stress-test, sprites, animation, camera, source-parity
- Assignee: codex
- Created: 2026-07-16
- Updated: 2026-07-16
- External ref: none

## Description

The Camera2d visibility regression in ISS-457 is fixed, but
`examples/stress_tests/many_animated_sprites` still does not faithfully match
the pinned Bevy example source. The mgstudio example uses deterministic index
patterns, a 28-by-28 custom sprite size, per-entity FPS values, zero initial
timer elapsed time, additive world-axis camera translation, and default window
configuration. Pinned Bevy uses random depth/rotation/scale, a 64-by-64 custom
size, one 0.1-second repeating timer with randomized elapsed time, local camera
transform composition, and a 1920-by-1080 no-vsync window.

These differences change the visual distribution, animation phase, culling
load, and performance workload even though the sprite field now renders.

## Design

Port the pinned Bevy example structure without deterministic substitutions:

- configure the primary window and diagnostics plugins as in Bevy;
- use a 320-by-320 grid with 64-by-64 sprite custom sizes;
- randomize translation depth, rotation, scale, and initial timer elapsed time
  through the project's source-equivalent random API;
- use one 0.1-second repeating animation period and derive atlas wrapping from
  the layout texture count;
- preserve Bevy's update ordering and local camera transform composition;
- keep the example suitable for the existing stress/performance workflow.

Do not tune the values to reproduce the current reference image. Validate
behavior and workload against the pinned Bevy source, then establish a new
human-reviewed reference only after source parity is complete.

## Acceptance Criteria

- [x] Scene construction fields and randomization match pinned Bevy ownership
      and value ranges.
- [x] Animation timer initialization and atlas advancement match pinned Bevy.
- [x] Camera movement and update ordering match pinned Bevy.
- [x] Window and diagnostics configuration match the fixed baseline where the
      corresponding mgstudio APIs exist.
- [x] A native capture shows the expected moving sprite field after delayed
      frames, and a focused test covers deterministic structural invariants.
- [x] Performance comparison uses the same 102,400-sprite workload as Bevy.

## Relationships
- Depends on: none
- Parent: ISS-001
- Related: ISS-021, ISS-047, ISS-157, ISS-457
- Discovered from: ISS-457

## Notes

- 2026-07-16: Created while closing the optimization-era visibility regression.
  The current frame-90 output is visually healthy but differs from the local
  reference with `AE=855001` and normalized `RMSE=0.107838`; source inspection
  shows the example itself is not a faithful Bevy port.
- 2026-07-16: Claimed for a source-faithful port and comparable native-release
  performance measurement against the pinned Bevy baseline.
- 2026-07-16: Ported the 320-by-320 randomized scene, 64-by-64 sprite size,
  randomized animation phase, 0.1-second repeating timer, atlas-length wrapping,
  local camera transform composition, update ordering, 1920-by-1080 no-vsync
  window, and Bevy-owned diagnostics plugin shape.
- 2026-07-16: The native frame-90 capture used the requested frame without a
  retry, produced an empty run log, and shows the expected randomized sprite
  field. The reviewed 1920-by-1080 artifact became the new local reference;
  random scene construction intentionally prevents pixel-deterministic replay.
- 2026-07-16: Comparable 20-second native-release runs used 102,400 sprites,
  1920-by-1080 resolution, scale factor 1.0, and no vsync. Mgstudio's final
  diagnostic window reported `8.0271 FPS` average (`121.4127 ms` current frame);
  pinned Bevy reported `410.3411 FPS` average (`2.7629 ms` average frame time).
  The `51.12x` FPS gap is a severe performance gap whose cause remains
  unverified and is tracked by ISS-463.

## Close Notes

The stress example now follows the pinned Bevy source instead of deterministic
local substitutions. Focused native-release tests cover benchmark window/timer
configuration and rotated-local-axis camera motion. The package check passes,
the frame-90 visual gate renders the complete randomized workload without a
runtime error, and comparable performance evidence is recorded above.

ISS-463 owns the required trace-first diagnosis of the remaining severe
performance gap; no speculative optimization is included in this source-parity
change.
