# ISS-493: mirror example renders no mirror reflection

## Metadata
- Type: bug
- Status: open
- Priority: 2
- Labels: render, reflections, examples, visual-parity
- Assignee: unassigned
- Created: 2026-08-05
- Updated: 2026-08-05
- External ref: none

## Description

Human judgment on the ISS-481 chunk-2 capture: "mirror has no mirror."
The fresh frame-90 capture of `examples/3d/mirror` is missing the
example's defining feature — the mirror reflection surface renders
without reflecting the scene. The old-vs-new delta was small (5.93
mean_abs_rgb, 8.4% strong), meaning the OLD committed reference is
missing the reflection THE SAME WAY — an already-broken-both-vintages
case that old-vs-new screening structurally cannot catch (the ISS-483 /
atmospheric_fog method limit again). Clean run log; nothing crashes.

`bevy/examples/3d/mirror.rs` exists at the pin, so a pinned-Bevy anchor
is available for the repair.

## Evidence

- New capture: `.private/parity/artifacts/iss481/c2_mirror.png` (clean
  log, sanity pass).
- Old-vs-new: 5.93 mean / 8.4% strong — two mirror-less frames.
- Human judgment: FAIL ("mirror has no mirror"), VIS-481-b2c2.

## Acceptance Criteria
- [ ] The missing reflection is root-caused at its owner (whatever the
  example uses: screen-space reflections, a reflection probe, a
  render-to-texture mirror camera — establish from the example source
  and the Bevy original first).
- [ ] A frame-90 capture shows the reflection and is human-judged
  against a pinned-Bevy spot capture; the reference then refreshes
  through ISS-481.

## Relationships
- Related: ISS-481 (sweep row held).
- Related: ISS-104 (ssr owner) if the example routes through SSR —
  unverified.

## Notes
- 2026-08-05: Filed from the VIS-481-b2c2 human review.
