# ISS-053: Fix UI text and widget capture failures

## Metadata
- Type: task
- Status: closed
- Priority: 2
- Labels: examples, screenshots, ui, text, widgets
- Assignee: unassigned
- Created: 2026-06-07
- Updated: 2026-06-19
- External ref: none

## Description
Two UI examples failed screenshot capture even though nearby UI and text
examples produced reference screenshots. These failures are likely to represent
example-specific capture or runtime bugs.

Failed packages:

- `examples/ui/text/text_debug`
- `examples/ui/widgets/vertical_slider`

## Design
Compare these packages with adjacent successful UI examples and the matching
Bevy source behavior. Fix the underlying UI/text/widget capture path or
classify the package as requiring a non-standard deterministic fixture.

## Acceptance Criteria
- [x] `examples/ui/text/text_debug` either produces `reference.png` or has a documented exclusion reason.
- [x] `examples/ui/widgets/vertical_slider` either produces `reference.png` or has a documented interaction fixture.
- [x] Any UI runtime bug discovered during triage is split into a follow-up issue or fixed before closure.

## Relationships
- Depends on: none
- Parent: ISS-047
- Related: ISS-021
- Discovered from: ISS-047

## Notes
- 2026-06-07: The surrounding UI suite largely captured successfully, so these should not be dismissed as a broad UI capture limitation.
- 2026-06-19: Follow-up ISS-139 tracks the remaining source-level visual parity question for dynamic UI text root/span layout in `text_debug`.

## Close Notes
Closed 2026-06-19.

- Fixed UI glyph rendering in the UI pass by routing UI text sprite slices through a UI textured-rect path instead of the generic sprite draw path.
- Aligned `DefaultPlugins` with Bevy-shaped default UI input/widget coverage by adding `InputDispatchPlugin` and `UiWidgetsPlugins`.
- Rewrote `examples/ui/text/text_debug` to use Bevy's `TextChanges` root marker and `TextUiWriter` flattened text indices instead of three separate mutable `TextSpan` singletons.
- Saved `mgstudio-engine/examples/ui/text/text_debug/reference.png` and `mgstudio-engine/examples/ui/widgets/vertical_slider/reference.png`.
- Verification: `moon -C mgstudio-engine check --target native examples/ui/text/text_debug`; `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/ui/text/text_debug /tmp/mgstudio_iss053_text_debug_final2.png`; `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/ui/widgets/vertical_slider /tmp/mgstudio_iss053_vertical_slider_fixed3.png`; `moon -C mgstudio-engine test --release ui`; `moon -C mgstudio-engine test --release ui_widgets`; `moon -C mgstudio-engine test --release input_focus`; `moon -C mgstudio-engine test --release text`; `moon -C mgstudio-engine check --target native`; `moon -C mgstudio-engine fmt`; `moon -C mgstudio-engine info`.
