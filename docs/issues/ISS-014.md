# ISS-014: Port RenderPlugin lifecycle and recovery semantics

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, render, renderer, lifecycle
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-22
- External ref: none

## Description
Bevy's `RenderPlugin` creates and configures the render sub-app, installs
render startup and recovery schedules, initializes GPU resources, extracts
shader and asset budget state, processes the pipeline cache, submits pending
command buffers, handles screenshot/readback commands, and presents windows.
Mgstudio has working renderer pieces, but the complete lifecycle is not yet
source-equivalent.

## Design
Use `bevy/crates/bevy_render/src/lib.rs` and
`bevy/crates/bevy_render/src/renderer/mod.rs` as the owner sources. Port the
startup, recovery, render-system, cleanup, and resource initialization behavior
directly. Keep render-device loss and recovery as first-class behavior, not as
incidental backend state.

## Acceptance Criteria
- [ ] `RenderStartup`, render recovery, render schedule order, and render sub-app update flow match Bevy's source ownership.
- [ ] `PendingCommandBuffers` and GPU resource initialization are owned by the render world and re-run on recovery where Bevy does so.
- [x] Pipeline cache processing is chained with the render system in the Bevy-shaped render set.
- [ ] Screenshot and GPU readback commands are submitted in the same lifecycle phase as Bevy.
- [ ] Window present behavior follows Bevy's view-target `needs_present` and initial-present logic as closely as the window dependency allows.
- [ ] Recovery and frame-lifecycle tests cover startup, render, cleanup, and present ordering.

## Relationships
- Depends on: ISS-013
- Parent: ISS-012
- Related: ISS-015, ISS-016
- Discovered from: none

## Notes
- 2026-06-02: Current roadmap says RenderPlugin has partial resource wiring, but renderer ownership and render-graph integration still diverge substantially.
- 2026-06-04: Started porting Bevy's `RenderRecovery` + `RenderScheduleOrder` update flow.
- 2026-06-04: Added Bevy-shaped `renderer_is_ready` gating to `run_render_schedule`; GPU resource installation now marks the render world ready before recovery runs the render schedule.
- 2026-06-04: Corrected render-state startup ownership. `RenderState::Initializing` now runs `RenderStartup` through the render sub-app schedule during extract before transitioning to `Ready`; resource installation no longer marks the renderer ready directly. The app sub-app extract phase now exposes the current sub-app schedule so extract code can run Bevy-shaped startup work against the render world.
- 2026-06-04: Pipeline cache processing cannot be honestly closed by adding a no-op schedule system. Mgstudio's global `PipelineCache` is still a local facade and PBR/material specialization allocates cached IDs outside it. The remaining work is a real port of Bevy's descriptor queue/state machine plus migration of material/PBR specializers to queue descriptors into that resource.
- 2026-06-04: Replaced the global `PipelineCache` string-map facade with Bevy-shaped `Pipeline`, `CachedPipeline`, `CachedPipelineState`, descriptor queueing, stable cached IDs, state queries, descriptor access, and waiting-pipeline tracking. This is still not enough to close pipeline processing: `process_queue` must create real GPU pipelines through a general descriptor-to-wgpu builder before the render schedule can claim Bevy parity.
- 2026-06-05: Ported Bevy's render-recovery time handoff. The time package now exposes Bevy-shaped `TimeSender`, `TimeReceiver`, and `create_time_channels`; `time_system` consumes render-world sent instants in automatic mode; `RenderPlugin` installs the receiver in the main world and sender in the render world; render recovery now runs the render schedule only when ready and sends time every recovery tick afterward.
- 2026-06-05: Added a Bevy-shaped render-schedule slot for pipeline queue admission before render graph execution. This only wires descriptor queue admission into the render lifecycle; GPU pipeline creation and shader-cache-backed descriptor-to-wgpu building remain incomplete local parity work, not an external blocker.
- 2026-06-05: Moved material and PBR mesh pipeline specialization IDs/descriptors into the render-world `PipelineCache`. `SpecializedMeshPipelines` now deduplicates Bevy-style final vertex layouts and queues render descriptors into the cache instead of owning descriptors locally; PBR main, motion-vector prepass, and shadow prepass pipelines are prewarmed before pipeline queue admission instead of being registered from render execution. A render-system set after pipeline queue admission registers queued render descriptors for the current host backend. Remaining work is still true GPU pipeline creation from `PipelineCache`, not PBR-local descriptor storage.
- 2026-06-05: Tightened the temporary host-backend descriptor bridge so it drains `PipelineCache` waiting ids once instead of rescanning every cached pipeline every frame. This keeps the current bridge closer to queue-processing semantics while leaving GPU pipeline Ok/Err state unclaimed until real descriptor-to-wgpu creation is ported.
- 2026-06-11: Split the old local `manage_views` render set into Bevy-shaped
  `CreateViews`, `Specialize`, and `PrepareViews` sets, and ordered
  `PrepareMeshes -> CreateViews -> Specialize -> PrepareViews -> Queue`.
  `core_pipeline` camera preparation and meshlet view configuration now attach
  to `CreateViews`. Added a render schedule ordering regression test and
  verified with `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine check --target native --package-path
  core_pipeline`, `moon -C mgstudio-engine check --target native
  --package-path pbr`, and `moon -C mgstudio-engine test render --release`.
- 2026-06-11: Added the missing Bevy `PrepareResourcesBatchPhases` and
  `PrepareResourcesWritePhaseBuffers` render-system sets and moved the
  prepare-resource subchain under the `Prepare` parent set:
  `PrepareResources -> PrepareResourcesBatchPhases ->
  PrepareResourcesWritePhaseBuffers -> PrepareResourcesCollectPhaseBuffers ->
  PrepareResourcesFlush -> PrepareBindGroups`. Added a regression test for the
  complete prepare-resource ordering and verified it with `moon -C
  mgstudio-engine check --target native --package-path render` plus `moon -C
  mgstudio-engine test render --release`.
- 2026-06-11: Moved pipeline queue admission and the temporary host-backend
  descriptor registration bridge under the Bevy-shaped `Render` parent set, so
  the local chain is now `process_pipeline_queue -> register_pipeline_descriptors
  -> render_graph_driver` inside the render phase. This matches Bevy's lifecycle
  shape more closely, where `PipelineCache::process_pipeline_queue_system` is
  chained immediately before the render system in `RenderSystems::Render`, while
  preserving the explicit note that real descriptor-to-wgpu GPU pipeline
  creation remains incomplete local parity work.
- 2026-06-11: Renamed the local `PipelineCache` queue admission API to Bevy's
  `process_queue` owner method and added the missing `Creating` state shape.
  This removes the old local `accept_new_pipelines_for_processing` terminology
  from source call sites without pretending that the current host-backend
  descriptor bridge has produced a real `Ok(RenderPipeline)` GPU object. The
  remaining local gap is still the descriptor-to-wgpu builder that should move
  queued pipelines into `Creating`/`Ok`/`Err` using real GPU pipeline creation.
- 2026-06-11: Moved `PendingCommandBuffers` initialization out of eager render
  resource unpack/ensure paths and into the Bevy-shaped `RenderStartup`
  schedule. `RenderPlugin` now installs
  `init_pending_command_buffers_resource` on `RenderStartup`, and the regression
  test verifies that the resource is absent after plugin construction but
  present after the render startup schedule runs. This matches Bevy's
  `render_app.init_gpu_resource::<renderer::PendingCommandBuffers>()` ownership
  shape while preserving the existing render-recovery startup path.
- 2026-06-11: Added regression coverage for Bevy's documented `RenderStartup`
  recovery semantics: the schedule can run repeatedly on a non-fresh render
  world and recreate GPU resources. The test removes
  `PendingCommandBuffers` after an initial startup run and verifies that a
  second startup run recreates an empty resource.
- 2026-06-11: Advanced the local `PipelineCache` state machine one step closer
  to Bevy. `process_queue` now moves queued render and compute descriptors into
  `CachedPipelineState::Creating` while preserving waiting-pipeline ids for the
  current host-backend descriptor bridge. This removes the previous ambiguity
  where processed descriptors still reported as `Queued`. Targeted validation:
  `moon -C mgstudio-engine check --target native --package-path render`,
  `moon -C mgstudio-engine test render --release`, `moon -C mgstudio-engine
  fmt`, and `moon -C mgstudio-engine info` passed with 29 render tests.
  Remaining local gap: descriptor-to-wgpu creation still does not transition
  pipelines into real `Ok(RenderPipeline)` / `Ok(ComputePipeline)` or `Err`
  states.
- 2026-06-20: Replaced the local render-graph-driver facade with a
  Bevy-shaped root `render_system` owner from
  `bevy_render/src/renderer/mod.rs`: the render set now runs pipeline queue
  processing, descriptor registration, and then `render_system`, whose body
  drives `RenderGraph::schedule()` before preparing active frame submissions,
  flushing `PendingCommandBuffers`, and presenting active frames. The
  frame-submission/flush/present systems are no longer registered as
  `RenderGraph` systems, so graph `Submit`/`Finish` remain graph phases while
  the backend submit/present tail matches Bevy's root render-system lifecycle.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine test render --release`, `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and the native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_render_system_custom_shader_instancing.png`.
  Remaining local gaps: `PipelineCache` still lacks real descriptor-to-wgpu
  `Ok`/`Err` creation, and window present is still backend-active-frame based
  rather than Bevy's exact `ViewTarget::needs_present` /
  `ExtractedWindow::needs_initial_present` query.
- 2026-06-20: Moved `RenderGraph` base set ordering into
  `RenderGraph::base_schedule()` itself, matching
  `bevy_render/src/renderer/mod.rs` where the returned schedule configures
  `Begin -> Render -> Submit -> Finish`. This required adding initial
  `Schedule::configure_set(s)` support so source-owned schedules can carry
  their own set graph instead of relying on `SubApp` helper calls after
  insertion. The mgstudio-only `configure_base_render_graph_sets` helper now
  only owns the local frame-submission extension sets layered under graph
  `Submit`/`Finish`. Validation: `moon -C mgstudio-engine check --target native
  --package-path app`, `moon -C mgstudio-engine check --target native
  --package-path render`, `moon -C mgstudio-engine test app --release`, `moon
  -C mgstudio-engine test render --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and native `examples/shader_advanced/custom_shader_instancing` capture
  at
  `/tmp/mgstudio_iss014_rendergraph_base_schedule_custom_shader_instancing.png`.
- 2026-06-20: Moved window-frame present decision one step closer to Bevy's
  `render_system` contract. Surface frames now carry a `needs_present` flag,
  surface targets start with `needs_initial_present`, and `target_view` marks a
  surface frame as present-needed only when render code actually writes through
  that surface target. `present_active_frames` now presents frames only when
  `needs_present || needs_initial_present`, and releases acquired-but-unwritten
  frames without presenting. This removes the previous unconditional
  active-frame present behavior while preserving the explicit remaining gap:
  mgstudio still acquires swapchain textures from the winit runner before the
  render app, whereas Bevy owns acquire in `WindowRenderPlugin::prepare_windows`
  before `prepare_view_attachments` / `prepare_view_targets`. Targeted
  validation: `moon -C mgstudio-engine check --target native --package-path
  render/renderer` and `moon -C mgstudio-engine test render/renderer
  --release`. Full validation: `moon -C mgstudio-engine check --target native
  --package-path render`, `moon -C mgstudio-engine test render --release`,
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_present_intent_custom_shader_instancing.png`.
- 2026-06-20: Removed per-frame swapchain acquire from the winit runner and
  installed a render-app `PrepareViews` system named
  `mgstudio_render::prepare_window_surfaces`. This moves the remaining
  initial-present acquire path into the render lifecycle, while camera-target
  surfaces are now acquired by render target activation when a render pass
  actually needs the surface. This is still not complete Bevy `WindowRenderPlugin`
  parity: mgstudio does not yet expose Bevy-shaped `ExtractedWindows`,
  `WindowSurfaces`, `ViewTargetAttachments`, or `ViewTarget` resources, so the
  camera-target acquire path is not yet owned by a `prepare_windows ->
  prepare_view_attachments -> prepare_view_targets` chain. Targeted validation:
  `moon -C mgstudio-engine check --target native --package-path
  render/renderer`, `moon -C mgstudio-engine check --target native
  --package-path render`, `moon -C mgstudio-engine check --target native
  --package-path winit`, `moon -C mgstudio-engine test render/renderer
  --release`, `moon -C mgstudio-engine test render --release`, and `moon -C
  mgstudio-engine test winit --release`. Full validation: `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`, `moon
  -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_render_owned_surface_acquire_custom_shader_instancing.png`.
- 2026-06-20: Removed the reversed `window -> render` screenshot dependency
  that blocked a Bevy-shaped `WindowRenderPlugin` boundary. `Screenshot`,
  `ScreenshotCaptured`, `Capturing`, `Captured`, the screenshot trigger system
  set, and the main-world screenshot queue/finalize systems now live in the
  `render` package, and `RenderPlugin` installs those systems. `WindowPlugin`
  now owns only window lifecycle systems and the `window` package no longer
  imports `Milky2018/mgstudio/render`. Callers were migrated from
  `@window.Screenshot` / `@window.save_to_disk` to
  `@render.Screenshot` / `@render.save_to_disk`. This does not close complete
  Bevy screenshot parity: `ISS-033` still tracks the missing render-app
  `RenderScreenshotTargets`, `RenderScreenshotsPrepared`, sender/receiver, and
  screenshot-to-screen pipeline resource ownership. Validation:
  `moon -C mgstudio-engine check --target native --package-path window`,
  `moon -C mgstudio-engine check --target native --package-path render`,
  `moon -C mgstudio-engine check --target native --package-path dev_tools`,
  `moon -C mgstudio-engine check --target native --package-path winit`,
  `moon -C mgstudio-engine test render --release`, `moon -C mgstudio-engine
  test window --release`, `moon -C mgstudio-engine test dev_tools --release`,
  `moon -C mgstudio-engine test winit --release`, and `moon -C
  mgstudio-engine check --target native`. The fixed native screenshot gate
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss014_screenshot_rehome_custom_shader_instancing.png`
  completed, and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Removed the remaining winit-runner calls into the ECS screenshot
  queue/finalize systems. `RenderPlugin` now names and installs
  `mgstudio_render::screenshot_clear_system` in `First`, and installs a chained
  `mgstudio_render::screenshot_queue_capture_requests ->
  mgstudio_render::screenshot_finalize_capture_requests` pair in `Update`
  under `screenshot_trigger_system_set`. This keeps screenshot ECS lifecycle
  ownership in the render plugin instead of the platform runner. The winit
  runner still owns the environment-variable visual-capture harness's direct
  surface/texture capture request path; that is separate from Bevy's
  `Screenshot` component API and remains outside this ECS owner fix. Validation:
  `moon -C mgstudio-engine check --target native --package-path render`,
  `moon -C mgstudio-engine check --target native --package-path winit`,
  `moon -C mgstudio-engine test render --release`, `moon -C mgstudio-engine
  test winit --release`, and `moon -C mgstudio-engine test dev_tools
  --release`. Full validation also passed with `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, and `moon -C
  mgstudio-engine info`. The fixed native screenshot gate
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/shader_advanced/custom_shader_instancing
  /tmp/mgstudio_iss014_runner_screenshot_system_removal_custom_shader_instancing.png`
  completed, and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Replaced the empty local `RenderPlugin` shell with a
  Bevy-shaped configuration owner. `RenderPlugin` now stores
  `render_creation`, `synchronous_pipeline_compilation`, and `debug_flags`;
  `RenderDebugFlags` exposes the
  `allow_copies_from_indirect_parameters` bit as a MoonBit boolean; and
  `RenderPlugin::build` passes the plugin value into the render install path.
  `Manual(RenderResources)` now unpacks resources into the main/render worlds
  after the render sub-app exists, instead of being impossible to express
  through the plugin. Remaining gap: automatic creation still follows the
  current host-backend initialization path, and the debug flag still needs to
  be threaded into the batching/preprocessing owners. Targeted validation:
  `moon -C mgstudio-engine check --target native --package-path render` and
  `moon -C mgstudio-engine test render --release`. Full validation: `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`, `moon
  -C mgstudio-engine info`, native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_render_plugin_config_custom_shader_instancing.png`, and
  `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-20: Threaded `RenderDebugFlags` into the batching/preprocessing
  owner instead of leaving it as inert plugin state. `GpuPreprocessingPlugin`
  now stores the indirect-parameter readback-copy flag and writes a
  Bevy-shaped `IndirectParametersBuffersSettings` resource into the render app;
  `RenderPlugin` derives that flag from
  `RenderDebugFlags::allow_copies_from_indirect_parameters`. Remaining gap:
  mgstudio's preprocessing buffers are still local typed arrays and renderer
  storage instead of Bevy's exact `IndirectParametersBuffers` GPU resources, so
  this closes the config propagation layer but not full batching resource
  parity. Targeted validation: `moon -C mgstudio-engine check --target native
  --package-path render/batching`, `moon -C mgstudio-engine check --target
  native --package-path render`, `moon -C mgstudio-engine test render/batching
  --release`, and `moon -C mgstudio-engine test render --release`. Full
  validation: `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_batching_debug_flags_custom_shader_instancing.png`,
  and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-21: Aligned `PipelineCache` retryable error semantics with Bevy's
  `ShaderNotLoaded` / `ShaderImportNotYetAvailable` flow. Cached pipeline
  errors are now classified as retryable or fatal; retryable errors stay in the
  waiting set and return to `Queued` through `process_queue`, while fatal errors
  leave the waiting set. The descriptor-registration bridge now only
  materializes `Creating` pipelines and reports shader-not-ready failures back
  to `PipelineCache` instead of driving a separate retry path. Validation:
  `moon -C mgstudio-engine check --target native --package-path
  render/render_resource`, `moon -C mgstudio-engine test
  render/render_resource --release`, `moon -C mgstudio-engine check --target
  native --package-path render`, `moon -C mgstudio-engine test render
  --release`, `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_pipeline_retry_custom_shader_instancing.png`.
  `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-21: Ported the Bevy `FutureRenderResources` recovery handoff shape
  for `RenderState::Reinitializing`. Mgstudio now stores future render
  resources in the main world, consumes them from the reinitializing branch,
  unpacks them into the main/render worlds, and returns the render state to
  `Initializing` so the next extract tick runs `RenderStartup` before marking
  the renderer ready. Remaining gap: the public error-handler policy surface is
  still mgstudio-local (`Recover` does not yet carry a `RenderCreation` value),
  so this closes the resource handoff branch but not the full Bevy
  `RenderErrorHandler::handle` policy API. Validation: `moon -C
  mgstudio-engine check --target native --package-path render`, `moon -C
  mgstudio-engine test render --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, native `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_reinitializing_custom_shader_instancing.png`, and
  `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-21: Added the Bevy-shaped render-owned `RenderErrorPolicy` and
  `RenderErrorHandler` resource and wired `RenderState::Errored` through it.
  `Ignore` now marks the renderer ready, `StopRendering` leaves the error state
  untouched, and `Recover(RenderCreation)` inserts future render resources and
  transitions to `Reinitializing`. `RenderPlugin` initializes the default
  handler resource, and the old render-package forwarding facades to the
  app-level runtime error API were removed. Regression tests cover both
  `Ignore` and `Recover` policies through the render-app extract path.
  Remaining gap at that point: the older app-level runtime rendering controls
  still existed for winit/hotpatch compatibility, so the app package API had
  not yet been deleted or fully reshaped around the render-owned handler.
  Validation: `moon -C
  mgstudio-engine check --target native --package-path render`, `moon -C
  mgstudio-engine test render --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, native `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_render_error_handler_custom_shader_instancing.png`,
  and `visual_image_sanity.py` passed for the generated PNG.
- 2026-06-21: Corrected the `RenderErrorHandler` resource owner to match
  Bevy's `bevy_render/src/error_handler.rs`: the handler now lives in the main
  world, `update_render_state` reads it from the main world while passing both
  main and render worlds into the handler, and the default handler writes
  `AppExit::error()` before returning `StopRendering`. Removed the older app
  package global render-runtime error policy, global rendering stop/resume
  flag, hotpatch forwarding of that flag, and winit runner dependency on
  `app_rendering_enabled`. The `examples/app/render_recovery` input path now
  mutates the main-world `@render.RenderErrorHandler` resource instead of
  calling app-level setter/reporting facades. Remaining gap: the example still
  lacks Bevy's render-app `cause_error` system that triggers real GPU errors
  from the extracted error resource, and `Recover(RenderCreation::default())`
  remains incomplete until automatic future render resource creation is
  Bevy-shaped. Validation: `moon -C mgstudio-engine check --target native
  --package-path render`, `moon -C mgstudio-engine check --target native
  --package-path app`, `moon -C mgstudio-engine check --target native
  --package-path winit`, `moon -C mgstudio-engine check --target native
  --package-path examples/app/render_recovery`, `moon -C mgstudio-engine check
  --target native --package-path examples/app/no_renderer`, `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`, `moon
  -C mgstudio-engine info`, `moon -C mgstudio-engine test app --release`,
  `moon -C mgstudio-engine test winit --release`, and `moon -C mgstudio-engine
  test render --release` passed. The native `custom_shader_instancing` capture
  at
  `/tmp/mgstudio_iss014_main_world_render_error_handler_custom_shader_instancing.png`
  also passed `visual_image_sanity.py`.
- 2026-06-21: Investigated moving `RenderCreation::Automatic` into the
  future-resource handoff path. A direct synchronous implementation passed
  render unit tests but failed the `custom_shader_instancing` native screenshot
  gate with `missing GPU clustering render resource: LightProbesBuffer`.
  Root cause: Bevy only unpacks `FutureRenderResources` in `RenderPlugin::finish`
  after all plugins have run `build`, while mgstudio currently unpacks manual
  resources inside `RenderPlugin` installation. Creating automatic resources in
  that immediate path lets render startup advance before later PBR plugins have
  installed their render startup resources. Correct fix: port Bevy-shaped plugin
  `finish` lifecycle or an equivalent app-level finish phase before enabling
  `Recover(RenderCreation::default())`. Do not patch this by eagerly inserting
  individual PBR resources.
- 2026-06-22: Ported the Bevy-shaped `RenderPlugin::finish` handoff for future
  render resources. `RenderPlugin` now creates automatic future resources when
  the primary backend is available, waits for them through plugin `ready`, and
  unpacks them into the main/render worlds from `finish` instead of during
  `build`. The previous `LightProbesBuffer` failure was fixed at its source:
  `LightProbePlugin` now installs a render-app `RenderStartup` initializer for
  `LightProbesBuffer` instead of placing the GPU buffer in the main world.
  The next exposed failure, `ViewUniforms`, was fixed by restoring the
  Bevy-owned `ViewPlugin` lifecycle symbol and registering it from
  `RenderPlugin`; `ViewPlugin::finish` initializes the render-world
  `ViewUniforms` resource after render resources have been unpacked. Validation:
  `moon -C mgstudio-engine check --target native --package-path
  pbr/light_probe`, `moon -C mgstudio-engine test pbr/light_probe --release`,
  `moon -C mgstudio-engine check --target native --package-path render/view`,
  `moon -C mgstudio-engine test render/view --release`, `moon -C
  mgstudio-engine check --target native --package-path render`, `moon -C
  mgstudio-engine test render --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and native `examples/shader_advanced/custom_shader_instancing`
  capture at
  `/tmp/mgstudio_iss014_plugin_finish_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
  Remaining ISS-014 gaps are unchanged for PipelineCache descriptor-to-wgpu
  `Ok`/`Err` creation and complete `WindowRenderPlugin` /
  `ViewTargetAttachments` parity.
- 2026-06-22: Advanced the remaining `ViewPlugin::finish` resource parity.
  Bevy initializes both `ViewUniforms` and `ViewTargetAttachments` from
  `ViewPlugin::finish`; mgstudio now does the same. This required introducing
  a Bevy-owned camera render-target foundation package for
  `ManualTextureViewHandle`, `ImageRenderTarget`, and `NormalizedRenderTarget`
  instead of keying view attachments by renderer-local target integers. The
  render texture package now exposes a real `OutputColorAttachment` carrying a
  texture view, view format, and first-write/present state. This does not claim
  complete view-target parity: the Bevy `clear_view_attachments`,
  `cleanup_view_targets_for_resize`, `prepare_view_attachments`, and
  `prepare_view_targets` systems still require the larger `ExtractedWindows`,
  `WindowSurfaces`, `ManualTextureViews`, `ExtractedCamera`, and `ViewTarget`
  owner migration. Validation: `moon -C mgstudio-engine test
  camera/render_target --release`, `moon -C mgstudio-engine test render/view
  --release`, `moon -C mgstudio-engine test render/texture --release`, `moon
  -C mgstudio-engine test window --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and native `examples/shader_advanced/custom_shader_instancing`
  capture at
  `/tmp/mgstudio_iss014_view_target_attachments_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Ported the `ManualTextureViews` resource slice required by
  Bevy's `TexturePlugin::build` and `prepare_view_attachments`. The old
  mgstudio manual texture view model only stored `texture_id + mip_level`
  integers, which could collide and did not represent Bevy's
  `ManualTextureView { texture_view, size, view_format }`. It now uses the
  camera-owned `ManualTextureViewHandle`, stores real `TextureView`, `UVec2`
  size, and `TextureFormat`, and exposes a `ManualTextureViews` resource keyed
  by handle. `RenderPlugin` now initializes that resource in both main and
  render worlds and installs the existing extract-resource path for it, matching
  the observable resource lifecycle used by Bevy before
  `prepare_view_attachments`. Remaining owner gap: mgstudio still needs a real
  `TexturePlugin` symbol or package split that can own render-app schedule
  registration without a render/texture import cycle, plus the full
  `prepare_view_attachments -> prepare_view_targets` chain. Validation: `moon
  -C mgstudio-engine test render/texture --release`, `moon -C mgstudio-engine
  test render --release`, `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_manual_texture_views_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Removed the remaining `render/view/window` local integer
  definition for `ManualTextureViewHandle`. Window helper code and screenshot
  texture-view targets now use the camera-owned `ManualTextureViewHandle`,
  matching Bevy's `RenderTarget::TextureView(ManualTextureViewHandle)` owner
  instead of creating a second handle type in the window package. The current
  screenshot bridge still lowers the handle through `.value()` when it reaches
  the existing renderer capture-id API; that is an explicitly remaining bridge
  until `prepare_view_attachments`, `prepare_view_targets`, and the Bevy-shaped
  screenshot render-world readback path replace the backend capture-id route.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  render/view/window`, `moon -C mgstudio-engine check --target native
  --package-path render`, `moon -C mgstudio-engine test render --release`,
  `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_window_manual_texture_handle_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Added the Bevy source-owner `WindowRenderPlugin` resource slice
  under `render/view/window`. The plugin now initializes render-world
  `ExtractedWindows` and `WindowSurfaces`, and `RenderPlugin` installs it
  during the render plugin setup instead of leaving all window render state as
  renderer-local surface bindings. The new `ExtractedWindow`, `ExtractedWindows`,
  `SurfaceData`, and `WindowSurfaces` symbols mirror Bevy's owner data shape
  closely enough for the next `extract_windows`, `create_surfaces`, and
  `prepare_windows` systems to target these resources directly. Remaining gaps
  are explicit: `ScreenshotPlugin` still lives in the top-level `render`
  package because its API and render-app helpers have not yet been rehomed to
  `view/window/screenshot`, and surface acquire/present still uses the existing
  renderer surface bridge until the full Bevy chain is ported. Validation:
  `moon -C mgstudio-engine check --target native --package-path
  render/view/window`, `moon -C mgstudio-engine test render/view/window
  --release`, `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine test render --release`, `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_window_render_plugin_resources_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Ported the Bevy `extract_windows` owner slice that is
  representable with the current mgstudio ECS APIs. The render schedule now
  runs `mgstudio_render::view::window::extract_windows` in the extract-command
  set, passing the main source world into `render/view/window`. The system
  extracts `Window`, `RawHandleWrapper`, and optional `PrimaryWindow` into
  render-world `ExtractedWindows`, updates physical size and present-mode
  change flags on subsequent frames, preserves the current swapchain texture
  optimization shape, and consumes `WindowClosing` through a render-world
  message cursor before removing both `ExtractedWindows` and `WindowSurfaces`
  entries. Remaining gap: Bevy also handles
  `RemovedComponents<RawHandleWrapper>`; mgstudio does not yet expose an
  equivalent removed-component reader for this cross-world extract path, so
  that subtarget remains incomplete rather than approximated. Validation:
  `moon -C mgstudio-engine check --target native --package-path
  render/view/window`, `moon -C mgstudio-engine test render/view/window
  --release`, `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine test render --release`, `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_extract_windows_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Ported the Bevy `need_surface_configuration` and present-mode
  decision helpers into the `WindowRenderPlugin` owner package. The new
  `need_surface_configuration` scans render-world `ExtractedWindows` against
  `WindowSurfaces.configured_windows` and returns true for unconfigured,
  resized, or present-mode-changed windows, matching Bevy's run condition for
  `create_surfaces`. The present-mode fallback helper now maps mgstudio
  `Window.PresentMode` requests to the same supported-mode order Bevy uses:
  `AutoVsync -> FifoRelaxed/Fifo`, `AutoNoVsync -> Immediate/Mailbox/Fifo`,
  and `Mailbox -> Mailbox/Immediate/Fifo`. This moves decision ownership out
  of renderer-local private helpers so the later `create_surfaces` port can use
  the source-owner logic directly. Validation: `moon -C mgstudio-engine check
  --target native --package-path render/view/window`, `moon -C mgstudio-engine
  test render/view/window --release`, `moon -C mgstudio-engine check --target
  native --package-path render`, `moon -C mgstudio-engine test render
  --release`, `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_window_surface_decision_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Ported the pure configuration-building portion of Bevy
  `create_surfaces` into the `WindowRenderPlugin` owner package. Mgstudio now
  has source-owned helpers for Bevy's supported-surface-format choice, sRGB
  texture-view-format derivation, composite-alpha mapping, desired maximum
  frame latency defaulting, and `SurfaceConfiguration` construction from an
  `ExtractedWindow`. This slice intentionally stops before claiming full
  `create_surfaces` parity: the actual `Surface` creation/configure side
  effect and `prepare_windows` swapchain acquisition still need to be moved
  from the existing renderer/winit bridge into the Bevy-shaped
  `WindowRenderPlugin` schedule. Validation:
  `moon -C mgstudio-engine check --target native --package-path
  render/view/window` and `moon -C mgstudio-engine test render/view/window
  --release`, `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine test render --release`, `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_surface_configuration_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Moved the next raw-window-handle boundary needed by
  `create_surfaces` to the window owner. `RawHandleWrapper` now exposes a
  `content_view_handle()` lookup backed by the host window package, so
  `render/view/window` does not need to import native windowing internals
  directly. `WindowRenderPlugin` now has a fallible
  `create_surface_for_window` helper that converts an `ExtractedWindow` plus
  Bevy-shaped `RenderInstance` into a wgpu surface through
  `Instance::create_surface_macos_ns_view_u64`, reporting missing native view
  handles through a local suberror instead of a Result/fallback path. This is
  still preparatory: the scheduled `create_surfaces` system and removal of
  winit-runner surface ownership remain the next owner-migration slice.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  window`, `moon -C mgstudio-engine test window --release`, `moon -C
  mgstudio-engine check --target native --package-path render/view/window`,
  `moon -C mgstudio-engine test render/view/window --release`, `moon -C
  mgstudio-engine check --target native --package-path render`, `moon -C
  mgstudio-engine test render --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and native `examples/shader_advanced/custom_shader_instancing`
  capture at
  `/tmp/mgstudio_iss014_raw_handle_surface_boundary_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Added the real Bevy-shaped `create_surfaces` owner function in
  `render/view/window`. The function now walks `ExtractedWindows`, creates
  missing wgpu surfaces from `RenderInstance`, builds capabilities-driven
  `SurfaceConfiguration` values from `RenderAdapter`, configures surfaces with
  `RenderDevice`, reconfigures existing surfaces on size or present-mode
  changes, drops stale swapchain texture/view references before reconfigure,
  stores `SurfaceData`, and marks configured windows. It is intentionally not
  registered in the render schedule yet, because the winit runner still owns
  the active renderer surface bridge; registering this now would create two
  surface owners. The next required slice is to remove the runner-owned
  surface creation/configuration path and connect this owner function to the
  Bevy `create_surfaces.before(prepare_windows)` schedule slot. Validation:
  `moon -C mgstudio-engine check --target native --package-path
  render/view/window`, `moon -C mgstudio-engine test render/view/window
  --release`, `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine test render --release`, `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_create_surfaces_owner_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Ported Bevy's `ExtractedWindow::set_swapchain_texture` owner
  method. Mgstudio now records the acquired surface frame, derives the sRGB
  swapchain view format from the frame texture format, and creates the
  corresponding 2D single-mip texture view before storing the frame for later
  presentation. This is a narrow `prepare_windows` prerequisite, not full
  `prepare_windows` parity; remaining local work is the scheduled
  `create_surfaces -> prepare_windows -> prepare_view_attachments ->
  prepare_view_targets` chain and removal of the old active-frame bridge.
  Validation: `moon -C mgstudio-engine test render/view/window --release`,
  `moon -C mgstudio-engine test render/render_resource --release`, `moon -C
  mgstudio-engine check --target native --package-path render`, `moon -C
  mgstudio-engine test render --release`, `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and native `examples/shader_advanced/custom_shader_instancing`
  capture at
  `/tmp/mgstudio_iss014_set_swapchain_texture_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Replaced the local `SortedCameras` entity-only shape with a
  Bevy-shaped `SortedCamera` list carrying render entity, order, normalized
  render target, HDR flag, and output write/skip mode. PBR now fills that
  richer data from its existing camera extraction path and derives the old
  entity order from `SortedCameras::camera_entities()` where fog/contact-shadow
  uniform preparation still consumes entity arrays. This is a prerequisite for
  a faithful `prepare_windows` port, because Bevy skips swapchain acquisition
  for windows that are not targeted by a `CameraOutputMode::Write` camera
  unless they still need initial present. Remaining source-owner work: move
  camera sorting itself fully under the render camera owner, expose the main
  `CameraOutputMode` field from the camera component owner instead of defaulting
  PBR-sorted cameras to write mode, and then connect `prepare_windows`.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test render --release`, `moon -C mgstudio-engine
  test pbr --release`, `moon -C mgstudio-engine check --target native`, `moon
  -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and native
  `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_sorted_cameras_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Split the sorted-camera owner data into a dedicated
  `render/camera_sort` package so `render/view/window` can depend on the
  Bevy-shaped camera-target metadata without importing the root `render`
  package and creating a cycle. The root `render` package now re-exports the
  existing public `SortedCamera(s)` API, while `render/view/window` owns the
  pure `prepare_windows` camera-target gating helper: a window is considered a
  camera target only when a sorted camera targets that window and has
  `CameraOutputMode::Write`. This does not yet acquire swapchain textures; it
  removes the package-boundary blocker for the real `prepare_windows` body.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  render/camera_sort`, `moon -C mgstudio-engine test render/camera_sort
  --release`, `moon -C mgstudio-engine check --target native --package-path
  render/view/window`, `moon -C mgstudio-engine test render/view/window
  --release`, `moon -C mgstudio-engine test render --release`, `moon -C
  mgstudio-engine test pbr --release`, `moon -C mgstudio-engine check --target
  native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  native `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_camera_sort_split_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.
- 2026-06-22: Ported the Bevy `prepare_windows` acquire/retry branch into the
  `render/view/window` owner package. The function now applies the Bevy
  camera-target/initial-present gate, reuses an existing unpresented swapchain
  texture when size and present mode are unchanged, stores Success/Suboptimal
  surface frames via `ExtractedWindow::set_swapchain_texture`, reconfigures and
  retries on Outdated, ignores Occluded, logs other acquire failures, and
  records the swapchain texture format from the surface configuration. This is
  intentionally still not registered in the render schedule: mgstudio must
  first remove the existing renderer active-frame bridge or wire it to consume
  `ExtractedWindows` to avoid two owners acquiring the same surface. Validation:
  `moon -C mgstudio-engine check --target native --package-path
  render/view/window`, `moon -C mgstudio-engine test render/view/window
  --release`, `moon -C mgstudio-engine check --target native --package-path
  render`, `moon -C mgstudio-engine test render --release`, `moon -C
  mgstudio-engine test pbr --release`, `moon -C mgstudio-engine check --target
  native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  native `examples/shader_advanced/custom_shader_instancing` capture at
  `/tmp/mgstudio_iss014_prepare_windows_owner_custom_shader_instancing.png`.
  `visual_image_sanity.py --image` passed for the generated PNG.

## Close Notes
