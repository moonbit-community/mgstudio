# ISS-044: Audit and complete Bevy dim2 primitive source parity

## Metadata
- Type: task
- Status: blocked
- Priority: 0
- Labels: parity, mesh, primitives, dim2, audit
- Assignee: unassigned
- Created: 2026-06-05
- Updated: 2026-06-12
- External ref: none

## Description
`mgstudio-engine/mesh/primitives/dim2.mbt` now routes primitive asset authoring
through the Bevy-shaped `Meshable -> Mesh` path for the `examples/2d/2d_shapes`
gate, but the owner has not yet been audited symbol by symbol against
`bevy/crates/bevy_mesh/src/primitives/dim2.rs`.

The file still contains local helper surfaces such as `Mesh2dGeometry` conversion
helpers and ring helper traits. These may be legitimate MoonBit implementation
fragments, but they must not be counted as complete parity until every Bevy
source-shape anchor has been classified and every local-only helper has been
removed, rehomed, or justified as private implementation detail.

## Design
Build a symbol inventory from Bevy `dim2.rs` first, then map each Bevy struct,
enum, builder, trait impl, method, constant, conversion, and test-relevant
behavior to the MoonBit implementation. Do not infer completion from visual
success in `2d_shapes`; each source symbol must be marked complete, facade,
partial, or blocked.

## Acceptance Criteria
- [x] Inventory every in-scope symbol from `bevy/crates/bevy_mesh/src/primitives/dim2.rs`.
- [x] Map each inventory item to the corresponding `mgstudio-engine/mesh/primitives/dim2.mbt` symbol or record why it is intentionally out of scope.
- [x] Remove or make private any local `Mesh2dGeometry` / ring helper surface that is not a Bevy-equivalent public API.
- [x] Add focused tests for primitive mesh topology, vertex attributes, indices, winding, and UV behavior where Bevy has source-level expectations.
- [x] Validate with `moon -C mgstudio-engine test mesh/primitives --release`, `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`, and `moon -C mgstudio-engine info`.

## Relationships
- Depends on: ISS-023, ISS-070
- Parent: ISS-028
- Related: ISS-012, ISS-024, ISS-029
- Discovered from: ISS-028

## Notes
- 2026-06-05: `examples/2d/2d_shapes` now renders all 22 expected meshes after routing primitive asset conversion through `Meshable`, but that proves only the exercised authoring path. The full Bevy dim2 owner remains unaudited.
- 2026-06-12: Started source-owner audit against
  `bevy/crates/bevy_mesh/src/primitives/dim2.rs`.
- 2026-06-12: Initial symbol inventory from Bevy dim2 includes
  `CircleMeshBuilder`, `CircularMeshUvMode`,
  `CircularSectorMeshBuilder`, `CircularSegmentMeshBuilder`,
  `ConvexPolygonMeshBuilder`, `RegularPolygonMeshBuilder`,
  `EllipseMeshBuilder`, `Segment2dMeshBuilder`, `Polyline2dMeshBuilder`,
  `AnnulusMeshBuilder`, `RhombusMeshBuilder`,
  `Triangle2dMeshBuilder`, `RectangleMeshBuilder`,
  `Capsule2dMeshBuilder`, generic `RingMeshBuilder<P>`, the `MeshBuilder`,
  `Meshable`, `Extrudable`, and `From<Primitive> for Mesh` impl surfaces, plus
  Bevy's annulus, regular polygon, and convex polygon source tests.
- 2026-06-12: Current mgstudio owner is partial, not complete. It has many
  corresponding concrete builders, but `Meshable` returns `Mesh3dGeometry`
  directly instead of Bevy's associated builder output, so calls such as
  `shape.mesh().resolution(...).build()` are not source-shaped. This exact
  generic trait shape is tracked by ISS-070.
- 2026-06-12: Current mgstudio owner also exposes local-only public
  `*_to_mesh2d_geometry` and `generic_*_ring_to_mesh2d_geometry` helpers.
  `rg` shows these are still used by `sprite_render/render/runtime.mbt`,
  `sprite_render/sprite_mesh`, `tilemap_chunk`, and multiple examples. They
  cannot be removed safely until callers move to Bevy-shaped primitive mesh
  authoring (`Meshable`/builder path or `IntoMeshPrimitiveAsset`) and the
  remaining helpers are made private implementation details or deleted.
- 2026-06-12: Migrated representative simple example callers from direct
  local `*_to_mesh2d_geometry(...).to_mesh()` authoring to
  `@mesh_primitives.add_mesh(mesh_assets, primitive)`: `examples/2d/2d_viewport_to_world`,
  `examples/2d/bloom_2d`, `examples/2d/mesh2d`,
  `examples/2d/mesh2d_custom_material`, `examples/2d/wireframe_2d`, and
  `examples/2d/pixel_grid_snap`. Remaining example callers are
  `mesh2d_arcs`, `tilemap_chunk`, `mesh2d_alpha_mode`,
  `mesh2d_repeated_texture`, `shader_material_2d`,
  `many_animated_sprite_meshes`, `spatial_audio_2d`, `easing_functions`,
  `many_sprite_meshes`, and `camera/2d_screen_shake`.
- 2026-06-12: Validation for the representative caller migration:
  `moon -C mgstudio-engine check --target native --package-path examples/2d/2d_viewport_to_world`,
  `moon -C mgstudio-engine check --target native --package-path examples/2d/bloom_2d`,
  `moon -C mgstudio-engine check --target native --package-path examples/2d/mesh2d`,
  `moon -C mgstudio-engine check --target native --package-path examples/2d/mesh2d_custom_material`,
  `moon -C mgstudio-engine check --target native --package-path examples/2d/wireframe_2d`,
  and `moon -C mgstudio-engine check --target native --package-path examples/2d/pixel_grid_snap`
  passed. Visual gate:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/2d/bloom_2d /tmp/mgstudio_iss044_bloom_2d_add_mesh.png`;
  `magick compare -metric AE /tmp/mgstudio_iss044_bloom_2d_add_mesh.png mgstudio-engine/examples/2d/bloom_2d/reference.png null:` reported `0`.
- 2026-06-12: Migrated the remaining example callers away from direct
  `*_to_mesh2d_geometry(...).to_mesh()` authoring. `rg` now finds no
  `*_to_mesh2d_geometry` or `generic_*_ring_to_mesh2d_geometry` calls under
  `mgstudio-engine/examples`. Additional checked packages:
  `examples/2d/mesh2d_arcs`, `examples/2d/mesh2d_alpha_mode`,
  `examples/2d/mesh2d_repeated_texture`, `examples/shader/shader_material_2d`,
  `examples/camera/2d_screen_shake`, `examples/stress_tests/many_animated_sprite_meshes`,
  `examples/stress_tests/many_sprite_meshes`, `examples/audio/spatial_audio_2d`,
  `examples/animation/easing_functions`, and `examples/2d/tilemap_chunk`.
  Visual gate:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/2d/mesh2d_arcs /tmp/mgstudio_iss044_mesh2d_arcs_add_mesh.png`;
  `magick compare -metric AE /tmp/mgstudio_iss044_mesh2d_arcs_add_mesh.png mgstudio-engine/examples/2d/mesh2d_arcs/reference.png null:` reported `0`.
- 2026-06-12: Migrated the remaining non-`mesh/primitives` local helper
  callers away from public `*_to_mesh2d_geometry` helpers. `sprite_render/sprite_mesh`
  and `sprite_render/tilemap_chunk` now mirror Bevy's
  `meshes.add(Rectangle::from_size(...))` shape through
  `@mesh_primitives.add_mesh`. `sprite_render/render` tests now validate
  primitive asset conversion through `add_mesh` instead of direct helper calls.
  `rg` now finds `*_to_mesh2d_geometry` and `generic_*_ring_to_mesh2d_geometry`
  only inside `mgstudio-engine/mesh/primitives/dim2.mbt`.
- 2026-06-12: The internal migration exposed a real Bevy-owner gap outside
  dim2: `SpritePlugin` bounds calculation for `Mesh2d` only accepted legacy
  `MeshGeometry::Geometry2d`, while Bevy primitive authoring produces a regular
  `Mesh` with position data. `camera/primitives` `MeshAabb` now computes AABBs
  for both mgstudio split geometry forms, and `sprite` delegates `Mesh2d`
  bounds to `@camera_primitives.mesh_compute_aabb`.
- 2026-06-12: Validation for the internal caller migration:
  `moon -C mgstudio-engine check --target native --package-path camera/primitives`,
  `moon -C mgstudio-engine check --target native --package-path sprite`,
  `moon -C mgstudio-engine check --target native --package-path sprite_render/render`,
  `moon -C mgstudio-engine check --target native --package-path sprite_render/sprite_mesh`,
  `moon -C mgstudio-engine check --target native --package-path sprite_render/tilemap_chunk`,
  `moon -C mgstudio-engine test camera/primitives --release`,
  `moon -C mgstudio-engine test sprite_render/render --release`,
  `moon -C mgstudio-engine test sprite_render/sprite_mesh --release`, and
  `moon -C mgstudio-engine test sprite_render/tilemap_chunk --release` passed.
  Visual gate:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/2d/mesh2d_arcs /tmp/mgstudio_iss044_mesh2d_arcs_internal_add_mesh.png`;
  `magick compare -metric AE /tmp/mgstudio_iss044_mesh2d_arcs_internal_add_mesh.png mgstudio-engine/examples/2d/mesh2d_arcs/reference.png null:` reported `0`.
  `examples/2d/tilemap_chunk` also rendered visible content, but its strict
  pixel compare differed by `12000` pixels because the captured red player
  position/overlap changed while tile content matched; do not use that example
  as a deterministic AE=0 gate without an input/time fixture.
- 2026-06-12: Removed the now-unused local-only 2D helper API surface from
  `mesh/primitives/dim2.mbt`: all `*_to_mesh2d_geometry`,
  `generic_*_ring_to_mesh2d_geometry`, and `IntoMeshPrimitiveRingGeometry`
  symbols are gone from source. `IntoMeshPrimitiveAsset` and `add_mesh` remain
  as the current MoonBit asset-authoring adapter because examples and tests use
  that Bevy-shaped `meshes.add(primitive)` path while exact
  `Meshable::Output` builder chaining remains blocked by ISS-070. Validation:
  `moon -C mgstudio-engine check --target native --package-path mesh/primitives`,
  `moon -C mgstudio-engine test mesh/primitives --release`,
  `moon -C mgstudio-engine check --target native --package-path sprite_render/render`,
  `moon -C mgstudio-engine check --target native --package-path sprite_render/sprite_mesh`,
  and `moon -C mgstudio-engine check --target native --package-path sprite_render/tilemap_chunk`
  passed.
- 2026-06-12: Ported Bevy's concrete dim2 source tests for annulus distinct
  positions, regular polygon positions/UVs/normals, and convex polygon
  positions/UVs/normals into `mesh/primitives/primitive_parity_wbtest.mbt`.
  The convex polygon test exposed a real parity bug: `convex_polygon_mesh`
  wrote positions and indices only. It now mirrors Bevy's AABB-normalized UV
  generation and `[0, 0, 1]` normals. Validation:
  `moon -C mgstudio-engine check --target native --package-path mesh/primitives`
  and `moon -C mgstudio-engine test mesh/primitives --release` passed. Visual
  gate:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/2d/2d_shapes /tmp/mgstudio_iss044_2d_shapes_bevy_dim2_tests.png`;
  `magick compare -metric AE /tmp/mgstudio_iss044_2d_shapes_bevy_dim2_tests.png mgstudio-engine/examples/2d/2d_shapes/reference.png null:` reported `0`.
- 2026-06-12: Source-owner classification against
  `bevy/crates/bevy_mesh/src/primitives/dim2.rs`:
  `CircleMeshBuilder`, `CircularMeshUvMode`, `CircularSectorMeshBuilder`,
  `CircularSegmentMeshBuilder`, `ConvexPolygonMeshBuilder`,
  `RegularPolygonMeshBuilder`, `EllipseMeshBuilder`,
  `Segment2dMeshBuilder`, `Polyline2dMeshBuilder`, `AnnulusMeshBuilder`,
  `RhombusMeshBuilder`, `Triangle2dMeshBuilder`,
  `RectangleMeshBuilder`, and `Capsule2dMeshBuilder` are implemented as
  concrete mgstudio builder symbols with matching build/perimeter behavior and
  focused parity tests where Bevy has source tests. `From<Primitive> for Mesh`
  is mapped to the current MoonBit `IntoMeshPrimitiveAsset` / `add_mesh`
  authoring adapter because MoonBit does not have Rust's blanket `From<T>`
  conversion surface. `Meshable` and generic `RingMeshBuilder<P>` are blocked
  for exact source shape by ISS-070: Bevy's `Meshable::Output` associated
  builder type and `RingMeshBuilder<P>::new(&Ring<P>)` /
  `with_inner(P::Output -> P::Output)` cannot be represented by the current
  mgstudio trait without collapsing the builder type to concrete geometry.
- 2026-06-12: Blocked state reason: all currently implementable dim2 cleanup,
  direct tests, helper removal, and visual gates are done, but exact source
  parity is not complete until ISS-070 is resolved or an accepted explicit
  non-generic per-type API plan replaces the associated-output trait shape.

## Close Notes
