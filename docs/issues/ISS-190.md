# ISS-190: Complete scrolling_fog volumetric fog visual gate group

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, screenshots, visual-parity
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-09
- External ref: none

## Description
`examples/3d/scrolling_fog` is the primary density-texture volumetric fog gate.
It must show fog/light scattering and visible density texture movement instead
of only distance fog or a plausible static scene.

The latest native capture completes with an empty run log, but it is visually
brighter than the checked-in reference and lacks the directional-light
shadow/light-shaft structure around the pillar. Treat this as a real render
chain gap, not a reference-refresh task.

## Design
Use this as the parent for smaller, independently verifiable slices:

- ISS-196 records a deterministic current-vs-reference diff for
  `scrolling_fog`.
- ISS-197 audits the directional `VolumetricLight` extraction, ordering, and GPU
  flag path.
- ISS-198 audits the directional shadow cascade/depth binding path consumed by
  volumetric fog.
- ISS-199 audits the WGSL density/depth/shadow sampling inputs against Bevy.
- ISS-200 performs the final accepted capture and reference refresh.

Do not refresh the reference until the child issues explain and fix the missing
light-shaft signal.

## Acceptance Criteria
- [x] ISS-196, ISS-197, ISS-198, ISS-199, and ISS-200 are closed.
- [x] The final capture completes with no volumetric fog renderer fatal or
      missing resource output.
- [x] The scene shows density-texture fog/light scattering and the
      directional-light shadow/light-shaft structure.
- [x] The checked-in reference is refreshed only after the output is accepted.

## Relationships
- Depends on: ISS-196, ISS-197, ISS-198, ISS-199, ISS-200
- Parent: ISS-181
- Related: ISS-091, ISS-104, ISS-196, ISS-197, ISS-198, ISS-199, ISS-200
- Discovered from: ISS-181

## Notes
- 2026-07-04: Split from ISS-181 so the primary scrolling density-texture gate
  has its own evidence and close notes.
- 2026-07-04: Latest capture
  `/tmp/mgstudio_iss189_scrolling_fog.png` completes with an empty run log, but
  it is not visually acceptable. Compared with
  `examples/3d/scrolling_fog/reference.png`, the current output is brighter and
  lacks the reference's directional-light shadow/light-shaft structure around
  the pillar. Do not refresh the reference to the current output until this
  render-chain gap is explained and fixed.
- 2026-07-04: Split into ISS-196 through ISS-200 so the visual regression is
  resolved through source-owner checks instead of one broad screenshot task.

## Close Notes
Closed on 2026-07-09.

The `scrolling_fog` visual gate group is complete. ISS-196 recorded the
current visual delta, ISS-197 verified directional volumetric light GPU flags,
ISS-198 verified directional shadow binding, ISS-199 completed the
shader-input parity group, and ISS-200 accepted the final capture and refreshed
`mgstudio-engine/examples/3d/scrolling_fog/reference.png` from the accepted
frame-90 native output.
