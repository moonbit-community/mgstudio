# ISS-025: Move camera and Core2d ownership out of sprite/render2d

## Metadata
- Type: task
- Status: open
- Priority: 0
- Labels: parity, camera, core2d, render2d, architecture
- Assignee: unassigned
- Created: 2026-06-03
- Updated: 2026-06-05
- External ref: none

## Description
`sprite/render2d.mbt` currently defines camera-adjacent types and behavior such
as `RenderLayers`, `Viewport`, `ClearColor`, `Camera`, `OrthographicProjection`,
`Projection`, `Camera2d`, `Camera2dBundle`, target sizing, viewport scaling,
camera extraction, and 2D camera render ordering. These owners map to Bevy's
`bevy_camera` and `bevy_core_pipeline/src/core_2d`, not to sprite rendering.

Keeping them in sprite render makes camera/core pipeline parity depend on a
sprite package and obscures Bevy's Core2d graph ownership.

## Design
Move camera data and conversion helpers to the camera package and Core2d phase
setup/extraction to `core_pipeline/core_2d`. Preserve public API names only
where they represent Bevy-equivalent authoring surfaces.

## Acceptance Criteria
- [ ] Camera data types and viewport math live under the camera/core pipeline owners instead of `sprite/render2d.mbt`.
- [ ] Core2d phase resources and camera extraction map to `bevy_core_pipeline/src/core_2d/mod.rs`.
- [ ] Sprite render depends on extracted Core2d view data rather than owning camera extraction itself.
- [ ] 2D viewport conversion examples pass after the move.

## Relationships
- Depends on: ISS-023
- Parent: ISS-022
- Related: ISS-013, ISS-021
- Discovered from: ISS-022

## Notes
- 2026-06-03: Evidence in `sprite/render2d.mbt` includes `RenderLayers` near the top of the file, Bevy's `Viewport::clamp_to_size` comment, `Camera`, `Projection`, `Camera2d`, camera bundle constructors, and camera extract systems.
- 2026-06-05: `Camera` and `RenderTarget` now have a real `camera/camera` owner package with component keys and JSON codecs. This is partial: `Camera2d`, the 2D `Projection` / `OrthographicProjection` owner, and Core2d extraction/runtime ordering still remain in `sprite/render2d.mbt`.
- 2026-06-05: `Camera2d`, 2D `Projection`, and `OrthographicProjection` now have concrete camera owner fragments under `camera/components2d` and `camera/projection2d`; `sprite` only aliases them for the remaining unmigrated callers. Remaining local work: move caller imports to camera owner APIs, port Bevy's full orthographic projection fields/`ScalingMode` behavior, add the Bevy-shaped `Camera2d -> Frustum` required component path, and move Core2d extraction/runtime ordering out of `sprite/render2d.mbt`.
- 2026-06-05: `Core2dPlugin` now owns the Bevy `Camera2d` post-process required components: `DebandDither::Disabled` and `Tonemapping::None`. Remaining Core2d gap: `Camera2d -> CameraRenderGraph(Core2d)` and extracted-view/runtime ordering are still not source-level complete.
- 2026-06-05: `sprite_render/render` no longer depends on `@sprite.Camera` or `@sprite.OrthographicProjection` for camera conversion forwarding signatures; it uses the camera owner fragments directly. Many application/example callers still use the sprite facade and remain local migration work.
- 2026-06-05: `ui` no longer uses sprite facade camera types or camera component keys for UI target-camera routing. Remaining local caller migration work is concentrated in examples, scene, picking, PBR helper paths, and the still-unsplit render2d runtime functions.
- 2026-06-05: PBR no longer uses sprite facade camera or render-target symbols. Remaining local caller migration work is concentrated in examples, scene, picking, light/cluster tests, and the still-unsplit 2D render runtime.
- 2026-06-05: The anti-alias packages no longer use sprite facade camera keys. Remaining non-example camera facade users include scene loading/spawning, picking, light clustering, camera visibility range tests, and the sprite package's own internal tests/runtime.
- 2026-06-05: Scene loading/spawning no longer uses sprite facade camera symbols. Remaining non-example camera facade users include picking, light clustering, camera visibility range tests, and the sprite package's own internal tests/runtime.
- 2026-06-05: Light clustering no longer uses sprite facade camera symbols. Remaining non-example camera facade users include picking, camera visibility range tests, and the sprite package's own internal tests/runtime.
- 2026-06-05: Picking no longer uses sprite facade camera or Camera2d symbols. Remaining non-example camera facade users include camera visibility range tests and the sprite package's own internal tests/runtime.

## Close Notes
