# ISS-261: Capture scrolling_fog final directional-shadow volumetric fog evidence

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, shadows, screenshots, visual-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
After the runtime shader variant and directional shadow resource path are
proven, `scrolling_fog` needs a final native capture artifact showing the fixed
path in the running example.

## Design
Run the standard native screenshot command for `examples/3d/scrolling_fog` and
compare the result against the local reference and Bevy expectation. This issue
does not hide remaining visual mismatches; any remaining non-equivalence must be
split into a narrower follow-up before closing.

## Acceptance Criteria
- [x] The standard native capture command completes without validation,
      missing-resource, unsupported, or fatal output.
- [x] The screenshot artifact is compared against the current local reference
      and Bevy visual expectation.
- [x] The result is either accepted as directional-shadow volumetric fog runtime
      evidence or a narrower follow-up issue records the remaining delta.
- [x] The issue notes include the exact capture command and artifact path used.

## Relationships
- Depends on: ISS-260
- Parent: ISS-257
- Related: ISS-091, ISS-104, ISS-190, ISS-199, ISS-205, ISS-215
- Discovered from: ISS-257

## Notes
- 2026-07-05: Split from ISS-257.

## Close Notes
- 2026-07-05: Closed after the standard no-retry native capture completed:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/scrolling_fog /tmp/mgstudio_iss260_scrolling_fog_shadow_binding.png`.
- 2026-07-05: The run log
  `/tmp/mgstudio_iss260_scrolling_fog_shadow_binding.run.log` is empty. The
  PNG is 2560x1440 RGBA, nonblank, and visually shows the pillar, fog, and
  directional shadow/light-shaft evidence.
- 2026-07-05: Compared against
  `mgstudio-engine/examples/3d/scrolling_fog/reference.png`; both images are
  2560x1440. Difference mean is approximately `[0.322, 0.321, 0.320, 0.000]`
  on 8-bit RGBA channels. This is accepted as no visible regression for this
  directional-shadow volumetric fog evidence slice.
