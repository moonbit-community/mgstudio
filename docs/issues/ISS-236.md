# ISS-236: Remove dual StandardMaterial mesh material carriers

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: pbr, materials, mesh, source-parity, ecs
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-05
- External ref: none

## Description
`scrolling_fog` currently needs both the legacy erased `MeshMaterial3d` carrier
and the typed `MeshMaterial3dTyped[StandardMaterial]` carrier to satisfy all
local render paths. The erased carrier is still used by mesh visibility and
extraction fragments, while the typed carrier is required by
`MaterialPlugin[StandardMaterial]` and `material_runtime_registry_resolve_material`.

Bevy has a single generic `MeshMaterial3d<StandardMaterial>` component for this
case. Requiring examples to author both carriers is a local source-parity gap,
not an external blocker.

## Design
Audit Bevy `bevy_pbr/src/material.rs` and the mgstudio mesh/material extraction
owners. Port the render extraction path so the typed StandardMaterial component
is sufficient for visibility, render material instance extraction, material
runtime resolution, and mesh phase queueing. Remove example-level dual carrier
authoring once the owner is fixed.

## Acceptance Criteria
- [x] StandardMaterial examples can author one Bevy-shaped typed mesh material
      component.
- [x] Mesh visibility and shadow visibility do not require a separate erased
      material component for StandardMaterial meshes.
- [x] `MaterialPlugin[StandardMaterial]` still resolves prepared materials for
      shadow and main-pass specialization.
- [x] `scrolling_fog` no longer needs to spawn both `MeshMaterial3d` and
      `MeshMaterial3dTyped[StandardMaterial]`.

## Relationships
- Depends on: ISS-232
- Parent: ISS-012
- Related: ISS-155, ISS-219, ISS-232
- Discovered from: ISS-232

## Notes
- 2026-07-04: Discovered while proving `scrolling_fog` shadow specialization
  inputs. Using only typed material authoring allowed material-runtime parity
  but broke the current shadow visible set, showing that another local owner
  still depends on the erased carrier.
- 2026-07-05: Source owner audit: Bevy `bevy_pbr/src/material.rs::MaterialPlugin<M>`
  registers `extract_mesh_materials::<M>` and
  `early_sweep_material_instances::<M>` in `ExtractSchedule`. mgstudio's typed
  material extraction was incorrectly registered in the render schedule and
  rebuilt its typed table with a full scan instead of Bevy-shaped changed and
  removed component handling.
- 2026-07-05: The broader repository still contains legacy
  `@pbr.MeshMaterial3d` authoring and APIs. That is local source-parity debt,
  not an external blocker, and is tracked separately in ISS-394.

## Close Notes
- 2026-07-05: Closed by moving typed `MaterialPlugin[M]` material extraction to
  the render extract schedule, adding typed material removed-component early
  sweep, and changing typed extraction to update `RenderMaterialInstances`
  through Bevy-shaped changed `ViewVisibility`/`MeshMaterial3d<M>` inputs.
  `scrolling_fog` now authors only `MeshMaterial3dTyped[StandardMaterial]`, and
  its whitebox test asserts there are zero legacy `ecs_key_mesh_material3d`
  components in the source world. Validation:
  `moon -C mgstudio-engine fmt`;
  `moon -C mgstudio-engine check --target native`;
  `moon -C mgstudio-engine info`;
  `moon -C mgstudio-engine test pbr/material_plugin_wbtest.mbt --target native --release`;
  `moon -C mgstudio-engine test examples/3d/scrolling_fog --target native --release`;
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/scrolling_fog /tmp/mgstudio_iss236_scrolling_fog.png`.
