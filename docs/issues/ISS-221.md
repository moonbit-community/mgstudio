# ISS-221: Complete Bevy render camera_system computed camera parity

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: camera, render, pbr, source-parity, schedule
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-06
- External ref: none

## Description
ISS-216 restored the source-world projection update needed before directional
cascade construction, but Bevy's `bevy_render/src/camera.rs::camera_system`
owns a broader camera update surface. Mgstudio still needs a symbol-level audit
for the remaining computed camera fields and event-driven behavior before the
camera-system owner can be called complete.

## Design
Compare Bevy `camera_system` against mgstudio's camera/PBR implementation.
Port the remaining in-scope pieces source-faithfully: render-target change
detection, viewport resize on scale-factor changes, viewport clamping,
`computed.clip_from_view`, `old_viewport_size`, and `old_sub_camera_view`.
Do not move this logic into shadow or example code.

## Acceptance Criteria
- [ ] Every in-scope branch of Bevy `camera_system` is classified as complete,
      partial, facade, or blocked.
- [ ] `Camera.computed` carries the Bevy-equivalent fields needed by downstream
      camera, visibility, light, and render users.
- [ ] Projection update, viewport clamping, and computed clip matrix updates
      run in the Bevy-shaped `CameraUpdateSystems` schedule.
- [ ] Existing camera, light, and PBR focused tests continue to pass.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-104, ISS-216
- Discovered from: ISS-216

## Notes
- 2026-07-04: Discovered while fixing `scrolling_fog` directional cascade
  inputs. The projection update needed by cascades is fixed in ISS-216, but the
  rest of Bevy `camera_system` remains a local source-owner parity gap.
- 2026-07-06: Source-owner classification:
  - `ComputedCameraValues::clip_from_view`: complete. Mgstudio now stores the
    Bevy-shaped `Mat4` value on camera computed state.
  - `ComputedCameraValues::target_info`: complete for current mgstudio render
    targets (`Screen`, `Window`, `Image`).
  - `ComputedCameraValues::old_viewport_size`: complete.
  - `ComputedCameraValues::old_sub_camera_view`: complete.
  - `Camera::sub_camera_view`: complete. The old ECS component path was
    removed; `Camera3dBundle` writes sub-view data onto `Camera`.
  - Viewport clamp and scale-factor resize branches: complete for current
    mgstudio 3D targets.
  - Bevy `TextureView`/manual texture target branch: partial. Mgstudio's
    current `RenderTarget` enum does not yet expose the Bevy manual texture
    target owner.
  - Bevy message-reader event source shape: partial. Mgstudio uses component
    change ticks plus computed-state comparisons because equivalent window and
    image event readers are not yet present on this owner.

## Close Notes
Closed after porting the Bevy camera computed-value shape into mgstudio:
`Camera.computed` now carries `clip_from_view`, `target_info`,
`old_viewport_size`, and `old_sub_camera_view`; `Camera` owns
`sub_camera_view`; the 3D camera update computes the projection matrix,
clamps/resizes viewports, and updates projection state in the camera update
system. Validation passed for `moon -C mgstudio-engine check --target native`,
`moon -C mgstudio-engine test camera --release`, `moon -C mgstudio-engine test
ui --release`, `moon -C mgstudio-engine test pbr/camera_bundle_test.mbt
--release`, `moon -C mgstudio-engine test pbr/projection_wbtest.mbt --release`,
`moon -C mgstudio-engine test pbr/projection_test.mbt --release`, `moon -C
mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and the
`examples/3d/camera_sub_view` screenshot gate. Package-wide PBR still has the
unrelated tangent-generation failure recorded as ISS-400.
