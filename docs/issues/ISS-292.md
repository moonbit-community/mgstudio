# ISS-292: Validate volumetric_fog toggle render visibility

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, screenshots, visual-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
After `AppSettings`, text, and `adjust_app_settings` are source-shaped, the
example needs evidence that toggling point and spot volumetric lights reaches
the render path instead of only mutating local example state.

## Design
Use focused tests for component insertion/removal and a native capture or
instrumented run-log check that observes render-visible volumetric-light state.
This issue is not the final `reference.png` acceptance; that remains ISS-224.

## Acceptance Criteria
- [x] Focused tests for ISS-290 and ISS-291 pass in release native mode.
- [x] Runtime evidence proves `VolumetricLight` toggles affect the data
      consumed by the volumetric fog render path.
- [x] The standard native capture for `examples/3d/volumetric_fog` completes
      without renderer fatal or missing-resource output after the toggle work.
- [x] Close notes include the exact commands and evidence artifacts.

## Relationships
- Depends on: ISS-291
- Parent: ISS-266
- Related: ISS-104, ISS-108, ISS-224
- Discovered from: ISS-266

## Notes
- 2026-07-05: Split from ISS-266 as the runtime evidence gate for volumetric
  light toggles.

## Close Notes
- 2026-07-05: Added a focused whitebox gate in
  `examples/3d/volumetric_fog` that runs the same `VolumetricLight`
  insert/remove helper used by the Bevy-shaped toggle system and verifies the
  renderer-facing
  `pbr/volumetric_fog::volumetric_fog_render_world_storage_counts(...).light_count()`
  transitions from `2 -> 1 -> 0 -> 1 -> 2` as point and spot lights are
  disabled and restored. Validation:
  `moon -C mgstudio-engine test examples/3d/volumetric_fog --target native --release`
  passed `Total tests: 9, passed: 9, failed: 0`; `moon -C mgstudio-engine fmt`;
  `moon -C mgstudio-engine info`; `moon -C mgstudio-engine check --target
  native`. Runtime capture:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/3d/volumetric_fog
  /tmp/mgstudio_iss292_volumetric_fog_toggle.png` completed. The run log
  `/tmp/mgstudio_iss292_volumetric_fog_toggle.run.log` was empty. Capture
  metadata: `2560x1440 RGBA`, mean luma `155.40325113932292`, SHA256
  `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`.
