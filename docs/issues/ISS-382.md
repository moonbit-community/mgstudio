# ISS-382: Prove volumetric_fog target format and load-store state

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: pbr, volumetric-fog, render-target, msaa, load-store, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Prove that mgstudio renders the real `examples/3d/volumetric_fog` fog pass
into the same target format, sample count, resolve path, and load/store state
that Bevy uses.

## Design
Use the owner map from ISS-381. Compare the Bevy render target and pass
descriptor against mgstudio for:

- color target identity and format;
- sample count and MSAA resolve behavior;
- load and store operations;
- depth attachment absence or presence as required by Bevy;
- whether the pass reads the same depth texture it expects while writing to the
  same color target.

## Acceptance Criteria
- [x] Bevy target and load/store source expressions are cited in close notes.
- [x] The corresponding mgstudio runtime values are asserted or recorded for
      the real example.
- [x] Any divergence is assigned to ISS-375 with exact owner and field name.
- [x] If no divergence is found, close with evidence that rules out target
      format and load/store state.

## Relationships
- Depends on: ISS-381
- Parent: ISS-374
- Related: ISS-285, ISS-346, ISS-347
- Discovered from: ISS-374

## Notes
- 2026-07-05: Split from ISS-374 because target and load/store mismatches can
  independently make a correct fog shader appear visually wrong.

## Close Notes
- 2026-07-05: Bevy source anchor: `bevy/crates/bevy_pbr/src/volumetric_fog/render.rs:282-307`
  queries `&ViewTarget`, `&ViewDepthTexture`, and `&Msaa`; `render.rs:362-366`
  sets the volumetric fog bind-group layout `MULTISAMPLED` bit from `Msaa`;
  `render.rs:391-406` opens the fog render pass with
  `view_target.main_texture_view()`, `resolve_target: None`, `LoadOp::Load`,
  `StoreOp::Store`, and `depth_stencil_attachment: None`.
- 2026-07-05: mgstudio PBR runtime anchor:
  `mgstudio-engine/pbr/volumetric_fog_execute.mbt:201-245` now records the
  prepared fog pipeline target format and MSAA sample count for each density
  variant. `mgstudio-engine/examples/3d/volumetric_fog/main_wbtest.mbt:719-739`
  consumes those counters from the real example's `DefaultPlugins` render
  setup, and `main_wbtest.mbt:800-813` asserts both textureless/textured fog
  variants, `RGBA16_FLOAT` target format, and multisampled view state.
- 2026-07-05: mgstudio renderer source anchor:
  `mgstudio-engine/render/renderer/mesh3d_volumetric_fog_pass.mbt:277-285`
  resolves the fog color attachment with `target_view(...)`, resolves the
  sampled depth input separately with `mesh3d_pass_view_depth_view(...)`, and
  records descriptor evidence counters before drawing. The draw loop opens the
  fog render pass through `begin_render_pass_color_load(color_view)`, which
  maps to a single color attachment with load/store and no resolve/depth
  attachment, matching Bevy's post-main-pass fog writeback shape.
- 2026-07-05: Verification: `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine test examples/3d/volumetric_fog --release`, and
  `MGSTUDIO_TIMELINE_TRACE=1 MGSTUDIO_TIMELINE_TRACE_OUTPUT=/tmp/mgstudio_iss382_volumetric_fog_trace.json MGSTUDIO_TIMELINE_TRACE_CATEGORIES=counter MGSTUDIO_TIMELINE_TRACE_FLUSH_FRAMES=90 MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/volumetric_fog /tmp/mgstudio_iss382_volumetric_fog.png`
  completed successfully. The capture command produced
  `/tmp/mgstudio_iss382_volumetric_fog.png`; the example does not install
  `TimelineTracePlugin`, so the timeline env did not produce a trace file
  without changing the Bevy-shaped example plugin set.
- 2026-07-05: No divergence was found for target format, MSAA key state,
  load/store state, resolve target, or depth attachment ownership. No external
  blocker is required for this issue.
