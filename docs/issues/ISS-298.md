# ISS-298: Capture repaired volumetric_fog visual output

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, screenshots, visual-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-06
- External ref: none

## Description
After the source-owner fixes for `volumetric_fog` are complete, capture the
mgstudio example again and prove that the oversized hull/surface symptom is
gone before ISS-224 is allowed to refresh the checked-in `reference.png`.

## Design
Run the standard native screenshot command for `examples/3d/volumetric_fog`
with the same capture timing used by the visual gate. Compare the new capture
against the Bevy baseline from ISS-294 and the old failure packet from ISS-293.

This issue may record the repaired artifact and close ISS-293, but the final
checked-in `reference.png` refresh remains owned by ISS-224.

## Acceptance Criteria
- [x] The standard native capture completes with an empty run log and no
      volumetric fog renderer fatal or missing-resource output.
- [x] The new capture shows the authored Bevy scene and volumetric lighting/fog
      response instead of a frame-dominating hull/surface.
- [x] Notes include the capture command, output path, log path, and comparison
      artifact paths.
- [x] ISS-224 remains open for the final human acceptance and `reference.png`
      refresh.

## Relationships
- Depends on: ISS-296, ISS-297, ISS-299, ISS-393
- Parent: ISS-293
- Related: ISS-224, ISS-104, ISS-108, ISS-299
- Discovered from: ISS-293

## Notes
- 2026-07-05: Split from ISS-293 as the final repaired-capture proof. Do not
  use this issue to accept the current failing capture.
- 2026-07-05: Capture after ISS-295 and ISS-297 still fails and exactly matches
  the previous failed artifact. Command:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/volumetric_fog /tmp/mgstudio_iss298_volumetric_fog_after_input_audit.png`.
  Output:
  `/tmp/mgstudio_iss298_volumetric_fog_after_input_audit.png`; log:
  `/tmp/mgstudio_iss298_volumetric_fog_after_input_audit.run.log`, size `0`.
  Screenshot SHA256
  `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`, size
  `2560x1440`, mean RGBA `[157.436204, 156.130897, 145.295103, 255.0]`.
  Diff against checked-in reference changed every pixel with mean RGBA
  `[111.785853, 120.696786, 114.295019, 0.0]`; diff artifact:
  `/tmp/mgstudio_iss298_volumetric_fog_diff.png`.
- 2026-07-05: The visible failure is unchanged: the frame is dominated by a
  near gray/white wall or scene surface, with only ceiling-slot detail visible.
  Since ISS-295 proved the authored camera/fog/light inputs and ISS-297 proved
  the inside-volume camera/uniform boundary, this issue now depends on ISS-299
  to audit the glTF scene render-space/visibility path.
- 2026-07-05: ISS-360 reran the final visual gate after ISS-359 closed and the
  result still matched the same failed frame hash
  `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`.
  This issue is now additionally blocked by ISS-393, the narrow follow-up for
  the remaining scene render-space owner.
- 2026-07-06: ISS-393 repaired the final local root cause by replacing
  mgstudio's tangent approximation with a Bevy-equivalent MikkTSpace-compatible
  generator for the normal-mapped GLB material. The repaired capture is
  `/tmp/mgstudio_vfog_meshopt_tangents.png`, run log
  `/tmp/mgstudio_vfog_meshopt_tangents.run.log`, SHA-256
  `6ab27c8fc48ab9322c450e28101c4f6fffd831f78d12473ebbe2c50aaac97151`. It
  shows the authored room, wall relief, volumetric light beams, red point-light
  fog, and UI text instead of the rejected near-gray surface.

## Close Notes
Closed on 2026-07-06.

The repaired visual output was captured with:

`MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/volumetric_fog /tmp/mgstudio_vfog_meshopt_tangents.png`

The run log is empty and the screenshot no longer matches the failed
`b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4` frame.
ISS-224 owns the checked-in `reference.png` refresh and was closed from the
same accepted capture.
