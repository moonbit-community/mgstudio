# ISS-092: Port Bevy skybox pipeline and cubemap source parity before reference refresh

## Metadata
- Type: bug
- Status: in_progress
- Priority: 1
- Labels: examples, screenshots, visual-parity, pbr, skybox, core-pipeline, source-parity
- Assignee: unassigned
- Created: 2026-06-13
- Updated: 2026-06-14
- External ref: none

## Description
`examples/3d/skybox` still has a transparent checked-in `reference.png`.
Retrying the standard native capture produced a numerically nonblank but
visually unusable dark frame instead of Bevy's visible Ryfjallet cubemap
skybox.

Current Bevy owns this path through `bevy_core_pipeline/src/skybox/mod.rs`
and `bevy_light/src/probe.rs::Skybox`: the plugin extracts `Skybox`, prepares
`SkyboxUniforms`, specializes a dedicated skybox render pipeline, validates a
cube texture view, prepares a bind group, and draws the skybox with the
core-3d depth format. Mgstudio currently stores the component in `pbr` and
draws a skybox proxy mesh through the generic mesh3d host path.

## Design
Port the Bevy skybox source owner directly instead of accepting a dark
reference image or patching the example. The implementation should map the
current Bevy `SkyboxPlugin`, extraction, uniforms, pipeline specialization,
bind-group preparation, cube texture validation, and draw ordering into
mgstudio owners. If MoonBit or wgpu_mbt cannot express an exact fragment,
record that as a precise blocker before leaving any local fallback in place.

## Acceptance Criteria
- [x] Every Bevy source-shape anchor in `bevy_core_pipeline/src/skybox/mod.rs`
      is classified as complete, partial, facade, blocked, or intentionally
      excluded.
- [x] `bevy_light/src/probe.rs::Skybox` maps to the mgstudio component owner
      without a PBR-only facade claiming complete source parity.
- [x] `examples/3d/skybox` renders a visible cubemap skybox through the
      standard native capture command and no longer produces a uniform dark
      frame.
- [x] The refreshed `examples/3d/skybox/reference.png` is accepted only after
      manual review confirms visible skybox/environment texture signal.
- [x] Focused render/PBR checks and the standard capture run have no
      validation, fatal, missing-resource, unsupported, or error output.
- [ ] The remaining skybox draw ownership is moved from renderer-local host
      draw logic into Bevy-shaped `SkyboxPlugin` render-app phase/node
      ownership.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-060, ISS-079, ISS-080, ISS-087, ISS-088
- Discovered from: ISS-060

## Notes
- 2026-06-13: Standard capture command:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=600 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/skybox mgstudio-engine/examples/3d/skybox/reference.png`.
  The run succeeded and printed `Swapping to textures/Ryfjallet_cubemap.png`,
  but the image was a uniform dark frame with `alpha_mean=1`,
  `gray_mean=0.387908`, and `size=2560x1440`. The generated image was
  discarded.
- 2026-06-14: Fixed the PNG cubemap asset path by adding Bevy-shaped
  `Image` descriptor metadata for array layout and texture view dimension.
  `examples/3d/skybox::asset_loaded` now mutates `Assets<Image>` like Bevy:
  when the loaded PNG still has one logical layer, it reinterprets the stacked
  2D image as an array and requests a cube texture view before assigning the
  handle to `Skybox`. Asset sync now rebuilds a cube GPU texture under the same
  handle id when dirty image metadata requires it.
- 2026-06-14: Source-shape classification for
  `bevy_core_pipeline/src/skybox/mod.rs`: component extraction, uniform
  upload, bind group preparation, cube texture validation, pipeline creation,
  and draw are implemented through `render/renderer/gpu_skybox.mbt` and the PBR
  skybox host draw path, but the owner remains `partial` because mgstudio still
  runs this as renderer-local draw logic rather than Bevy's render-app
  `SkyboxPlugin` phase/node ownership. `bevy_light/src/probe.rs::Skybox`
  remains open because the component is still owned under `pbr`.
- 2026-06-14: Validation: `moon -C mgstudio-engine check --target native
  --deny-warn --package-path image`; `moon -C mgstudio-engine check --target
  native --deny-warn --package-path asset`; `moon -C mgstudio-engine check
  --target native --deny-warn --package-path render/renderer`; `moon -C
  mgstudio-engine check --target native --deny-warn --package-path
  examples/3d/skybox`; `moon -C mgstudio-engine test --release image`;
  `moon -C mgstudio-engine test --release asset`; `moon -C mgstudio-engine
  fmt`; `moon -C mgstudio-engine info`; and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/skybox
  /tmp/mgstudio_iss092_skybox_fixed.png`. The refreshed capture is visible
  Ryfjallet cubemap content, `2560x1440`, RGBA mean approximately
  `[160.39,154.45,153.93,255.0]`, and the run log only printed
  `Swapping to textures/Ryfjallet_cubemap.png`.
- 2026-06-14: Moved the real `Skybox` component owner to
  `Milky2018/mgstudio/light`, matching Bevy's `bevy_light::Skybox`. `pbr`
  now aliases the light-owned type and forwards `ecs_key_skybox`; `core_pipeline`
  re-exports `@light.Skybox`; the stale empty `core_pipeline/skybox::Skybox`
  component was removed. Validation: `moon -C mgstudio-engine check --target
  native --deny-warn --package-path light`; `moon -C mgstudio-engine check
  --target native --deny-warn --package-path pbr`; `moon -C mgstudio-engine
  check --target native --deny-warn --package-path core_pipeline`; `moon -C
  mgstudio-engine check --target native --deny-warn --package-path
  core_pipeline/skybox`; `moon -C mgstudio-engine check --target native
  --deny-warn --package-path examples/3d/skybox`; `moon -C mgstudio-engine
  test --release light`; `moon -C mgstudio-engine test --release pbr`; `moon -C
  mgstudio-engine fmt`; `moon -C mgstudio-engine info`; and the standard skybox
  capture command to `/tmp/mgstudio_iss092_skybox_owner_migration.png`, which
  matched the checked-in reference byte-for-byte.

## Close Notes
