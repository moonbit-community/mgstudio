# ISS-031: Restore motion_blur example ECS source shape

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: parity, example, motion-blur, ecs, input, ui
- Assignee: unassigned
- Created: 2026-06-04
- Updated: 2026-06-04
- External ref: none

## Description
`examples/3d/motion_blur` now renders and its CI screenshot exit path works, but
the example itself is still not source-level equivalent to Bevy's
`bevy/examples/3d/motion_blur.rs`. Bevy models the example with ECS resources
and components: `CameraMode`, `Moves`, `CameraTracked`, `Rotates`, typed
queries, `Text` plus `TextSpan` children, `ButtonInput<KeyCode>`, and
`TextUiWriter`. Mgstudio currently keeps most runtime state in a process-global
`Ref[MotionBlurExampleState]`, including camera/tracked entities, car and wheel
arrays, motion blur values, and camera mode.

## Design
Re-port the example from the Bevy source owner, symbol by symbol. The MoonBit
version may use explicit keys and package-specific constructors, but the runtime
ownership must match Bevy: persistent example state should live in ECS resources
or components, systems should query ECS data instead of global arrays, and input
should mutate the camera's `MotionBlur` component and `CameraMode` resource.
Do not preserve the current global-state compatibility layer.

## Acceptance Criteria
- [x] `CameraMode`, moving car phase, tracked camera marker, and wheel rotation marker are represented as ECS resource/components rather than fields in a global `Ref`.
- [x] `keyboard_inputs`, `move_cars`, and `move_camera` operate through ECS queries and resources matching Bevy's source shape, not through stored entity arrays.
- [x] UI text remains a `Text` root with `TextSpan` children and is updated through the text writer path.
- [x] Generated ground texture data and sampler behavior remain byte/semantic equivalent to Bevy.
- [x] `MGSTUDIO_SCREENSHOT_DEBUG=1 MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_CAPTURE_RGBA8_BLOB=/tmp/mgstudio_motion_blur.rgba moon -C mgstudio-engine run examples/3d/motion_blur --release` exits successfully and captures a nonblank screenshot after the refactor.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-030, ISS-021
- Discovered from: ISS-030

## Notes
- 2026-06-04: Audit finding. The current example is behaviorally usable but is a partial port: Bevy's `Moves`, `CameraTracked`, and `Rotates` components are represented by `CarBody` and `WheelState` arrays; `CameraMode` is stored inside `MotionBlurExampleState` instead of an ECS resource; systems manually fetch and replace components through stored entities.

## Close Notes
- 2026-06-04: Removed the example-global `MotionBlurExampleState` `Ref` and
  restored Bevy-shaped ECS ownership in
  `mgstudio-engine/examples/3d/motion_blur/main.mbt`: `CameraMode` is now an
  ECS resource, `Moves`/`CameraTracked`/`Rotates` are ECS components, car and
  wheel updates query ECS state, input mutates the camera `MotionBlur`
  component plus `CameraMode` resource, and UI text is still a `Text` root with
  `TextSpan` children updated through `TextUiWriter`.
- Verification: `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_SCREENSHOT_DEBUG=1 MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_CAPTURE_RGBA8_BLOB=/tmp/mgstudio_motion_blur.rgba moon -C
  mgstudio-engine run examples/3d/motion_blur --release` all completed
  successfully. The captured `/tmp/mgstudio_motion_blur.png` is nonblank and
  shows the motion blur scene plus help text.
