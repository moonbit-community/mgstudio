# ISS-224: Accept volumetric_fog visual reference

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, screenshots, visual-parity
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-06
- External ref: none

## Description
After the `volumetric_fog` example source shape and runtime controls are
restored, the final gate is a native screenshot/reference acceptance. This
issue must not hide unresolved source divergence by refreshing a plausible but
wrong image.

## Design
Run the standard native screenshot command for `examples/3d/volumetric_fog`.
Compare against the current checked reference and Bevy expected behavior.
Refresh `reference.png` only after render logs are clean and the visible output
uses the real Bevy-shaped volumetric fog path.

## Acceptance Criteria
- [x] The standard native capture completes with no volumetric fog renderer
      fatal or missing-resource output.
- [x] The image shows the authored Bevy scene and volumetric lighting/fog
      response, not proxy distance fog.
- [x] Close notes include the exact command, output path, and comparison
      artifact used for acceptance.
- [x] The checked-in `reference.png` is updated only after acceptance.

## Relationships
- Depends on: ISS-223, ISS-293
- Parent: ISS-191
- Related: ISS-104, ISS-108
- Discovered from: ISS-191

## Notes
- 2026-07-04: Split from ISS-191 as the final visual gate for the
  `volumetric_fog` example.
- 2026-07-05: ISS-223 is closed, but the fresh capture
  `/tmp/mgstudio_iss292_volumetric_fog_toggle.png` is not acceptable as a
  reference: it differs from the checked-in reference on every pixel and shows
  the scene dominated by an oversized light/fog hull or camera-adjacent
  surface. Created ISS-293 for the local visual/parity fix. Do not refresh
  `reference.png` until ISS-293 is closed and a human visual review accepts the
  result.
- 2026-07-06: ISS-393 identified and repaired the final local render owner: the
  normal-mapped GLB primitive needed a Bevy-equivalent MikkTSpace tangent basis.
  The accepted capture is `/tmp/mgstudio_vfog_meshopt_tangents.png`, SHA-256
  `6ab27c8fc48ab9322c450e28101c4f6fffd831f78d12473ebbe2c50aaac97151`, with an
  empty run log at `/tmp/mgstudio_vfog_meshopt_tangents.run.log`.

## Close Notes
Closed on 2026-07-06.

The checked-in reference was refreshed from the accepted native capture:

`MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/volumetric_fog /tmp/mgstudio_vfog_meshopt_tangents.png`

The capture shows the authored Bevy scene: normal-mapped red and gray walls,
window light beams, floor light patches, red volumetric point light, and UI
instructions. It no longer matches the rejected camera-adjacent gray surface
frame. `mgstudio-engine/examples/3d/volumetric_fog/reference.png` has the same
SHA-256 as the accepted capture:
`6ab27c8fc48ab9322c450e28101c4f6fffd831f78d12473ebbe2c50aaac97151`.
