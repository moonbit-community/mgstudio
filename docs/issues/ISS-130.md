# ISS-130: Restore spotlight source parity

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: examples, pbr, lighting, spot-light, shadows, ui, source-parity, visual-parity
- Assignee: unassigned
- Created: 2026-06-16
- Updated: 2026-06-16
- External ref: none

## Description
`mgstudio-engine/examples/3d/spotlight/main.mbt` is not a symbol-level port of
`bevy/examples/3d/spotlight.rs`.

The render-side spot-light path exists and the example preserves many visible
constants: ambient brightness, light intensity, grid dimensions, cone angles,
camera placement, HDR, instruction text, movement keys, and light-sway math.
The strict source gaps are local:

- Bevy's `Movable` marker component is replaced by a global tracked entity
  array;
- Bevy's deterministic `ChaCha8Rng::seed_from_u64(19878367467713)` is replaced
  by a local LCG seeded with a truncated value;
- Bevy's `setup`, `light_sway`, `movement`, and `rotation` system parameters
  are replaced by direct world systems and tracked entity ids;
- Bevy's UI `Text` + absolute `Node` tuple is replaced by a world-space
  `Text2dBundle`;
- Bevy's `rotate_around` camera mutation is approximated by recomputing a
  look-at transform;
- local helper geometry replaces the Bevy primitive-builder owner shape;
- the direction marker writes its `Parent` component twice.

These are local source-parity gaps, not backend blockers.

## Design
Port the example directly from `bevy/examples/3d/spotlight.rs`:

- define and use a real `Movable` component;
- use source-equivalent ChaCha8 deterministic RNG or record a precise RNG API
  blocker if no equivalent exists;
- keep setup data flow through `Commands`, mesh assets, and material assets;
- preserve marker mesh construction from Bevy's `Sphere` primitive builder as
  closely as the primitive API permits;
- spawn marker children once, including `NotShadowCaster` on the direction
  marker;
- preserve UI as `Text` plus absolute `Node` rather than world-space `Text2d`;
- implement `light_sway`, `movement`, and `rotation` with source-shaped query
  ownership;
- use source-equivalent camera `rotate_around` semantics.

If the current ECS cannot express a required system-param or query shape,
record the exact ECS blocker rather than keeping global tracked arrays as the
parity implementation.

## Acceptance Criteria
- [x] `Movable` is a component and movement uses a `With<Movable>` query.
- [x] The cube positions are generated from a source-equivalent ChaCha8 seeded
      RNG, or a precise RNG API blocker is recorded.
- [x] `setup` uses Bevy-shaped commands/assets parameters where possible.
- [x] Marker meshes preserve `Sphere::new(...).mesh().uv(32, 18)` semantics or
      record a precise primitive-builder blocker.
- [x] Spot-light child marker entities are parented once and the direction
      marker has `NotShadowCaster`.
- [x] UI uses source-shaped `Text` and absolute `Node`.
- [x] `light_sway` mutates `Transform` and `SpotLight` through a query rather
      than tracked entity ids.
- [x] `movement` mutates movable transforms through a query.
- [x] `rotation` uses source-equivalent `rotate_around` semantics.
- [x] `SpotlightState`, tracked arrays, local LCG helpers, duplicate parent
      writes, and direct-world helper paths are removed unless precise blockers
      are recorded.
- [x] A fresh `examples/3d/spotlight/reference.png` is captured after the
      source-shaped port still renders correctly.

## Relationships
- Depends on: none
- Parent: ISS-105
- Related: ISS-012, ISS-021, ISS-070, ISS-129
- Discovered from: ISS-105

## Notes
- 2026-06-16: Created from the ISS-105 symbol audit. The spot-light runtime
  path exists; the issue is source-shape fidelity in the example.
- 2026-06-16: Local source-shape gaps were fixed. The only remaining exact
  source-form gap is Bevy's primitive-builder owner shape for
  `Sphere::new(...).mesh().uv(32, 18)`, tracked globally by ISS-070.

## Close Notes
- 2026-06-16: Rebuilt `examples/3d/spotlight` around Bevy-shaped symbols:
  `Movable`, command/assets startup params, ChaCha8 seeded RNG,
  `Text + Node` UI, query-owned `light_sway`/`movement`, `Single` camera
  rotation with `Transform::rotate_around`, and single parent writes for marker
  children. Captured `mgstudio-engine/examples/3d/spotlight/reference.png` with
  the fixed native screenshot command.
- 2026-06-26: Follow-up engine source-owner repair for
  `bevy_light/src/spot_light.rs`: `SpotLight::SpotLight()` now defaults to
  `inner_angle = 0.0` and `outer_angle = FRAC_PI_4` instead of the old local
  `0.6/0.8` values. `with_inner_angle` and `with_outer_angle` also now
  preserve supplied values directly, and PBR spot-light extraction keeps
  Bevy's authored `(inner_angle, outer_angle)` pair instead of clamping
  `outer_angle` to `inner_angle`. The Bevy default now flows through PBR
  lighting and renderer pass fallback state. Validation: `moon -C
  mgstudio-engine check --target native --package-path light --deny-warn`,
  `moon -C mgstudio-engine test light --release --deny-warn`, `moon -C
  mgstudio-engine check --target native --package-path pbr/render --deny-warn`,
  `moon -C mgstudio-engine test pbr/render --release --deny-warn`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and `moon -C
  mgstudio-engine check --target native`.
