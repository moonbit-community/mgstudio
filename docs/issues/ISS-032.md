# ISS-032: Complete ci_testing plugin source parity

## Metadata
- Type: task
- Status: blocked
- Priority: 1
- Labels: parity, dev_tools, ci_testing, scheduling, screenshot
- Assignee: unassigned
- Created: 2026-06-04
- Updated: 2026-06-11
- External ref: none

## Description
The `ScreenshotAndExit` bug exposed a real source-shape drift in
`mgstudio-engine/dev_tools/ci_testing`: it previously polled screenshot
components in a local pending list, while Bevy writes `AppExit::Success` from
the `ScreenshotCaptured` observer. That specific bug is fixed, but the owner is
still only partial compared with `bevy_dev_tools/src/ci_testing`.

Bevy's plugin loads `CiTestingConfig` from `CI_TESTING_CONFIG`/RON by default,
adds `EasyCameraMovementPlugin` when missing, conditionally adds screen
recording support, configures fixed frame time, registers
`CiTestingCustomEvent`, and schedules `systems::send_events` in `Update` before
`trigger_screenshots`, before `close_when_requested`, and before terminal
Ctrl-C exit handling. Mgstudio currently reads only a small set of environment
variables, has no event-sender set ordering equivalent, and lacks the
screen-recording event surface.

## Design
Audit `bevy/crates/bevy_dev_tools/src/ci_testing/{config.rs,mod.rs,systems.rs}`
and port every in-scope item. Keep the fixed-frame and screenshot exit behavior
that now works, but replace local-only configuration and scheduling shortcuts
with Bevy-shaped plugin build behavior. If RON parsing or screen recording needs
dependencies that do not exist in MoonBit, record those as blockers; otherwise
implement them directly.

## Acceptance Criteria
- [x] `CiTestingConfig`, `CiTestingSetup`, `CiTestingEventOnFrame`, and `CiTestingEvent` cover Bevy's in-scope fields and variants, with screen recording tracked by ISS-069.
- [ ] Plugin build reads config from an existing resource or default config file semantics equivalent to Bevy, with RON loading tracked by ISS-068 and environment-only configuration kept only where it is explicitly documented as an additional mgstudio test convenience.
- [x] `send_events` has Bevy-equivalent schedule ordering before screenshot triggering, window close handling, and terminal Ctrl-C handling.
- [x] `ScreenshotAndExit` continues to save the screenshot and write `AppExit::Success` from the capture observer.
- [ ] Relevant `dev_tools` and screenshot tests pass with `--release`, and the motion_blur screenshot-exit gate remains green.

## Relationships
- Depends on: ISS-068, ISS-069
- Parent: ISS-012
- Related: ISS-021, ISS-030
- Discovered from: ISS-030

## Notes
- 2026-06-04: Audit finding. The fixed `ScreenshotAndExit` observer path is now source-shaped, but the plugin owner remains partial because config loading, event-set ordering, easy camera movement integration, and screen-recording event handling are not source-level equivalent to Bevy.
- 2026-06-04: Ported the Bevy `CameraMovement` / `EasyCameraMovementPlugin` fragment into a separate MoonBit package to avoid a parent-package import cycle, changed `MoveCamera` to insert `CameraMovement`, added `EventSenderSystems`, and ordered it before window close and terminal Ctrl-C handling. The issue remains incomplete because Bevy's RON-backed `CiTestingConfig` loading and feature-gated `EasyScreenRecordPlugin`/`RecordScreen` owner do not have a faithful MoonBit dependency/owner yet; env-only config and no-op screen recording cannot be counted as complete parity.
- 2026-06-11: Exposed a Bevy-shaped `window_screenshot_trigger_system_set` and scheduled screenshot queueing in `WindowPlugin`'s `Update` schedule. `CiTestingPlugin` now orders `ci_testing_send_events_system` before screenshot triggering, window close handling, and terminal Ctrl-C handling. Added a regression test proving custom CI events are visible to systems in the screenshot trigger set in the same frame. Targeted `dev_tools/ci_testing` release tests pass; the motion_blur screenshot-exit gate was not rerun in this slice. Remaining source gaps are now represented by blocker issues ISS-068 and ISS-069 instead of local no-op parity claims.

## Close Notes
