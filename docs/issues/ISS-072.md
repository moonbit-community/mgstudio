# ISS-072: Port Mesh2d ViewKeyCache and full pipeline key owner

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, sprite_render, mesh2d, render2d, view-key, render-phase
- Assignee: unassigned
- Created: 2026-06-12
- Updated: 2026-06-12
- External ref: none

## Description
Bevy's `bevy_sprite_render/src/mesh2d/mesh.rs` computes a per-view
`Mesh2dPipelineKey` in `ViewKeyCache`, combining MSAA, target format,
compositing space, tonemapping, deband dither, primitive topology, strip index
format, and material alpha bits before `Material2dPipeline<M>::specialize`.

mgstudio now wires `Material2d::specialize` into host Mesh2d pipeline creation,
and draw-time keys include primitive topology and strip index format. The
remaining full key bits still come from render2d's immediate draw path instead
of a Bevy-shaped `ViewKeyCache` and render-phase specialization owner.

## Design
Port the Bevy source owner shape from `mesh2d/mesh.rs`: add a render-world
view-key cache, derive view key bits from extracted 2D camera state, combine
those bits with mesh topology/index-format and material alpha bits before
specializing material pipelines, and make phase queue/draw code consume the
cached pipeline ID rather than constructing view-dependent keys at immediate
draw time. This issue must coordinate with ISS-029, because the full fix spans
Core2d phase ownership rather than only `Material2d`.

## Acceptance Criteria
- [ ] Add a MoonBit `ViewKeyCache` equivalent for Mesh2d that records Bevy's
  target format, MSAA, compositing, tonemapping, and deband dither bits.
- [ ] Combine view key bits, primitive topology/strip index bits, and material
  alpha bits before `Material2dPipeline::specialize`.
- [ ] Ensure `Material2dKey[M]` receives the full Bevy-shaped mesh key, not a
  reduced alpha/topology-only key.
- [ ] Add tests covering at least target format, tonemapping/deband, primitive
  topology, and material alpha key separation.
- [ ] Validate with representative Mesh2d and shader material examples under
  `--release`.

## Relationships
- Depends on: ISS-029
- Parent: ISS-045
- Related: ISS-029, ISS-071
- Discovered from: ISS-071

## Notes
- 2026-06-12: Split from ISS-071. This is not a `Material2d::specialize` hook
  gap; it is the remaining cross-owner gap between Bevy's `ViewKeyCache`,
  `SpecializedMaterial2dPipelineCache`, and mgstudio's current render2d
  immediate draw path.
- 2026-06-12: Moved the Bevy primitive topology and strip index-format key-bit
  helpers out of `Material2d` and into the Mesh2d runtime owner. This does not
  change draw behavior yet, but it gives the future ViewKeyCache one canonical
  Mesh2d key owner instead of keeping topology encoding in material-local code.
  Validation: `moon -C mgstudio-engine test sprite_render/mesh2d/runtime
  --release`, `moon -C mgstudio-engine test sprite_render/mesh2d/material
  --release` (no test entry), and `moon -C mgstudio-engine check --target
  native --package-path sprite_render/render`.
- 2026-06-12: Added the missing Bevy `CompositingSpace` camera component owner
  and ported Mesh2d view-key bit encoding for target format, MSAA,
  compositing, tonemapping, and deband dither into
  `sprite_render/mesh2d/runtime`. This still does not route draw behavior
  through a render-world `ViewKeyCache`; it establishes the source-shaped key
  calculation that the later render-phase integration must consume.
  Validation: `moon -C mgstudio-engine test sprite_render/mesh2d/runtime
  --release`, `moon -C mgstudio-engine test camera --release`, and `moon -C
  mgstudio-engine check --target native --package-path sprite_render/render`.
- 2026-06-12: Split `Material2dProperties.mesh_pipeline_key_bits` from render
  phase selection. The key bits now match Bevy's `alpha_mode_pipeline_key`
  shape: opaque contributes no bit, blend contributes `BLEND_ALPHA`, and mask
  contributes `MAY_DISCARD`; phase selection remains driven by `alpha_mode`
  itself. Validation: `moon -C mgstudio-engine test
  sprite_render/mesh2d/material --release`, `moon -C mgstudio-engine test
  sprite_render/render --release`, and `moon -C mgstudio-engine check --target
  native --package-path sprite_render/render`.
- 2026-06-12: Wired `CompositingSpace` into render2d camera extraction and
  added a white-box accessor test for the extracted camera field. Full
  `ViewKeyCache` behavior is still incomplete because the current render2d
  immediate path does not own the Bevy `ExtractedView::target_format`; that
  target-format source must be introduced with the Core2d/ExtractedView owner
  split instead of guessed in sprite render. Validation: `moon -C
  mgstudio-engine test sprite_render/render --release` and `moon -C
  mgstudio-engine check --target native --package-path sprite_render/render`.
- 2026-06-12: Added render/view-owned helpers for Bevy's target-format
  selection rules, including window BGRA8 SRGB normalization and HDR/sRGB
  compositing overrides. This gives the future `ViewKeyCache` the right owner
  for target-format bits, but it is not yet wired into a full render-world
  `ExtractedView` or Mesh2d queue path. Validation: `moon -C mgstudio-engine
  test render/view --release` and `moon -C mgstudio-engine check --target
  native --package-path render/view`.
- 2026-06-12: Routed the current render2d camera extraction fragment through
  the render/view target-format helper and stored the result as
  `ExtractedCamera2d.target_format_raw`. This is still an intermediate fragment:
  Mesh2d `ViewKeyCache` does not consume it yet, and full Bevy
  `ExtractedView` ownership remains under ISS-029. Validation: `moon -C
  mgstudio-engine test sprite_render/render --release` and `moon -C
  mgstudio-engine check --target native --package-path sprite_render/render`.

## Close Notes
