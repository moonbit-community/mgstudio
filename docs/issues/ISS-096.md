# ISS-096: Fix custom_shader_instancing shader asset loading before reference refresh

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: examples, screenshots, visual-parity, shader, asset-loading, source-parity
- Assignee: unassigned
- Created: 2026-06-13
- Updated: 2026-06-19
- External ref: none

## Description
`examples/shader_advanced/custom_shader_instancing/reference.png` needed to be
refreshed only after the shader asset path and custom instancing render path
could produce visible scene content without asset-loader or missing-resource
errors.

## Design
Audit the example and shader plugin/resource ownership against the current Bevy
`examples/shader_advanced/custom_shader_instancing.rs` source. Fix the missing
shader asset loader or plugin registration path directly instead of accepting a
background-only capture. Do not classify this as an external blocker unless the
audit proves a missing MoonBit or dependency capability.

## Acceptance Criteria
- [x] The example registers the shader asset loader/plugin path required before
      loading custom shader handles.
- [x] The standard native capture completes with no validation, fatal,
      missing-resource, unsupported, panic, abort, or asset-loader error output.
- [x] The refreshed `reference.png` contains visible custom shader instancing
      scene content, not only the default background.
- [x] Any Bevy source-shape differences in the example, shader material, or
      render path are fixed or split into precise follow-up issues.

## Relationships
- Depends on: none
- Parent: ISS-060
- Related: ISS-012
- Discovered from: ISS-060

## Notes
- 2026-06-13: Discovered while retrying ISS-060 reference regeneration with:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=600 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/shader_advanced/custom_shader_instancing mgstudio-engine/examples/shader_advanced/custom_shader_instancing/reference.png`.
  The capture wrote an opaque default-background image
  (`alpha_mean=1`, `gray_mean=0.181000`, `size=2560x1440`,
  `non_black_ratio=1.0`, `mean_luma=46.186`) but the run log contained the
  missing shader asset loader error. The generated image was rejected.
- 2026-06-16: ISS-105 symbol audit found that this is broader than shader
  asset loading. Bevy's example is a custom low-level instancing render path:
  one mesh entity with `InstanceMaterialData`, render-world extraction,
  `CustomPipeline`, instance-buffer preparation, transparent phase queueing,
  and `DrawMeshInstanced`. The current mgstudio example substitutes many
  ordinary mesh entities plus local transform animation, so this must be fixed
  as source-parity work rather than treated as a loader-only issue.
- 2026-06-19: Fixed one real prerequisite in `SpecializedMeshPipelines`: the
  pipeline cache now preserves and deduplicates the full array of vertex buffer
  layouts, including Bevy's mesh vertex buffer plus instance-rate vertex
  buffer shape. The remaining custom instancing owner migration is tracked by
  ISS-134; do not refresh this reference until that source owner is complete.
- 2026-06-19: The standard native capture now renders the visible 10x10
  colored custom-instanced cube grid, and the run log is empty. Full Bevy
  source-owner parity for the custom instancing pipeline remains tracked by
  ISS-134.

## Close Notes
Closed after verifying:

- `moon -C mgstudio-engine check --target native`
- `moon -C mgstudio-engine test --release render/renderer`
- `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/shader_advanced/custom_shader_instancing /tmp/mgstudio_iss096_custom_shader_instancing.png`

`/tmp/mgstudio_iss096_custom_shader_instancing.run.log` had zero lines, and the
capture matched
`mgstudio-engine/examples/shader_advanced/custom_shader_instancing/reference.png`.
This issue is closed as the shader asset loading/reference refresh problem; the
broader Bevy source-owner migration remains open in ISS-134.
