# ISS-086: Port occlusion_culling GPU readback and true culling source parity

## Metadata
- Type: bug
- Status: blocked
- Priority: 1
- Labels: examples, render, occlusion-culling, gpu-preprocessing, readback, source-parity
- Assignee: unassigned
- Created: 2026-06-13
- Updated: 2026-07-10
- External ref: none

## Description
`examples/3d/occlusion_culling/reference.png` can now serve as a nontransparent
visual baseline, but the example is not source-level faithful to Bevy. The
current MoonBit source explicitly states that Bevy reads indirect draw
parameters back from the GPU while mgstudio uses a scene-space heuristic to
approximate culled counts.

Bevy's `examples/3d/occlusion_culling.rs` installs render/PBR debug flags,
uses `OcclusionCulling`, `DepthPrepass`, GPU preprocessing support, indirect
parameter buffers, staging buffers, GPU-to-CPU map/copy readback, and render-app
systems to report the number of meshes actually rendered. Mgstudio needs that
render-app source shape or precise blockers for the missing GPU/API pieces.

## Design
Audit and port Bevy `examples/3d/occlusion_culling.rs` as the migration unit:

- preserve `RenderDebugFlags::ALLOW_COPIES_FROM_INDIRECT_PARAMETERS` behavior;
- expose or port the required GPU preprocessing support and indirect parameter
  buffer owners;
- implement the staging-buffer copy/map readback path used by
  `ReadbackIndirectParametersPlugin`;
- display real GPU-derived rendered/culled counts instead of scene-space
  heuristic counts;
- keep the `Space` toggle, depth prepass, occlusion-culling component, and
  rotating cube scene source-shaped.

Do not count the current heuristic counter path as source parity. If wgpu_mbt,
wgpu-native, or mgstudio render-app APIs cannot expose the needed readback
surface yet, record the exact blocker and the deletion condition for the
heuristic path.

## Acceptance Criteria
- [x] Every source-shape anchor in Bevy `examples/3d/occlusion_culling.rs` is
      classified as complete, partial, facade, blocked, or intentionally
      excluded.
- [ ] The example installs Bevy-equivalent render debug flags and render-app
      readback systems.
- [x] Indirect parameter data and count are copied/read back from the GPU, or a
      precise dependency/API blocker records why that cannot be implemented.
- [ ] The scene displays real rendered/culled counts rather than heuristic
      scene-space estimates.
- [x] The approximation note in
      `examples/3d/occlusion_culling/main.mbt` is removed or replaced by
      precise blocker references.
- [x] `examples/3d/occlusion_culling/reference.png` remains visually valid
      after the source parity fix.

## Relationships
- Depends on: ISS-100
- Parent: ISS-012
- Related: ISS-060, ISS-014, ISS-019, ISS-021
- Discovered from: ISS-060

## Notes
- 2026-06-13: Discovered while refreshing
  `examples/3d/occlusion_culling/reference.png`. The refreshed image is
  visually usable, but the source still uses a heuristic counter instead of
  Bevy's GPU indirect parameter readback path.
- 2026-06-14: Removed the scene-space heuristic counter from
  `examples/3d/occlusion_culling/main.mbt`; the example now reports that GPU
  indirect-parameter readback is unavailable instead of displaying fake culled
  counts. The Space toggle now adds/removes the real `DepthPrepass` and
  `OcclusionCulling` camera components.
- 2026-06-14: Source-shape anchor classification:
  - Complete: scene constants, small/large cube scene, rotation systems,
    light, camera `DepthPrepass`/`OcclusionCulling` components, Space toggle.
  - Blocked: `RenderDebugFlags::ALLOW_COPIES_FROM_INDIRECT_PARAMETERS`,
    `ReadbackIndirectParametersPlugin`, `IndirectParametersStagingBuffers`,
    `SavedIndirectParameters`, `init_saved_indirect_parameters`,
    `create_indirect_parameters_staging_buffers`,
    `readback_indirect_parameters_node`, `readback_indirect_parameters`,
    `readback_buffer`, and GPU-derived rendered mesh count. See ISS-100.
  - Partial: text presentation uses the current mgstudio text surface while
    the readback blocker is unresolved.
  - Facade or intentionally excluded: none.
- 2026-06-14: Verification: `moon -C mgstudio-engine check --release
  --package-path examples/3d/occlusion_culling`.
- 2026-06-14: Visual gate:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/3d/occlusion_culling /tmp/mgstudio_iss086_occlusion_culling.png`;
  refreshed `examples/3d/occlusion_culling/reference.png`.
- 2026-06-16: ISS-105 symbol audit corrected the acceptance checklist: the
  current example no longer reports heuristic counts, but it also does not
  display Bevy's real GPU-derived rendered/culled counts. That remains blocked
  by ISS-100.
- 2026-07-10: The ISS-019 early/late prepass audit exposed an additional local
  runtime failure before readback: a supervised native capture aborts during
  `host_gpu_downsample_mesh3d_depth_pyramid` because the
  `mgstudio_mesh3d_preprocess_downsample_depth_bg` bind group is invalid at
  queue submission. A strict detached-worktree A/B at parent commit
  `a418b54ce` produced the identical 1152-byte log and error, proving the
  failure predates the early/late scheduling change. Current and parent logs:
  `/tmp/mgstudio_iss019_early_late_occlusion_culling_repro.run.log` and
  `/tmp/mgstudio_iss019_parent_a418_occlusion_culling.run.log`. This is a local
  occlusion-culling runtime gap, not a new external blocker; the existing
  ISS-086/ISS-100 owner pair remains the deletion and validation boundary.
- 2026-07-10: Diagnosed and fixed that local downsample failure against pinned
  Bevy `bevy_core_pipeline/src/mip_generation/experimental/depth.rs`. A
  one-variable probe made the default `Sample4` camera fail while `Msaa::Off`
  captured successfully, proving that mgstudio's single-sample-only binding 0
  layout was invalid for the multisampled view depth texture. Mgstudio now
  creates Bevy's separate standard and multisample layouts, compiles first and
  second pipelines for both variants with the `MULTISAMPLE` shader definition,
  selects them from the actual view-depth sample count, and binds depth-pyramid
  storage views 0 through 11 at bindings 1 through 12 instead of the previous
  off-by-one 1 through 12 mapping. Renderer white-box regressions cover the four
  pipeline keys and mip-binding endpoints. This clears the local runtime
  failure, but ISS-086 remains blocked by ISS-100's separate public indirect
  parameter readback surface.
- 2026-07-10: Verification:
  `MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine test render/renderer --target native --release`
  passed 102/102;
  `MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine test pbr --target native --release`
  passed 467/467; native check completed with 0 errors; `moon info`, `moon fmt`,
  and `git diff --check` passed. The supervised default-MSAA frame-90 gate
  produced `/tmp/mgstudio_iss086_downsample_msaa_frame90.png` with an empty
  `/tmp/mgstudio_iss086_downsample_msaa_frame90.run.log` and left no example
  process.

  ### VIS-086-downsample-msaa-frame90

  - Scope: `examples/3d/occlusion_culling` multisampled depth-pyramid
    downsampling and late occlusion preprocessing.
  - Bevy source owner:
    `bevy_core_pipeline/src/mip_generation/experimental/depth.rs`.
  - Mgstudio artifact:
    `/tmp/mgstudio_iss086_downsample_msaa_frame90.png`.
  - Bevy reference artifact:
    `mgstudio-engine/examples/3d/occlusion_culling/reference.png` (local pinned
    review artifact; official Bevy comparison is still required).
  - Human judgment: `PENDING`.
  - Visible symptom: the repaired frame is nonblank and contains the expected
    rotating center cube and surrounding small cubes, but its dynamic pose and
    visible occluded-cube set differ from the checked-in reference frame.
  - Severity: `UNKNOWN`.
  - Logs: `/tmp/mgstudio_iss086_downsample_msaa_frame90.run.log`; summary:
    empty, with no fatal, shader, bind-group, or runtime error.
  - Source trail:
    `bevy_core_pipeline/src/mip_generation/experimental/depth.rs` and
    `mgstudio-engine/render/renderer/mesh3d_preprocess_{pipeline_specialization,resources,runtime}.mbt`.
  - Required next evidence: capture the pinned Bevy example at an equivalent
    fixed frame and obtain human judgment; ISS-100 must also expose real GPU
    indirect-parameter readback before ISS-086 can close.

  ### VIS-086-downsample-msaa-cross-3d-shapes

  - Scope: `examples/3d/3d_shapes` default-MSAA PBR regression gate using a
    different example from the occlusion-specific acceptance gate.
  - Bevy source owner: `bevy_pbr/src/render/mesh.rs` and
    `bevy_core_pipeline/src/core_3d/mod.rs`.
  - Mgstudio artifact:
    `/tmp/mgstudio_downsample_msaa_3d_shapes_frame90.png` (SHA-256
    `27a404f620d6a7dd96dfe6e0f2dc4af65dc8f7c65a39b5f28af45b125b36c4c4`).
  - Bevy reference artifact:
    `mgstudio-engine/examples/3d/3d_shapes/reference.png` (SHA-256
    `a40d8637914a42e41fb195ded3ae70f8c0103d8501d6e73272752771f20ec894`).
  - Human judgment: `PENDING`.
  - Visible symptom: the current capture contains the complete three rows of
    textured shapes and their shadows, but it also contains the current help UI
    and a different upper background/ground framing from the checked-in
    reference.
  - Severity: `UNKNOWN`.
  - Logs: `/tmp/mgstudio_downsample_msaa_3d_shapes_frame90.run.log`; summary:
    empty, with no fatal, shader, bind-group, or runtime error.
  - Source trail: `mgstudio-engine/pbr/render_execute*.mbt` and
    `mgstudio-engine/render/renderer/mesh3d_preprocess_*.mbt`.
  - Required next evidence: compare the same scene state with an equivalent
    pinned-Bevy capture and obtain human judgment; do not refresh the local
    reference from this packet alone.

## Close Notes
