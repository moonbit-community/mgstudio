# ISS-016: Align PbrPlugin build flow with Bevy

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, pbr, plugin, render-app
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-20
- External ref: none

## Description
Bevy's `PbrPlugin` installs shader libraries, mesh rendering, materials,
standard material support, SSAO, fog, lightmaps, light probes, GPU preprocess,
volumetric fog, SSR, transmission, decals, contact shadows, medium, atmosphere,
GPU clustering, GLTF integration, deferred lighting, default material state,
embedded PBR resources, render startup systems, extract systems, render systems,
GPU resources, and light view observers.

Mgstudio installs many of these pieces, but the build flow is decomposed
differently and has not been proven source-equivalent in order, ownership, and
resource lifecycle.

## Design
Port `bevy/crates/bevy_pbr/src/lib.rs::PbrPlugin::build` and `finish` as the
source of truth. Keep package splits, but preserve plugin order, render-app
system placement, resource insertion conditions, default material initialization,
light extraction/sync behavior, and observer semantics.

## Acceptance Criteria
- [ ] PBR shader library registration matches the Bevy source list, including dummy meshlet visibility resolve behavior.
- [ ] Feature plugin installation order matches Bevy where order has semantic impact.
- [ ] Default `StandardMaterial` handle initialization follows Bevy behavior.
- [ ] Render startup systems for shadow samplers, clusterable object metadata, and fallback bindless resources are installed in the Bevy-shaped phase.
- [ ] Light, ambient light, shadow filtering, shadow LOD origin, and material instance extraction systems are installed with Bevy-equivalent ordering.
- [ ] Light view entity observers and sync-component targets are represented with source-equivalent behavior.
- [ ] `PbrPlugin::finish` initializes global cluster settings from the render world as Bevy does.

## Relationships
- Depends on: ISS-014
- Parent: ISS-012
- Related: ISS-017, ISS-018, ISS-019
- Discovered from: none

## Notes
- 2026-06-02: Current mgstudio PBR plugin has many feature plugins but still needs exact source-owner audit against Bevy `PbrPlugin`.
- 2026-06-05: While validating the sprite-render wireframe owner move, `examples/2d/2d_shapes --release` exposed a real PBR build-flow drift: `PbrPlugin` was still registering `Mesh3d -> Transform` and `Mesh3d -> Visibility` required components even though current Bevy owns `Mesh3d -> Transform` in `bevy_mesh/src/components.rs` via `#[require(Transform)]`, and mgstudio's `mesh` owner already registers that edge. Removed the duplicate PBR-owned Mesh3d required-component registrations. This keeps the broader PBR plugin issue open because Camera3d/core-pipeline/render required-component ownership still needs a full Bevy source-owner audit.
- 2026-06-05: Moved the Bevy `CameraPlugin`-owned `Camera3d -> ColorGrading` and `Camera3d -> Exposure` required-component registrations into `camera_plugin`, matching `bevy_render/src/camera.rs`. Removed the stale `Camera3d -> Exposure` registration from PBR's render3d bundle helper and added a camera plugin test for the new owner. Remaining local work: `Camera3d -> Camera/Projection` still lives in the PBR helper because mgstudio still owns the `Camera3d` type in the PBR package, and `Core3dPlugin`-owned camera defaults still need a separate source-owner migration.
- 2026-06-20: Added the missing Bevy `PbrPlugin.debug_flags` configuration
  path and propagated it through `PbrPluginRuntimeConfig` into
  `MeshRenderPlugin` / `MeshRenderPluginState`, matching the first-level
  `PbrPlugin -> MeshRenderPlugin { debug_flags: self.debug_flags }` source
  flow in `bevy_pbr/src/lib.rs` and `bevy_pbr/src/render/mesh.rs`. This does
  not close the full issue: `MaterialsPlugin`, `MaterialPlugin`, prepass, and
  render-phase plugins still need their own debug flag ownership audited and
  ported. Targeted validation: `moon -C mgstudio-engine check --target native
  --package-path pbr`, `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and a successful
  `examples/shader_advanced/custom_shader_instancing` native screenshot sanity
  gate. `examples/3d/motion_blur` still fails on the already tracked
  motion-vector/prepass ownership gap; the new evidence is recorded in
  `ISS-034`.
- 2026-06-20: Completed the rest of the Bevy debug-flag plugin propagation
  path covered by `bevy_pbr/src/lib.rs` and `bevy_pbr/src/material.rs`:
  `PbrPlugin.debug_flags` now flows into `MaterialsPlugin`,
  `PrepassPlugin::new(debug_flags)`, `StandardMaterialPlugin`, and the generic
  `MaterialPlugin<M>` configuration surface. The generic material plugin now
  mirrors Bevy's field even though Bevy does not currently consume it inside
  `MaterialPlugin<M>::build`. Validation: `moon -C mgstudio-engine check
  --target native --package-path pbr`, `moon -C mgstudio-engine test pbr
  --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `examples/shader_advanced/custom_shader_instancing` native screenshot sanity
  gate.
- 2026-06-20: Aligned Bevy's default `StandardMaterial` handle
  initialization. `PbrPlugin::build` initializes the typed material asset
  resource, then inserts handle `0` with a magenta
  `StandardMaterial { base_color: Color::srgb(1.0, 0.0, 0.5), .. }`; mgstudio
  previously only initialized the asset resource with `StandardMaterial()`,
  whose constructor default is white. Added a direct resource test for handle
  `0`. Validation: `moon -C mgstudio-engine check --target native
  --package-path pbr`, `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `examples/shader_advanced/custom_shader_instancing` native screenshot sanity
  gate.
- 2026-06-20: Added the missing Bevy `PbrPlugin::finish` lifecycle hook for
  global cluster settings. `light/cluster::GlobalClusterSettings` now carries
  Bevy's `gpu_clustering: Option<GlobalClusterGpuSettings>` shape, and
  `pbr_make_global_cluster_settings` returns the real light-cluster resource
  instead of the old PBR-local shadow type. `PbrPlugin::finish` now inserts
  this resource into the main world after the render sub-app has a
  `RenderDevice`, deriving storage-buffer support and GPU clustering from
  device limits instead of hard-coded defaults. This matches the
  `bevy_pbr/src/lib.rs` owner boundary and the core
  `bevy_pbr/src/cluster/mod.rs` capability path. Remaining gap: mgstudio still
  lacks Bevy's exact `RenderAdapter::get_downlevel_capabilities()` flags check,
  so the Android/iOS-simulator/downlevel compute-shader branch is not yet
  source-exact. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test light/cluster --release`,
  `moon -C mgstudio-engine check --target native --package-path
  render/renderer`, `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_iss016_pbr_finish_cluster_settings_pbr.png`.
  The screenshot passed `visual_image_sanity.py --image`, and the run log has
  no fatal or validation errors.
- 2026-06-20: Installed the Bevy-shaped `RenderStartup` resource initialization
  slice from `bevy_pbr/src/lib.rs::PbrPlugin::build`: `init_shadow_samplers`,
  `init_global_clusterable_object_meta`, and
  `init_fallback_bindless_resources` now run in the render sub-app startup
  schedule and populate `ShadowSamplers`, `GlobalClusterableObjectMeta`, and
  `FallbackBindlessResources` from the current `RenderDevice`. This closes the
  lifecycle and resource-owner part of the startup acceptance item; later
  cluster/light/material upload systems still need symbol-level parity audit
  before the broader issue can close. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_iss016_render_startup_resources_pbr.png`.
  The screenshot passed `visual_image_sanity.py --image`, and the run log has
  no fatal or validation errors.
- 2026-06-20: Added the adjacent Bevy `PbrPlugin::build` render-resource
  initialization slice: `LightMeta`, `RenderMaterialBindings`, and
  `RenderShadowLodOrigin` now have owner-shaped MoonBit resources and are
  initialized in the render world when `PbrPlugin` configures the render app.
  `RenderShadowLodOrigin` lives under the `bevy_render/src/view/mod.rs` owner,
  `LightMeta` under `bevy_pbr/src/render/light.rs`, and
  `RenderMaterialBindings` under `bevy_pbr/src/material.rs` /
  `material_bind_groups.rs` fragments. Remaining work: the dynamic uniform
  upload, material binding population, extraction systems, render system
  ordering, and observer hooks still need separate source-level audit before
  the broader issue can close. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test render/view --release`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_iss016_render_resource_init_pbr.png`.
  The screenshot passed `visual_image_sanity.py --image`, and the run log has
  no fatal or validation errors.
- 2026-06-20: Ported the Bevy
  `bevy_pbr/src/render/light.rs::extract_ambient_light_resource` slice into
  the render extract schedule installed by `PbrPlugin::build`. The MoonBit
  implementation tracks the source resource changed tick so unchanged
  `GlobalAmbientLight` values do not dirty the render-world resource every
  extract. Validation: `moon -C mgstudio-engine test pbr/render --release`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_ambient_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights`, `extract_ambient_light` component
  extraction, `extract_shadow_filtering_method`, `extract_shadow_lod_origin`,
  and `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/render/light.rs::extract_shadow_filtering_method` slice into
  the render extract schedule installed by `PbrPlugin::build`. The system reads
  `(RenderEntity, ShadowFilteringMethod)` from the source world and inserts the
  copied value on the synchronized render entity, preserving Bevy's
  `try_insert_batch` semantics without removing components for absent source
  rows. Validation: `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_shadow_filter_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights`, `extract_ambient_light` component
  extraction, `extract_shadow_lod_origin`, and
  `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy `AmbientLight` camera override component from
  `bevy_light/src/ambient_light.rs`, including the `AmbientLight -> Camera`
  required component edge, and installed
  `bevy_pbr/src/render/light.rs::extract_ambient_light` in the PBR render
  extract schedule. The extraction copies `(RenderEntity, AmbientLight)` from
  the source world onto synchronized render entities. Validation:
  `moon -C mgstudio-engine test light --release`, `moon -C mgstudio-engine
  test pbr --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_ambient_light_component_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights`, `extract_shadow_lod_origin`, and
  `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy
  `bevy_light/src/lib.rs::get_shadow_lod_origin` helper and installed
  `bevy_pbr/src/render/light.rs::extract_shadow_lod_origin` in the PBR render
  extract schedule. The helper preserves Bevy's selection priority: explicit
  `ShadowLodOrigin`, then a window-backed camera, then any camera with the
  same warning-once behavior when point or spot lights exist. In mgstudio,
  `RenderTarget::Screen` is treated as the primary window-backed target to
  match the engine's local camera target representation. Validation:
  `moon -C mgstudio-engine test light --release`, `moon -C mgstudio-engine
  test pbr --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_shadow_lod_origin_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights` and `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy `bevy_pbr/src/material.rs` material instance
  extraction/sweep slice for the default 3D material path. `extract_mesh_materials`
  now runs in the render extract command phase, `early_sweep_material_instances`
  removes stale `MeshMaterial3d` removals before the late sweep, and
  `late_sweep_material_instances` removes stale `Mesh3d` removals while
  advancing `RenderMaterialInstances::current_change_tick` exactly once per
  extract pass. The previous execute-stage `late_sweep_stale_mesh_materials`
  substitute was removed. Because mgstudio's render sub-app reads events from
  the main/source world, `RenderMaterialInstances` now owns a source-world
  event cursor so the three extraction/sweep systems share one Bevy-shaped
  tick window. Validation: `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_material_instance_extract_sweep.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gap in this issue: `extract_lights`.

## Close Notes
