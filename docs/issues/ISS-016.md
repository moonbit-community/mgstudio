# ISS-016: Align PbrPlugin build flow with Bevy

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, pbr, plugin, render-app
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-21
- External ref: none

## Description
Bevy's `PbrPlugin` installs shader libraries, mesh rendering, materials,
standard material support, SSAO, fog, lightmaps, light probes, GPU preprocess,
volumetric fog, SSR, transmission, decals, contact shadows, medium, atmosphere,
GPU clustering, GLTF integration, deferred lighting, default material state,
embedded PBR resources, render startup systems, extract systems, render systems,
GPU resources, and light view observers.

Mgstudio installs many of these pieces, but the build flow is decomposed
differently and has not been proven source-equivalent in order, ownership, and
resource lifecycle.

## Design
Port `bevy/crates/bevy_pbr/src/lib.rs::PbrPlugin::build` and `finish` as the
source of truth. Keep package splits, but preserve plugin order, render-app
system placement, resource insertion conditions, default material initialization,
light extraction/sync behavior, and observer semantics.

## Acceptance Criteria
- [x] PBR shader library registration matches the Bevy source list, including dummy meshlet visibility resolve behavior.
- [x] Feature plugin installation order matches Bevy where order has semantic impact.
- [x] Default `StandardMaterial` handle initialization follows Bevy behavior.
- [x] Render startup systems for shadow samplers, clusterable object metadata, and fallback bindless resources are installed in the Bevy-shaped phase.
- [x] Light, ambient light, shadow filtering, shadow LOD origin, and material instance extraction systems are installed with Bevy-equivalent ordering.
- [ ] Light view entity observers and sync-component targets are represented with source-equivalent behavior.
- [x] `PbrPlugin::finish` initializes global cluster settings from the render world as Bevy does.

## Relationships
- Depends on: ISS-014
- Parent: ISS-012
- Related: ISS-017, ISS-018, ISS-019
- Discovered from: none

## Notes
- 2026-06-02: Current mgstudio PBR plugin has many feature plugins but still needs exact source-owner audit against Bevy `PbrPlugin`.
- 2026-06-05: While validating the sprite-render wireframe owner move, `examples/2d/2d_shapes --release` exposed a real PBR build-flow drift: `PbrPlugin` was still registering `Mesh3d -> Transform` and `Mesh3d -> Visibility` required components even though current Bevy owns `Mesh3d -> Transform` in `bevy_mesh/src/components.rs` via `#[require(Transform)]`, and mgstudio's `mesh` owner already registers that edge. Removed the duplicate PBR-owned Mesh3d required-component registrations. This keeps the broader PBR plugin issue open because Camera3d/core-pipeline/render required-component ownership still needs a full Bevy source-owner audit.
- 2026-06-05: Moved the Bevy `CameraPlugin`-owned `Camera3d -> ColorGrading` and `Camera3d -> Exposure` required-component registrations into `camera_plugin`, matching `bevy_render/src/camera.rs`. Removed the stale `Camera3d -> Exposure` registration from PBR's render3d bundle helper and added a camera plugin test for the new owner. Remaining local work: `Camera3d -> Camera/Projection` still lives in the PBR helper because mgstudio still owns the `Camera3d` type in the PBR package, and `Core3dPlugin`-owned camera defaults still need a separate source-owner migration.
- 2026-06-20: Added the missing Bevy `PbrPlugin.debug_flags` configuration
  path and propagated it through `PbrPluginRuntimeConfig` into
  `MeshRenderPlugin` / `MeshRenderPluginState`, matching the first-level
  `PbrPlugin -> MeshRenderPlugin { debug_flags: self.debug_flags }` source
  flow in `bevy_pbr/src/lib.rs` and `bevy_pbr/src/render/mesh.rs`. This does
  not close the full issue: `MaterialsPlugin`, `MaterialPlugin`, prepass, and
  render-phase plugins still need their own debug flag ownership audited and
  ported. Targeted validation: `moon -C mgstudio-engine check --target native
  --package-path pbr`, `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and a successful
  `examples/shader_advanced/custom_shader_instancing` native screenshot sanity
  gate. `examples/3d/motion_blur` still fails on the already tracked
  motion-vector/prepass ownership gap; the new evidence is recorded in
  `ISS-034`.
- 2026-06-20: Completed the rest of the Bevy debug-flag plugin propagation
  path covered by `bevy_pbr/src/lib.rs` and `bevy_pbr/src/material.rs`:
  `PbrPlugin.debug_flags` now flows into `MaterialsPlugin`,
  `PrepassPlugin::new(debug_flags)`, `StandardMaterialPlugin`, and the generic
  `MaterialPlugin<M>` configuration surface. The generic material plugin now
  mirrors Bevy's field even though Bevy does not currently consume it inside
  `MaterialPlugin<M>::build`. Validation: `moon -C mgstudio-engine check
  --target native --package-path pbr`, `moon -C mgstudio-engine test pbr
  --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `examples/shader_advanced/custom_shader_instancing` native screenshot sanity
  gate.
- 2026-06-20: Aligned Bevy's default `StandardMaterial` handle
  initialization. `PbrPlugin::build` initializes the typed material asset
  resource, then inserts handle `0` with a magenta
  `StandardMaterial { base_color: Color::srgb(1.0, 0.0, 0.5), .. }`; mgstudio
  previously only initialized the asset resource with `StandardMaterial()`,
  whose constructor default is white. Added a direct resource test for handle
  `0`. Validation: `moon -C mgstudio-engine check --target native
  --package-path pbr`, `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `examples/shader_advanced/custom_shader_instancing` native screenshot sanity
  gate.
- 2026-06-20: Added the missing Bevy `PbrPlugin::finish` lifecycle hook for
  global cluster settings. `light/cluster::GlobalClusterSettings` now carries
  Bevy's `gpu_clustering: Option<GlobalClusterGpuSettings>` shape, and
  `pbr_make_global_cluster_settings` returns the real light-cluster resource
  instead of the old PBR-local shadow type. `PbrPlugin::finish` now inserts
  this resource into the main world after the render sub-app has a
  `RenderDevice`, deriving storage-buffer support and GPU clustering from
  device limits instead of hard-coded defaults. This matches the
  `bevy_pbr/src/lib.rs` owner boundary and the core
  `bevy_pbr/src/cluster/mod.rs` capability path. Remaining gap: mgstudio still
  lacks Bevy's exact `RenderAdapter::get_downlevel_capabilities()` flags check,
  so the Android/iOS-simulator/downlevel compute-shader branch is not yet
  source-exact. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test light/cluster --release`,
  `moon -C mgstudio-engine check --target native --package-path
  render/renderer`, `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_iss016_pbr_finish_cluster_settings_pbr.png`.
  The screenshot passed `visual_image_sanity.py --image`, and the run log has
  no fatal or validation errors.
- 2026-06-20: Installed the Bevy-shaped `RenderStartup` resource initialization
  slice from `bevy_pbr/src/lib.rs::PbrPlugin::build`: `init_shadow_samplers`,
  `init_global_clusterable_object_meta`, and
  `init_fallback_bindless_resources` now run in the render sub-app startup
  schedule and populate `ShadowSamplers`, `GlobalClusterableObjectMeta`, and
  `FallbackBindlessResources` from the current `RenderDevice`. This closes the
  lifecycle and resource-owner part of the startup acceptance item; later
  cluster/light/material upload systems still need symbol-level parity audit
  before the broader issue can close. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_iss016_render_startup_resources_pbr.png`.
  The screenshot passed `visual_image_sanity.py --image`, and the run log has
  no fatal or validation errors.
- 2026-06-20: Added the adjacent Bevy `PbrPlugin::build` render-resource
  initialization slice: `LightMeta`, `RenderMaterialBindings`, and
  `RenderShadowLodOrigin` now have owner-shaped MoonBit resources and are
  initialized in the render world when `PbrPlugin` configures the render app.
  `RenderShadowLodOrigin` lives under the `bevy_render/src/view/mod.rs` owner,
  `LightMeta` under `bevy_pbr/src/render/light.rs`, and
  `RenderMaterialBindings` under `bevy_pbr/src/material.rs` /
  `material_bind_groups.rs` fragments. Remaining work: the dynamic uniform
  upload, material binding population, extraction systems, render system
  ordering, and observer hooks still need separate source-level audit before
  the broader issue can close. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test render/view --release`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_iss016_render_resource_init_pbr.png`.
  The screenshot passed `visual_image_sanity.py --image`, and the run log has
  no fatal or validation errors.
- 2026-06-20: Ported the Bevy
  `bevy_pbr/src/render/light.rs::extract_ambient_light_resource` slice into
  the render extract schedule installed by `PbrPlugin::build`. The MoonBit
  implementation tracks the source resource changed tick so unchanged
  `GlobalAmbientLight` values do not dirty the render-world resource every
  extract. Validation: `moon -C mgstudio-engine test pbr/render --release`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_ambient_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights`, `extract_ambient_light` component
  extraction, `extract_shadow_filtering_method`, `extract_shadow_lod_origin`,
  and `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/render/light.rs::extract_shadow_filtering_method` slice into
  the render extract schedule installed by `PbrPlugin::build`. The system reads
  `(RenderEntity, ShadowFilteringMethod)` from the source world and inserts the
  copied value on the synchronized render entity, preserving Bevy's
  `try_insert_batch` semantics without removing components for absent source
  rows. Validation: `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_shadow_filter_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights`, `extract_ambient_light` component
  extraction, `extract_shadow_lod_origin`, and
  `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy `AmbientLight` camera override component from
  `bevy_light/src/ambient_light.rs`, including the `AmbientLight -> Camera`
  required component edge, and installed
  `bevy_pbr/src/render/light.rs::extract_ambient_light` in the PBR render
  extract schedule. The extraction copies `(RenderEntity, AmbientLight)` from
  the source world onto synchronized render entities. Validation:
  `moon -C mgstudio-engine test light --release`, `moon -C mgstudio-engine
  test pbr --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_ambient_light_component_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights`, `extract_shadow_lod_origin`, and
  `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy
  `bevy_light/src/lib.rs::get_shadow_lod_origin` helper and installed
  `bevy_pbr/src/render/light.rs::extract_shadow_lod_origin` in the PBR render
  extract schedule. The helper preserves Bevy's selection priority: explicit
  `ShadowLodOrigin`, then a window-backed camera, then any camera with the
  same warning-once behavior when point or spot lights exist. In mgstudio,
  `RenderTarget::Screen` is treated as the primary window-backed target to
  match the engine's local camera target representation. Validation:
  `moon -C mgstudio-engine test light --release`, `moon -C mgstudio-engine
  test pbr --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_shadow_lod_origin_extract.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gaps in this issue: `extract_lights` and `late_sweep_material_instances`.
- 2026-06-21: Ported the Bevy `bevy_pbr/src/material.rs` material instance
  extraction/sweep slice for the default 3D material path. `extract_mesh_materials`
  now runs in the render extract command phase, `early_sweep_material_instances`
  removes stale `MeshMaterial3d` removals before the late sweep, and
  `late_sweep_material_instances` removes stale `Mesh3d` removals while
  advancing `RenderMaterialInstances::current_change_tick` exactly once per
  extract pass. The previous execute-stage `late_sweep_stale_mesh_materials`
  substitute was removed. Because mgstudio's render sub-app reads events from
  the main/source world, `RenderMaterialInstances` now owns a source-world
  event cursor so the three extraction/sweep systems share one Bevy-shaped
  tick window. Validation: `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_material_instance_extract_sweep.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining extraction
  gap in this issue: `extract_lights`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/render/light.rs::extract_lights` path into the PBR render
  extract schedule. `ExtractedPointLight3d`, `ExtractedDirectionalLight3d`,
  and `ExtractedRectLight3d` are now render-world components populated by
  `extract_lights`, and the lighting collection path reads those extracted
  components instead of rescanning authoring light components during render
  collection. `PbrPlugin` now installs Bevy-shaped `SyncComponentPlugin`
  registrations for `DirectionalLight`, `PointLight`, `SpotLight`, and
  `RectLight`; light owner required-component metadata supplies the Bevy
  `Transform`/`Visibility`/`ViewVisibility` edges. Existing tests were updated
  to treat cluster assignment as separate from light extraction, matching
  Bevy's source boundary. Validation: `moon -C mgstudio-engine test pbr/render
  --release`, `moon -C mgstudio-engine test pbr --release`, full `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_extract_lights.png`.
  The screenshot passed `visual_image_sanity.py --image`. The extraction
  systems listed in this issue are now represented; remaining work in ISS-016
  is broader shader-library, plugin-order, resource lifecycle, and observer
  parity.
- 2026-06-21: Completed the Bevy `PbrPlugin::build` sync-component target
  slice for `ShadowFilteringMethod` and camera-local `AmbientLight`. Mgstudio
  already registered directional, point, spot, and rect light sync targets;
  this commit adds the two missing targets from Bevy's source list so
  entities carrying either component receive `SyncToRenderWorld` through the
  PBR-owned required-component edge instead of relying on tests or examples to
  insert it manually. Validation: `moon -C mgstudio-engine test pbr --release`,
  full `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_sync_targets.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining work in
  the sync/observer acceptance item: light-view entity observer behavior still
  needs source-level audit and porting.
- 2026-06-21: Ported the next Bevy sync target cleanup slice. Mgstudio's
  `SyncComponentPlugin` now has a target-removal extension point so PBR can
  model Bevy's `SyncComponent<PbrPlugin>::Target` cleanup instead of removing
  only the source component from the render world. Directional, point, spot,
  and rect light sync registrations now remove their Bevy target bundles,
  including extracted light components, render shadow-map visibility tables,
  and light-view entity table components. `render/view/visibility` now owns
  the missing `RenderShadowMapVisibleEntities`,
  `RenderExtractedVisibleEntities`, and
  `RenderExtractedShadowMapVisibleEntities` component shapes needed by those
  targets. Validation so far: `moon -C mgstudio-engine test pbr --release`,
  `moon -C mgstudio-engine test pbr/render --release`, `moon -C
  mgstudio-engine test render/view/visibility --release`, and `moon -C
  mgstudio-engine test render --release`, full `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_sync_target_cleanup.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining work in
  the sync/observer acceptance item: the light-view entity add/remove observer
  routines themselves still need source-level porting once the shadow view
  spawning path is represented.
- 2026-06-21: Ported the Bevy `PbrPlugin::build` dummy meshlet shader
  registration slice. Bevy always loads
  `meshlet/dummy_visibility_buffer_resolve.wgsl` from `PbrPlugin` so normal
  PBR shaders can import `bevy_pbr::meshlet_visibility_buffer_resolve` even
  when `MeshletPlugin` is not installed. Mgstudio previously registered this
  dummy shader only through the meshlet plugin's full embedded WGSL registry.
  `PbrPlugin` now registers just the dummy source from the existing
  Bevy-copied WGSL asset, without importing the meshlet package or loading the
  full meshlet shader set. Validation so far: `moon -C mgstudio-engine check
  --target native --package-path pbr` and `moon -C mgstudio-engine test pbr
  --release`, full `moon -C mgstudio-engine check --target native`, `moon -C
  mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_dummy_meshlet_shader.png`.
  The screenshot passed `visual_image_sanity.py --image`. Remaining
  shader-library work: audit the rest of the Bevy PBR shader source list
  against mgstudio's plugin-owned registrations.
- 2026-06-21: Ported the adjacent unconditional deferred shader-library
  registration slice from Bevy `PbrPlugin::build`. Bevy loads
  `deferred/pbr_deferred_types.wgsl` and
  `deferred/pbr_deferred_functions.wgsl` before checking
  `add_default_deferred_lighting_plugin`, so these imports exist even when the
  default deferred lighting plugin is disabled. Mgstudio now registers the
  deferred embedded WGSL sources from `PbrPlugin` itself instead of relying on
  `DeferredPbrLightingPlugin::build`. Validation so far: `moon -C
  mgstudio-engine check --target native --package-path pbr` and `moon -C
  mgstudio-engine test pbr --release`, full `moon -C mgstudio-engine check
  --target native`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_deferred_shader_registration.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Completed the shader-library acceptance item with an
  owner-level regression test that enumerates every Bevy
  `PbrPlugin::build` `load_shader_library!` path, including deferred libraries
  and the dummy meshlet visibility resolve shader, while
  `add_default_deferred_lighting_plugin` is disabled. Mgstudio's PBR plugin
  already covered the remaining render shader paths through
  `pbr/render.register_embedded_wgsl_sources`; the new test prevents future
  drift from being inferred only from visually plausible examples. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test pbr --release`, full `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_shader_library_list.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Started the Bevy feature-plugin ordering slice. Mgstudio now
  installs the already-ported feature plugins in the same adjacent order as
  Bevy for `ScreenSpaceAmbientOcclusionPlugin`, `FogPlugin`, `LightmapPlugin`,
  `LightProbePlugin`, `GpuMeshPreprocessPlugin`,
  `VolumetricFogPlugin`, `ScreenSpaceReflectionsPlugin`,
  `ClusteredDecalPlugin`, `ContactShadowsPlugin`,
  `ForwardDecalPlugin`, `ScatteringMediumPlugin`, and `AtmospherePlugin`.
  This also fixes a concrete data-flow bug: `PbrPlugin.use_gpu_instance_buffer_builder`
  is now forwarded into `GpuMeshPreprocessPlugin` instead of being dropped by
  the default constructor. The feature-plugin acceptance item is still open:
  `ScreenSpaceTransmissionPlugin` and full `GpuClusteringPlugin` source owners
  are not yet represented as Bevy-shaped plugins in mgstudio.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  pbr`, `moon -C mgstudio-engine test pbr --release`, full `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_feature_plugin_order.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/transmission/mod.rs::ScreenSpaceTransmissionPlugin` build
  slice that can be represented with current mgstudio render infrastructure.
  The new PBR-owned plugin registers `transmission.wgsl`, installs
  `ExtractComponentPlugin<ScreenSpaceTransmission>`, and owns the
  `Camera3d -> ScreenSpaceTransmission` required-component edge instead of
  leaving that edge in the generic `register_render3d_required_components`
  helper. `PbrPlugin` now installs this plugin between SSR and clustered
  decals, matching Bevy's adjacent plugin order. The remaining
  source-level gap is the render-phase portion of Bevy's plugin:
  `DrawFunctions<Transmissive3d>`, `ViewSortedRenderPhases<Transmissive3d>`,
  `DrawMaterial`, `extract_transmissive_camera_phases`,
  `prepare_core_3d_transmission_textures`, and `main_transmissive_pass_3d`
  still depend on the broader render-phase owner work tracked by ISS-019.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  pbr`, `moon -C mgstudio-engine test pbr --release`, full `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_screen_space_transmission_plugin.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/cluster/gpu.rs::GpuClusteringPlugin::build` slice. The PBR
  root package now owns the compiled `GpuClusteringPlugin`, registers
  `cluster.wgsl`, embeds `cluster_z_slice.wgsl`, `cluster_raster.wgsl`, and
  `cluster_allocate.wgsl`, installs
  `ExtractResourcePlugin<GlobalClusterSettings, GpuClusteringPlugin>`, and
  installs the plugin after `AtmospherePlugin` to match Bevy's adjacent plugin
  order. The old `pbr/cluster/gpu.mbt` file was deleted because that directory
  had no MoonBit package and the file was only an uncompiled source-shape
  anchor. Remaining GPU clustering work: Bevy
  `GpuClusteringPlugin::finish` render resources, pipelines, node, and
  systems still require a separate source-owner port. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_gpu_clustering_plugin.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the first Bevy
  `bevy_pbr/src/cluster/gpu.rs::GpuClusteringPlugin::finish` render-resource
  slice. `GpuClusteringPlugin::finish` now enters the render sub-app, checks
  `RenderDevice.limits().max_storage_buffers_per_shader_stage` before
  installing GPU clustering state, and initializes the Bevy-owned resources:
  specialized raster/Z-slicing/allocation pipeline caches,
  `RenderViewClusteringReadbackData`, `GpuClusteringMeshBuffers`,
  `ClusteringRasterPipeline`, `ClusteringZSlicingPipeline`, and
  `ClusteringAllocationPipeline`. The pipeline types now expose
  Bevy-shaped bind group layout descriptors and specialization descriptors,
  with concrete resource types standing in for Bevy's generic
  `SpecializedRenderPipelines<T>` / `SpecializedComputePipelines<T>` resource
  instances. Remaining GPU clustering work: `prepare_clustering_pipelines`,
  dummy textures, per-view GPU buffers, bind groups, and the `cluster_on_gpu`
  render command path still need source-level porting. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_gpu_clustering_finish_resources.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/cluster/gpu.rs::prepare_clustering_pipelines` slice. The
  system now runs in the render `Prepare` set, queries render-world
  `ExtractedView` components when the Bevy-shaped
  `gpu_clustering_is_enabled` run condition sees
  `GlobalClusterSettings.gpu_clustering.is_some()`, queues the five Bevy GPU
  clustering pipeline specializations in source order, and writes
  `ViewGpuClusteringPipelineIds` onto each view entity. This required adding a
  `bevy_render/src/view/mod.rs::ExtractedView` component anchor in
  `render/view`; that anchor is still partial because mgstudio's current
  render/view owner cannot yet express Bevy's full matrix and color-grading
  fields without creating a reverse dependency into PBR/post-process. Do not
  count the full `ExtractedView` extraction chain as complete until the camera
  and shadow view extraction owners populate the full Bevy-shaped component.
  Remaining GPU clustering work: dummy textures, per-view GPU buffers, bind
  groups, and the `cluster_on_gpu` render command path still need source-level
  porting. Validation: `moon -C mgstudio-engine check --target native
  --package-path render/view`, `moon -C mgstudio-engine check --target native
  --package-path pbr`, `moon -C mgstudio-engine test render/view --release`,
  `moon -C mgstudio-engine test pbr --release`, full `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_gpu_clustering_prepare_pipelines.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the Bevy texture-cache owner needed by the GPU
  clustering dummy-texture path. `render/texture::TextureCache` now follows
  `bevy_render/src/texture/texture_cache.rs`: per-frame `get` calls allocate
  fresh textures while `update` reclaims untaken entries across frames, and the
  render app runs cache cleanup in the render cleanup set as Bevy's
  `TexturePlugin` does. Also ported the Bevy
  `bevy_pbr/src/cluster/mod.rs::ExtractedClusterConfig` anchor and
  `bevy_pbr/src/cluster/gpu.rs::prepare_cluster_dummy_textures` slice. The
  GPU clustering render app now prepares one `ViewClusteringDummyTexture` per
  extracted view with clustering config, using Bevy's 32-pixel alignment and
  `R8Unorm` render-attachment/copy-dst descriptor shape. Remaining GPU
  clustering work: `prepare_clusters_for_gpu_clustering`,
  `upload_view_gpu_clustering_buffers`, `prepare_clustering_bind_groups`, and
  the `cluster_on_gpu` render command path still need source-level porting.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  render/texture`, `moon -C mgstudio-engine check --target native
  --package-path render/render_resource`, `moon -C mgstudio-engine check
  --target native --package-path light/cluster`, `moon -C mgstudio-engine
  check --target native --package-path pbr`, `moon -C mgstudio-engine test
  render/texture --release`, `moon -C mgstudio-engine test light/cluster
  --release`, `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_gpu_clustering_dummy_textures.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/cluster/gpu.rs::prepare_clusters_for_gpu_clustering` data
  preparation slice. `GpuClusteredLights` now carries Bevy-shaped clustered
  light data/count state instead of only a binding-type enum, and
  `ViewClusterBindings`, `ViewGpuClusteringBuffers`, `ClusterMetadata`,
  `ClusterRasterIndirectDrawParams`, and `ClusterableObjectZSlice` now provide
  the per-view storage-buffer data surface needed by the later bind-group and
  dispatch systems. `pbr/light_probe` now exposes concrete MoonBit components
  for the two Bevy generic instances,
  `RenderViewLightProbes<EnvironmentMapLight>` and
  `RenderViewLightProbes<IrradianceVolume>`, so GPU clustering can count
  reflection probes and irradiance volumes separately. The render `Prepare` set
  now installs `prepare_clusters_for_gpu_clustering`, which creates per-view
  cluster bindings, per-view GPU clustering buffers, cluster metadata, z-slice
  capacity, scratchpad capacity, and stale readback-data retention following
  Bevy's render-world ownership. Current limitation: the wider light-probe
  owner still needs to populate those two concrete components in the
  Bevy-shaped gather/upload schedule; that remains covered by the light-probe
  owner audit in `ISS-105` rather than closing this GPU clustering chain.
  Remaining GPU clustering work: `upload_view_gpu_clustering_buffers`,
  `prepare_clustering_bind_groups`, and the `cluster_on_gpu` render command
  path still need source-level porting. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine check --target native --package-path pbr/decal`,
  `moon -C mgstudio-engine test pbr --release`, `moon -C mgstudio-engine test
  pbr/decal --release`, `moon -C mgstudio-engine test light/cluster
  --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_gpu_clustering_prepare_buffers.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the Bevy
  `bevy_pbr/src/cluster/gpu.rs::upload_view_gpu_clustering_buffers` buffer
  materialization slice. `ViewClusterBindings::write_buffers` now uploads the
  per-view clusterable-object index list and cluster offset/count storage
  buffers during `prepare_clusters_for_gpu_clustering`, matching Bevy's source
  placement before component insertion. `ViewGpuClusteringBuffers` now owns
  real wgpu storage buffers and capacities for z-slices, cluster metadata, and
  scratchpad offsets/counts; the upload system preserves Bevy's uninitialized
  z-slice and scratchpad allocation behavior, ensures an empty scratchpad
  receives one default element before allocation, and gives cluster metadata
  Bevy's extra `COPY_SRC | INDIRECT` usages. `ClusterMetadata` was reordered
  to match the Bevy/WGSL `repr(C)` layout. Remaining GPU clustering work:
  `prepare_clustering_bind_groups` and the `cluster_on_gpu` render command
  path still need source-level porting. Validation:
  `moon -C mgstudio-engine check --target native --package-path pbr`,
  `moon -C mgstudio-engine test pbr --release`, full
  `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_gpu_clustering_upload_buffers.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the upstream GPU resource binding surface that
  `bevy_pbr/src/cluster/gpu.rs::prepare_clustering_bind_groups` requires
  before the bind-group system can be represented faithfully. `LightProbesBuffer`
  now mirrors Bevy's `DynamicUniformBuffer<LightProbesUniform>` binding
  lifecycle by uploading its gathered uniform bytes to a real uniform buffer
  and returning no binding before data is written. `DecalsBuffer` now mirrors
  Bevy's `RawBufferVec<RenderClusteredDecal>` storage-buffer surface with a
  real storage buffer created from the existing clustered-decal GPU packing.
  `LightMeta.view_gpu_lights` now exposes the Bevy-shaped view-lights dynamic
  uniform upload and binding surface using the existing `GpuLights3d`
  serializer. Remaining GPU clustering work: `ViewUniforms` under
  `bevy_render/src/view/mod.rs`, `prepare_clustering_bind_groups`, and the
  `cluster_on_gpu` render command path still need source-level porting.
  Validation: `moon -C mgstudio-engine check --target native --package-path
  pbr/light_probe`, `moon -C mgstudio-engine check --target native
  --package-path pbr/decal`, `moon -C mgstudio-engine check --target native
  --package-path pbr/render`, `moon -C mgstudio-engine test pbr/light_probe
  --release`, `moon -C mgstudio-engine test pbr/decal --release`,
  `moon -C mgstudio-engine test pbr/render --release`, full `moon -C
  mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_gpu_resource_bindings.png`.
  The screenshot passed `visual_image_sanity.py --image`.
- 2026-06-21: Ported the Bevy `bevy_render/src/view/mod.rs::ViewUniform`
  resource surface required by GPU clustering bind groups. `render/view` now
  owns `ColorGradingUniform`, `ViewUniform`, `ViewUniforms`, and
  `ViewUniformOffset`, including Bevy/WGSL layout-sized byte packing, dynamic
  uniform offsets, and real wgpu buffer upload/binding behavior. Mgstudio still
  lacks a shared `math::Mat4`, so `ViewUniform` currently uses local
  `ViewUniformMat4` / `ViewUniformMat3` layout carriers rather than depending
  from render back into PBR's `ProjectionMatrix3d`; this keeps the source owner
  direction correct while preserving the GPU memory layout. Remaining GPU
  clustering work: `prepare_clustering_bind_groups` and the `cluster_on_gpu`
  render command path still need source-level porting. Validation: `moon -C
  mgstudio-engine check --target native --package-path render/view`, `moon -C
  mgstudio-engine test render/view --release`, `moon -C mgstudio-engine test
  pbr --release`, full `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/pbr /tmp/mgstudio_pbr_view_uniforms.png`.
  The screenshot passed `visual_image_sanity.py --image`.

## Close Notes
