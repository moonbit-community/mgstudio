# ISS-126: Restore RectLight render owner and rect_light example parity

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: pbr, lighting, rect-light, area-light, examples, render, source-parity, visual-parity
- Assignee: unassigned
- Created: 2026-06-16
- Updated: 2026-06-16
- External ref: none

## Description
`mgstudio-engine/examples/3d/rect_light/main.mbt` now ports the Bevy example
source shape, including `FloorMaterial`, `RoughnessDisplay`, the floor/sphere
scene, two `RectLight` entities with `ShowLightGizmo`, free camera, UI text,
roughness controls, and gizmo toggling.

The underlying runtime owner still needs a final LUT-resource audit, but the
current PBR render path has Bevy-shaped `ExtractedRectLight3d`,
`GpuRectLight3d`, `MAX_RECT_LIGHTS`, `SyncComponent<PbrPlugin> for RectLight`,
and CPU upload into `GpuLights3d.rect_lights`.

ISS-017 remains responsible for exact Bevy LTC LUT asset/resource
initialization. This issue covers RectLight's component sync, extraction, GPU
light upload, and the `rect_light` example source shape.

## Design
Port the Bevy source owners directly:

- Restore `bevy_light/src/rect_light.rs` semantics, including required
  transform/visibility/class ownership or a documented ECS limitation issue if
  required components cannot be expressed yet.
- Port the `PbrPlugin` RectLight sync owner from `bevy_pbr/src/lib.rs`.
- Add Bevy-shaped `ExtractedRectLight`, `GpuRectLight`, and `MAX_RECT_LIGHTS`
  CPU-side owners.
- Extract rect lights from `RectLight`, `GlobalTransform`, and
  `ViewVisibility`, using Bevy's effective width/height and
  `intensity / (effective_width * effective_height * PI)` calculation.
- Populate `gpu_lights.n_rect_lights` and `gpu_lights.rect_lights[index]`
  during view light preparation.
- Preserve Bevy's warning path when area light LUTs are unavailable, while
  keeping ISS-017 responsible for the actual LUT resource initialization.
- Implement `examples/3d/rect_light` from Bevy source: plugin setup,
  `FloorMaterial`, `RoughnessDisplay`, floor/sphere scene, two `RectLight`
  entities with `ShowLightGizmo`, free camera, UI text, `adjust_roughness`, and
  `toggle_gizmos`.

Do not approximate rect lights with point/spot lights, emissive meshes, or
hard-coded shader constants.

## Acceptance Criteria
- [x] RectLight extraction creates an `ExtractedRectLight` equivalent from
      source-world `RectLight`, `GlobalTransform`, and `ViewVisibility`.
- [x] CPU-side `GpuRectLight` packing and `MAX_RECT_LIGHTS = 8` match Bevy.
- [x] View light preparation writes `n_rect_lights` and rect-light data consumed
      by `pbr_lighting.wgsl`.
- [ ] Area-light LUT resource use is wired to the Bevy-shaped binding path or
      explicitly blocked by ISS-017.
- [x] `examples/3d/rect_light` contains the Bevy scene, systems, controls, UI,
      free camera, and gizmo toggle behavior.
- [x] A new reference screenshot is captured after the example first renders
      real RectLight illumination.

## Relationships
- Depends on: ISS-016, ISS-017, ISS-019, ISS-023
- Parent: ISS-105
- Related: ISS-012, ISS-021, ISS-064
- Discovered from: ISS-105

## Notes
- 2026-06-26: Ported `examples/3d/rect_light` from
  `bevy/examples/3d/rect_light.rs` and captured
  `mgstudio-engine/examples/3d/rect_light/reference.png`. Verified with
  `moon -C mgstudio-engine check --target native --package-path
  examples/3d/rect_light --deny-warn`, `moon -C mgstudio-engine test --release
  examples/3d/rect_light --deny-warn`, `moon -C mgstudio-engine test
  pbr/render --release --deny-warn`, `moon -C mgstudio-engine fmt`, `moon -C
  mgstudio-engine info`, `moon -C mgstudio-engine check --target native`, and
  the standard 90-frame native screenshot gate. The remaining open subtarget is
  the area-light LTC LUT resource binding audit tracked through ISS-017's owner
  surface.
