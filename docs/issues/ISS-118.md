# ISS-118: Restore specular_tint source parity

## Metadata
- Type: bug
- Status: closed
- Priority: 1
- Labels: examples, screenshots, visual-parity, pbr, specular-tint, material, source-parity
- Assignee: unassigned
- Created: 2026-06-16
- Updated: 2026-06-19
- External ref: none

## Description
`examples/3d/specular_tint` exercises the specular tint render feature, but it
is not a symbol-level port of Bevy's `bevy/examples/3d/specular_tint.rs`.

The current implementation keeps the visible intent: a rotating reflective
sphere, solid specular hue cycling, a map toggle using `textures/AlphaNoise.png`,
skybox/environment lighting, and help text. The strict gaps are source-shape
issues: Bevy's `AppStatus` resource, `AppAssets::from_world`, material asset
queries, camera queries, UI text query, app-level ambient/window setup, source
sphere primitive, and color conversion anchors are replaced by a global
`SpecularTintState`, tracked handles, direct world mutation, local HSV math, and
a local UV-sphere generator.

The local sphere uses radius `1.0`, while Bevy's `Sphere::default()` radius is
`0.5`, so this is also a concrete visual/source mismatch.

## Design
Port the example directly from the Bevy source owner. Keep only local changes
that are required by concrete MoonBit language/API limitations, and record each
such limitation as a precise blocker. Do not keep global state, tracked handles,
direct world mutation, local primitive generation, or local color conversion as
the parity path when the Bevy source has a specific owner.

## Acceptance Criteria
- [x] `AppStatus` is represented as a Bevy-shaped resource with `tint_type`
      and `hue`.
- [x] `AppAssets` is represented as a Bevy-shaped resource loaded from world
      state, or a precise `FromWorld` capability blocker is recorded.
- [x] `TintType` preserves Bevy's default and toggle order.
- [x] `main` preserves Bevy's window setup, resource initialization, ambient
      insertion, startup system, update systems, chain ordering, and
      `shift_hue.after(toggle_specular_map)` ordering where the ECS API can
      express them.
- [x] User-visible strings use mgstudio branding while preserving Bevy source
      meaning.
- [x] `setup` exposes Bevy-shaped commands, asset server, app-status resource,
      mesh assets, and material assets where possible.
- [x] The sphere mesh preserves `Sphere::default().mesh().uv(32, 18)` and
      therefore Bevy's default radius, or records a precise primitive-builder
      blocker.
- [x] Solid tint creation uses the engine color API equivalent of
      `Color::hsva(app_status.hue, 1.0, 1.0, 1.0)` rather than local ad hoc HSV
      conversion.
- [x] `rotate_camera` uses a Bevy-shaped camera query rather than a tracked
      camera entity.
- [x] `shift_hue` uses Bevy-shaped mutable app status, material query, and
      mutable `Assets<StandardMaterial>` access.
- [x] `toggle_specular_map` uses Bevy-shaped keyboard, app-status, app-assets,
      material query, and material asset access.
- [x] Help text uses UI `Text` plus `Node`, and `update_text` mutates text
      through a Bevy-shaped query.
- [x] `SpecularTintState`, tracked camera/material/noise/help handles,
      `wrap_hue_deg`, `tint_from_hue_deg`, and local `uv_sphere_mesh` are
      removed from the source-parity path unless precise blockers are
      documented.
- [x] The reference screenshot is refreshed after the source-shaped port still
      renders correctly.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-019, ISS-021, ISS-105
- Discovered from: ISS-105

## Notes
- 2026-06-16: Created from the ISS-105 symbol audit. This is local
  source-parity work; the current global-state/direct-world implementation and
  radius mismatch are not external blockers.
- 2026-06-19: Started direct source-owner port against
  `bevy/examples/3d/specular_tint.rs`.

## Close Notes
- 2026-06-19: Restored the example around Bevy's
  `examples/3d/specular_tint.rs` owner shape: `AppStatus`, `AppAssets`,
  `TintType`, app-level ambient/window setup, Bevy-shaped startup/update
  systems, query-driven camera rotation, material update systems, UI
  `Text`/`Node`, and `Sphere::default().mesh().uv(32, 18)` via mgstudio's
  sphere builder.
- 2026-06-19: Removed the previous global `SpecularTintState`, tracked camera,
  material, noise, and help-text handles, direct world mutation path, ad hoc HSV
  conversion, local UV-sphere generator, UI root/2D camera overlay path, and
  Bevy-branded window title.
- 2026-06-19: Verified with
  `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/specular_tint /tmp/mgstudio_iss118_specular_tint_source_parity.png`.
  Refreshed `mgstudio-engine/examples/3d/specular_tint/reference.png`.
