# ISS-155: Port Bevy GPU-building RenderMeshInstances owner

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: performance, many_foxes, mesh, pbr, render, source-parity
- Assignee: unassigned
- Created: 2026-06-24
- Updated: 2026-06-27
- External ref: none

## Description
`ISS-153` classified the current mesh preparation source anchors. The remaining
`many_foxes` `prepare_meshes` cost is not repeated mesh upload:
`/tmp/mgstudio_many_foxes_iss152_direct_finish_aabb3d/frametime_breakdown.md`
reports `mgstudio.render.prepare_meshes=3.034 ms/frame` with
`prepared_meshes=1001`, `buffer_uploads=1`, `skipped_unchanged_meshes=1000`,
`mesh_upload_cache_hits=1000`, and `preprocess_static_cache_hits=1000`.

The true source gap is that mgstudio still builds render mesh queue data in the
monolithic `mgstudio-engine/pbr/render_execute_mesh_collect.mbt` path and then
stores a single `Map`-backed `RenderMeshInstances` resource. Bevy's current
owner in `bevy_pbr/src/render/mesh.rs` splits this into CPU/GPU building modes.
The GPU path uses changed queries, per-worker queues, `MeshesToReextractNextFrame`,
`RenderMeshInstanceGpuBuilder::prepare`, and
`collect_meshes_for_gpu_building` update/fast-path logic.

## Design
Port Bevy's owner shape directly:

- Replace the single-map `RenderMeshInstances` facade with a source-shaped
  `CpuBuilding` / `GpuBuilding` representation.
- Port `extract_meshes_for_gpu_building` changed-query semantics as closely as
  MoonBit ECS allows, recording a smaller blocker if query filters or removed
  component readers cannot express the source shape.
- Port `RenderMeshInstanceGpuQueue`, `RenderMeshInstanceGpuBuilder`,
  `RenderMeshInstanceGpuPrepared`, and `MeshesToReextractNextFrame`.
- Move mesh instance preparation out of the monolithic
  `render3d_extract_mesh_for_entity` / `render3d_execute_collect_meshes` path.
- Keep `RenderMeshAssets` and `MeshAllocator` as authoritative render-world
  resources instead of using host upload cache state as a substitute.

Do not add dirty-skip shortcuts that alter Bevy-visible mesh extraction
semantics.

## Acceptance Criteria
- [x] `RenderMeshInstances` has Bevy-shaped CPU and GPU building variants.
- [x] GPU mesh instance extraction records changed, removed, and reextract-next
      frame entities through source-shaped resources or documented blockers.
- [x] Mesh instance preparation consumes `MeshAllocator`, material bindings,
      lightmaps, skin uniforms, morph indices, and frame count in the Bevy
      owner path.
- [ ] The monolithic PBR collect path no longer owns mesh upload/cache and mesh
      instance preparation together.
- [ ] `many_foxes` reports `mgstudio.render.prepare_meshes < 1 ms/frame` at
      `1000` foxes on the current development machine, or a more specific ECS
      blocker is recorded with exact evidence.
- [x] Relevant `mesh`, `pbr/render`, and `pbr` release tests pass.

## Relationships
- Depends on: none
- Parent: ISS-001
- Related: ISS-010, ISS-012, ISS-064, ISS-153
- Discovered from: ISS-153

## Notes
- 2026-06-24: Created from the `ISS-153` source-anchor audit. This is local
  source-owner work, not an external dependency blocker.
- 2026-06-24: Replaced the single-map `RenderMeshInstances` facade with
  Bevy-shaped `CpuBuilding(RenderMeshInstancesCpu)` and
  `GpuBuilding(RenderMeshInstancesGpu)` variants in
  `mgstudio-engine/pbr/render_mesh_instances.mbt`. Existing CPU-building
  callers still use the same queue-data API through variant dispatch, and a new
  white-box test covers the explicit GPU-building resource shape. Remaining
  work: port changed extraction queues, `MeshesToReextractNextFrame`, prepared
  GPU instance updates, and move instance preparation out of
  `render_execute_mesh_collect.mbt`.
- 2026-06-24: Added Bevy-shaped `RenderMeshInstanceGpuQueue` state,
  `RenderMeshInstanceGpuQueues`, `MeshesToReextractNextFrame`,
  `RenderMeshInstanceGpuBuilder`, `RenderMeshInstanceGpuPrepared`, and
  `mark_all_meshes_for_reextraction` anchors in
  `mgstudio-engine/pbr/render_mesh_instances.mbt`. The queue state machine now
  records changed CPU-culling, changed GPU-culling, and removed entities with
  white-box coverage. The actual `extract_meshes_for_gpu_building` changed
  query and `collect_meshes_for_gpu_building` prepare/update owner remain
  incomplete.
- 2026-06-24: Replaced the queue-data-backed `RenderMeshInstancesCpu` and
  `RenderMeshInstancesGpu` maps with source-shaped `RenderMeshInstanceCpu`,
  `RenderMeshInstanceGpu`, `RenderMeshInstanceSharedFlat`,
  `RenderMeshInstanceGpuFlat`, and `MeshTransforms` records. Existing queue data
  access is now derived from those records: CPU building returns a default
  uniform index, while GPU building returns the stored GPU current uniform index,
  matching the Bevy owner shape. The full material/allocator/lightmap/skin/morph
  `prepare` and `update` flow is still incomplete.
- 2026-06-24: Added Bevy-shaped `MeshInputUniform`,
  `RenderMeshInstanceGpuPrepared::update`, and `remove_mesh_input_uniform`
  behavior over `RenderMeshInstancesGpu` and
  `render/batching::InstanceInputUniformBuffer`. New white-box tests cover
  inserting a new prepared GPU instance, updating an existing instance while
  pushing the previous input uniform for motion-vector use, and removing an
  invisible instance from both the GPU instance map and current input buffer.
  The prepared data is still constructed by tests; the real Bevy
  `RenderMeshInstanceGpuBuilder::prepare` resource lookups remain incomplete.
- 2026-06-24: Ported the next `RenderMeshInstanceGpuBuilder::prepare` slice.
  The builder now mirrors Bevy's resource lookups for non-dummy material
  binding readiness, mesh allocator vertex/index slices, render lightmap slot
  and slab data, skin byte offset, morph descriptor index, and frame timestamp
  when building `MeshInputUniform`. New white-box tests cover the Bevy early
  return when a non-dummy material binding is not ready and the successful
  allocator/material/lightmap/morph uniform construction path. The collector
  still does not call this owner path from real GPU-building queues.
- 2026-06-24: Added the first Bevy-shaped
  `collect_meshes_for_gpu_building` owner slice. It consumes
  `RenderMeshInstanceGpuQueues`, updates `RenderMeshInstancesGpu`,
  `InstanceInputUniformBuffer`, `MeshCullingDataBuffer`, and
  `RenderGpuCulledEntities`, records material-not-ready entities into
  `MeshesToReextractNextFrame`, clears consumed queues, and handles removed
  entities. Because MoonBit does not currently provide Bevy's parallel taskpool
  and channel shape, this slice preserves the producer/consumer state
  transitions with sequential loops. The extraction changed-query producer and
  real runtime wiring remain incomplete.
- 2026-06-24: Wired the real `render3d_prepare_meshes_system` GPU-builder path
  into Bevy-shaped `RenderMeshInstanceGpuQueues` instead of dropping the
  `RenderMeshInstances` resource. The new extraction helper builds
  `RenderMeshInstanceGpuBuilder` records from the extracted mesh payload,
  mirrors Bevy's lightmap UV rect packing, preserves previous input indices for
  motion-vector data, splits CPU/GPU culling queues, and queues stale GPU
  instances for removal. This is still not the final Bevy
  `extract_meshes_for_gpu_building` producer because it is driven by the
  existing full mesh extraction result rather than Bevy's changed-query and
  removed-component readers.
- 2026-06-24: Wired the runtime GPU-building collector into the PBR prepare
  path after material bind group preparation. The path now prepares
  `RenderMaterialBindings` from the bindless `StandardMaterial` allocator,
  keeps a concrete `BatchedInstanceBuffers<Unit, MeshInputUniform>` render
  resource, synchronizes the backend `MeshAllocator`, and calls the Bevy-shaped
  `collect_meshes_for_gpu_building` owner with `MeshAllocator`,
  `RenderMaterialInstances`, `RenderMaterialBindings`, `RenderLightmaps`,
  `SkinUniforms`, `MorphIndices`, and the render backend frame count. New
  white-box coverage verifies bindless material binding allocation, material
  prepare binding writes, and runtime queue consumption into the GPU input
  buffer. Remaining work: replace the full extracted-mesh producer with Bevy's
  changed-query/removed-component producer, move more ownership out of the
  monolithic collect path, and expose a source-equivalent non-bindless material
  binding id path instead of leaving non-bindless allocation unprepared.
- 2026-06-24: Moved the GPU-building extraction producer closer to Bevy's
  `extract_meshes_for_gpu_building` semantics. The render-world producer now
  tracks a source-world `last_source_change_tick`, tests the Bevy changed
  component set against the source world, folds Bevy's removed optional
  component readers into a potential reextraction set, and skips already-known
  GPU instances unless they changed or are listed in
  `MeshesToReextractNextFrame`. White-box coverage verifies the unchanged
  known-instance fast path and reextract override. Remaining divergence: the
  system is still fed by the existing full extracted mesh array, not by direct
  Bevy-shaped `changed_meshes_query` and `all_meshes_query` source queries.
- 2026-06-24: Added the Bevy `RemovedComponents<Mesh3d>` producer slice. GPU
  extraction now reads source-world removed `Mesh3d` entities with the same
  source tick window as the changed-query producer and queues removals before
  the temporary missing-from-extracted-list cleanup. New white-box coverage
  verifies that the source removed `Mesh3d` reader produces the expected main
  entities. Remaining divergence: direct `changed_meshes_query` and
  `all_meshes_query` are still represented by the current full extracted mesh
  array, and the missing-from-list cleanup remains until that direct query
  shape replaces it.
- 2026-06-24: Split the GPU-building producer into a Bevy-shaped changed pass
  and a separate reextract pass over an `all_meshes_query` entity index. The
  changed pass now queues changed or newly observed meshes, records the entities
  it handled, and the reextract pass uses `all_meshes_query.get(entity)`-style
  lookup to avoid double-processing changed entities. New white-box coverage
  verifies that a source-world changed `Mesh3d` component queues an existing GPU
  instance without relying on `MeshesToReextractNextFrame`. Remaining
  divergence: the `changed_meshes_query` and `all_meshes_query` inputs are still
  built from the existing extracted mesh array instead of direct ECS query rows,
  so the temporary missing-from-list cleanup remains.
- 2026-06-24: Replaced the changed pass's per-extracted-mesh
  `is_changed_by_key` scan with a Bevy-shaped changed entity set built from the
  source world's changed component readers for the full GPU mesh extraction
  component set. The producer now iterates that changed set and resolves each
  row through the `all_meshes_query` entity index. The source tick window also
  now uses the ECS component-write lower bound of tick `1`, matching MoonBit's
  component write semantics during first-frame extraction. White-box fixtures
  were updated to use real source-world entity identities.
- 2026-06-24: Ported the Bevy `extract_mesh_for_gpu_building` invisible-row
  removal branch into the current producer shape. Changed `ViewVisibility`
  entities whose source value is hidden now queue a GPU instance removal even
  when the existing extracted mesh array has no row for the entity, matching
  Bevy's `if !view_visibility.get() { queue.remove(...) }` behavior. The
  temporary missing-from-list cleanup now skips entities already handled by the
  changed pass to avoid duplicate removals. New white-box coverage exercises
  the hidden changed mesh path.
- 2026-06-24: Replaced the GPU-building producer's builder input with a
  Bevy-shaped source-world extraction row that reads only
  `GpuMeshExtractionQuery` fields (`ViewVisibility`, `GlobalTransform`,
  `Mesh3d`, optional lightmap/tag/layers/Aabb, and marker components). The
  producer now resolves `all_meshes_query.get(entity)`-style rows directly from
  the source world when the old extracted-mesh cache has no material-resolved
  row, so material-unready meshes still enter `RenderMeshInstanceGpuQueues` and
  are recorded in `MeshesToReextractNextFrame` by
  `collect_meshes_for_gpu_building`, matching Bevy's late material readiness
  semantics. White-box coverage now verifies real source rows for changed,
  hidden, unchanged/reextract, lightmap packing, and material-unready reextract
  cases. Remaining divergence: `render3d_execute_collect_meshes` still builds
  the old full `ExtractedMesh3d` array for the rest of the render pipeline.
- 2026-06-24: Removed the non-Bevy temporary GPU-instance
  missing-from-extracted-list cleanup from the GPU-building producer. Bevy
  removes GPU mesh instances through the `extract_mesh_for_gpu_building`
  invisible-row branch and `RemovedComponents<Mesh3d>` only; it does not scan
  existing `RenderMeshInstancesGpu` entries and delete entities solely because
  they were absent from one extracted-mesh array. White-box coverage now locks
  this behavior by keeping a stale GPU instance alive unless the source world
  emits a real removal signal.
- 2026-06-24: Removed the `Array[ExtractedMesh3d]` input from the
  GPU-building extraction resource path. `render3d_prepare_meshes_system` now
  calls the GPU producer with the render world, source world, and GPU-culling
  state only; changed and reextract passes fetch their
  `GpuMeshExtractionQuery`-shaped rows directly from the source world. This
  eliminates the last producer-side dependency on the monolithic
  material-resolved extracted mesh array. Remaining divergence:
  `render3d_execute_collect_meshes` still builds that old array for phase
  queueing, material bind group preparation, shader prewarming, and the
  CPU-building path.
- 2026-06-24: Replaced no-camera `RenderMeshInstances` resource removal with a
  Bevy-shaped variant-preserving clear. Bevy inserts `RenderMeshInstances` as a
  persistent render-app resource and `mark_all_meshes_for_reextraction` clears
  the current CPU/GPU instance map instead of deleting the resource. The
  no-camera branches in `render3d_prepare_meshes_system` now clear the existing
  variant, with white-box coverage for CPU and GPU variant preservation.
- 2026-06-24: Replaced the CPU-building `ExtractedMesh3d` array-to-queue-data
  conversion with a Bevy-shaped source-world extraction query. The CPU path now
  clears and repopulates the persistent `CpuBuilding(RenderMeshInstancesCpu)`
  resource from `Mesh3d`, `ViewVisibility`, `GlobalTransform`,
  `PreviousGlobalTransform`, `MeshTag`, `VisibilityRange`, `RenderLayers`, and
  the Bevy marker components, then stores full
  `RenderMeshInstanceCpu { shared, transforms, render_layers }` records. The
  shared mesh flag helper now consumes `RenderVisibilityRanges` for LOD index
  and AABB-based visibility bits on both CPU and GPU extraction rows. Removed
  stale duplicate fields from the legacy `ExtractedMesh3d` payload after their
  semantics moved to `RenderMeshInstanceFlags` and source-row mesh flags.
  Verification: `moon -C mgstudio-engine test --release
  pbr/render_mesh_instances_wbtest.mbt --deny-warn`, `moon -C mgstudio-engine
  test --release pbr/render --deny-warn`, `moon -C mgstudio-engine test
  --release pbr --deny-warn`, and `moon -C mgstudio-engine test --release mesh
  --deny-warn`.

- 2026-06-24: Moved the backend `MeshAllocator` snapshot refresh out of the
  PBR and wireframe consumer paths. The render plugin now registers the refresh
  as a render-schedule `PrepareAssets` system, matching Bevy's
  `MeshAllocatorPlugin` owner boundary where allocator work runs before mesh
  consumers. `render3d_prepare_meshes_system` and wireframe queue/prepare
  systems now only read the already-prepared `MeshAllocator` resource.
  Remaining divergence: mgstudio still mirrors backend allocator state rather
  than running Bevy's exact `allocate_and_free_meshes` over
  `ExtractedAssets<RenderMesh>`, and the old full `ExtractedMesh3d` array is
  still built for phase queueing, material bind group preparation, and shader
  prewarming.
- 2026-06-24: Removed material binding allocation from the old per-mesh
  `ExtractedMesh3d` bind-group loop. `render3d_prepare_bind_groups_system` now
  prepares `RenderMaterialBindings` directly from `RenderMaterialInstances`,
  while the remaining mesh loop only prepares concrete texture bind groups for
  backend drawing. White-box coverage verifies that two visible entities
  sharing the same material allocate one bindless material binding. Remaining
  divergence: the old full `ExtractedMesh3d` array is still built for phase
  queueing, texture bind-group preparation, and shader prewarming.
- 2026-06-24: Moved extension material shader path registration out of the old
  `ExtractedMesh3d` payload. `extract_mesh_materials` now records shader paths
  from visible `MeshMaterial3d` extension metadata into
  `RenderMaterialShaderPaths`, and `render3d_prepare_shader_asset_paths`
  consumes that resource instead of scanning the old extracted mesh array. The
  stale shader path fields were removed from `Mesh3dExtractedPayload` and
  `Render3dResolvedMeshMaterial`. Remaining divergence: the old full
  `ExtractedMesh3d` array is still built for phase queueing, texture bind-group
  preparation, and pipeline prewarming.
- 2026-06-24: Rehomed `RenderMaterialBindings` preparation from
  `render_execute.mbt` to the material bind-group source owner. This matches
  Bevy's `RenderMaterialBindings` ownership in `bevy_pbr/src/material.rs`:
  render execution now calls the material owner instead of owning material-id
  binding allocation itself. The backend texture bind-group loop is still
  extracted-mesh-driven, so the old full `ExtractedMesh3d` array remains for
  phase queueing, texture bind-group preparation, and pipeline prewarming.
- 2026-06-27: Ported the Bevy
  `bevy_pbr/src/render/mesh.rs::set_mesh_motion_vector_flags` slice into
  `RenderMeshInstances`. The prepare path now sets previous-skin and
  previous-morph `RenderMeshInstanceFlags` after CPU/GPU mesh instance
  extraction, matching Bevy's deferred flag update after skin and morph
  extraction. White-box coverage exercises both CPU-building and GPU-building
  variants. Remaining symbol gap: mgstudio's current `MorphIndices` owner only
  represents the uniform `current`/`prev` map shape, so Bevy's storage
  morph-index branch is still part of the broader morph owner parity work.
- 2026-06-27: While validating the motion-vector slice,
  `examples/3d/motion_blur` exposed a render-pass descriptor mismatch:
  a motion-vector-only prepass was beginning a depth-only render pass while the
  specialized pipeline expected the sparse Bevy color target vector
  `[None, Some(MOTION_VECTOR_PREPASS_FORMAT)]`. The renderer now uses a two-slot
  sparse prepass descriptor for motion-vector-only passes and preserves the
  four-slot deferred descriptor shape for deferred prepasses, matching
  `bevy_core_pipeline/src/prepass/mod.rs::prepass_target_descriptors`.
  Verification: `moon -C mgstudio-engine test --release
  pbr/render_mesh_instances_wbtest.mbt --deny-warn`, `moon -C mgstudio-engine
  test --release render/renderer --deny-warn`, `moon -C mgstudio-engine test
  --release pbr/render --deny-warn`, `moon -C mgstudio-engine test --release
  pbr --deny-warn`, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/motion_blur
  /tmp/mgstudio_iss155_motion_vector_flags_motion_blur.png`.
- 2026-06-27: Removed a non-Bevy fallback from the material queue pipeline
  lookup. When `render3d_queue_mesh_phase_items` receives a view-specific
  `SpecializedMaterialPipelineCache`, a missing view/entity pipeline entry now
  returns `None` instead of falling back to the old
  `ExtractedMesh3d.payload.material_pipeline_id`. This matches
  `bevy_pbr/src/material.rs::queue_material_meshes`, where a missing
  view-specialization cache or entity pipeline cache causes the entity to be
  skipped rather than queued with stale extracted payload state. Source-file
  white-box coverage verifies cache miss, cache hit, and the un-migrated
  no-cache caller boundary. Verification: `moon -C mgstudio-engine test
  --release pbr --deny-warn`.
- 2026-06-27: Added the generic Bevy `DirtySpecializations` owner fragment to
  `render/view/visibility` rather than `render/camera`, because mgstudio splits
  Bevy's render crate into packages and the parent `render` package cannot
  depend on the visibility child package without a cycle. The new fragment
  preserves Bevy's changed renderables, removed renderables, dirty views,
  `iter_to_despecialize`, `iter_to_specialize`, `iter_to_dequeue`, and
  `iter_to_queue` behavior over `RenderVisibleEntitiesClass` and
  `PendingQueues` data. This prepares the PBR material queue to stop using the
  extracted-mesh array as its change driver. Remaining work: wire
  `queue_material_meshes`/specialization paths to this shared owner instead of
  the local monolithic queue build. Verification: `moon -C mgstudio-engine test
  --release render/view/visibility --deny-warn`.
- 2026-06-27: Added the Bevy `clear_dirty_specializations` and
  `expire_specializations_for_views` helpers to the shared
  `DirtySpecializations` fragment, and initialized that resource from the PBR
  material render-resource startup path. This mirrors Bevy's RenderApp
  resource setup as closely as mgstudio's current package split allows; the
  parent `render` package still cannot own the resource directly because it
  would need to import the visibility child package. Verification:
  `moon -C mgstudio-engine test --release render/view/visibility --deny-warn`
  and `moon -C mgstudio-engine test --release
  pbr/materials_plugin_wbtest.mbt --deny-warn`.
- 2026-06-27: Wired the Bevy
  `bevy_pbr/src/material.rs::extract_entities_needs_specialization` and
  `extract_entities_that_need_specializations_removed` semantics into the PBR
  render extraction path. Since MoonBit cannot instantiate Bevy's generic
  `EntitiesNeedingSpecialization<M>` resource shape directly for every
  material type, mgstudio now computes the same changed/removed entity sets
  from ECS `changed_components`/`removed_components`, writes changed entities
  into both `EntitySpecializationTicks` and render-world
  `DirtySpecializations`, and drains removed `Mesh3d`/`MeshMaterial3d` entities
  into `DirtySpecializations` before the late material-instance sweep advances
  the source tick. White-box coverage verifies the Mesh3d-with-material filter,
  material-only changes, removed Mesh3d/material components, and deduplication.
  Verification: `moon -C mgstudio-engine test --release pbr --deny-warn`.
- 2026-06-27: Aligned `SpecializedMaterialPipelineCache` view ownership with
  Bevy's retained-view cache shape. The cache is now keyed by
  `RetainedViewEntity` instead of a bare camera `Entity`, and exposes per-view
  clear plus stale-view expiration helpers needed by Bevy's
  `specialize_material_meshes` lifecycle. Existing prewarm and queue lookup
  call sites now construct the retained view from the extracted camera's main
  entity while the broader queue owner migration is still in progress.
  Verification: `moon -C mgstudio-engine test --release pbr --deny-warn`.

## Close Notes
