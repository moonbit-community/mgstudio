# ISS-018: Align PBR render-app system ordering

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, pbr, render, scheduling
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-27
- External ref: none

## Description
PBR render systems must run in the same source-owned phases as Bevy: startup,
extract, create views, prepare resources, specialize, prepare views, queue,
prepare bind groups, render, and cleanup. Mgstudio has many Bevy-shaped system
set names and several completed schedule realignments, but the full PBR runtime
order is still not proven source-equivalent.

## Design
Use Bevy's `PbrPlugin`, `MaterialsPlugin`, `MaterialPlugin`, `PrepassPlugin`,
`MeshletPlugin`, and PBR render modules as the ordering source. Audit each
mgstudio system that owns PBR extraction, material specialization, light
preparation, cluster preparation, skin extraction, mesh preparation, queueing,
bind groups, and execution.

## Acceptance Criteria
- [ ] Each PBR system has a documented Bevy source owner and Bevy render-system set.
- [ ] Mgstudio system placement matches the Bevy owner set or has a recorded blocker.
- [ ] Material preparation, material bind-group allocation, specialization, queueing, and queue sweep run in Bevy-equivalent order.
- [ ] Light extraction, cluster preparation, shadow view creation, and GPU light preparation run in Bevy-equivalent order.
- [ ] Skin extraction, mesh preparation, GPU preprocess, and phase item queueing are ordered consistently with Bevy.
- [ ] A schedule-order test or generated report verifies the PBR render-app ordering.

## Relationships
- Depends on: ISS-014, ISS-016
- Parent: ISS-012
- Related: ISS-003, ISS-004, ISS-005, ISS-006, ISS-007
- Discovered from: none

## Notes
- 2026-06-02: This issue is also related to the many_foxes performance issues because ordering mistakes can create unnecessary per-frame work.
- 2026-06-27: Ported the next `bevy_pbr/src/material.rs::specialize_material_meshes`
  consumer slice into the PBR execute path. Mgstudio now extracts camera
  `VisibleEntities<Mesh3d>` into render-world `RenderVisibleEntities`, then
  material prewarm selects candidates through
  `DirtySpecializations::iter_to_specialize` and per-view
  `PendingMeshMaterialQueues` instead of scanning every extracted mesh for
  every camera. White-box coverage verifies active-camera visible extraction
  and added/changed/pending specialization candidates. Remaining work: make the
  later material queue phase consume the same source-shaped visible queues and
  finish the full render-system set/order audit.
- 2026-06-27: Found that directly filtering material queue items by
  `RenderVisibleEntities<Mesh3d>` blanked `examples/3d/motion_blur`, because
  mgstudio's root visibility system only updated `ViewVisibility` and did not
  fill camera `VisibleEntities` like Bevy's `check_visibility_cpu_culling`.
  Ported the missing `VisibilityClass` component and registered Mesh2d/Mesh3d
  required visibility classes, then added a camera/visibility producer that
  fills active cameras' `VisibleEntities` by visibility class and render layer.
  The queue phase must still wait for persistent Bevy-shaped phase resources
  before switching from full visible-per-frame rebuilds to dirty-only
  `iter_to_queue` consumption.
- 2026-06-27: Tightened the new camera visibility producer toward Bevy
  `check_visibility_cpu_culling`: it now requires source-shaped
  `GlobalTransform`/`Frustum` data, skips `NoCpuCulling` entities in the CPU
  path, consumes `VisibleEntityRanges`, performs AABB/sphere frustum culling
  unless `NoFrustumCulling` is present, sorts visible entity lists, and updates
  `ViewVisibility` only after a view accepts the entity. Added Bevy-shaped
  `camera::primitives::Sphere` component ownership and frustum intersection
  helpers needed by that producer.

## Close Notes
