# ISS-248: Verify scrolling_fog volumetric shadow binding runtime evidence

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, shadows, volumetric-fog, frame-data, visual-parity
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
After the source audit and focused test pass, capture runtime evidence that
`examples/3d/scrolling_fog` reaches the volumetric fog pass with the same
current-frame directional shadow binding that was rendered by the shadow pass.

## Design
Run the standard no-retry native visual gate for `scrolling_fog` and inspect
the run log for validation, missing-resource, unsupported, fatal, panic, abort,
or `wgpuQueueSubmit` output. If source-level test coverage is insufficient to
prove runtime consumption, add a narrow debug artifact or test hook that records
the active shadow binding identity without becoming a permanent workaround.

## Acceptance Criteria
- [ ] `examples/3d/scrolling_fog` standard no-retry native capture succeeds.
- [ ] The run log contains no validation, missing-resource, unsupported, fatal,
      panic, abort, or `wgpuQueueSubmit` output.
- [ ] Runtime evidence confirms the volumetric fog pass consumes the same
      directional shadow target id/layer/cascade frame proven by ISS-247.
- [ ] Any mismatch is fixed source-faithfully or recorded as a precise blocker.

## Relationships
- Depends on: ISS-247
- Parent: ISS-220
- Related: ISS-091, ISS-104, ISS-190, ISS-198, ISS-199, ISS-212
- Discovered from: ISS-220

## Notes
- 2026-07-04: Split from ISS-220 so the native runtime gate is separate from
  source audit and white-box test work.
- 2026-07-04: Runtime evidence uses the standard no-retry native capture:
  `/tmp/mgstudio_iss248_scrolling_fog_delay90.png` is a 2560x1440 RGBA PNG
  with visible pillar, fog, and directional shadow/light-shaft contribution.
  `/tmp/mgstudio_iss248_scrolling_fog_delay90.run.log` is empty, and keyword
  scanning found no validation, missing-resource, unsupported, fatal, panic,
  abort, or `wgpuQueueSubmit` output. ISS-247 provides the focused proof that
  the runtime pass state consumed here carries the same directional shadow
  target id/layer/cascade frame as the rendered shadow phase views.

## Close Notes
Closed after the no-retry `scrolling_fog` native visual gate succeeded and the
run log remained empty. Validation:

- `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/scrolling_fog /tmp/mgstudio_iss248_scrolling_fog_delay90.png`
- `/tmp/mgstudio_iss248_scrolling_fog_delay90.run.log` is empty.
- `/tmp/mgstudio_iss248_scrolling_fog_delay90.png` is 2560x1440 RGBA and shows
  the expected active scene/fog/shadow signal.
