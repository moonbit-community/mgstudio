# ISS-322: Port Bevy glTF light spawn extension hooks

## Metadata
- Type: task
- Status: open
- Priority: 2
- Labels: gltf, scene, light, extension, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Bevy's glTF light import branch invokes extension callbacks after spawning
directional, point, and spot lights. Mgstudio's current glTF extension handler
API does not expose equivalent light-spawn callbacks, so general source-level
parity for glTF extension participation is incomplete even though
`VolumetricFogExample.glb` does not depend on those callbacks.

## Design
Compare Bevy's `GltfExtensionHandler::on_spawn_light_directional`,
`on_spawn_light_point`, and `on_spawn_light_spot` hooks against mgstudio's
`GltfExtensionHandler`. Add Bevy-shaped callback slots and invoke them from the
glTF light import/spawn path without changing built-in KHR_lights_punctual
semantics.

## Acceptance Criteria
- [ ] Bevy source anchors for the three light-spawn extension hooks are listed.
- [ ] Mgstudio extension handler API exposes equivalent light-spawn callbacks
      or records a precise blocker if MoonBit cannot express the callback
      shape.
- [ ] Mgstudio invokes the callbacks for directional, point, and spot glTF
      lights at the Bevy-equivalent spawn boundary.
- [ ] Focused tests prove registered handlers can observe the spawned light
      entity for all three light kinds.

## Relationships
- Depends on: ISS-319
- Parent: ISS-012
- Related: ISS-301, ISS-310
- Discovered from: ISS-319

## Notes
- 2026-07-05: Discovered during ISS-319. This is a local source-parity gap,
  not an external blocker and not a current `VolumetricFogExample.glb` runtime
  divergence because that asset does not register a light-spawn extension
  handler.

## Close Notes
