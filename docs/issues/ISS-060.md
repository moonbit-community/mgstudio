# ISS-060: Regenerate remaining transparent example references

## Metadata
- Type: task
- Status: open
- Priority: 1
- Labels: examples, screenshots, visual-parity, reference, capture
- Assignee: unassigned
- Created: 2026-06-09
- Updated: 2026-06-13
- External ref: none

## Description
After refreshing the first batch of broken references and upgrading dependency blockers, 188 `reference.png` files still fail the blank/near-black triage scan: 179 are fully transparent and 9 are near-black. These references cannot be used as visual regression baselines.

## Design
After dependency blockers are resolved, regenerate each remaining stale reference with the standard native capture command shape and keep only captures that pass image sanity checks. Do not treat a generated non-blank image as Bevy visual parity; this issue only covers replacing unusable local reference artifacts.

## Acceptance Criteria
- [ ] No `mgstudio-engine/examples/**/reference.png` file is fully transparent.
- [ ] No `reference.png` file is an unintended opaque black screen.
- [ ] Every refreshed reference was produced by `mgstudio-engine/scripts/p0_visual_capture_native.sh` or an equivalent checked-in capture gate.
- [ ] Failed or screenshot-ineligible examples are recorded in dedicated issue notes instead of using blank placeholder images.

## Relationships
- Depends on: ISS-057, ISS-058, ISS-059
- Parent: ISS-047
- Related: ISS-061
- Discovered from: ISS-047

## Notes
- 2026-06-09: Batch refresh input count was 188. Updated 6 references successfully: `examples/3d/3d_scene`, `examples/3d/3d_viewport_to_world`, `examples/3d/animated_material`, `examples/3d/anisotropy`, `examples/3d/bloom_3d`, and `examples/3d/camera_sub_view`.
- 2026-06-09: `examples/stress_tests/many_foxes/reference.png` was refreshed separately and is valid.
- 2026-06-09: Current remaining bad reference count is 182. Batch report: `/tmp/mgstudio_reference_regen/report.jsonl`.
- 2026-06-10: Dependency blockers ISS-057, ISS-058, and ISS-059 are closed after upgrading dependencies and migrating local constructor call sites. This issue is ready to retry reference regeneration.
- 2026-06-10: Fresh downsampled image scan found 383 checked-in `reference.png` files, 437 example packages, 54 packages without references, 195 sanity-pass references, 179 fully transparent references, 9 near-black references, and 188 total bad references. Near-black references: `examples/3d/blend_modes`, `examples/3d/clustered_decals`, `examples/3d/motion_blur`, `examples/3d/specular_tint`, `examples/3d/wireframe`, `examples/animation/animation_events`, `examples/ui/styling/transparency_ui`, `examples/window/screenshot`, and `examples/window/window_drag_move`.
- 2026-06-10: Refreshed and checked in three near-black references that now pass default image sanity: `examples/3d/motion_blur`, `examples/window/screenshot`, and `examples/window/window_drag_move`.
- 2026-06-10: Four near-black 3D references still cannot be refreshed because the native capture script does not receive an RGBA8 blob: `examples/3d/blend_modes`, `examples/3d/clustered_decals`, `examples/3d/specular_tint`, and `examples/3d/wireframe`. This capture failure class is tracked by ISS-062.
- 2026-06-10: Two near-black examples produce deterministic dark-scene captures but fail the default image sanity thresholds: `examples/animation/animation_events` and `examples/ui/styling/transparency_ui`. Their classification is tracked by ISS-063.
- 2026-06-10: After the three reference refreshes, the downsampled scan reports 185 bad references: 179 fully transparent references and 6 near-black references.
- 2026-06-10: ISS-062 closed after refreshing `examples/3d/blend_modes`, `examples/3d/clustered_decals`, `examples/3d/specular_tint`, and `examples/3d/wireframe`. The expected remaining stale-reference set is now 181 bad references: 179 fully transparent references plus the 2 intentional dark-scene candidates tracked by ISS-063.
- 2026-06-12: Fresh ImageMagick scan still reports 179 fully transparent references. First retry target `examples/3d/boxes3` failed to produce an engine-native RGBA8 blob with both 90-frame and 1-frame capture delays; its `reference.png` remains fully transparent and must be refreshed after the capture path can produce a blob.
- 2026-06-12: Fixed `mgstudio-engine/scripts/p0_visual_capture_native.sh` so failed refresh attempts write to a temporary PNG and do not delete an existing checked-in reference image before a successful capture.
- 2026-06-12: Fixed the native capture script to use `moon run --build-only` followed by the built executable, so the capture timeout measures app runtime instead of a second native C compile. The executable is run from `mgstudio-engine` while output/log/blob paths are resolved to absolute paths before launch.
- 2026-06-12: Refreshed `examples/3d/boxes3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.564783`, `size=2560x1440`; full reference scan now reports 178 fully transparent references.
- 2026-06-12: Refreshed `examples/3d/generate_custom_mesh/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.40093`, `size=2560x1440`; full reference scan now reports 177 fully transparent references.
- 2026-06-12: Refreshed `examples/3d/vertex_colors/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.431228`, `size=2560x1440`; full reference scan now reports 176 fully transparent references.
- 2026-06-12: Refreshed `examples/3d/orthographic/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.41724`, `size=2560x1440`; full reference scan now reports 175 fully transparent references.
- 2026-06-12: `examples/3d/voxels3` produced a nonblank but unusable standard capture: the visible scene content is reduced to a tiny central point because the current camera framing does not cover the spawned content. The generated image was discarded and the fix is tracked by ISS-073.
- 2026-06-12: Refreshed `examples/3d/spotlight/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.421121`, `size=2560x1440`; full reference scan now reports 174 fully transparent references.
- 2026-06-12: Refreshed `examples/3d/texture/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=0.906539`, `gray_mean=0.384567`, `size=2560x1440`; full reference scan now reports 173 fully transparent references.
- 2026-06-12: `examples/3d/decal` produced a nonblank but visually empty standard capture. The generated image was discarded; the Bevy source-shape and forward decal material parity fix is tracked by ISS-074.
- 2026-06-12: After ISS-074 closed, a fresh ImageMagick scan reports 172 fully transparent references and 1 near-black reference. `examples/3d/auto_exposure` produced an opaque but visually unusable standard capture with only a left-edge wall panel visible; the generated image was discarded and the source-parity fix is tracked by ISS-075.
- 2026-06-12: Refreshed `examples/3d/clearcoat/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.316793`, `size=2560x1440`; expected full-reference scan count is now 171 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/clustered_decal_maps/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=0.994916`, `gray_mean=0.389561`, `size=2560x1440`; expected full-reference scan count is now 170 fully transparent references plus the 1 near-black reference tracked separately. This does not claim Bevy source parity; the example's proxy-mesh approximation is tracked by ISS-076.
- 2026-06-12: Refreshed `examples/3d/depth_of_field/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.816322`, `size=2560x1440`; expected full-reference scan count is now 169 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/character_controller3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.569423`, `size=2560x1440`; expected full-reference scan count is now 168 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/contact_filter3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.391413`, `size=2560x1440`; expected full-reference scan count is now 167 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: `examples/3d/custom_system_setup3` produced an opaque but visually empty standard capture containing only the default dark background. The generated image was discarded; the source/scene/camera fix is tracked by ISS-077.
- 2026-06-12: Refreshed `examples/3d/debug_toggle3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.564783`, `size=2560x1440`; expected full-reference scan count is now 166 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/despawn3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.564813`, `size=2560x1440`; expected full-reference scan count is now 165 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/events3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.389657`, `size=2560x1440`; expected full-reference scan count is now 164 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/irradiance_volumes/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.377132`, `size=2560x1440`; expected full-reference scan count is now 163 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/joints3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.585199`, `size=2560x1440`; expected full-reference scan count is now 162 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/joints_despawn3/reference.png` with the native capture script using `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=1` and retry disabled after the standard 90-frame run aborted before producing a script-accepted capture. Sanity check: `alpha_mean=1`, `gray_mean=0.388452`, `size=2560x1440`; expected full-reference scan count is now 161 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/light_probe_blending/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.329473`, `size=2560x1440`; expected full-reference scan count is now 160 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: `examples/ui/render_ui_to_texture` produced an opaque but visually unusable standard capture containing only near-black render-to-texture output. The generated image was discarded; the source/render-to-texture parity fix is tracked by ISS-078.
- 2026-06-12: Refreshed `examples/3d/order_independent_transparency/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.387865`, `size=2560x1440`; expected full-reference scan count is now 159 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/spherical_area_lights/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.550124`, `size=2560x1440`; expected full-reference scan count is now 158 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/rapier_to_bevy_mesh/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.413901`, `size=2560x1440`; expected full-reference scan count is now 157 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/physics3d_events3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.825263`, `size=2560x1440`; expected full-reference scan count is now 156 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/physics3d_joints/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.529794`, `size=2560x1440`; expected full-reference scan count is now 155 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/transparency_3d/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.44301`, `size=2560x1440`; expected full-reference scan count is now 154 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/physics3d_queries/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.558334`, `size=2560x1440`; expected full-reference scan count is now 153 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/light_textures/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.40798`, `size=2560x1440`; expected full-reference scan count is now 152 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: Refreshed `examples/3d/multiple_colliders3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.406623`, `size=2560x1440`; expected full-reference scan count is now 151 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-12: `examples/3d/rotate_environment_map` produced a numerically nonblank but visually rejected standard capture: the Bevy source expects visible skybox/environment-map lighting on a metallic clearcoat sphere, while the generated image showed only a dark featureless sphere. The generated image was discarded; the source/PBR parity fix is tracked by ISS-079.
- 2026-06-12: Refreshed `examples/3d/locked_rotations3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.419068`, `size=2560x1440`; expected full-reference scan count is now 150 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: Refreshed `examples/3d/static_trimesh3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.426017`, `size=2560x1440`; expected full-reference scan count is now 149 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: `examples/3d/reflection_probes` produced a numerically nonblank but visually rejected standard capture: the Bevy source expects visible skybox, cubes-scene, and reflection-probe output, while the generated image was too dark and lacked the expected reflection/probe visual signal. The generated image was discarded; the source/PBR parity fix is tracked by ISS-080.
- 2026-06-13: Refreshed `examples/3d/shadow_caster_receiver/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.681948`, `size=2560x1440`; expected full-reference scan count is now 148 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: Refreshed `examples/3d/shadow_biases/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.572869`, `size=2560x1440`; expected full-reference scan count is now 147 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: Refreshed `examples/3d/mesh_ray_cast/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.276941`, `size=2560x1440`; expected full-reference scan count is now 146 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: Refreshed `examples/3d/two_passes/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=0.0626398`, `gray_mean=0.0328936`, `size=2560x1440`; expected full-reference scan count is now 145 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: Refreshed `examples/3d/split_screen/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.430674`, `size=2560x1440`; expected full-reference scan count is now 144 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: Refreshed `examples/3d/multi_contexts3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.414271`, `size=2560x1440`; expected full-reference scan count is now 143 fully transparent references plus the 1 near-black reference tracked separately.
- 2026-06-13: `examples/3d/ssr` produced a numerically nonblank but visually rejected standard capture: `alpha_mean=1`, `gray_mean=0.387916`, `size=2560x1440`, `non_black_ratio=1.0`, and `mean_luma=46.18836278955987`, but the image was effectively a dark scene with only a sparse diagonal line. Bevy's source expects a visible deferred SSR scene with water/base/model/reflection signal, while mgstudio's example still documents a standard-material approximation for Bevy's custom deferred water material extension. The generated image was discarded; the source/PBR parity fix is tracked by ISS-081.
- 2026-06-13: Refreshed `examples/3d/parallax_mapping/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.435778`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=167.625`; the run log had no validation or fatal output. A full reference scan after this refresh reports 142 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact and does not claim source-level example parity.
- 2026-06-13: Refreshed `examples/3d/lighting/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=0.975722`, `gray_mean=0.584633`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=179.911`; the run log had no validation or fatal output. A full reference scan after this refresh reports 141 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact and does not claim source-level example parity.
- 2026-06-13: `examples/3d/lightmaps` produced a numerically nonblank but visually rejected standard capture: `alpha_mean=1`, `gray_mean=0.335457`, `size=2560x1440`, `non_black_ratio=1.0`, and `mean_luma=141.812`, but the frame was only a dark square on the default background instead of Bevy's visible CornellBox lightmap scene. The generated image was discarded; the source/PBR/asset-loading parity fix is tracked by ISS-082.
- 2026-06-13: Refreshed `examples/3d/lines/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.387997`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=150.613`; the run log had no validation or fatal output. A full reference scan after this refresh reports 140 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact; the example's custom-material source parity gap is tracked by ISS-083.
- 2026-06-13: Refreshed `examples/3d/mirror/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.434991`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=165.526`; the run log had no validation or fatal output. A full reference scan after this refresh reports 139 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact; the example's mirror material and inverted-culling source parity gap is tracked by ISS-084.
- 2026-06-13: Refreshed `examples/3d/mixed_lighting/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.596219`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=185.831`; the run log had no validation or fatal output. A full reference scan after this refresh reports 138 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact; the example's real Lightmap/UI/picking source parity gap is tracked by ISS-085.
- 2026-06-13: Refreshed `examples/3d/occlusion_culling/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.405913`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=153.681`; the run log had no validation or fatal output. A full reference scan after this refresh reports 137 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact; the example's GPU indirect readback and true occlusion-culling source parity gap is tracked by ISS-086.
- 2026-06-13: Refreshed `examples/3d/parenting/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.393348`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=151.548`; the run log had no validation or fatal output. A full reference scan after this refresh reports 136 fully transparent references and 3 near-black references.
- 2026-06-13: Refreshed `examples/3d/pbr/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.412227`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=155.153`; the run log had no validation or fatal output. A full reference scan after this refresh reports 135 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact; the missing Bevy UI label/source-shape parity is tracked by ISS-087.
- 2026-06-13: `examples/3d/pccm` produced a numerically nonblank but visually rejected standard capture: `alpha_mean=1`, `gray_mean=0.338644`, `size=2560x1440`, `non_black_ratio=1.0`, and `mean_luma=142.253`, but the frame contained only the default dark background instead of Bevy's visible parallax-corrected cubemap room and reflective inner cube. The generated image was discarded; the source/PBR/asset parity fix is tracked by ISS-088.
- 2026-06-13: Refreshed `examples/3d/pcss/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.545194`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=178.72`; the run log had no validation or fatal output. A full reference scan after this refresh reports 134 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact; the example's widget and PCSS source-shape parity gap is tracked by ISS-089.
- 2026-06-13: Refreshed `examples/3d/ray_casting3/reference.png` with the standard 90-frame native capture path. Sanity check: `alpha_mean=1`, `gray_mean=0.564783`, `size=2560x1440`, `non_black_ratio=1.0`, `mean_luma=180.46`; the run log had no validation or fatal output. A full reference scan after this refresh reports 133 fully transparent references and 3 near-black references. This refresh only replaces the unusable transparent artifact and does not claim Bevy source-level visual parity because this package is a mgstudio physics3d/rapier example without a direct same-named Bevy example owner.

## Close Notes
