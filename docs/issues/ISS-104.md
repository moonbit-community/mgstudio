# ISS-104: Port Bevy volumetric fog render pass ownership

## Metadata
- Type: task
- Status: in_progress
- Priority: 1
- Labels: pbr, volumetric-fog, render, source-parity, blocker
- Assignee: unassigned
- Created: 2026-06-14
- Updated: 2026-06-15
- External ref: none

## Description
`examples/3d/scrolling_fog` is source-shaped at the example layer, but it still
cannot render the Bevy visual result because mgstudio does not yet execute the
Bevy volumetric fog render path. Current code exposes `VolumetricFog`,
`FogVolume`, `VolumetricLight`, shader text, and several render-side helper
structs, but the render pipeline does not consume `FogVolume` as a post-main
volumetric pass. The existing renderer only folds camera `VolumetricFog` into
ordinary distance-fog-like mesh shading.

Bevy's owner is `bevy/crates/bevy_pbr/src/volumetric_fog/{mod.rs,render.rs}`:
`VolumetricFogPlugin` syncs `FogVolume`, extracts `VolumetricFog`/`FogVolume`/
`VolumetricLight`, prepares specialized pipelines, prepares per-view depth
textures, writes `VolumetricFogUniformBuffer`, and runs `volumetric_fog` after
`Core3dSystems::MainPass` and before early postprocess.

## Design
Port the Bevy source owner directly instead of approximating the effect as
distance fog:

- install a Bevy-shaped `VolumetricFogPlugin` render-app owner;
- extract/sync `VolumetricFog`, `FogVolume`, and `VolumetricLight` into render
  state with the same cleanup behavior when no volumetric lights are present;
- prepare `FogAssets` plane/cube meshes and `VolumetricFogPipeline`
  specializations for textureless and 3D-density-textured volumes;
- prepare `ViewVolumetricFog`, `ViewVolumetricFogPipelines`, and
  `VolumetricFogUniformBuffer` from camera/fog-volume transforms;
- prepare depth textures required by the pass before the Core3d depth texture
  owner;
- execute the volumetric fog pass after the main 3D pass and before bloom,
  tonemapping, and other early postprocess stages;
- keep `volumes/fog_noise.ktx2` as the actual density texture for the scrolling
  example.

Do not replace this with a fullscreen overlay, distance-fog color tweak, or
screenshot-only fallback. If a backend primitive is missing, record that as a
new precise blocker with the upstream API requirement.

## Acceptance Criteria
- [ ] `pbr/volumetric_fog` contains the Bevy source-owner systems and data flow,
      not only helper structs.
- [ ] `FogVolume` density texture and density offset affect rendered pixels.
- [ ] Directional lights with `VolumetricLight` and shadow maps produce visible
      light shafts through the volumetric fog pass.
- [ ] `examples/3d/scrolling_fog` capture shows the Bevy-shaped fog/light
      scattering signal instead of only pillar/ground distance fog.
- [ ] Focused PBR volumetric fog tests and the standard scrolling_fog native
      capture pass without validation, missing-resource, unsupported, or fatal
      output.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-016, ISS-019, ISS-021, ISS-091, ISS-108, ISS-109
- Discovered from: ISS-091

## Notes
- 2026-06-14: Created after auditing `bevy/examples/3d/scrolling_fog.rs`,
  `bevy/crates/bevy_pbr/src/volumetric_fog/mod.rs`, and
  `bevy/crates/bevy_pbr/src/volumetric_fog/render.rs`. Mgstudio currently has
  component APIs plus helper structs, but no real render execute pass for
  volumetric fog volumes.
- 2026-06-14: Started source-owner inventory against Bevy
  `volumetric_fog/render.rs`. First implementation slice targets
  `prepare_volumetric_fog_uniforms`: local/view transform composition, camera
  inside-volume classification, far-plane extraction, clip/UVW matrix
  preparation, density texture metadata, and uniform-buffer offsets. Remaining
  owner symbols still include extraction cleanup, render-pass execution, depth
  texture shader usage, pipeline specialization through the renderer, and
  plugin schedule placement.
- 2026-06-14: Ported the Bevy `VolumetricFogPlugin` asset setup slice:
  `FogAssets` now holds concrete plane/cube mesh handles, plugin install
  creates the Bevy fog hull plane/cube meshes from `Plane3d::new(Vec3::Z,
  Vec2::ONE)` and `Cuboid::new(1, 1, 1)`, and extract-count bookkeeping now
  clears view/volume counts when no `VolumetricLight` exists. Remaining
  extraction work must still use a render-world equivalent before it may remove
  render-side components; it must not delete user components from the main
  world.
- 2026-06-14: Ported the Bevy pipeline-specialization data shape that can be
  represented before the real GPU pipeline is wired: `VolumetricFogPipelineKey`
  now carries the mesh view layout key, target texture format, and density
  texture flag; specialization derives the Bevy shader defs
  `SHADOW_FILTER_METHOD_HARDWARE_2X2`, `MULTISAMPLED`, `ATMOSPHERE`, and
  `DENSITY_TEXTURE`, the bind-group layout key, back-face culling, and the
  additive alpha blend state. Remaining work must still create/cache the actual
  render pipeline and bind group layouts in the renderer.
- 2026-06-14: Wired the PBR camera render path to prepare Bevy-shaped
  volumetric fog per-view data after the main 3D pass: it counts
  `VolumetricLight`, collects only `FogVolume` entities with `GlobalTransform`,
  preserves Bevy's "clear when no volumetric lights exist" behavior through the
  plugin state counters, and prepares `VolumetricFogUniformBuffer` entries from
  the camera `clip_from_view` and `world_from_view` matrices. The GPU hull draw
  pass is still not executed yet, so this slice intentionally does not claim
  visual parity.
- 2026-06-14: Moved the prepared data one step closer to Bevy render-world
  ownership: `ViewVolumetricFog` and `ViewVolumetricFogPipelines` are now ECS
  components, `VolumetricFogUniformBuffer` is a render-world resource, the
  buffer is cleared once before the camera render loop, and each camera writes
  its `ViewVolumetricFog` component on the render view entity. The next required
  slice is still the actual renderer/backend hull draw pass using those
  components and uniform offsets.
- 2026-06-15: Fixed the prepared uniform data shape to match Bevy's dynamic
  uniform-buffer contract: `ViewFogVolume.uniform_buffer_offset` now stores a
  byte offset, not a logical array index, and `VolumetricFogUniform` can pack
  itself into the WGSL host layout used by `volumetric_fog.wgsl`. The next
  required slice is still the renderer/backend pass that uploads this packed
  buffer and binds it with the recorded dynamic offsets.
- 2026-06-15: Added the renderer-side upload boundary for the packed
  `VolumetricFogUniformBuffer`: PBR now forwards the Bevy-shaped packed bytes
  into a dedicated mesh3d volumetric-fog uniform buffer owned by the GPU
  backend. Remaining work still needs the Bevy `VolumetricFogPipeline` bind
  group layout, pipeline specialization, depth texture binding, and hull draw
  pass.
- 2026-06-15: Added the Bevy `VolumetricFogBindGroupLayoutKey` group-1 layout
  combinations in the renderer backend: dynamic uniform at binding 0,
  depth texture at binding 1, and optional 3D density texture/sampler at
  bindings 2/3. Remaining work still needs the actual specialized render
  pipelines, per-volume bind groups, and hull draw pass.
- 2026-06-15: Aligned `VolumetricFogPipeline`'s shader label with the embedded
  WGSL owner path `pbr/volumetric_fog/volumetric_fog.wgsl`, matching the
  package prebuild registry used as mgstudio's equivalent of Bevy's
  `load_embedded_asset!("volumetric_fog.wgsl")`. Remaining work still needs the
  renderer pipeline specialization to consume that shader path and draw fog
  hulls.
- 2026-06-15: Added renderer-side volumetric-fog render pipeline
  specialization/cache keyed by target color format, MSAA, density texture, and
  atmosphere flags. The descriptor now uses the Bevy-shaped group layout
  `[mesh view, volumetric view]`, position-only vertex input at location 0,
  back-face culling, no depth attachment, and additive alpha blending. PBR now
  asks the renderer to prepare textureless/textured variants for each prepared
  volumetric-fog view. `examples/3d/scrolling_fog` native capture completes
  without pipeline validation failure, but visual parity is still incomplete
  because per-volume bind groups and fog hull drawing are not executed yet.
- 2026-06-15: Prepared the Bevy `FogAssets` hull meshes for the render path by
  uploading the plugin-owned plane and cube mesh handles through the existing
  `Render3dMeshRuntimeState` mesh upload cache. Prepared volumetric-fog output
  now carries the GPU mesh ids needed by the next draw-pass slice to choose
  Bevy's `exterior ? cube : plane` hull mesh per `ViewFogVolume`.
- 2026-06-15: Added the renderer-side per-volume volumetric fog draw pass.
  PBR now converts each `ViewFogVolume` into a host draw entry, keeps the
  Bevy-shaped main-pass ordering so the mesh view bind group state remains
  available, and the renderer ends the main pass before opening Bevy's
  color-load `volumetric lighting pass`. The pass binds group 0 to the mesh
  view bind group, group 1 to the dynamic volumetric uniform buffer plus depth
  texture and optional 3D density texture/sampler, then draws the Bevy
  `exterior ? cube : plane` hull mesh. `moon -C mgstudio-engine check --release
  pbr`, `moon -C mgstudio-engine test --release pbr`, and the standard
  `examples/3d/scrolling_fog` native capture all complete. Visual parity is
  still incomplete: the capture now renders a nonblank scene, but it still
  lacks the expected Bevy light-shaft/fog-scattering signal.
- 2026-06-15: Fixed the first visible volumetric-fog rendering path for
  `examples/3d/scrolling_fog`. Three Bevy-parity bugs were found and corrected:
  uniforms now pack `Mat4` values in WGSL column order instead of the old
  row-major host order; `FogAssets` hull meshes are read from the source world
  and uploaded into the render-world mesh runtime instead of being looked up in
  the wrong world; and the renderer pipeline now keys Bevy's view layout and
  hull vertex stride into the volumetric-fog pipeline cache, matching Bevy's
  `VolumetricFogPipelineKey { mesh_pipeline_view_key, vertex_buffer_layout,
  target_format, has_density_texture }`. The pass also treats a missing
  density image like Bevy's `RenderAssets<Image>::get(id) == None`, avoiding a
  fallback 2D texture in the 3D density binding. The standard scrolling-fog
  capture now completes with an empty run log and visible fog/light scattering;
  remaining parity work should compare density-texture sampling and the exact
  Bevy visual response before closing this issue.
- 2026-06-15: Restored Bevy's scrolling density texture path for
  `examples/3d/scrolling_fog`: `volumes/fog_noise.ktx2` is a 64x64x64
  `VK_FORMAT_R32_SFLOAT` KTX2 volume texture, but the local KTX2 decoder only
  knew the tonemapping LUT formats and therefore treated this Bevy asset as an
  unsupported 3D texture. Added the Vulkan-format mapping to WebGPU
  `R32Float`, covered it with a Bevy asset regression test, reran the standard
  native capture with an empty run log, and updated the example reference image.
- 2026-06-15: Verified that `FogVolume.density_texture_offset` now affects
  rendered pixels: captures at frame 30 and frame 180 both complete with empty
  run logs and produce a large ImageMagick pixel diff across the fog volume.
  This satisfies the density-texture movement part of the issue, but exact
  Bevy visual-response parity still requires a side-by-side Bevy capture audit.
- 2026-06-15: Tightened the local render-world data-flow order to match Bevy's
  prepare-before-pass shape: `ViewVolumetricFog` is now written to the render
  view immediately after volumetric-fog preparation and before GPU upload,
  pipeline preparation, and draw. The standard scrolling-fog capture remains
  valid with an empty run log.
- 2026-06-15: Aligned pipeline preparation with Bevy's per-view specialization
  lifecycle. Bevy always specializes both textureless and textured
  `ViewVolumetricFogPipelines` for each volumetric-fog view; mgstudio had been
  preparing only the currently observed volume variant. The local prepare step
  now requests both variants whenever a volumetric-fog view and fog hull mesh
  are available, with a white-box regression test.
- 2026-06-15: Installed Rust `1.95.0` locally and ran the pinned Bevy
  `scrolling_fog` example with `bevy_ci_testing`. The Bevy CI screenshot path
  produced `screenshot-90.png`, but the image was all black; a direct
  `screencapture` showed the desktop was on the lock/hidden-window screen, so
  this environment cannot currently provide a trustworthy Bevy visual baseline
  through window screenshots. Do not use that black Bevy capture as parity
  evidence.
- 2026-06-15: Ported Bevy's
  `prepare_view_depth_textures_for_volumetric_fog` semantics into mgstudio's
  extract/render payload boundary. Instead of mutating the source-world
  `Camera3d`, camera collection now computes the render-side depth texture
  usage from the extracted `Camera3d` clone, adds `TEXTURE_BINDING` whenever
  any `VolumetricFog` view exists, and passes the resulting usage bits through
  the PBR prepass/motion-vector depth target cache into renderer depth texture
  creation. This keeps Bevy's render-world clone behavior and avoids a local
  source-world side effect. Verified with `moon -C mgstudio-engine check
  --release pbr`, `moon -C mgstudio-engine check --release render`,
  `moon -C mgstudio-engine test --release pbr/volumetric_fog_execute_wbtest.mbt`,
  and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_depth_usage.png`.
- 2026-06-15: Moved the local volumetric-fog draw call to match Bevy's
  `Core3d` ordering more closely. Mgstudio had been drawing volumetric fog
  before the later transmission and transparent/wireframe passes inside
  `render3d_execute_camera_main_pass`; Bevy schedules `volumetric_fog` after
  `Core3dSystems::MainPass` and before `Core3dSystems::EarlyPostProcess`, so
  the pass now runs after the local transmission and transparent/wireframe
  main-pass fragments. Verified with `moon -C mgstudio-engine check --release
  pbr` and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_pass_order.png`. The capture remains valid but appears
  darker than the previous local ordering; exact visual acceptance still needs
  a trustworthy Bevy side-by-side capture.
- 2026-06-15: Aligned the PBR-side `VolumetricFogPipelineKey` data shape with
  Bevy's source owner by adding the hull `MeshVertexBufferLayoutRef` to the key.
  `volumetric_fog_specialize_pipeline` now derives the position-only vertex
  buffer layout from that mesh layout at shader location 0, matching Bevy's
  `key.vertex_buffer_layout.get_layout(&[Mesh::ATTRIBUTE_POSITION.at_shader_location(0)])`
  step instead of treating target format and density texture as the whole key.
  Verified with `moon -C mgstudio-engine test --release pbr/volumetric_fog`,
  `moon -C mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`,
  and `moon -C mgstudio-engine info`.
- 2026-06-15: Aligned the `ViewVolumetricFogPipelines` data flow with Bevy's
  `prepare_volumetric_fog_pipelines` system. The renderer-side volumetric-fog
  pipeline cache now assigns a real opaque pipeline id to each prepared or
  failed specialization entry, `prepare_mesh3d_volumetric_fog_pipeline` returns
  that id, and PBR writes `ViewVolumetricFogPipelines { textureless, textured }`
  onto the render view only after both Bevy variants have been specialized.
  This removes the previous local gap where mgstudio warmed the backend cache
  but never inserted the per-view pipeline-id component used by the Bevy draw
  query. Verified with `moon -C mgstudio-engine check --release render`,
  `moon -C mgstudio-engine check --release pbr`, `moon -C mgstudio-engine test
  --release pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and the standard
  `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_scrolling_fog_pipeline_ids.png`.
- 2026-06-15: Made the local draw path consume those Bevy-shaped pipeline ids
  instead of re-specializing by key inside the renderer draw pass. PBR now reads
  `ViewVolumetricFog` and `ViewVolumetricFogPipelines` from the render view
  entity before issuing the host draw command, and the renderer selects the
  textureless or textured pipeline by id from the cache for each volume, matching
  Bevy's `pipeline_cache.get_render_pipeline(view_pipelines.*)` shape more
  closely. Verified with `moon -C mgstudio-engine check --release render`,
  `moon -C mgstudio-engine check --release pbr`, `moon -C mgstudio-engine test
  --release pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and the standard
  `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_scrolling_fog_draw_by_pipeline_id.png`.
- 2026-06-15: Aligned the light-facing volumetric component mutation helpers
  with Bevy's plain-data component semantics. `VolumetricFog` and `FogVolume`
  fields in Bevy are ordinary `f32` fields; mgstudio's convenience setters had
  been clamping ambient intensity, jitter, density, absorption, scattering,
  scattering asymmetry, and light intensity before the values reached the PBR
  uniform path. Those setters now preserve the caller-provided values, with a
  light package regression test covering the negative/out-of-range cases.
  Verified with `moon -C mgstudio-engine test --release light`,
  `moon -C mgstudio-engine test --release pbr/volumetric_fog`,
  `moon -C mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and the standard `examples/3d/scrolling_fog`
  native capture at `/tmp/mgstudio_iss104_volumetric_plain_fields.png`.
- 2026-06-15: Kept `VolumetricFogBindGroupLayoutKey` as a private
  specialization helper, matching Bevy's private bitflags owner instead of
  exposing the key, constructor, or layout-index helper through
  `pkg.generated.mbti`. Mgstudio does not currently have a PBR-side bind group
  layout resource equivalent to Bevy's `init_volumetric_fog_pipeline`, so the
  Bevy debug-description helper is not represented as public PBR API. Verified
  with `moon -C mgstudio-engine test --release pbr/volumetric_fog`, `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`, and
  `moon -C mgstudio-engine info`.
- 2026-06-15: Removed the unused PBR-side
  `VolumetricFogPipeline`/`VolumetricFogPipelineKey`/
  `VolumetricFogPipelineDescriptor` facade and its descriptor-only tests. The
  real mgstudio implementation fragment for Bevy's
  `SpecializedRenderPipeline for VolumetricFogPipeline` is the native renderer
  owner in `render/renderer/mesh3d_pipeline_build.mbt`, where shader defines,
  view layout selection, vertex layout, target format, culling, multisampling,
  and blend state are used to build actual `wgpu` render pipelines. Verified
  with `moon -C mgstudio-engine test --release pbr/volumetric_fog`, `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`, and
  `moon -C mgstudio-engine info`.
- 2026-06-15: Matched Bevy's private helper boundary for
  `UVW_FROM_LOCAL`, `camera_is_inside_fog_volume`, `get_far_planes`, and
  `calculate_fog_volume_clip_from_local_transforms`: mgstudio no longer exposes
  `volumetric_fog_uvw_from_local`, `volumetric_fog_camera_is_inside`,
  `volumetric_fog_far_planes`, or `volumetric_fog_clip_from_local` through
  `pkg.generated.mbti`. The helper regression coverage moved to white-box
  tests while public tests continue to exercise the Bevy-shaped uniform prepare
  path. Verified with `moon -C mgstudio-engine test --release
  pbr/volumetric_fog`, `moon -C mgstudio-engine check --release pbr`, `moon -C
  mgstudio-engine fmt`, and `moon -C mgstudio-engine info`.
- 2026-06-15: Tightened the `VolumetricFogUniform` public shape to match Bevy's
  GPU-uniform boundary. Bevy exposes the uniform type but keeps its fields and
  buffer mutation path internal to `prepare_volumetric_fog_uniforms`; mgstudio
  now exposes `VolumetricFogUniform` only as an opaque type in
  `pkg.generated.mbti`, hides the uniform-buffer storage field, and removes the
  public constructor, raw `bytes`, `push`, and `uniform_at` APIs. Public tests
  now exercise the prepare path, while white-box tests validate byte layout
  without creating an external construction API. Verified with `moon -C
  mgstudio-engine test --release pbr/volumetric_fog`, `moon -C mgstudio-engine
  test --release pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`, and
  `moon -C mgstudio-engine info`.
- 2026-06-15: Aligned the renderer-side volumetric fog draw control flow with
  Bevy's `volumetric_fog` pass resource gates. The backend now resolves both
  textureless and textured pipelines plus the dynamic uniform buffer before the
  per-volume loop, and returns for a missing selected fog hull mesh instead of
  continuing to later volumes. This matches Bevy's early `return` for missing
  render pipelines, uniform-buffer binding, or `RenderMesh`, while preserving
  the existing per-volume skip for missing allocator-like slices and missing
  bind group construction. Verified with `moon -C mgstudio-engine check
  --release render`, `moon -C mgstudio-engine check --release pbr`,
  `moon -C mgstudio-engine test --release pbr/volumetric_fog
  pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine fmt`,
  `moon -C mgstudio-engine info`, and the standard `examples/3d/scrolling_fog`
  native capture at `/tmp/mgstudio_iss104_pass_missing_resource_control_flow.png`.
- 2026-06-15: Removed local public convenience facades from the volumetric fog
  render owner: `VolumetricFogUniform::with_step_count`,
  `VolumetricFogUniform::with_density`,
  `VolumetricFogUniform::with_jitter_strength`,
  `volumetric_fog_default_settings`, `fog_volume_default_settings`,
  `volumetric_fog_with_step_count`, and `fog_volume_with_density`. Bevy's
  `VolumetricFogUniform` is render-internal data populated from
  `VolumetricFog` and `FogVolume`; exposing alternate public setters in
  mgstudio reintroduced local sanitize semantics and made tests bypass the
  Bevy-shaped prepare path. Tests now generate uniforms through
  `prepare_volumetric_fog_view_uniforms`. Verified with `moon -C
  mgstudio-engine test --release pbr/volumetric_fog`, `moon -C mgstudio-engine
  check --release pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_remove_uniform_facades.png`.
- 2026-06-15: Removed the two remaining unused local default facades from the
  volumetric fog plugin owner: `pbr_volumetric_fog_default` and
  `pbr_fog_volume_default`. Bevy's `mod.rs` exposes the plugin and `FogAssets`
  owner, while default values live on `VolumetricFog`/`FogVolume` themselves.
  Verified with `moon -C mgstudio-engine test --release pbr/volumetric_fog`,
  `moon -C mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`,
  and `moon -C mgstudio-engine info`.
- 2026-06-15: Aligned hull mesh preparation with Bevy's per-view pipeline
  specialization lifecycle. Bevy's `prepare_volumetric_fog_pipelines` only
  requires a `VolumetricFog` view, the plane mesh, and a view layout key; it
  does not depend on any current `FogVolume`. Mgstudio now prepares the fog
  hull mesh owner whenever volumetric lights and a volumetric-fog view exist,
  so the later `ViewVolumetricFogPipelines` component can be prepared even for
  an empty `ViewVolumetricFog`. Verified with `moon -C mgstudio-engine test
  --release pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine
  check --release pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_prepare_pipelines_without_volumes.png`.
- 2026-06-15: Matched Bevy's `prepare_volumetric_fog_uniforms`
  `local_from_world_matrices` data flow. Mgstudio now precomputes each
  `FogVolume` inverse transform once while collecting render fog volumes, then
  passes that `local_from_world` matrix into per-view uniform preparation
  instead of recomputing `inverse()` inside every camera prepare call. Verified
  with `moon -C mgstudio-engine test --release pbr/volumetric_fog
  pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine check
  --release pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_local_from_world.png`.
- 2026-06-15: Removed public construction/mutation convenience APIs for
  `ViewFogVolume` and `ViewVolumetricFog`. Bevy creates these render-world
  view records inside `prepare_volumetric_fog_uniforms`; mgstudio now keeps
  `ViewVolumetricFog::ViewVolumetricFog` and `ViewVolumetricFog::push`
  package-private, deletes the unused public `ViewFogVolume` constructor, and
  makes inactive execute state store `ViewVolumetricFog?` instead of fabricating
  an empty component. Verified with `moon -C mgstudio-engine test --release
  pbr/volumetric_fog pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`, `moon
  -C mgstudio-engine info`, and the standard `examples/3d/scrolling_fog`
  native capture at `/tmp/mgstudio_iss104_view_data_api.png`.
- 2026-06-15: Matched Bevy's private `ViewFogVolume` and
  `ViewVolumetricFog` field boundary. Mgstudio no longer exposes mutable view
  volume storage or per-volume render fields through `pkg.generated.mbti`; the
  cross-package execute fragment consumes a read-only `ArrayView` through
  `ViewVolumetricFog::volumes()` and uses `ViewFogVolume` accessor methods for
  renderer handoff. Verified with `moon -C mgstudio-engine test --release
  pbr/volumetric_fog pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`, `moon
  -C mgstudio-engine info`, and the standard `examples/3d/scrolling_fog`
  native capture at `/tmp/mgstudio_iss104_private_view_fields.png`.
- 2026-06-15: Matched Bevy's public-field construction shape for
  `ViewVolumetricFogPipelines`. Mgstudio changed the component to `pub(all)`
  and now constructs it with a direct struct literal in the prepare pipeline
  path, deleting the local default constructor and
  `view_volumetric_fog_pipelines` helper facade. Verified with `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine test
  --release pbr/volumetric_fog pbr/volumetric_fog_execute_wbtest.mbt`, `moon
  -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and the standard
  `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_pipeline_struct_literal.png`.
- 2026-06-15: Removed the unused local default constructor for
  `PreparedVolumetricFogView`. Bevy's `prepare_volumetric_fog_uniforms` is a
  system that writes the render-world component directly; mgstudio still needs
  a return payload for its package split, but it no longer exposes a zero-value
  construction facade for that payload. Verified with `moon -C mgstudio-engine
  test --release pbr/volumetric_fog pbr/volumetric_fog_execute_wbtest.mbt`,
  `moon -C mgstudio-engine check --release pbr`, `moon -C mgstudio-engine
  fmt`, `moon -C mgstudio-engine info`, and the standard
  `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_remove_prepared_view_default.png`.
- 2026-06-15: Matched Bevy's private `FogAssets` resource boundary. Mgstudio
  no longer exposes `FogAssets` fields, `FogAssets::new`, or
  `volumetric_fog_plugin_state_with_assets` through the public interface; the
  plugin remains the owner that creates the plane/cube hull meshes, and the
  cross-package render execute fragment only reads mesh handles through
  accessors. Verified with `moon -C mgstudio-engine test --release
  pbr/volumetric_fog pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`, `moon
  -C mgstudio-engine info`, and the standard `examples/3d/scrolling_fog`
  native capture at `/tmp/mgstudio_iss104_private_fog_assets.png`.
- 2026-06-15: Removed dead extracted-count state from the local volumetric fog
  plugin resource. Bevy does not keep a public `VolumetricFogPluginState`
  count cache; mgstudio's count fields were only written by the render path and
  never read in production. The plugin state now only stores `FogAssets`, and
  the render path keeps the existing timeline counters for actual diagnostics.
  Verified with `moon -C mgstudio-engine test --release pbr/volumetric_fog
  pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine check
  --release pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_remove_dead_extract_counts.png`.
- 2026-06-15: Replaced the remaining local `VolumetricFogPluginState` wrapper
  with Bevy-shaped `FogAssets` resource ownership. Bevy inserts `FogAssets`
  directly into the render app; mgstudio now makes `FogAssets` an
  `@app.Resource`, inserts it directly during plugin installation, and reads it
  directly when uploading fog hull meshes. Verified with `moon -C
  mgstudio-engine test --release pbr/volumetric_fog
  pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine check
  --release pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_fog_assets_resource.png`.
- 2026-06-15: Tightened the package boundary for `VolumetricFogMat4`.
  Mgstudio still needs this opaque helper to pass the projection matrix across
  the local `pbr`/`pbr/volumetric_fog` package split, but Bevy does not expose
  a volumetric-fog-specific matrix API. The type now has private fields and
  only exposes the cross-package `from_cols` constructor; affine conversion,
  multiplication, and WGSL column packing remain package-private render-owner
  helpers. Verified with `moon -C mgstudio-engine test --release
  pbr/volumetric_fog pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C
  mgstudio-engine check --release pbr`, `moon -C mgstudio-engine fmt`, `moon
  -C mgstudio-engine info`, and the standard `examples/3d/scrolling_fog`
  native capture at `/tmp/mgstudio_iss104_private_mat4_helpers.png`.
- 2026-06-15: Removed the public `volumetric_fog_create_fog_assets` helper.
  Bevy creates the hull mesh handles inside `VolumetricFogPlugin::build` and
  inserts a `FogAssets` resource; it does not expose a separate asset factory
  API. Mgstudio now keeps the factory package-private while preserving the same
  plugin install behavior. Verified with `moon -C mgstudio-engine test
  --release pbr/volumetric_fog`, `moon -C mgstudio-engine check --release
  pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and the
  standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_private_asset_creation.png`.
- 2026-06-15: Removed the local `PreparedVolumetricFogView` return wrapper.
  Bevy's prepare system writes the dynamic uniform buffer and produces a
  `ViewVolumetricFog` component; it does not create an extra public payload
  type. Mgstudio's package-split helper now returns `ViewVolumetricFog`
  directly, while uniform counts are read from `VolumetricFogUniformBuffer`.
  Verified with `moon -C mgstudio-engine test --release pbr/volumetric_fog
  pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine check
  --release pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_return_view_fog.png`.
- 2026-06-15: Removed the public volumetric-fog plugin install facades. Bevy's
  public API is the `VolumetricFogPlugin` type; installation details live in
  `Plugin::build`. Mgstudio no longer exports `volumetric_fog_plugin_install`,
  and keeps the package-local install helper private for the plugin build
  implementation. Verified with `moon -C mgstudio-engine test --release
  pbr/volumetric_fog`, `moon -C mgstudio-engine check --release pbr`, `moon
  -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and the standard
  `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_private_plugin_install.png`.
- 2026-06-15: Matched Bevy's per-volume missing-hull-mesh control flow in the
  renderer volumetric fog pass. Bevy skips a fog volume when the selected hull
  mesh vertex slice is missing, but continues drawing later volumes; mgstudio
  had returned from the whole pass on a missing local GPU mesh. The renderer
  now continues to the next volume in that case while preserving Bevy's early
  return behavior for missing pipelines, uniform buffer binding, and render
  mesh assets. Verified with `moon -C mgstudio-engine check --release render`,
  `moon -C mgstudio-engine check --release pbr`, `moon -C mgstudio-engine test
  --release pbr/volumetric_fog pbr/volumetric_fog_execute_wbtest.mbt`, `moon
  -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and the standard
  `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_skip_missing_fog_mesh.png`.
- 2026-06-15: Matched Bevy's extract-gated depth texture usage semantics.
  Bevy removes render-world `VolumetricFog` views when no `VolumetricLight`
  exists, so `prepare_view_depth_textures_for_volumetric_fog` does not mark
  depth textures as shader-readable in that case. Mgstudio's camera collection
  now requires both a volumetric fog view and at least one volumetric light
  before adding `TEXTURE_BINDING` usage, with a regression test covering the
  no-light case. Verified with `moon -C mgstudio-engine test --release
  pbr/volumetric_fog_execute_wbtest.mbt`, `moon -C mgstudio-engine check
  --release pbr`, `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine
  info`, and the standard `examples/3d/scrolling_fog` native capture at
  `/tmp/mgstudio_iss104_depth_usage_requires_light.png`.

## Close Notes
