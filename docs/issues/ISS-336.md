# ISS-336: Verify fog_volumes density texture GPU readiness

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, fog-volumes, assets, gpu-texture, volumetric-fog
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-06
- External ref: none

## Description
Prove the decoded `fog_volumes` density asset reaches the render path as the
3D texture view required by Bevy's volumetric fog bind group.

## Design
Inspect mgstudio render-asset preparation, texture view dimension, sampler
selection, and volumetric-fog bind group readiness for the density texture.
Do not substitute a generated texture or proxy geometry.

## Acceptance Criteria
- [ ] The decoded density asset is prepared as a GPU texture with the required
      3D view dimension.
- [ ] The volumetric fog pass can resolve the texture through the same
      render-asset lookup used by production.
- [ ] Focused evidence proves the textured fog-volume branch is reachable for
      this asset.
- [ ] Any backend limitation is recorded as a precise blocker.

## Relationships
- Depends on: ISS-335
- Parent: ISS-327
- Related: ISS-104, ISS-109, ISS-267, ISS-328
- Discovered from: ISS-327

## Notes
- 2026-07-05: Split from ISS-327.

## Close Notes
- 2026-07-06: Added a `render/texture` whitebox test that loads Bevy's
  `volumes/bunny.ktx2` through `AssetServer::load`, the registered production
  asset texture backend, and the renderer GPU texture store. The test verifies
  the loaded image is 64x64, uses `TEXTURE_FORMAT_R32_FLOAT`, owns a
  `TEXTURE_DIMENSION_3D` GPU texture with a view, and is present in the same
  renderer texture store that the volumetric-fog density texture lookup uses to
  select Bevy's textured branch. The local anchors match Bevy's
  `bevy_image/src/ktx2.rs` 3D
  `TextureDimension::D3` / `TextureViewDimension::D3` setup and
  `bevy_pbr/src/volumetric_fog/render.rs` `RenderAssets<Image>::get(id)`
  textured-pipeline selection. Validation: `MOONBIT_NEW_NATIVE=1 moon -C
  mgstudio-engine test render/texture/tonemapping_lut_wbtest.mbt --target
  native --release`; `MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine test
  render/renderer/gpu_wbtest.mbt --target native --release --filter
  '*volumetric fog density texture lookup*'`.
