# ISS-419: Release renderer registries and prove complete backend teardown

## Metadata
- Type: task
- Status: closed
- Priority: 0
- Labels: render, native, wgpu, resource-lifecycle, shutdown, tests
- Assignee: unassigned
- Created: 2026-07-12
- Updated: 2026-07-12
- External ref: none

## Description
Texture, sampler, mesh, and mesh-allocation registries are the final backend-owned resource roots. Surface ownership is closed with the frame/surface owner area in ISS-415. Complete teardown also needs an end-to-end resource-count oracle rather than only area-level tests.

## Design
Release every remaining registry entry according to its explicit ownership contract, release queue/device/adapter/instance last, and add a full create/use/shutdown GlobalReport regression that audits every WGPU resource category exposed by the report.

## Acceptance Criteria
- [x] Texture, sampler, mesh, and mesh-allocation registries release every owned reference.
- [x] Queue, device, adapter, and instance are released last in dependency order.
- [x] A full create/use/shutdown test returns all observable WGPU live-handle categories to baseline.
- [x] Repository-native checks and the complete renderer test suite pass.

## Relationships
- Depends on: ISS-415, ISS-416, ISS-417, ISS-418
- Parent: ISS-413
- Related: ISS-409
- Discovered from: ISS-413

## Notes
- Mesh allocator slab buffers are owned by `mesh_allocator_buffers`; mesh entries borrow slab buffers and own only buffers whose slab id is negative.

## Close Notes
- Normalized allocator slab ownership to remove per-mesh reference leaks during allocation and reallocation.
- Added final registry teardown for textures, prepass depth textures, samplers, meshes, and allocator buffers.
- Added a complete native WGPU report regression covering every exposed live-handle category; native check passes with 44 pre-existing warnings and the renderer suite passes 124/124 tests.
