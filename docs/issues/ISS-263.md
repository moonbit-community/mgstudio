# ISS-263: Port volumetric_fog camera skybox and fog setup

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, camera, source-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
Bevy's `volumetric_fog` example camera owns `Camera3d`, a precise transform,
`Tonemapping::TonyMcMapface`, `Bloom`, `Skybox`, and camera
`VolumetricFog { ambient_intensity: 0.0, ..default() }`.

## Design
Port this camera/render component set directly after ISS-262 restores the
authored glTF scene. Do not substitute unrelated fog, exposure, or sky
settings to make the screenshot plausible.

## Acceptance Criteria
- [x] Camera transform and look-at target match Bevy.
- [x] Tonemapping, bloom, skybox image, and skybox brightness match Bevy.
- [x] Camera `VolumetricFog` values match Bevy's default-plus-override shape.
- [x] A focused check proves the camera fog component reaches the render path.

## Relationships
- Depends on: ISS-262
- Parent: ISS-222
- Related: ISS-104, ISS-108
- Discovered from: ISS-222

## Notes
- 2026-07-05: Split from ISS-222 so camera/render settings can be audited
  separately from scene loading and light entities.

## Close Notes
Closed on 2026-07-05.

Bevy source owner audited:

- `bevy/examples/3d/volumetric_fog.rs::setup` camera spawn:
  `Camera3d::default()`,
  `Transform::from_xyz(-1.7, 1.5, 4.5).looking_at(vec3(-1.5, 1.7, 3.5), Vec3::Y)`,
  `Tonemapping::TonyMcMapface`, `Bloom::default()`, `Skybox` with
  `environment_maps/pisa_specular_rgb9e5_zstd.ktx2` at brightness `1000.0`,
  and `VolumetricFog { ambient_intensity: 0.0, ..default() }`.

Mgstudio change:

- `setup_camera` now inserts `@pbr.VolumetricFog::VolumetricFog().with_ambient_intensity(0.0F)`
  on the camera in addition to the existing transform, TonyMcMapface, Bloom,
  and Skybox setup.
- Added `main_wbtest.mbt` coverage for the camera transform, tonemapping,
  Bloom presence, Skybox image/brightness, and VolumetricFog default-plus-
  ambient override.

Verification:

- `moon -C mgstudio-engine test examples/3d/volumetric_fog --target native --release --filter '*camera skybox and volumetric fog setup matches Bevy*'`
  passed: 1 test.
- `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/volumetric_fog /tmp/mgstudio_iss263_volumetric_fog_camera_fog.png`
  completed.
- `/tmp/mgstudio_iss263_volumetric_fog_camera_fog.run.log` is empty.
- The capture is a 2560x1440 RGBA PNG, nonblank, with SHA256
  `0c18343613566057fae885abc0e6ace4b9281ad55fddb19d5aa444418f49b185`.
- `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
  `moon -C mgstudio-engine check --target native` completed.
