# ISS-305: Repair confirmed volumetric_fog scene render-space divergence group

## Metadata
- Type: epic
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, render, source-parity, visual-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-06
- External ref: none

## Description
Once ISS-300 through ISS-304 identify the source-owner boundary responsible for
the near wall-like frame dominance, finish the remaining render-space diagnosis
and implement the actual Bevy-shaped fix. This issue groups the remaining
smaller source-owner checks; it is not itself an executable implementation
slice.

Current evidence has ruled out the GLB Cube being missing from extraction,
phase queue, or material depth-state selection. The latest mgstudio capture is
still byte-identical to the earlier failed near-grey frame and still diverges
from the checked-in reference that shows the Pisa skybox/street signal.

## Design
Drive the repair through leaf issues:

- ISS-346 records the current visual delta and artifact evidence.
- ISS-347 proves the skybox render-entity extraction/prepare/draw path.
- ISS-348 audits the main opaque pass ordering and depth/load-state boundary.
- ISS-349 groups the skybox shader, cubemap, exposure, and bind-group sampling
  audit; executable work is split into ISS-351 through ISS-354.
- ISS-350 applies the source-shaped fix once the responsible owner is proven.

The fix must not be a screenshot workaround, camera adjustment, asset edit,
heuristic culling rule, or local compatibility path.

If one of the audit issues already performed the complete source-shaped fix,
close this issue by linking that issue's evidence and running the same
validation here.

## Acceptance Criteria
- [x] ISS-346 through ISS-350 are closed or replaced by narrower closed child
      issues.
- [x] The final fix cites the exact Bevy source owner and mgstudio fragment.
- [x] Focused tests or source-level audits prove the repaired boundary.
- [x] `moon -C mgstudio-engine test examples/3d/volumetric_fog --target native
      --release` passes if the example package remains testable.
- [x] `moon -C mgstudio-engine check --target native`, `moon -C
      mgstudio-engine fmt`, and `moon -C mgstudio-engine info` are run after
      any code changes.
- [x] ISS-298 is ready to perform the final visual capture.

## Relationships
- Depends on: ISS-350
- Parent: ISS-299
- Related: ISS-298, ISS-300, ISS-301, ISS-302, ISS-303, ISS-304
- Discovered from: ISS-299

## Notes
- 2026-07-05: Split from ISS-299 as the repair issue after source-owner
  diagnosis has narrowed the root cause.
- 2026-07-05: Split the remaining repair into ISS-346 through ISS-350 so the
  skybox, main opaque pass, shader/cubemap sampling, and final fix can be
  verified independently.
- 2026-07-05: Split ISS-349 further into ISS-351 through ISS-354 so shader
  anchors, cubemap texture views, brightness/view inputs, and the final
  skybox root-cause conclusion are independently executable.
- 2026-07-05: Split ISS-350 into ISS-355 through ISS-360 so the final repair
  path has separate evidence capture, pass-input audit, hull/uniform-offset
  audit, shader/pipeline audit, implementation, and visual acceptance leaves.
- 2026-07-06: ISS-350 is closed. The final fix is recorded in ISS-393 and the
  accepted capture/reference refresh is recorded in ISS-224 and ISS-298.

## Close Notes
Closed on 2026-07-06.

The confirmed scene/render-space divergence group is complete. The final local
owner was the GLB normal-map tangent basis rather than camera transform, scene
visibility, volumetric uniforms, or skybox/pass ordering. ISS-393 records the
source owner and validation commands; ISS-224 records the checked-in reference
refresh.
