# ISS-083: Port lines custom material and line strip source parity

## Metadata
- Type: bug
- Status: in_progress
- Priority: 1
- Labels: examples, render, material, lines, wgsl, mesh, source-parity
- Assignee: unassigned
- Created: 2026-06-13
- Updated: 2026-06-30
- External ref: none

## Description
`examples/3d/lines` must be source-level faithful to Bevy's
`examples/3d/lines.rs`. Bevy's example demonstrates a custom 3D line material:
`MaterialPlugin::<LineMaterial>`, a `LineMaterial` asset with a uniform color,
`shaders/line_material.wgsl`, `PrimitiveTopology::LineList`, and
`PrimitiveTopology::LineStrip` with explicit indices containing `u16::MAX` as a
primitive restart marker.

The mesh data-model side is complete: line-list topology, line-strip topology,
U16 indices, primitive restart, and the final strip-index pipeline descriptor
are now represented without the earlier wgpu_mbt blocker. The native line
example now renders through mgstudio's typed custom-material PBR integration
path.

Current whitebox coverage proves the isolated typed material path can:

- extract and prepare `MaterialPlugin[M]` / `MeshMaterial3dTyped[M]` assets;
- create render material instances and material bind-group data;
- apply the Bevy-shaped base material specialization hook;
- mark entities dirty when a typed material is first prepared;
- prewarm a mesh pipeline and queue a draw item with a pipeline id.

The native `examples/3d/lines` capture now shows the expected green line list
and blue indexed line strip, with an empty WebGPU validation log. The source of
the previous blank capture was local mgstudio divergence in the custom-material
host materialization path: the renderer re-derived shader definitions from a
fixed forward-PBR default set instead of respecting the Bevy-shaped
`RenderPipelineDescriptor` produced by material specialization, and the example
also used a local UV-bearing line mesh instead of Bevy's position-only
`LineList` / `LineStrip` mesh shape.

The current fix keeps `shaders/line_material.wgsl` byte-for-byte equal to
Bevy's asset, removes the old unlit `StandardMaterial` substitute, keeps the
line meshes position-only, and lets custom pipeline preprocessing use the
descriptor shader definitions produced from the actual mesh layout. The
remaining local work in this issue is the broader typed-material dirty tracking
case for asset changes after first preparation; the line example itself is no
longer visually blocked.

Do not restore the old unlit `StandardMaterial` approximation, add a
lines-specific path, or classify this as an external blocker without new
evidence. The correct fix is to make the Bevy-shaped typed custom material path
render through the normal PBR pass.

## Design
Port Bevy `examples/3d/lines.rs` as a source-shaped custom material example
and repair the engine path required by that source shape:

- add a real `LineMaterial` asset owner with color uniform data;
- register the equivalent of `MaterialPlugin<LineMaterial>`;
- embed and bind `shaders/line_material.wgsl`;
- preserve `LineList` and `LineStrip` mesh construction, including primitive
  topology and the `u16::MAX` primitive restart index;
- remove the unlit `StandardMaterial` substitute from this example once the
  custom material path exists.

Do not solve this by restoring `StandardMaterial`, special-casing the lines
example, or adding a fallback path. The correct fix is to make the typed custom
material path render through the same Bevy-shaped ownership model that the
example exercises.

Source-shape anchors:

- `MaterialPlugin::<LineMaterial>`: partial. The mgstudio API surface exists
  and typed material extraction / preparation is covered by whitebox tests,
  including Bevy-shaped base material specialization and first-prepared
  material dirty propagation. A prepared typed material also prewarms and queues
  through the source-shaped PBR helper path in whitebox coverage. The native
  line example now renders through this path.
- `LineMaterial { color: LinearRgba }` plus `#[uniform(0)]`: complete for the
  current line example. The
  source-shaped asset and uniform bytes can be represented, and the prepared
  material binding is created and used by the native draw.
- `shaders/line_material.wgsl`: complete for the current line example. The Bevy
  shader asset exists byte-for-byte and can
  be selected by `LineMaterial`; a prepared material can specialize a mesh
  pipeline and queue a draw item with the material bind group layout in
  whitebox tests. The full native frame now proves collection, queueing, custom
  pipeline materialization, material bind-group use, and draw execution.
- `PrimitiveTopology::LineList`: complete.
- `PrimitiveTopology::LineStrip`: complete. Mgstudio preserves the indexed U16
  primitive restart mesh shape and now writes the strip index format into the
  final GPU pipeline descriptor.
- `Indices::U16([0, 1, u16::MAX, 2, 3, 4])`: complete at the mesh asset data
  model, validation, and native rendering level.

Native-frame anchors:

- render-app extraction and material instance collection: complete for the
  current failure boundary.
- render-app prewarm and phase queueing from collected entities: complete for
  the current failure boundary.
- host renderer material descriptor registration: complete for the current line
  example.
- custom pipeline creation for `pbr/render/mesh.wgsl` plus
  `shaders/line_material.wgsl`: complete for the current line example. The
  renderer now respects descriptor shader definitions from material
  specialization instead of reintroducing fixed forward-PBR vertex attribute
  defines.
- material bind-group creation and draw-command binding: complete for the
  current line example.

## Acceptance Criteria
- [x] Every source-shape anchor in Bevy `examples/3d/lines.rs` is classified as
      complete, partial, facade, blocked, or intentionally excluded.
- [x] `LineMaterial` is a real custom material with the same shader and uniform
      ownership shape as Bevy's example, and it renders through the normal PBR
      pass.
- [x] First prepared typed material availability marks the
      affected mesh entities for specialization in the same source-shaped role
      as Bevy's material extraction/render-asset path.
- [ ] Typed material asset changes after first preparation mark affected mesh
      entities for specialization without per-frame dirty pollution.
- [x] A prepared typed custom material prewarms a mesh pipeline and queues a
      mesh draw item with a prepared custom-material pipeline in focused
      render-world whitebox coverage.
- [x] Full native-frame diagnostics prove whether the typed custom-material path
      is lost before the helper path or after queueing in host renderer
      materialization.
- [x] The custom-material mesh pipeline preprocesses/composes Bevy's PBR vertex
      shader source fully before creating a shader module.
- [x] The custom-material mesh pipeline derives Bevy-shaped mesh-layout shader
      definitions and vertex outputs so `VertexOutput` imported by custom
      material fragments matches the compiled vertex stage for line meshes.
- [x] The proven native-frame loss point has a Bevy-shaped fix with no fallback
      and no lines-specific special case.
- [x] The line-list and line-strip meshes preserve Bevy's primitive topology and
      primitive-restart indexed strip behavior.
- [x] The standard-material substitute is removed from the example.
- [x] `examples/3d/lines/reference.png` is regenerated from a nonblank native
      capture after the source parity fix.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-060, ISS-019, ISS-021
- Discovered from: ISS-060

## Notes
- 2026-06-13: Initial source-parity gap: the example used an unlit
  `StandardMaterial` approximation instead of Bevy's custom `LineMaterial`.
- 2026-06-14: Mesh data-model parity landed for U16 indexed line strips and
  primitive restart, but final pipeline descriptor support still depended on
  wgpu_mbt strip-index-format support.
- 2026-06-30: ISS-099 closed the strip-index-format blocker. The mesh/topology
  side is no longer blocked externally.
- 2026-06-30: Typed custom material whitebox coverage now proves extraction,
  preparation, base specialization, first-prepared dirty propagation, pipeline
  prewarm, and draw-item queueing in focused render-world setup.
- 2026-06-30: Native visual evidence is still failing: a 90-frame capture of
  `examples/3d/lines` is a single background color. The attempted timeline trace
  produced no trace file, so the next step must add or use a reliable
  native-frame diagnostic and then remove any temporary instrumentation before
  commit.
- 2026-06-30: Native-frame diagnostics now show that two line mesh phase items
  are queued. The previous shader-module creation failure caused by unprocessed
  Bevy WGSL directives is fixed in the current local WIP. The remaining native
  failure is later: wgpu rejects custom render pipeline creation because the
  fragment shader expects `VertexOutput` location 1, while the compiled line
  vertex stage does not provide it.
- 2026-06-30: Fixed the native line rendering boundary. The line meshes now use
  Bevy's position-only shape, custom material shader preprocessing uses the
  specialized descriptor shader definitions instead of fixed forward-PBR
  attribute defines, and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/lines /tmp/mgstudio_iss083_lines_after_layout_defs.png`
  produced a nonblank green/blue line capture with an empty run log. The
  resulting image was saved to `examples/3d/lines/reference.png`.

## Close Notes
