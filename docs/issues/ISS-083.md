# ISS-083: Port lines custom material and line strip source parity

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: examples, render, material, lines, wgsl, mesh, source-parity
- Assignee: unassigned
- Created: 2026-06-13
- Updated: 2026-06-30
- External ref: https://github.com/moonbit-community/wgpu-mbt/issues/28

## Description
`examples/3d/lines/reference.png` can now serve as a nontransparent visual
baseline, but the example is not source-level faithful to Bevy. Bevy's
`examples/3d/lines.rs` demonstrates a custom 3D line material:
`MaterialPlugin::<LineMaterial>`, a `LineMaterial` asset with a uniform color,
`shaders/line_material.wgsl`, `PrimitiveTopology::LineList`, and
`PrimitiveTopology::LineStrip` with explicit indices containing `u16::MAX` as a
primitive restart marker.

Mgstudio's current example uses unlit `StandardMaterial` as a substitute and
does not preserve Bevy's custom material owner or indexed primitive-restart line
strip shape. That is a local approximation and must not be counted as render or
example source parity.

## Design
Port Bevy `examples/3d/lines.rs` as a source-shaped custom material example:

- add a real `LineMaterial` asset owner with color uniform data;
- register the equivalent of `MaterialPlugin<LineMaterial>`;
- embed and bind `shaders/line_material.wgsl`;
- preserve `LineList` and `LineStrip` mesh construction, including primitive
  topology and the `u16::MAX` primitive restart index;
- remove the unlit `StandardMaterial` substitute from this example once the
  custom material path exists.

If mgstudio lacks a required generic material plugin or custom material
pipeline API, record the exact engine/API blocker instead of keeping the
standard-material approximation.

Source-shape anchors:

- `MaterialPlugin::<LineMaterial>`: partial. Mgstudio still lacks a Bevy-shaped
  generic 3D `MaterialPlugin<M>` registration surface for a standalone
  uniform-only material asset.
- `LineMaterial { color: LinearRgba }` plus `#[uniform(0)]`: partial. The
  example still uses `StandardMaterial` until the custom material owner exists.
- `shaders/line_material.wgsl`: partial. The shader source is identified from
  Bevy, but not yet wired through a real `LineMaterial` bind group.
- `PrimitiveTopology::LineList`: complete.
- `PrimitiveTopology::LineStrip`: complete. Mgstudio preserves the indexed U16
  primitive restart mesh shape and now writes the strip index format into the
  final GPU pipeline descriptor.
- `Indices::U16([0, 1, u16::MAX, 2, 3, 4])`: complete at the mesh asset data
  model, validation, and native rendering level.

## Acceptance Criteria
- [x] Every source-shape anchor in Bevy `examples/3d/lines.rs` is classified as
      complete, partial, facade, blocked, or intentionally excluded.
- [ ] `LineMaterial` is a real custom material with the same shader and uniform
      ownership shape as Bevy's example.
- [x] The line-list and line-strip meshes preserve Bevy's primitive topology and
      primitive-restart indexed strip behavior.
- [ ] The standard-material substitute is removed from the example or replaced
      by a precise blocker note.
- [ ] `examples/3d/lines/reference.png` remains visually valid after the source
      parity fix.

## Relationships
- Depends on: ISS-099
- Parent: ISS-012
- Related: ISS-060, ISS-019, ISS-021
- Discovered from: ISS-060

## Notes
- 2026-06-13: Discovered while refreshing `examples/3d/lines/reference.png`.
  The refreshed image is visually usable, but the source path still uses
  unlit `StandardMaterial` instead of Bevy's custom `LineMaterial` and omits the
  primitive-restart indexed line-strip shape.
- 2026-06-14: Fixed the local mesh data-model part: 3D mesh geometry now
  preserves index format, `BaseMeshPipelineKey` carries strip index format,
  render `PrimitiveState` has a `strip_index_format` field, and the lines
  example uses Bevy's five-point indexed U16 line strip with `0xffff` primitive
  restart. Full render parity remains blocked by wgpu_mbt#28 because the
  builder cannot set `WGPUPrimitiveState.stripIndexFormat`.
- 2026-06-30: ISS-099 is closed. Native capture of `examples/3d/lines` renders
  both the line list and indexed line strip with a clean WebGPU validation log.
  The remaining source-parity gap in this issue is the local `StandardMaterial`
  substitute for Bevy's `LineMaterial` custom material owner and WGSL binding
  shape.

## Close Notes
