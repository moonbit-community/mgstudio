# ISS-393: Prove volumetric_fog scene render-space owner after final visual gate

## Metadata
- Type: bug
- Status: open
- Priority: 1
- Labels: examples, pbr, volumetric-fog, gltf, render-space, source-parity, visual-parity
- Assignee: unassigned
- Created: 2026-07-05
- Updated: 2026-07-05
- External ref: none

## Description
The final `examples/3d/volumetric_fog` visual gate still captures a frame
dominated by a camera-adjacent wall or surface after ISS-372 through ISS-375
ruled out volumetric uniforms, light/shadow numeric inputs, and post-pass
composition. Identify the exact remaining Bevy source owner and repair the
mgstudio fragment source-faithfully.

## Design
Start from the current failing capture, not from screenshots alone. Compare the
Bevy `examples/3d/volumetric_fog` scene spawn, glTF scene import, node
transform, primitive/camera-space render extraction, culling, and material
paths against the corresponding mgstudio fragments. Classify every source-shape
anchor touched by the failing frame as complete, facade, blocked, or partial.

Do not tune the camera, edit the asset, alter reference images, add local
fallbacks, or introduce screenshot-specific code. If the exact Bevy behavior
cannot be represented because of a dependency or MoonBit language limitation,
record a separate blocker and stop instead of approximating.

## Acceptance Criteria
- [ ] The Bevy source owner for the current camera-adjacent surface symptom is
      identified with source anchors.
- [ ] The corresponding mgstudio fragments are audited and classified.
- [ ] The repair, if local, preserves Bevy source data flow and cites both
      owners.
- [ ] `moon -C mgstudio-engine test examples/3d/volumetric_fog --target native
      --release` passes after the repair.
- [ ] `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine info`, and
      `moon -C mgstudio-engine check --target native` pass after the repair.
- [ ] A fresh standard native capture no longer matches the failed
      `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`
      frame and is close enough for ISS-360 to rerun final comparison.

## Relationships
- Depends on: ISS-359
- Parent: ISS-350
- Related: ISS-298, ISS-299, ISS-305, ISS-360
- Discovered from: ISS-360

## Notes
- 2026-07-05: Created from the failed ISS-360 final visual gate. Current
  mgstudio capture command:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/volumetric_fog /tmp/mgstudio_iss360_volumetric_fog.png`.
  Capture path: `/tmp/mgstudio_iss360_volumetric_fog.png`; run log:
  `/tmp/mgstudio_iss360_volumetric_fog.run.log`, size `0`. Screenshot SHA-256:
  `b4126f6fa35fbc071059de4d4f78c91289b16a0aa142df31b7d240ce7a692bb4`, size
  `2560x1440`. Sanity passed with mean luma `155.6260607440679` and center
  mean luma `120.44125530774618`.
- 2026-07-05: Comparison against
  `mgstudio-engine/examples/3d/volumetric_fog/reference.png` failed with
  `mean_abs_rgb=115.59255271629051` and
  `changed_ratio=0.9992385525173612`. Comparison against
  `/tmp/bevy_iss371_volumetric_fog_content.png` failed with
  `mean_abs_rgb=112.38912642867477` and
  `changed_ratio=0.9823152669270834`.
- 2026-07-05: Human-visible symptom: the frame shows UI text, a large
  near-white/gray camera-adjacent surface, and ceiling-slot geometry, while the
  checked-in reference and Bevy baseline show the intended alley/room
  composition. This points to a scene render-space/import/extraction boundary,
  not to the volumetric-fog pass inputs already ruled out by ISS-372 through
  ISS-375.
- 2026-07-05: Audited `bevy_light/src/cluster/assign.rs` against
  `mgstudio-engine/light/cluster/assign.mbt`. The mgstudio fragment still used a
  projected-AABB cluster fill for CPU assignment instead of Bevy's iterative
  sphere refinement and spot-cone culling. This was repaired source-faithfully
  and covered by a spot-cone cluster regression test. The standard
  `examples/3d/volumetric_fog` capture remained byte-identical to the prior
  failed frame (`mean_abs_rgb=0.0` versus
  `/tmp/mgstudio_iss360_volumetric_fog.png`), so clustered light assignment is
  now classified as repaired but ruled out as the immediate owner of this
  symptom.
- 2026-07-05: Audited the camera main-pass light-probe binding setup against
  Bevy's render-world lighting data flow. `prepare_light_probe_bindings` was
  rebuilding and uploading cluster buffers from the source world after
  `render3d_begin_pass_3d` had already prepared render-world cluster buffers.
  That overwrite path had no Bevy source-owner equivalent and was removed; light
  probe binding setup now only uploads light-probe resources. Validation passed:
  `moon -C mgstudio-engine fmt`, `moon -C mgstudio-engine check --target
  native`, `moon -C mgstudio-engine test examples/3d/volumetric_fog --target
  native --release`, and the standard capture command. The fresh capture
  `/tmp/mgstudio_iss393_cluster_overwrite_fix.png` remained byte-identical to
  the prior failed frame (`mean_abs_rgb=0.0` versus
  `/tmp/mgstudio_iss360_volumetric_fog.png`), so this source-parity cleanup is
  repaired but ruled out as the immediate visual owner. The next owner to audit
  is material pipeline selection and draw-path shader/layout binding.

## Close Notes
