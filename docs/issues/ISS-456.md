# ISS-456: Migrate trace-proven dense query hotspots to table batches

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: ecs, performance, query, batch, visibility, transform, sprite
- Assignee: codex
- Created: 2026-07-15
- Updated: 2026-07-15
- External ref: none

## Description

Apply the retained `ISS-449` dense table-batch API to the remaining
performance-sensitive query call sites. A call site is eligible only after a
native trace shows material cost and the batch traversal can preserve its exact
query data, filter, change-detection, deferred-command, and source-owner
semantics.

## Migration Matrix

| Order | Call site | Discovery trace | Current batch fit | Validation example | State |
| ---: | --- | ---: | --- | --- | --- |
| 0 | `visibility::reset_view_visibility_system` | `10.539-10.685 ms/frame` | Dense write plus archetypal `Without`; supported | `stress_tests/many_sprites` | Retained by ISS-449: `8.180 ms/frame` |
| 1 | `visibility::mark_newly_hidden_entities_invisible_system` | `2.223 ms/frame` | Dense write plus archetypal `Without`; supported | `stress_tests/many_cubes` | Ready |
| 2 | `camera::check_visibility_visible_entities_system` | `52.169-56.8 ms/frame` | Dense required/optional/presence columns plus archetypal `Without`; supported, but requires a source-shaped multi-column port | `stress_tests/many_foxes` | Pending source audit |
| 3 | `sprite_render::render_extract_system_ecs` sprite loop | `10.865-13.4 ms/frame` | Dense reads are supported; current per-entity optional lookups must first be compared with Bevy extract-query ownership | `stress_tests/many_sprite_meshes` | Pending source audit |
| 4 | `picking::picking_sprite2d_hit_test_system` collection path | `9.017-11.36 ms/frame` | Unknown until the helper queries and sorting boundary are isolated | `picking/sprite_picking` | Pending isolation |
| 5 | `sprite::sprite_calculate_bounds_2d` | `12.534-14.2 ms/frame` | Dense reads are supported, but the current manual changed-entity sets and per-entity presence lookups need a Bevy source audit | `stress_tests/many_animated_sprites` | Pending source audit |
| 6 | `transform::mark_dirty_trees_system` | `14.813-16.1 ms/frame` | Unsupported: per-row `Changed`/`Added`/`Or` filter | `stress_tests/transform_hierarchy` | Blocked on exact tick-filter batch design |
| 7 | `transform::sync_simple_transforms` | `7.723-8.47 ms/frame` | Unsupported: per-row `Changed`/`Added`/`Or` filter plus orphan point queries | `stress_tests/bevymark_3d` | Blocked on exact tick-filter batch design |
| 8 | `sprite::compute_slices_on_sprite_change_system` | `2.738-2.9 ms/frame` | Unsupported: per-row `Changed<Sprite>` filter and deferred commands | `2d/sprite_slice` | Blocked on exact tick-filter batch design |

The discovery numbers come from the retained `many_sprites` traces and only
prioritize investigation. Every row must establish a fresh before/after trace
using its own validation example before it can be retained.

## Acceptance Criteria

- [ ] Every matrix row has a retained, rejected, or externally blocked result.
- [ ] Every retained migration preserves the pinned Bevy query/filter and
      change-detection semantics.
- [ ] Every retained migration runs one focused test at a time with
      `MOONBIT_NEW_NATIVE=1`.
- [ ] Every retained migration uses the row's distinct example for its runtime
      gate and performs a valid visual comparison when the example renders.
- [ ] Every retained or rejected call-site experiment is committed separately.
- [ ] Unsupported per-row filters are not weakened or evaluated as table-wide
      predicates.

## Relationships
- Depends on: ISS-449
- Parent: ISS-444
- Related: ISS-001, ISS-035, ISS-154, ISS-450, ISS-453
- Discovered from: ISS-449

## Notes

- 2026-07-15: Created after the corrected first production migration improved
  `reset_view_visibility_system` by `22.38-23.44%` and steady frame interval by
  `10.97-13.42%`. The table deliberately separates directly supported dense
  queries from tick-filtered queries that require an exact API extension.

## Close Notes
