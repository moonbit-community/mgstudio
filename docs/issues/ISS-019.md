# ISS-019: Wire PBR prepass, deferred, shadow, main, and transmission phases to real runtime commands

## Metadata
- Type: task
- Status: open
- Priority: 0
- Labels: parity, pbr, prepass, deferred, shadows, transmission, render-commands
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-07-01
- External ref: moonbit-community/wgpu-mbt#24 (resolved by wgpu_mbt@0.14.0)

## Description
Mgstudio has made strong progress on descriptor-level PBR parity: material
pipeline specialization, mesh bind layouts, morph/skinning/lightmap bindings,
prepass descriptors, motion-vector descriptors, deferred planning, and phase
classification exist. The remaining gap is runtime consumption: the Bevy-shaped
decisions must drive actual prepass, deferred, shadow, main, and transmission
phase commands with visual parity.

## Design
Port Bevy's runtime phase ownership rather than adding local draw-path patches.
Use the current Bevy `material.rs`, `prepass/mod.rs`, `render/mesh.rs`,
`render/light.rs`, `transmission`, and shadow pass sources to connect phase
items, draw functions, bind groups, pipelines, and render graph nodes.

## Acceptance Criteria
- [ ] Opaque, alpha-mask, transparent, transmissive, deferred, prepass, shadow, and motion-vector phase items are consumed by Bevy-equivalent draw commands.
- [ ] Prepass and deferred-prepass runtime scheduling matches Bevy's early/late gating and material eligibility rules.
- [ ] Shadow passes use Bevy-equivalent material and depth-only draw function selection.
- [ ] Transmission phase ordering and target usage match Bevy's screen-space transmission owner model.
- [ ] Runtime commands consume the already ported descriptor layouts rather than rebuilding local fallback descriptors.
- [ ] Motion blur, contact shadows, lightmap, deferred rendering, transmission, and meshlet-related examples have parity screenshots or specific blocker issues.

## Relationships
- Depends on: ISS-013, ISS-016, ISS-018
- Parent: ISS-012
- Related: ISS-015, ISS-020, ISS-021
- Discovered from: none

## Notes
- 2026-06-02: This is the runtime half of the roadmap item `pbr/render/mesh-bind-groups`.
- 2026-06-03: `examples/3d/motion_blur --release` exposed two concrete runtime gaps after the RenderGraph schedule split: mesh3d frame view bind groups did not consume the ported prepass binding 20 descriptor, and the motion blur bind group layout diverged from Bevy by declaring binding 2 as `Depth` instead of unfilterable float texture sampling.
- 2026-06-03: The first local renderer gap was confirmed and fixed at the bind-group propagation layer: Bevy-shaped prepass bindings 21/22/23 must consume the actual normal, motion-vector, and deferred prepass texture views instead of failing as unsupported frame-view bindings.
- 2026-06-03: `examples/3d/motion_blur --release` then exposed the deeper Bevy parity requirement: native Bevy keeps default MSAA enabled, creates depth and motion-vector prepass textures with `sample_count = msaa.samples()`, specializes the prepass pipeline with the same multisample count, and uses a sparse color attachment list shaped like `[None, motion_vectors]` so WGSL `@location(1)` remains unchanged.
- 2026-06-03: `wgpu_mbt@0.14.0` resolves the external descriptor gap from `moonbit-community/wgpu-mbt#24`: mgstudio can now set render-pipeline multisample counts, build sparse prepass color attachments shaped like `[None, motion_vectors]`, and mark unused color targets instead of faking write-masked formats.
- 2026-06-03: `wgpu_mbt@0.14.1` resolves the macOS NSView surface acquire issue tracked in `moonbit-community/wgpu-mbt#23`: `examples/3d/motion_blur --release` can enqueue, complete, and read a surface screenshot capture instead of looping on `Occluded`.
- 2026-06-03: The remaining motion-blur gate failure is local runtime ownership, not the previous `wgpu_mbt` API gaps: the latest run captured a nonblank surface image, but motion blur execution still reports `skip: no settings buffer`, so settings/resource installation and postprocess runtime wiring remain incomplete.
- 2026-06-27: Ported the Bevy `bevy_pbr/src/cluster/mod.rs` CPU clustering render-world consumption slice: `ExtractedClusterableObjects`, CPU cluster extraction, CPU cluster preparation, and `ViewClusterBindings` writes are now wired into PBR render extract/prepare scheduling. `RenderViewLightProbes` now carries the Bevy-required `MainEntity -> render light probe index` map needed for reflection and irradiance volume cluster records.
- 2026-06-27: Added the shadow material pipeline cache maintenance API needed
  by Bevy `bevy_pbr/src/render/light.rs::specialize_shadows`: per-view
  `contains`/`clear` and whole-cache stale retained-view expiration. This is a
  real prerequisite for porting the shadow specialize/queue systems, but those
  systems are still not complete.
- 2026-06-27: Ported the core Bevy
  `check_views_lights_need_specialization` key/dirty behavior for one retained
  shadow view: views without a shadow phase are ignored, directional lights use
  depth-prepass plus orthographic and unclipped-depth key bits, point/spot
  lights use depth-prepass plus perspective key bits, and changed keys mark the
  retained view dirty. The remaining work is the ECS query wrapper and render
  schedule registration against real shadow phases.
- 2026-06-27: Ported the Bevy
  `specialize_shadows` dirty-cache maintenance slice for
  `SpecializedShadowMaterialPipelineCache`: a dirty retained shadow view clears
  its view cache, while changed/removed renderables remove only their cached
  shadow pipelines from that view. The remaining shadow specialize work is the
  work-item collection, material/mesh readiness handling, prepass
  specialization call, and queue consumption.
- 2026-06-27: Ported the Bevy
  `specialize_shadows` pending-readiness checks for missing material instances
  and missing render mesh queue data. These now enqueue the
  `(render_entity, visible_entity)` pair into `PendingShadowQueues.current_frame`
  instead of using the material queue path. The prepared-material availability
  check, shadow material filtering, shadow-caster filtering, work-item assembly,
  prepass specialization call, and queue consumption remain to be ported.
- 2026-06-27: Ported the Bevy `RenderMeshInstanceFlags::SHADOW_CASTER`
  filtering slice for shadow specialization readiness. Meshes with material and
  mesh queue data but without the shadow-caster flag are skipped without being
  placed in `PendingShadowQueues`. Prepared-material availability,
  `material.properties.shadows_enabled`, work-item assembly, prepass
  specialization call, and queue consumption remain to be ported.
- 2026-06-27: Ported the Bevy prepared-material availability and
  `material.properties.shadows_enabled` shadow specialization gates. The
  readiness helper now resolves prepared material state through
  `MaterialRuntimeRegistry`, queues missing prepared materials in
  `PendingShadowQueues.current_frame`, and skips materials with shadows disabled
  without adding pending work. Remaining local work: shadow work-item assembly,
  render-mesh asset lookup, prepass specialization call, specialized shadow
  cache insertion, and pending queue consumption.
- 2026-06-27: Ported the Bevy prepared render-mesh asset gate for shadow
  specialization readiness. Entities with material instance, mesh queue data,
  prepared material, and shadow-caster flags now still skip specialization when
  `RenderMeshAssets` lacks the queued `mesh_asset_id`, without adding pending
  work. Remaining local work: shadow work-item key/layout assembly, lightmap and
  `MAY_DISCARD` key bits, prepass specialization call, specialized shadow cache
  insertion, and pending queue consumption.
- 2026-06-27: Ported the Bevy shadow specialization work-item assembly slice:
  the light key, prepared render-mesh key bits, `RenderLightmaps` membership,
  and alpha-mode `MAY_DISCARD` rules now produce the shadow mesh key used with
  the prepared mesh layout and material properties. The following slice ports
  the prepass specialize/cache insertion path.
- 2026-06-27: Ported the Bevy shadow work-item specialization slice: a ready
  work item now carries the material type tag, skips missing prepass specialize
  hooks, selects `ShadowsDepthOnlyDrawFunction` only for opaque depth-only
  cases, selects `ShadowsDrawFunction` when the prepass must read material data,
  calls the material prepass specialize hook, and inserts the resulting
  pipeline/draw-function pair into `SpecializedShadowMaterialPipelineCache`.
- 2026-06-27: Ported the Bevy `work_items.drain(..)` and expired shadow-view
  cache-retain slice for shadow specialization. The batch helper now consumes
  all assembled work items, clears the work-item array, and retains only live
  shadow views in `SpecializedShadowMaterialPipelineCache`. Remaining local
  work: assemble/drain real work-item queues from the full `specialize_shadows`
  query loop and register the full system in the render schedule.
- 2026-06-27: Ported the Bevy shadow specialization candidate source slice:
  `render3d_shadow_specialization_candidates` now reads the light-owned
  `RenderShadowMapVisibleEntities` subview, dirty specialization state, and
  `PendingShadowQueues.prev_frame()` with the same shape as Bevy
  `specialize_shadows`. Remaining local work: connect this candidate source to
  the real shadow-view/light-key loop and render schedule.
- 2026-06-28: Ported the Bevy `get_shadow_map_visible_entities`
  owner/subview key mapping for directional, point, and spot light shadow views
  onto `Render3dLightEntity`, and added the light-entity candidate wrapper that
  the real `specialize_shadows` loop can call.
- 2026-06-28: Ported the Bevy `queue_shadows` data-decision slice: cache
  lookup, dirty dequeue/queue iteration, shadow-caster and render-layer
  filtering, pending shadow queue writes, material draw-function selection, and
  material bind-group index selection now produce Bevy-shaped queued shadow
  phase data. Remaining local work: wire the data into the real `Shadow`
  binned render phase owner and replace the older immediate shadow item path.
- 2026-06-28: Added Bevy-shaped `ShadowBatchSetKey` and `ShadowBinKey`
  equivalents for the queued shadow phase data, including the slab-indexed
  predicate and per-item batch/bin key derivation. Remaining local work: replace
  the legacy `ShadowBatchSetKey3d` immediate-render sorting path with this real
  Bevy phase key shape.
- 2026-06-28: Added a Bevy-shaped `BinnedRenderPhase<Shadow>` view-phase data
  slice with `Shadow` phase items, batch ranges, extra indices, mesh phase
  types, `remove`, `add`, and queued-data application. This connects the
  previously ported `queue_shadows` decisions to a real phase container, but
  the renderer still needs to consume this phase instead of the legacy
  immediate shadow phase view.
- 2026-06-29: Ported the Bevy `MeshPipeline::specialize` and
  `PrepassPipeline::specialize` view-projection shader-def mapping for
  `VIEW_PROJECTION_NONSTANDARD`, `VIEW_PROJECTION_PERSPECTIVE`, and
  `VIEW_PROJECTION_ORTHOGRAPHIC`. The matching `MeshPipelineKey` bits were
  already preserved, but the PBR and prepass descriptors did not consume them,
  so view transformation WGSL was compiled without the Bevy owner-selected
  projection mode.
- 2026-06-29: Ported the Bevy `MeshPipeline::specialize` and
  `PrepassPipeline::specialize` primitive-state ownership for mesh-key topology
  and strip index format into the base PBR and prepass descriptors.
  `StandardMaterial` no longer owns strip-index propagation locally; it only
  applies its material cull-mode override, matching the upstream owner split.
  Final GPU builder emission of non-`None` strip index format remains blocked
  by ISS-099 / moonbit-community/wgpu-mbt#28.
- 2026-06-29: Ported the Bevy `StandardMaterial::specialize` depth-bias
  descriptor write into mgstudio's render descriptor model:
  `StandardMaterialKey` now decodes the signed 32-bit bias constant and writes
  it to `DepthStencilState`. The backend cannot yet emit non-default depth
  bias fields because `wgpu_mbt` only exposes depth format/write/compare on its
  render pipeline builder; ISS-161 tracks that external blocker.
- 2026-06-29: Ported the Bevy `MeshPipeline::specialize` shader-def mapping
  for main PBR pass view/prepass keys: `MULTISAMPLED`, `NORMAL_PREPASS`,
  `DEPTH_PREPASS`, `MOTION_VECTOR_PREPASS`, `HAS_PREVIOUS_SKIN`,
  `HAS_PREVIOUS_MORPH`, `DEFERRED_PREPASS`, and the Bevy-gated
  `LOAD_PREPASS_NORMALS` condition for opaque single-sample normal prepass
  loads.
- 2026-06-29: Ported the Bevy `MeshPipeline::specialize` shadow-filter
  shader-def mapping for `SHADOW_FILTER_METHOD_HARDWARE_2X2`,
  `SHADOW_FILTER_METHOD_GAUSSIAN`, and `SHADOW_FILTER_METHOD_TEMPORAL`. The
  default zero-valued key now explicitly emits the hardware 2x2 shader def,
  matching Bevy rather than compiling with no shadow filtering method defined.
- 2026-06-29: Ported the remaining Bevy `MeshPipeline::specialize` main-pass
  boolean key shader defs for `MAY_DISCARD`, `VISIBILITY_RANGE_DITHER`, and
  `ATMOSPHERE`, which had only been present in prepass or view-layout paths.
- 2026-06-28: Added the Bevy-shaped
  `ViewBinnedRenderPhases<Shadow>` resource map and initialized it from the PBR
  materials startup path while preserving existing phase contents. Remaining
  local work: write real per-light queued shadow data into this resource and
  switch shadow pass execution from the legacy immediate phase view to the
  Bevy-owned phase resource.
- 2026-07-01: Removed a local silent fallback in the shadow visible-entity
  access path. Bevy `get_shadow_map_visible_entities` treats a missing
  `RenderShadowMapVisibleEntities` component or missing shadow subview as an
  invariant violation via `expect`, while a present subview with no `Mesh3d`
  class remains an empty queue. Mgstudio now matches that split for both shadow
  specialization candidate collection and shadow queue data collection.
- 2026-07-01: Removed the shadow queue wrapper's local
  `GpuPreprocessingSupport::None` fallback. Bevy `queue_shadows` requires
  `Res<GpuPreprocessingSupport>`, and mgstudio now treats the missing resource
  as a render-batching initialization error instead of silently changing the
  shadow phase batching mode.
- 2026-07-01: Fixed the render-batching initialization gap exposed by that
  invariant: `GpuPreprocessingPlugin` now installs `GpuPreprocessingSupport`
  into the render sub-app world, matching Bevy's render-app GPU resource
  ownership. Shadow phase preparation now also requires that resource instead
  of locally falling back to `GpuPreprocessingMode::None`.
- 2026-06-28: Ported the Bevy render-phase ownership boundary for queued
  shadow data: `prepare_for_new_frame` creates or clears retained shadow
  phase views, while queue application now only writes to an existing
  `ViewBinnedRenderPhases<Shadow>` view and skips missing views like Bevy's
  `queue_shadows`. Remaining local work: call this path from the real
  per-light shadow queue loop and make shadow passes consume the resource.
- 2026-06-28: Added the per-light `queue_shadows` wrapper that reads
  light-owned `RenderShadowMapVisibleEntities`, resolves the retained shadow
  subview through `Render3dLightEntity`, builds queued shadow phase data, and
  writes it into the existing `ViewBinnedRenderPhases<Shadow>` resource.
  Remaining local work: schedule this wrapper from the real render loop and
  replace the legacy immediate shadow pass consumer.
- 2026-06-28: Split Bevy's shadow retained-view identity into explicit
  visibility-subview keys and phase-view keys. `prepare_lights` parity now has
  a local helper that prepares `ViewBinnedRenderPhases<Shadow>` views using
  light-owned retained phase entities, prepares matching `PendingShadowQueues`,
  and retains only live shadow phase views. Remaining local work: feed this
  helper from the real light preparation loop and consume the resulting phases
  in shadow passes.
- 2026-06-28: Wired the shadow phase preparation helper into the current 3D
  render-camera outer loop so live point, spot, and directional shadow phase
  views are prepared once per frame across all cameras. This preserves Bevy's
  light-owned point/spot phase identity and per-camera directional cascade
  identity. Remaining local work: replace legacy immediate shadow pass
  queue/execute with the prepared `ViewBinnedRenderPhases<Shadow>` resource.
- 2026-06-28: Corrected the local tests and wrapper semantics for
  `get_shadow_map_visible_entities`: its retained-view input is the shadow
  light `ExtractedView.retained_view_entity`, not the ordinary camera retained
  view. Point/spot visibility keys are now derived from light-owned phase
  retained views, matching Bevy's shared shadow-map behavior.
- 2026-06-28: Wired current runtime shadow frustum visibility into Bevy-shaped
  `RenderShadowMapVisibleEntities` on the light owner. Point, spot, and
  directional shadow passes now publish their visible mesh pairs into the same
  light-owned subviews that `queue_shadows` reads. The light model now carries
  both the render-world light owner and the main-world light identity, matching
  Bevy's split between `LightEntity.light_entity` component ownership and
  `MainEntity` retained-view keys. Remaining local work: replace the immediate
  shadow item queue/executor with `queue_shadows` plus
  `ViewBinnedRenderPhases<Shadow>` consumption.
- 2026-06-28: Replaced the remaining immediate shadow item queue/executor path
  with the Bevy-shaped shadow phase resource. Runtime shadow passes now publish
  visible meshes, update the per-view light key, specialize dirty shadow
  materials, queue through `render3d_queue_shadow_phase_data_for_light`, and
  draw from `Render3dShadowPhasesData` phase items. The old local
  `ShadowPhaseItem3d` sorting path and eager shadow prewarm scan were removed.
  Remaining local work: audit the queued phase draw command payload against
  Bevy's `Shadow` render command tuple and remove any residual host-draw
  assumptions that belong in command data.
- 2026-06-28: Aligned shadow pass material bind-group payload selection with
  Bevy `ShadowBatchSetKey.material_bind_group_index`: depth-only shadow items
  now execute without material bind-group data, while material shadow draw
  items keep the mesh material bind-group payload. Remaining local work:
  continue auditing shadow draw command payload fields such as batch ranges,
  extra indices, indirect draw data, and mesh draw command ownership against
  Bevy `bevy_pbr/src/render/light.rs` and `bevy_pbr/src/render/mesh.rs`.
- 2026-06-28: Ported the direct `BatchableMesh` shadow phase range merge from
  Bevy `BinnedRenderPhase`: consecutive items with the same
  `ShadowBatchSetKey` and `ShadowBinKey` now extend one `Shadow` phase item's
  `batch_range` instead of always producing one item per entity. The current
  implementation still leaves multidraw-indirect `extra_index` generation and
  indirect batch-set buffer ownership as local parity gaps.
- 2026-06-28: Fixed the runtime consumer for the new batched shadow phase
  ranges: shadow pass execution now expands each `Shadow.batch_range` through
  `RenderMeshInstances.current_uniform_index`, reserves draw storage for every
  resolved instance, and uploads each resolved extracted mesh before issuing
  the local binned draw. Remaining gap: replace this host-side expansion with
  Bevy-equivalent indirect phase buffer ownership once the PBR phase batching
  resources are ported.
- 2026-06-28: Ported the Bevy
  `UnbatchableBinnedEntityIndexSet` state machine for shadow phases, including
  `NoEntities`, sparse instance ranges, sparse indirect-parameter ranges,
  dense demotion for non-contiguous or dynamic-offset cases, indexed lookup,
  and clear semantics. Remaining local work: connect this state to the
  `UnbatchableMesh` branch of `BinnedRenderPhase<Shadow>::add` and to the
  render-time unbatchable shadow item generation path.
- 2026-06-28: Connected shadow `UnbatchableMesh` phase entries to a separate
  Bevy-shaped unbatchable bin container instead of appending them to the direct
  batchable item array. `Render3dShadowViewPhaseData::items()` now materializes
  per-entity `Shadow` items from the unbatchable buffer indices with
  one-instance batch ranges, and `remove`/`prepare_for_new_frame` clear the
  separate path. Remaining local work: port `MultidrawableMesh` batch sets,
  indirect-parameter buffers, batch-set count buffers, and the Bevy
  prepare/collect phase-buffer systems.
- 2026-06-28: Ported the CPU ownership half of Bevy
  `RenderMultidrawableBatchSet<Shadow>`: shadow phase data now has
  multidrawable bins, binned mesh instance indices, CPU/GPU mirror records,
  reusable bin indices, tightly packed indirect-parameter offsets, bin removal,
  displaced-entity index repair, and offset swap-remove repair. Remaining
  local work: connect `MultidrawableMesh` entries from
  `Render3dShadowViewPhaseData::add`, generate render-time multidraw phase
  items with `IndirectParametersIndex`, and port the actual phase buffer
  prepare/collect GPU resource systems.
- 2026-06-28: Connected `Render3dShadowViewPhaseData::add` to the
  `MultidrawableMesh` branch: Culling-mode shadow entries now route into
  Bevy-shaped multidrawable batch-set containers, while `PreprocessingOnly`
  still forces multidrawable entries down to `BatchableMesh` like Bevy.
  `remove` and `prepare_for_new_frame` now clear the multidraw path as well.
  Remaining local work: produce render-time multidraw `Shadow` items with
  `IndirectParametersIndex` and port the phase buffer prepare/collect GPU
  resource systems.
- 2026-06-28: Ported the render-time materialization slice for shadow
  multidraw batch sets: `RenderMultidrawableBatchSet<Shadow>` data can now
  produce Bevy-shaped `BinnedRenderPhaseBatchSet`/`BinnedRenderPhaseBatch`
  equivalents and expand `IndirectParametersIndex` ranges with optional batch
  set indices. Remaining local work: wire these materialized items into the
  shadow executor and port Bevy's phase buffer prepare/collect GPU resource
  systems.
- 2026-06-28: Aligned runtime shadow phase preparation and queueing with
  Bevy's `GpuPreprocessingSupport` dependency: shadow phase views now preserve
  the supported preprocessing mode, and queued shadow items become
  `MultidrawableMesh` entries when culling is supported. Remaining local work:
  make the shadow executor consume those materialized multidraw items through
  Bevy-equivalent indirect phase buffer ownership.
- 2026-06-28: Connected shadow pass phase item collection to the materialized
  multidraw path. The executor now consumes direct/unbatchable shadow items and
  Bevy-shaped multidraw `Shadow` items from the same retained phase view.
  Remaining local work: replace the current host-side draw expansion with
  Bevy's indirect phase buffer prepare/collect resource systems and device
  feature-aware `multi_draw_indirect_count` handling.
- 2026-06-28: Aligned the shared GPU preprocessing buffer clear semantics with
  Bevy `clear_batched_gpu_instance_buffers`: clearing batched instance buffers
  now preserves the per-phase buffer table and clears each phase buffer for
  allocation reuse instead of deleting the phase entries. Remaining local work:
  port Bevy's per-phase prepare/collect resource exchange and indirect
  parameter buffer ownership for shadow/PBR phases.
- 2026-06-28: Ported the instance-buffer half of Bevy
  `collect_buffers_for_phase`: per-phase batched instance buffers can now be
  exchanged into the global batched instance buffer table while the previous
  global phase entry is cleared and returned to the per-phase resource for
  allocation reuse. Remaining local work: port the matching
  `PhaseIndirectParametersBuffers` exchange and the actual shadow/PBR
  prepare systems that populate these resources.
- 2026-06-28: Added Bevy-shaped shared indirect parameter buffer ownership:
  `IndirectBatchSet`, mesh-class indirect parameter buffers,
  `UntypedPhaseIndirectParametersBuffers`, `PhaseIndirectParametersBuffers`,
  the global `IndirectParametersBuffers` render resource, plugin
  initialization, allocation/batch-set counting, and per-phase collect exchange
  now exist in the shared GPU preprocessing package. Remaining local work:
  connect shadow/PBR phase preparation to populate these buffers and upload the
  GPU-side data.
- 2026-06-28: Added the Bevy-shaped combined `collect_buffers_for_phase`
  entry point over the per-phase batched instance buffers and indirect
  parameter buffers. Remaining local work: call this from shadow/PBR phase
  preparation systems once the phase-specific work-item population logic is
  ported.
- 2026-06-28: Ported the batchable-shadow slice of Bevy
  `batch_and_prepare_binned_render_phase`: `Render3dShadowViewPhaseData` can
  now populate per-view preprocess work-item buffers, allocate indirect
  parameters for batchable shadow items, and update the prepared `Shadow`
  phase item range/extra index. Remaining local work: port unbatchable and
  multidrawable phase preparation, then schedule the prepare/collect systems.
- 2026-06-28: Ported the unbatchable-shadow slice of Bevy
  `batch_and_prepare_binned_render_phase`: unbatchable shadow bins now rebuild
  their per-entity buffer indices from prepared work items, with direct output
  indices in non-indirect mode and one indirect parameter/batch set per entity
  in indirect mode. Remaining local work: port multidrawable phase preparation
  and schedule the prepare/collect systems.
- 2026-06-28: Added the shared GPU preprocessing representation needed for
  Bevy `PartialBufferVec::push_multiple_uninit`: preprocess work-item buffers
  can now reserve uninitialized indexed or non-indexed work-item slots without
  implicitly extending late occlusion-culling work-item lists. Remaining local
  work: use this in the shadow multidrawable phase preparation branch, then
  schedule the prepare/collect systems.
- 2026-06-28: Replaced the shared mesh-class indirect-parameter buffer's
  CPU-metadata counter with Bevy-shaped `IndirectParametersCpuMetadata`
  storage and indexed/non-indexed set/get accessors. The buffer still models
  indirect draw parameters and GPU metadata as uninitialized lengths, matching
  the parts Bevy writes on GPU. Remaining local work: populate this metadata
  from the shadow multidrawable phase preparation branch.
- 2026-06-28: Ported the shadow multidrawable buffer-population slice of Bevy
  `MultidrawableBatchSetPreparer::prepare_multidrawable_binned_batch_set`:
  the retained shadow multidraw bins now reserve uninitialized work-item slots,
  extend the per-phase data buffer, populate per-bin
  `IndirectParametersCpuMetadata`, reserve late occlusion-culling work-item
  slots, and return Bevy-shaped `BinnedRenderPhaseBatchSet` metadata. Remaining
  local work: store and consume those prepared batch sets through the real
  phase prepare/collect systems instead of the current host-side render item
  materialization path.
- 2026-06-28: Added the 3D GPU-preprocess payload conversion entry points for
  Bevy-shaped shared phase buffers: `PreprocessWorkItemBuffers` can now be
  converted into `GpuPreprocessWorkItemBuffers3d`, and
  `UntypedPhaseIndirectParametersBuffers` can be converted into
  `GpuPreprocessIndirectMetadata3d`. Remaining local work: replace the current
  draw-item-derived work-item and indirect-metadata builder with the collected
  phase-buffer data.
- 2026-06-28: Replaced the PBR-specific queue-entry work-item and indirect
  metadata construction with a shared phase-buffer conversion path. The
  remaining local work is to feed this path from the real Bevy
  prepare/collect phase-buffer systems instead of constructing temporary
  per-camera phase buffers from draw items.
- 2026-06-28: Added render-world resources for
  `PhaseBatchedInstanceBuffers<Shadow>` and
  `PhaseIndirectParametersBuffers<Shadow>`, plus a PBR collect wrapper that
  exchanges shadow phase buffers into the shared global preprocessing tables.
- 2026-06-28: Registered the shadow phase-buffer collect wrapper in the
  Bevy-equivalent `PrepareResourcesCollectPhaseBuffers` render set. Remaining
  local work: make shadow phase preparation populate these resources from the
  real `ViewBinnedRenderPhases<Shadow>` owner.
- 2026-06-28: Added the Bevy-shaped combined
  `batch_and_prepare_binned_render_phase` entry for
  `ViewBinnedRenderPhases<Shadow>`: shadow phase preparation now initializes
  one work-item buffer per retained shadow view and processes unbatchable,
  batchable, then multidrawable mesh classes in source order. Remaining local
  work: move shadow queueing earlier so this combined prepare entry can run
  from the scheduled `PrepareResourcesBatchPhases` system instead of the
  current execute-time shadow queue path.
- 2026-06-28: Added the render-world system wrapper that prepares shadow phase
  buffers from the real `ViewBinnedRenderPhases<Shadow>` resource into
  `PhaseBatchedInstanceBuffers<Shadow>` and
  `PhaseIndirectParametersBuffers<Shadow>`. It is intentionally not scheduled
  yet because shadow queueing is still execute-time; scheduling it now would
  prepare empty phase views.
- 2026-06-28: Added a Bevy-shaped batch queue entry over prepared shadow phase
  views. `render3d_queue_shadow_phase_data_for_views` now loops light/view
  pairs and reuses the existing `queue_shadows` data path, which gives the
  later schedule migration a single per-frame queue entry instead of the
  current per-shadow-pass execute-time call sites.
- 2026-06-28: Routed the current point, spot, and directional shadow pass
  call sites through the batch shadow phase preparation wrapper. Behavior is
  still execute-time, but the call surface is now the same per-view list shape
  needed by a future scheduled `queue_shadows` system.
- 2026-06-28: Factored the current shadow-pass visibility update plus queued
  shadow phase preparation into one per-shadow-view helper. The point, spot,
  and directional shadow pass call sites now share the same work-unit shape,
  which is the next prerequisite for moving Bevy `queue_shadows` out of pass
  execution and into render-system scheduling.
- 2026-06-28: Moved point-light cubemap shadow face queueing ahead of point
  shadow pass execution inside the current runtime path. All six point shadow
  phase views are now visibility-updated and queued before any point shadow
  face render pass consumes them, narrowing the gap toward Bevy's
  queue-before-render phase ordering.
- 2026-06-28: Applied the same local queue-before-render split to
  directional shadow cascades. Directional cascade visibility and queued phase
  data are now prepared for all cascades before any directional shadow render
  pass consumes the retained `Shadow` phase views.
- 2026-06-28: Split spot shadow preparation from spot shadow pass execution
  inside the combined directional/spot shadow path. The current runtime now
  queues directional cascades and the spot shadow phase view before executing
  those shadow render passes.
- 2026-06-28: Connected the existing Bevy-shaped shadow
  `batch_and_prepare_binned_render_phase` and `collect_buffers_for_phase`
  wrappers to the current runtime queue-before-render boundary. Point,
  directional, and spot shadow phase views now run prepare/collect after queue
  data is written and before their shadow render passes execute. This is still
  an execute-time bridge; the remaining parity work is to move the same calls
  into the render schedule once all shadow visibility/queue producers are
  scheduled.
- 2026-06-28: Lifted the shadow prepare/collect bridge from the point and
  directional/spot local paths to the camera shadow boundary. Point cubemap
  faces, directional cascades, and the spot shadow view are now all queued
  first, then the camera runs one shadow phase prepare/collect pass, then the
  retained shadow phase views render in the original pass order. This avoids
  replacing the global `Shadow` phase buffer table twice in one camera.
- 2026-06-28: Registered the shadow phase buffer prepare wrapper in the
  Bevy-equivalent `PrepareResourcesBatchPhases` render set and locked its
  order before `collect_shadow_phase_buffers` with the render schedule graph
  test. The execute-time camera bridge remains until shadow visibility and
  `queue_shadows` are also scheduled before `PrepareResourcesBatchPhases`.
- 2026-06-28: Split camera shadow handling into an explicit
  `Render3dCameraShadowPassPlan` boundary. Point, directional, and spot shadow
  paths now prepare bindings and retained shadow phase execution views into one
  camera-level plan before the phase buffer prepare/collect bridge runs.
  Remaining work is to lift this plan producer out of per-camera execution and
  make it feed a scheduled `queue_shadows` equivalent.
- 2026-06-28: Lifted camera shadow planning to the render-camera outer loop.
  Each ordered camera now gets a `Render3dCameraExecutionPlan` before camera
  execution, all shadow phase views for the ordered camera set are queued
  first, and the shadow phase buffer prepare/collect bridge runs once before
  any camera consumes its retained shadow phase views. This matches Bevy's
  frame-level phase preparation more closely, but it is still an execute-time
  bridge until the visibility and queue producers become render-schedule
  systems.
- 2026-06-28: Split the execute-time helper that updated
  `RenderShadowMapVisibleEntities` and immediately queued shadow phase data.
  Point, directional, and spot shadow pass planning now explicitly performs
  the shadow visible-entity update first, then calls the `queue_shadows`-shaped
  phase queue helper. Runtime order is unchanged, but the two Bevy source
  owners are no longer hidden behind one local combined helper.
- 2026-06-28: Moved the `queue_shadows`-shaped shadow phase queue call out of
  individual point, directional, and spot shadow pass planning. Each shadow
  pass plan now only updates `RenderShadowMapVisibleEntities` and returns its
  `Render3dQueuedShadowPhaseView`; the render-camera outer loop gathers all
  queued shadow views, queues them together, and then runs the shadow
  prepare/collect bridge once.
- 2026-06-28: Moved shadow phase queueing out of RenderGraph camera execution
  and into a dedicated `pbr.render3d.queue_shadows` render-schedule system in
  the Bevy `QueueMeshes` phase. `pbr.render3d.queue_meshes` now stores the
  queued frame, `pbr.render3d.queue_shadows` fills the per-camera execution
  plans and queues shadow phase data, and the existing scheduled shadow
  prepare/collect systems consume that queued data before RenderGraph
  execution. The old execute-time prepare/collect wrapper was deleted.
- 2026-06-28: Moved shadow phase view preparation into a scheduled
  `mgstudio_pbr::prepare_shadow_phase_views` system in `CreateViews`, ordered
  after `mgstudio_pbr::prepare_lights`. The system prepares and retains
  shadow phase views from the pending frame before `QueueMeshes`, so
  `pbr.render3d.queue_shadows` now only queues shadow phase data and fills
  per-camera execution plans.
- 2026-06-29: Split shadow material pipeline specialization out of
  `pbr.render3d.queue_shadows` into a dedicated
  `pbr.render3d.specialize_shadows` system. The new system computes the
  camera execution plans, applies dirty shadow specialization state, expires
  stale shadow pipeline cache views, and stores the shadow phase view list for
  `pbr.render3d.queue_shadows` to consume.
- 2026-06-29: Moved `pbr.render3d.specialize_shadows` into the Bevy-equivalent
  `mgstudio.render.specialize` render set. Shadow execution data is now stored
  separately from the queued frame during specialization, then combined with
  material queue data by `pbr.render3d.queue_meshes` before
  `pbr.render3d.queue_shadows` consumes the specialized shadow view list.
- 2026-06-29: Split Bevy
  `check_views_lights_need_specialization` out of shadow specialization into
  its own `pbr.render3d.check_views_lights_need_specialization` system in the
  Bevy-equivalent `mgstudio.render.specialize` set. The system now owns
  shadow light-key cache updates and dirty-view marking before
  `pbr.render3d.specialize_shadows`.
- 2026-06-29: Aligned queued shadow phase views with Bevy
  `queue_shadows` view-layer input by carrying the camera `RenderLayers` into
  `Render3dQueuedShadowPhaseView` instead of queueing shadow views with a
  missing layer filter.
- 2026-06-29: Moved `PendingShadowQueues.prepare_for_new_frame` and stale-view
  expiration out of shadow phase view preparation and into the
  `pbr.render3d.specialize_shadows` path, matching Bevy
  `specialize_shadows` ownership of pending shadow queue frame state.
- 2026-06-29: Threaded queued shadow view `RenderLayers` through the scheduled
  `pbr.render3d.queue_shadows` wrapper into the Bevy-shaped shadow phase data
  queueing path, so layer filtering now uses the view-layer input carried by
  `Render3dQueuedShadowPhaseView`.
- 2026-06-29: Matched Bevy `queue_shadows` pending-queue ownership by reading
  per-view `PendingShadowQueues` with non-creating `get`/expect semantics in
  specialization and queueing. The queue path no longer silently creates a
  missing view queue that should have been initialized by
  `pbr.render3d.specialize_shadows`.
- 2026-06-29: Updated the Bevy-shaped `queue_shadows` missing mesh/material
  branches to write directly into the already-fetched per-view
  `ViewPendingQueues`, rather than routing through a global helper that could
  create the view queue during queueing.
- 2026-06-29: Aligned `queue_shadows` early-exit order with Bevy by checking
  that the retained `Shadow` phase exists before reading the specialized
  shadow cache or per-view pending queues. Missing phases now skip without
  forcing pending queue lookup.
- 2026-06-29: Updated `specialize_shadows` missing material/mesh readiness
  branches to write directly into the prepared per-view `ViewPendingQueues`,
  matching Bevy `view_pending_shadow_queues.current_frame.insert(...)`
  ownership instead of routing through a global creating helper during
  specialization.
- 2026-06-29: Added Bevy's `specialize_shadows` cached-entry skip gate before
  material/mesh readiness checks. Cached visible entities now continue without
  re-specializing or writing pending queues, matching the
  `maybe_specialized_shadow_material_pipeline_cache.contains_key(...)` branch.
- 2026-06-29: Moved shadow specialization pipeline creation out of the
  per-candidate loop. `pbr.render3d.specialize_shadows` now collects
  `ShadowSpecializationWorkItem` data for all live shadow views, expires
  pending queues, then drains the work-item list through the existing
  Bevy-shaped apply path.
- 2026-06-29: Replaced the shadow specialization apply path's hard-coded
  unclipped-depth emulation setting with the Bevy-equivalent render-device
  `DEPTH_CLIP_CONTROL` feature check used by `PrepassPipeline`.
- 2026-06-29: Added the missing render descriptor `PrimitiveState.unclipped_depth`
  field and wired PBR prepass specialization to Bevy's
  `UNCLIPPED_DEPTH_ORTHO` native/emulation split. Native depth-clip support
  now sets descriptor primitive state; missing support forces the prepass
  fragment shader and material bind group like Bevy.
- 2026-06-29: Ported the Bevy `ColorTargetState.blend` descriptor shape into
  `render_resource`, wired pipeline-cache materialization to emit blend
  components through `wgpu_mbt`, and updated PBR `MeshPipeline::specialize`
  blend-pass ownership for alpha, premultiplied alpha, multiply,
  alpha-to-coverage, and OIT labels/shader defs/depth-write/MSAA state.
- 2026-06-29: Ported Bevy's `MeshPipelineKey::from_target_format` /
  `target_format` flow for main PBR pipelines: `render/view` now owns the
  5-bit texture-format code table, camera view keys include the extracted
  target format, meshlet material view keys retain the same target format
  input, and the PBR descriptor color target no longer hard-codes
  `RGBA8_UNORM`.
- 2026-07-01: Aligned the shadow-map visible-entity producer with Bevy
  `collect_visible_cpu_culled_entities` ordering requirements. Shadow visible
  mesh pairs are now sorted by `MainEntity` before
  `RenderVisibleEntitiesClass::update_cpu_culled_entities`, so consecutive
  shadow frames compute added/removed CPU-cull lists from a Bevy-shaped sorted
  table. Added a two-frame regression test that would previously report the
  retained smaller `MainEntity` as both added and removed after an unsorted
  first frame. Validation: `moon -C mgstudio-engine test --release pbr`
  passed 381/381; `moon -C mgstudio-engine check --target native`,
  `moon -C mgstudio-engine fmt`, and `moon -C mgstudio-engine info` passed;
  standard capture
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/shadow_biases
  /tmp/mgstudio_shadow_visible_sorted_shadow_biases.png` produced a nonblank
  2560x1440 PNG with an empty run log.
- 2026-07-01: Aligned the `queue_shadows` helper precondition order with
  Bevy: per-view specialized shadow cache and `PendingShadowQueues` are now
  resolved before the Mesh3d visible class early return, preserving the
  invariant that `specialize_shadows` created the pending queue for every
  queued shadow view. Added a regression test for an empty visible class with
  an existing cache and pending queue. Validation: `moon -C mgstudio-engine
  test --release pbr` passed 382/382; `moon -C mgstudio-engine check --target
  native`, `moon -C mgstudio-engine fmt`, and `moon -C mgstudio-engine info`
  passed; standard capture
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/shadow_biases
  /tmp/mgstudio_shadow_queue_order_shadow_biases.png` produced a nonblank
  2560x1440 PNG with an empty run log.
- 2026-07-10: The render-world `MeshAllocator` now owns the actual GPU
  vertex/index slabs consumed by main-pass indirect drawing instead of only
  publishing offsets beside legacy per-mesh buffers. This closes the
  allocator/preprocess mismatch for `examples/3d/pbr` (pixel-identical to its
  reference with GPU preprocessing enabled) and restores the complete
  `examples/3d/3d_shapes` grid. The same validation exposed the next local
  runtime phase gap: `examples/3d/transparency_3d` still differs in transparent
  direct draws and shadow output after meshes move into shared slabs. Continue
  this issue at the transparent/shadow phase consumer; no external API blocker
  was observed.
- 2026-07-10: Removed the mgstudio-only opaque/alpha-mask filter from main
  phase GPU preprocess work-item construction. Pinned Bevy
  `gpu_preprocessing::batch_and_prepare_sorted_render_phase` processes every
  sorted mesh phase item, including `Transparent3d`, and uses the render mesh
  instance input index rather than the sorted phase position. Mgstudio now
  does the same for opaque, alpha-mask, transmissive, and transparent items;
  a white-box test assigns non-phase-order uniform indices to all four classes
  and verifies both indexed and non-indexed work-item/metadata output.
  Validation: `MOONBIT_NEW_NATIVE=1 moon -C mgstudio-engine test --target
  native --release pbr/mesh_flags_wbtest.mbt` (27 passed), and GPU-preprocess
  captures of `examples/3d/pbr` and `examples/3d/3d_shapes`; `pbr` remained
  pixel-identical to its reference and the `3d_shapes` output hash was
  unchanged. The current `transparency_3d` source no longer matches its old
  checked-in reference scene, while official Bevy capture remains tracked by
  ISS-404, so that old reference was not treated as an acceptance oracle.

## Close Notes
