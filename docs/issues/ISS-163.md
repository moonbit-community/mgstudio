# ISS-163: Port Bevy-shaped optional StandardMaterial texture feature gates

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: pbr, material-bind-groups, shader-defs, source-parity, bevy-feature-gates
- Assignee: unassigned
- Created: 2026-07-01
- Updated: 2026-07-01
- External ref: none

## Description
Bevy gates several expensive `StandardMaterial` texture surfaces behind
separate Cargo features:

- `pbr_transmission_textures`
- `pbr_multi_layer_material_textures`
- `pbr_anisotropy_texture`
- `pbr_specular_textures`

Mgstudio now has an explicit `StandardMaterialTextureFeatureSet` that maps to
those Bevy feature names. The PBR mesh pipeline shader defs, StandardMaterial
layout descriptor, StandardMaterial bind-group payload, renderer-side prepared
binding creation, and specialization-time sampler-limit guard all consume that
same feature state.

The default runtime feature surface intentionally remains anisotropy-only,
matching the previously working native pipeline shape. The remaining risk is
not the core feature-state plumbing; it is the example/runtime feature-owner
route. Examples that use feature-gated texture fields must explicitly select a
matching feature set, and non-bindless devices must reject unsupported
sampler-count combinations before GPU pipeline creation.

During `ISS-155`, unconditionally enabling transmission, multi-layer, and
specular texture shader defs and expanding the non-bindless StandardMaterial
layout to bindings 0..30 made `examples/3d/pbr` stop drawing meshes. The native
run log showed:

```text
Too many bindings of type Samplers in Stage ShaderStages(FRAGMENT), limit is 16, count was 23.
```

That failure is expected for the wrong feature shape. Bevy treats these as
optional features because the combined non-bindless sampler surface can exceed
device limits. Mgstudio must keep the source-shaped feature gate and explicit
unsupported-feature path until the bindless StandardMaterial route is wired for
runtime feature selection.

## Design
Port the Bevy feature owner shape instead of adding global shader defs:

- Represent the four optional PBR material texture features as explicit
  engine-level feature state that maps back to Bevy's Cargo feature names.
- Keep the default runtime feature surface at the currently working
  anisotropy-only shape unless a requested example/package explicitly requires
  another Bevy feature.
- When a feature would exceed non-bindless sampler limits, route through the
  Bevy-shaped bindless StandardMaterial path if the render device supports it;
  otherwise report a precise unsupported-feature blocker instead of silently
  dropping textures or leaving a broken pipeline.
- Make `standard_material_bind_group_layout_descriptor`,
  `StandardMaterial::bind_group`, `MeshPipeline::specialize`, and the renderer
  bind-group creation path consume the same feature state.
- Treat feature-specific examples as consumers that must declare the Bevy
  feature-equivalent surface they need. Do not infer example correctness from
  visual plausibility when the example writes a gated texture field.

Do not collapse samplers, merge unrelated texture bindings, or globally enable
shader defs as a workaround.

## Acceptance Criteria
- [x] The enabled StandardMaterial optional texture feature set is represented
      explicitly and maps to Bevy's feature names.
- [x] The main PBR mesh pipeline only defines
      `PBR_TRANSMISSION_TEXTURES_SUPPORTED`,
      `PBR_MULTI_LAYER_MATERIAL_TEXTURES_SUPPORTED`,
      `PBR_ANISOTROPY_TEXTURE_SUPPORTED`, and
      `PBR_SPECULAR_TEXTURES_SUPPORTED` when the corresponding feature is
      enabled.
- [x] The StandardMaterial descriptor layout is derived from the same feature
      state as the main PBR mesh pipeline shader defs.
- [x] `StandardMaterial::bind_group` and renderer-side prepared bind-group
      creation consume that same feature state instead of assuming one global
      optional texture surface.
- [x] Enabling a feature set that exceeds non-bindless sampler limits either
      uses the Bevy-shaped bindless path or reports an explicit unsupported
      feature condition before pipeline creation.
- [x] `examples/3d/pbr` still renders meshes after the default feature state is
      applied.
- [x] Feature-specific examples such as anisotropy, clearcoat, specular tint,
      and transmission are audited against their Bevy required features, with
      follow-up runtime feature-selection work recorded instead of hidden in
      this issue.

## Relationships
- Depends on: none
- Parent: ISS-155
- Related: ISS-145, ISS-162
- Discovered from: ISS-155

## Notes
- 2026-07-01: Created after a failed attempt to globally enable all optional
  StandardMaterial texture shader defs. The failure proved that this must be a
  feature-gated owner migration, not a blanket shader-def/layout expansion.
- 2026-07-01: Added `StandardMaterialTextureFeatureSet` with fields matching
  Bevy's four optional PBR material texture feature names. The PBR material
  pipeline specializer now derives the corresponding shader defs from that
  feature set, and `standard_material_bind_group_layout_descriptor` derives the
  descriptor layout from the same feature set. The default feature set remains
  anisotropy-only, preserving the current native sampler-limit-safe runtime
  behavior. Remaining work: pass the same feature set into
  `StandardMaterial::bind_group` and renderer bind-group creation before
  enabling additional optional features.
- 2026-07-01: Extended `MaterialBindGroupContext` so
  `PreparedMaterial::from_material`, `StandardMaterial::bind_group`, and
  `StandardMaterial::bind_group_layout_descriptor` consume the same
  `StandardMaterialTextureFeatureSet`. The default context remains
  anisotropy-only, and white-box coverage verifies that default bind-group
  payload and uniform texture flags omit disabled transmission, multi-layer,
  and specular texture ranges while an explicit all-features context restores
  the full Bevy binding-number surface through binding 30.
- 2026-07-01: Verified the default anisotropy-only feature state with
  `examples/3d/pbr` native screenshot capture at
  `/tmp/mgstudio_iss163_context_payload_pbr.png`; the capture renders the PBR
  material sphere grid and the run log contains no sampler-limit or pipeline
  creation errors.
- 2026-07-01: Added a specialization-time sampler-limit guard that counts
  fragment-visible samplers across the full `RenderPipelineDescriptor` layout.
  If an enabled StandardMaterial feature set would exceed the configured
  non-bindless `max_samplers_per_shader_stage`, specialization now raises
  `SpecializedMeshPipelineError::UnsupportedFeature` before the descriptor can
  reach GPU pipeline creation. White-box coverage verifies the current default
  layout uses 12 fragment-visible samplers under a 16-sampler limit, while the
  all-feature StandardMaterial layout is rejected at 20 samplers.
- 2026-07-01: Audited the feature-specific Bevy examples against Mgstudio
  counterparts. `examples/3d/specular_tint` writes
  `specular_tint_texture`, so it requires the `pbr_specular_textures`
  equivalent. `examples/3d/clearcoat` writes `clearcoat_normal_texture`, so it
  requires the `pbr_multi_layer_material_textures` equivalent. The
  `examples/3d/anisotropy` glTF material path requires the
  `pbr_anisotropy_texture` equivalent for faithful import, while its local
  sphere fallback writes only scalar anisotropy fields. `examples/3d/transmission`
  writes scalar transmission fields in this example and does not use
  `diffuse_transmission_texture`, but screen-space transmission itself remains
  a separate runtime feature path. This issue now tracks the completed shared
  StandardMaterial feature-state plumbing; per-example feature selection and
  bindless runtime routing should be handled as follow-up work.

## Close Notes
Closed after the shared StandardMaterial optional texture feature gate was
ported through the PBR mesh pipeline shader defs, StandardMaterial descriptor
layout, StandardMaterial bind-group payload, renderer prepared material path,
and specialization-time sampler-limit guard.

Verification evidence:

- `moon -C mgstudio-engine test --release pbr/material_pipeline_specializer_wbtest.mbt --deny-warn`
- `moon -C mgstudio-engine test --release pbr/material_bind_groups_test.mbt --deny-warn`
- `moon -C mgstudio-engine test --release pbr/pbr_material_test.mbt --deny-warn`
- `moon -C mgstudio-engine test --release pbr/material_plugin_wbtest.mbt --deny-warn`
- `moon -C mgstudio-engine check --target native --deny-warn`
- `moon -C mgstudio-engine fmt`
- `moon -C mgstudio-engine info`
- Native visual capture of `examples/3d/pbr` rendered the PBR material sphere
  grid without sampler-limit or pipeline creation errors.

Follow-up: ISS-164 tracks per-example feature selection and bindless
StandardMaterial runtime routing for examples that use gated texture fields.
