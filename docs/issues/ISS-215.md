# ISS-215: Capture scrolling_fog final shader-input visual evidence

## Metadata
- Type: task
- Status: closed
- Priority: 1
- Labels: examples, pbr, volumetric-fog, screenshots, visual-parity
- Assignee: unassigned
- Created: 2026-07-04
- Updated: 2026-07-04
- External ref: none

## Description
After the smaller `scrolling_fog` source-owner and shader-input audits close,
run the final standard capture and record whether the scene is ready for
ISS-200 to refresh the checked-in reference.

## Design
Use the standard native screenshot command. Compare the output against
`mgstudio-engine/examples/3d/scrolling_fog/reference.png` and expected Bevy
behavior. This issue should not fix renderer code; if the capture still fails,
open a new narrowly scoped issue for the newly identified owner.

## Acceptance Criteria
- [x] The standard native capture completes with an empty run log.
- [x] The capture shows density-texture fog motion and visible geometry-depth
      interaction.
- [x] The capture shows directional shadow/light-shaft structure after
      ISS-211 through ISS-214.
- [x] Close notes include the exact command, output path, and comparison
      artifact path used for acceptance.

## Relationships
- Depends on: ISS-211, ISS-212, ISS-213, ISS-214
- Parent: ISS-205
- Related: ISS-091, ISS-104, ISS-190, ISS-199, ISS-200
- Discovered from: ISS-205

## Notes
- 2026-07-04: Split from ISS-205 as the final evidence gate, separated from the
  actual source-owner debugging work.

## Close Notes
- 2026-07-05: Closed after two standard native captures completed with empty
  run logs:
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=1 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/scrolling_fog /tmp/mgstudio_iss215_scrolling_fog_frame1.png`
  and
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90 MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240 MGSTUDIO_PARITY_CAPTURE_DISABLE_RETRY=1 mgstudio-engine/scripts/p0_visual_capture_native.sh examples/3d/scrolling_fog /tmp/mgstudio_iss260_scrolling_fog_shadow_binding.png`.
- 2026-07-05: `frame1` and `frame90` are both 2560x1440 RGBA. The
  `frame1` to `frame90` difference mean is approximately
  `[7.651, 7.646, 7.629, 0.000]`, with nonzero RGB ratio `0.903523`, which
  records visible density/fog evolution between frames.
- 2026-07-05: The frame-90 capture shows the pillar, geometry-depth
  interaction, and directional shadow/light-shaft structure. Compared against
  `mgstudio-engine/examples/3d/scrolling_fog/reference.png`, the frame-90
  difference mean is approximately `[0.322, 0.321, 0.320, 0.000]`; the
  reference comparison artifact is the captured PNG
  `/tmp/mgstudio_iss260_scrolling_fog_shadow_binding.png`.
