# ISS-023: Split sprite/render2d.mbt into Bevy-shaped source owners

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, render2d, sprite, architecture
- Assignee: unassigned
- Created: 2026-06-03
- Updated: 2026-06-04
- External ref: none

## Description
`mgstudio-engine/sprite/render2d.mbt` is a 9553-line file that combines logic
from many Bevy owners instead of mapping to one Bevy source file. Evidence in
the file includes `RenderLayers`, `Viewport`, `Camera`, `Camera2d`, shape mesh
builders, `SpriteBundle`, `Camera2dBundle`, `Mesh2dBundle`, `TilemapChunk`,
`TilemapChunkMaterial`, sprite extract/queue/runtime state, UI overlay payloads,
bloom/CAS/tonemapping helpers, and the large `render_execute_system`.

Bevy distributes these owners across `bevy_camera`, `bevy_core_pipeline`,
`bevy_sprite_render/src/render`, `bevy_sprite_render/src/mesh2d`,
`bevy_sprite_render/src/texture_slice`, `bevy_sprite_render/src/tilemap_chunk`,
`bevy_ui_render`, and postprocess/core pipeline modules.

## Design
Use this as the umbrella task for removing the file-level monolith. Move code
out by Bevy owner, keep behavior intact during each move, and delete the old
sections instead of leaving forwarding compatibility layers in `render2d.mbt`.

## Acceptance Criteria
- [ ] `sprite/render2d.mbt` no longer contains camera, core 2D, UI render, tilemap, shape conversion, bundle construction, material, and runtime execute ownership in one file.
- [ ] Each moved section has a MoonBit file/package whose path clearly maps to its Bevy owner.
- [ ] Callers import the owner package directly rather than using `sprite.render2d_*` as the cross-module hub.
- [ ] `moon -C mgstudio-engine check --target native`, `moon -C mgstudio-engine fmt`, and `moon -C mgstudio-engine info` pass after the split.
- [ ] At least one 2D sprite example and one UI-render example pass a screenshot gate after the split.

## Relationships
- Depends on: none
- Parent: ISS-022
- Related: ISS-015, ISS-021, ISS-024, ISS-025, ISS-026, ISS-027, ISS-028, ISS-029
- Discovered from: ISS-022

## Notes
- 2026-06-03: `wc -l` reported `9553 mgstudio-engine/sprite/render2d.mbt`; the corresponding Bevy sprite-render files are split across many files, with the largest relevant owner files around `bevy_sprite_render/src/mesh2d/material.rs`, `mesh.rs`, `wireframe2d.rs`, and `render/mod.rs`.
- 2026-06-04: Started the split with Bevy `bevy_camera/src/visibility/render_layers.rs`. Added `Milky2018/mgstudio/camera/visibility/render_layers` as the real owner for `RenderLayers`, its component key, and JSON boundary codec. Removed the concrete `RenderLayers` definition and component registration from `sprite/render2d.mbt` / `sprite/ecs_keys.mbt`, and moved external callers from `@sprite.RenderLayers` / `@sprite.ecs_key_render_layers` to the owner package.
- 2026-06-04: `sprite` still contains a private package-local alias for `RenderLayers` while the remaining sprite owner sections are split. This is not a public facade, but `sprite/render2d.mbt` remains partial for ISS-023 until the other owner chunks are removed.
- 2026-06-04: Validation for the render-layers owner slice: `moon -C mgstudio-engine check --target native`; `moon -C mgstudio-engine test camera/visibility/render_layers --release` (0 tests); `moon -C mgstudio-engine test camera/visibility --release`; `moon -C mgstudio-engine test sprite --release`; `moon -C mgstudio-engine test pbr --release`; `moon -C mgstudio-engine test text --release`; `moon -C mgstudio-engine test ui --release`; `moon -C mgstudio-engine test ui_render --release`.
- 2026-06-04: Attempted visual capture with `examples/camera/2d_on_ui` and `examples/2d/mesh2d_custom_material` using `scripts/p0_visual_capture_native.sh`. Both examples built and launched, but the capture hook did not produce `MGSTUDIO_PARITY_CAPTURE_RGBA8_BLOB`; run logs were empty. Keep the visual gate unchecked until capture is debugged or a different representative gate is available.
- 2026-06-04: Continued the split with Bevy `bevy_camera/src/camera.rs::Viewport`. Added `Milky2018/mgstudio/camera/viewport` as the real owner for `Viewport`, its Bevy-shaped constructors, clamp logic, accessors, and JSON boundary codec. Removed the concrete `Viewport` definition from `sprite/render2d.mbt`, replaced external `@sprite.Viewport` references with `@camera_viewport.Viewport` or `@camera.Viewport`, and kept only a private package-local alias inside `sprite` for the remaining unsplit camera runtime code.
- 2026-06-04: Validation for the viewport owner slice: `moon -C mgstudio-engine check --target native`; `moon -C mgstudio-engine test camera/viewport --release` (0 tests); `moon -C mgstudio-engine test camera --release`; `moon -C mgstudio-engine test sprite --release`; `moon -C mgstudio-engine test pbr/render --release`; `moon -C mgstudio-engine test ui --release`.

## Close Notes
