# ISS-085: Port mixed_lighting real lightmap, picking, and widget source parity

## Metadata
- Type: bug
- Status: blocked
- Priority: 1
- Labels: examples, pbr, lightmaps, picking, ui, gltf, source-parity
- Assignee: unassigned
- Created: 2026-06-13
- Updated: 2026-06-23
- External ref: none

## Description
`examples/3d/mixed_lighting/reference.png` can now serve as a nontransparent
visual baseline, but the example is not source-level faithful to Bevy. The
current MoonBit source explicitly states that Bevy's per-mesh `Lightmap`
component pipeline and UI widget radio buttons are not exposed in the current
mgstudio runtime surface, and that the port uses keyboard-driven mode selection
and lighting proxies instead.

Bevy's `examples/3d/mixed_lighting.rs` loads
`models/MixedLightingExample/MixedLightingExample.gltf`, observes
`WorldInstanceReady`, writes `LightingModeChanged`, assigns and removes
`Lightmap` components with per-mesh UV rectangles, mutates material
`lightmap_exposure`, uses `MeshPickingPlugin` and `PointerInteraction` to move
the sphere, and uses the shared widgets/radio-button UI flow. Mgstudio needs
the same source shape or precise blockers for unavailable engine/API pieces.

## Design
Audit and port Bevy `examples/3d/mixed_lighting.rs` as the migration unit:

- preserve `WorldInstanceReady` observer behavior and initial
  `LightingModeChanged` dispatch;
- implement the real per-mesh `Lightmap` component path, including UV rects and
  material exposure;
- preserve the four lighting modes and the exact static/dynamic lightmap
  transitions;
- use Bevy-shaped picking data to move the sphere instead of a proxy movement
  path;
- use the shared widget/radio-button UI flow instead of keyboard-only mode
  selection, unless a precise UI blocker is recorded.

Do not count proxy lighting, keyboard-only mode selection, or missing lightmap
components as source parity.

## Acceptance Criteria
- [x] Every source-shape anchor in Bevy `examples/3d/mixed_lighting.rs` is
      classified as complete, partial, facade, blocked, or intentionally
      excluded.
- [x] Lightmaps are represented by real `Lightmap` components with Bevy-shaped
      UV rects and material `lightmap_exposure` behavior.
- [x] The mode-change flow uses Bevy-shaped messages/observers and preserves
      baked, mixed-direct, mixed-indirect, and real-time semantics.
- [x] Sphere movement uses Bevy-shaped picking input data or records the exact
      missing picking blocker.
- [x] The approximation note in `examples/3d/mixed_lighting/main.mbt` is
      removed or replaced by precise blocker references.
- [ ] `examples/3d/mixed_lighting/reference.png` remains visually valid after
      the source parity fix.

## Relationships
- Depends on: ISS-148
- Parent: ISS-012
- Related: ISS-060, ISS-019, ISS-021, ISS-082, ISS-148
- Discovered from: ISS-060

## Notes
- 2026-06-13: Discovered while refreshing
  `examples/3d/mixed_lighting/reference.png`. The refreshed image is visually
  usable, but the source still documents local approximations for real
  lightmaps, UI widgets, and interaction flow.
- 2026-06-14: Started replacing the proxy implementation with Bevy-shaped
  `Lightmap`, scene-ready message, widgets, and picking flow.
- 2026-06-14: Ported the core source flow and refreshed the checked-in
  reference capture. Source-shape anchor classification:
  - Complete: `LightingMode`, `AppStatus`, `LightingModeChanged`, `HelpText`,
    `LIGHTMAP_EXPOSURE`, `SPHERE_OFFSET`, `INITIAL_SPHERE_POSITION`,
    `uv_rect_opengl`, static lightmap UV rectangles, `SPHERE_UV_RECT`,
    `setup`, `spawn_camera`, `spawn_scene`, `spawn_buttons`,
    `spawn_help_text`, `update_lightmaps`, `update_directional_light`,
    `update_radio_buttons`, `handle_lighting_mode_change`,
    `reset_sphere_position`, `move_sphere`, `adjust_help_text`, and
    `create_help_text`.
  - Partial: Bevy's `WorldInstanceReady` maps to mgstudio
    `SceneInstanceReady`, which is the current scene-root ready event.
    Bevy's generic `Name` lookups map to `GltfMeshName` because the imported
    glTF mesh-name component is the available source-owned name marker in this
    runtime. The UI root/default UI camera are spawned explicitly to match the
    current mgstudio UI runtime surface.
  - Blocked: none discovered for this example after the current lightmap,
    picking, and widget APIs.
  - Facade or intentionally excluded: none.
- 2026-06-14: Verification: `moon -C mgstudio-engine check --release
  --package-path examples/3d/mixed_lighting`; `moon -C mgstudio-engine test
  --release pbr/lightmap`; `moon -C mgstudio-engine test --release picking`;
  `moon -C mgstudio-engine test --release examples/helpers/widgets_lib`
  (no tests in helper package); screenshot command
  `MGSTUDIO_PARITY_CAPTURE_DELAY_FRAMES=90
  MGSTUDIO_PARITY_RUN_TIMEOUT_SECONDS=240
  mgstudio-engine/scripts/p0_visual_capture_native.sh
  examples/3d/mixed_lighting /tmp/mgstudio_iss085_mixed_lighting.png`.
- 2026-06-23: The previous completion note was too optimistic. Current
  `MixedIndirect` capture still renders only the dynamic sphere and UI; the
  static lightmapped meshes disappear. `RealTime` mode renders the same glTF
  scene correctly, and disabling GPU preprocess does not change the failure.
  A temporary non-binding-array lightmap probe restored the static meshes,
  isolating the remaining problem to the texture/sampler binding-array lightmap
  path rather than glTF loading, UV1 preservation, material exposure, or ECS
  lightmap component insertion.
- 2026-06-23: Mgstudio now carries Bevy-shaped render lightmap slab/slot data
  through `RenderLightmaps`, `MeshGpuDrawPayload3d`,
  `HostMesh3dMainPassDrawEntry`, binned phase keys, mesh uniform packing, and
  renderer draw bind group creation. The visual failure remains because the
  Bevy WGSL path uses both `binding_array<texture_2d<f32>, 4>` and
  `binding_array<sampler, 4>` with dynamic slot indexing. This is tracked as
  the external wgpu_mbt blocker ISS-148 / upstream
  https://github.com/moonbit-community/wgpu-mbt/issues/31.

## Close Notes
