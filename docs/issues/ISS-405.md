# ISS-405: Unblock fully qualified WGSL module function calls

## Metadata
- Type: bug
- Status: closed
- Priority: 0
- Labels: external-blocker, wgsl, shader-compose, pbr, deferred, fog
- Assignee: unassigned
- Created: 2026-07-10
- Updated: 2026-07-21
- External ref: https://github.com/moonbit-community/moon_wgsl/issues/17

## Description
Pinned Bevy shaders call functions by fully qualified composable-module path,
including `bevy_pbr::fog::linear_fog` in
`bevy_pbr/src/render/pbr_functions.wgsl` and
`irradiance_volume::irradiance_volume_light` in the same source owner. The
`Milky2018/wgsl@0.16.0` and `Milky2018/moon_wgsl_naga_oil@0.16.0` resolved a
minimal qualified module function call, but still misclassified the
corresponding call in the complete pinned-Bevy deferred-lighting import graph
as a WGSL type during IR lowering. It reported `unsupported WGSL type` instead
of resolving and flattening `bevy_pbr::fog::linear_fog`. The upstream
transitive-call fix is available in `0.17.0`; mgstudio has upgraded and
verified the reduced reproduction plus both affected runtime examples.

This blocks both forward PBR and the real deferred-lighting pipeline whenever
the valid Bevy `DISTANCE_FOG` shader specialization is enabled. In
`examples/3d/deferred_rendering`, after the local false irradiance-volume key
was fixed, the valid Bevy distance-fog call fails every frame. In
`examples/3d/anti_aliasing`, the same specialization leaves only UI over the
clear color; removing only `DistanceFog` restores the complete scene.

## Design
Fix module-qualified function resolution in the external WGSL compose/IR
dependency. Do not edit copied Bevy WGSL, replace qualified calls with local
aliases, disable `DISTANCE_FOG`, or add an mgstudio-side preprocessing rewrite.
Those changes would hide the dependency gap and diverge from the pinned Bevy
source.

### EXT-405: Milky2018/wgsl lacks fully qualified module function lowering

- Dependency: fixed by `Milky2018/wgsl@0.17.0` with
  `Milky2018/moon_wgsl_naga_oil@0.17.0`.
- Missing capability: resolve the pinned-Bevy qualified function callee from
  the complete conditional composable-module graph and lower it as a function
  call. A two-module minimal call now succeeds, so that is no longer a valid
  reproduction of the remaining blocker.
- Bevy requirement: `bevy_pbr/src/render/pbr_functions.wgsl` calls
  `bevy_pbr::fog::linear_fog` and other module-qualified functions.
- Blocked Bevy source owner: `bevy_pbr/src/render/mesh.rs` forward mesh pipeline
  specialization and `bevy_pbr/src/deferred/mod.rs` deferred-lighting pipeline
  specialization and execution.
- Mgstudio implementation fragments affected:
  `mgstudio-engine/pbr/render/pbr_functions.wgsl`,
  `mgstudio-engine/pbr/material_pipeline_specializer.mbt`,
  `mgstudio-engine/pbr/deferred/deferred_lighting.wgsl`,
  `mgstudio-engine/render/renderer/mesh3d_pipeline_build.mbt`, and
  `mgstudio-engine/render/renderer/mesh3d_deferred_lighting.mbt`.
- Minimum local capability check:
  `moon -C mgstudio-engine test --target native --release --deny-warn
  shader/compile/namespace_function_external_blocker_test.mbt`; the test now
  requires successful composition and verifies that the qualified call is
  absent from emitted WGSL. The remaining runtime reproduction is the
  fixed-frame `examples/3d/deferred_rendering` capture with `DISTANCE_FOG`
  enabled.
- Verification expectation: update the reproduction to expect successful
  composition with no qualified callee left in the emitted WGSL; then capture
  both `examples/3d/anti_aliasing` with its complete fogged scene and
  `examples/3d/deferred_rendering` without a deferred-lighting shader fatal.
- Local substitute implementation: not allowed.
- Upstream/community link:
  https://github.com/moonbit-community/moon_wgsl/issues/17.

## Acceptance Criteria
- [x] The WGSL compose/IR pipeline resolves the minimal fully qualified module
      function call and emits valid flattened WGSL.
- [x] The blocker reproduction is changed from an expected failure to an
      expected success and passes against the upgraded dependency.
- [x] Both `bevy_pbr::fog::linear_fog` and
      `irradiance_volume::irradiance_volume_light` compose without invalid-IR
      errors when their Bevy shader definitions are enabled.
- [x] A fixed-frame `examples/3d/deferred_rendering` capture has no
      deferred-lighting WGSL lowering fatal.
- [x] A fixed-frame `examples/3d/anti_aliasing` capture retains its
      `DistanceFog` component and contains the FlightHelmet, cubes, ground,
      lighting, and shadows.

## Relationships
- Depends on: none
- Parent: ISS-019
- Related: ISS-122, ISS-168, ISS-403
- Discovered from: ISS-403

## Notes
- 2026-07-10: Runtime evidence:
  `/tmp/mgstudio_iss403_loaded_irradiance_key_deferred_rendering.run.log`
  reports `unsupported WGSL type: bevy_pbr::fog::linear_fog` on every deferred
  lighting attempt. The matching PNG remains the UI-only blank-scene artifact
  with SHA-256
  `1704951314464b609d0f3833e77dc788de74bbb114d99fec662aa845c2703581`.
- 2026-07-10: The source example faithfully includes the same linear
  `DistanceFog` as pinned Bevy `examples/3d/deferred_rendering.rs`; disabling
  fog is therefore not a valid local fix.
- 2026-07-10: ISS-122 confirmed the same blocker on the forward PBR path.
  Removing only `Hdr`, changing only MSAA, and removing only the environment
  map did not restore `examples/3d/anti_aliasing`; removing only
  `DistanceFog` restored the complete scene in
  `/tmp/mgstudio_iss122_probe_no_fog.png`. The example was restored unchanged.
- 2026-07-20: Upgraded `moon_wgsl`, `wgsl`, `moon_wgsl_naga`, and
  `moon_wgsl_naga_oil` from `0.15.3` to `0.16.0`. The two-module qualified-call
  check now composes and flattens successfully. The full
  `examples/3d/deferred_rendering` frame-90 capture still logs
  `unsupported WGSL type: bevy_pbr::fog::linear_fog` on every deferred-lighting
  attempt, and `/tmp/mgstudio_wgsl_0160_facade_deferred_rendering.png` retains
  the prior blank-scene SHA-256
  `1704951314464b609d0f3833e77dc788de74bbb114d99fec662aa845c2703581`.
  Therefore ISS-405 remains blocked on a narrower real-Bevy import-graph case.
- 2026-07-20: Filed upstream issue
  `moonbit-community/moon_wgsl#17` with a standalone four-source reproduction.
  It proves that composing the qualified-call owner as the root succeeds, but
  importing that owner through one additional root module fails with
  `unsupported WGSL type: test::fog::linear_fog`. The same reduced test also
  reaches `ComposerError.InvalidIr` in the local `0.16.1` workspace.
- 2026-07-21: Upgraded the four synchronized WGSL dependencies to `0.17.0`.
  The exact four-module reproduction now passes with no qualified callee left
  in the emitted WGSL. The frame-90 `deferred_rendering` capture produced the
  complete scene at `/tmp/mgstudio_wgsl_017_deferred_rendering.png` with
  SHA-256 `1f012ac53aa23f97a530f50613e98b30ce9aa64c754505b4a38cf16a1d808123`
  and an empty run log. The frame-90 `anti_aliasing` capture retained
  `DistanceFog` and produced the FlightHelmet, cubes, ground, lighting, and
  shadows at `/tmp/mgstudio_wgsl_017_anti_aliasing.png` with SHA-256
  `4ddce8b33e5c056bfae4f84713161db6124e04cae40a812762b1f34a3533ed74`
  and an empty run log.
- 2026-07-21: The frame-90 `irradiance_volumes` capture exercised the real
  `IRRADIANCE_VOLUME` shader path without `irradiance_volume_light`, invalid-IR,
  or other WGSL errors in the run log. The capture is stored at
  `/tmp/mgstudio_wgsl_017_irradiance_volumes.png` with SHA-256
  `1b04826c1a18649d9d338a70530b0f82fd358baab7170dd28bcd0f8126629450`.
  Its scene is not visually Bevy-equivalent; the already-related ISS-019 owns
  the remaining irradiance-volume render wiring and visual result. This issue
  closes only the external qualified-call composition blocker.

## Close Notes
Upgrading the synchronized WGSL dependency set to `0.17.0` incorporates the
upstream transitive qualified-call fix. The exact four-module regression passes,
and the deferred, forward fog, and irradiance-volume runtime shader paths
produce no WGSL composition errors. The dependency's filesystem scanner moved
to an unpublished tools package in `0.17.0`; mgstudio preserves its public
shader-source scan API with the upstream adapter implementation at the existing
asset/filesystem ownership boundary.
