# ISS-013: Implement the real Bevy-shaped RenderGraph schedule

## Metadata
- Type: task
- Status: in_progress
- Priority: 0
- Labels: parity, render, render-graph, renderer
- Assignee: unassigned
- Created: 2026-06-02
- Updated: 2026-06-04
- External ref: none

## Description
Bevy's `RenderGraph` is a dedicated root render graph schedule with
`Begin -> Render -> Submit -> Finish` systems, driven once per frame by
`render_system`. Mgstudio currently exposes a `RenderGraph` shell in
`mgstudio-engine/render/schedule.mbt`, but it is effectively an alias over the
canonical render schedule rather than a real render graph owner.

## Design
Port the ownership shape from `bevy/crates/bevy_render/src/renderer/mod.rs`:
`RenderGraph`, `RenderGraphSystems`, and the schedule that is run from the
render system. Keep MoonBit package boundaries as needed, but the runtime owner
semantics should match Bevy instead of remaining renderer-local.

## Acceptance Criteria
- [x] `RenderGraph` has explicit `Begin`, `Render`, `Submit`, and `Finish` system sets.
- [x] The render backend drives the `RenderGraph` schedule once per frame instead of treating it as a passive alias.
- [x] Existing render systems are moved or bridged to the correct Bevy-shaped graph phase with no compatibility fallback path.
- [ ] Screenshot, readback, command-buffer submission, and present ownership are no longer hidden inside broad renderer-local execution.
- [x] Render schedule tests prove the ordering matches Bevy's `RenderGraph::base_schedule`.

## Relationships
- Depends on: none
- Parent: ISS-012
- Related: ISS-014, ISS-015, ISS-018, ISS-019
- Discovered from: none

## Notes
- 2026-06-02: Current source evidence is `mgstudio-engine/render/schedule.mbt` defining an empty `RenderGraph` shell.
- 2026-06-03: Started implementation.
- 2026-06-03: Added the distinct render graph schedule and moved 2D/3D/PBR render pass execution onto graph `Render`; command-buffer flushing now runs in graph `Submit`. Remaining open work is renderer-local screenshot/readback/present ownership in `GpuBackend::end_frame_binding`, which overlaps `ISS-015`.
- 2026-06-03: `examples/3d/motion_blur --release` screenshot gate previously exited with renderer validation errors: mesh3d frame view bind group rejected `depth_prepass_texture` binding 20, and the local motion blur layout declared binding 2 as a depth texture while Bevy's shader/pipeline use unfilterable float sampling. These are runtime PBR command/layout gaps tracked with `ISS-019`; `ISS-013` remains open until present/readback ownership is moved out of `GpuBackend::end_frame_binding`.
- 2026-06-03: After fixing those two runtime layout gaps, the same motion_blur gate no longer produced validation output but also did not create `tmp/data/screenshot-30.png` or exit after more than 10 minutes in `motion_blur.exe`; the run was manually terminated and must not be counted as a passing visual gate. Gate infrastructure tracking belongs to `ISS-021`.
- 2026-06-04: Moved active surface command-buffer preparation to RenderGraph `Submit`, existing `PendingCommandBuffers` flushing remains the graph submission owner, and surface present/cleanup now runs in RenderGraph `Finish`. Screenshot/readback ownership is still open and remains tracked by `ISS-033`.

## Close Notes
