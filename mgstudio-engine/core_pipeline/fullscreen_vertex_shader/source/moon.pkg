import {
  "Milky2018/mgstudio/shader/source" @shader_source,
}

supported_targets = "native"

options(
  "pre-build": [
    {
      "command": ":embed --input $input --output $output --name fullscreen_wgsl_source",
      "input": "../fullscreen.wgsl",
      "output": "fullscreen_wgsl.mbt",
    },
  ],
)
