import {
  "Milky2018/mgstudio/ecs" @ecs,
  "moonbitlang/core/json",
  "Milky2018/mgstudio/shader/source" @shader_source,
}

supported_targets = "native"

options(
  "pre-build": [
    {
      "command": ":embed --input $input --output $output --name motion_blur_wgsl_source",
      "input": "motion_blur.wgsl",
      "output": "motion_blur_wgsl.mbt",
    },
  ],
)
