// Generated using `moon info`, DON'T EDIT IT
package "Milky2018/mgstudio/pbr/volumetric_fog"

import {
  "Milky2018/mgstudio/app",
  "Milky2018/mgstudio/asset",
  "Milky2018/mgstudio/ecs",
  "Milky2018/mgstudio/light",
  "Milky2018/mgstudio/math",
  "Milky2018/mgstudio/mesh",
}

// Values
pub fn prepare_volumetric_fog_view_uniforms(VolumetricFogUniformBuffer, @light.VolumetricFog, @math.Affine3, VolumetricFogMat4, Array[(@light.FogVolume, @math.Affine3)]) -> PreparedVolumetricFogView

pub fn register_embedded_wgsl_sources() -> Unit

pub fn volumetric_fog_create_fog_assets(@ecs.World) -> FogAssets

pub fn volumetric_fog_plugin_install(@app.App[@ecs.World]) -> @app.App[@ecs.World]

pub fn volumetric_fog_plugin_install_with(@app.App[@ecs.World], VolumetricFogPlugin) -> @app.App[@ecs.World]

pub fn volumetric_fog_plugin_state_with_extracted_counts(VolumetricFogPluginState, Int, Int, Int) -> VolumetricFogPluginState

// Errors

// Types and methods
pub struct FogAssets {
  // private fields
}
pub fn FogAssets::cube_mesh(Self) -> @asset.Handle[@mesh.Mesh]
pub fn FogAssets::plane_mesh(Self) -> @asset.Handle[@mesh.Mesh]

pub struct PreparedVolumetricFogView {
  view_fog : ViewVolumetricFog
  uniform_count : Int
}

pub struct ViewFogVolume {
  // private fields
}
pub fn ViewFogVolume::density_texture_id(Self) -> Int
pub fn ViewFogVolume::exterior(Self) -> Bool
pub fn ViewFogVolume::has_density_texture(Self) -> Bool
pub fn ViewFogVolume::uniform_buffer_offset(Self) -> UInt

pub struct ViewVolumetricFog {
  // private fields
}
pub fn ViewVolumetricFog::count(Self) -> Int
pub fn ViewVolumetricFog::volumes(Self) -> ArrayView[ViewFogVolume]
pub impl @ecs.Component for ViewVolumetricFog

pub(all) struct ViewVolumetricFogPipelines {
  textureless : Int
  textured : Int
}
pub impl @ecs.Component for ViewVolumetricFogPipelines

pub struct VolumetricFogMat4 {
  x_axis : @math.Vec4
  y_axis : @math.Vec4
  z_axis : @math.Vec4
  w_axis : @math.Vec4
}
pub fn VolumetricFogMat4::from_affine3(@math.Affine3) -> Self
pub fn VolumetricFogMat4::from_cols(@math.Vec4, @math.Vec4, @math.Vec4, @math.Vec4) -> Self
pub fn VolumetricFogMat4::identity() -> Self
pub fn VolumetricFogMat4::mul(Self, Self) -> Self
pub fn VolumetricFogMat4::mul_affine3(Self, @math.Affine3) -> Self
pub fn VolumetricFogMat4::to_cols_array(Self) -> Array[Float]

pub struct VolumetricFogPlugin {
}
pub fn VolumetricFogPlugin::VolumetricFogPlugin() -> Self
pub impl @app.Plugin for VolumetricFogPlugin

pub struct VolumetricFogPluginState {
  fog_assets : FogAssets?
  extracted_view_count : Int
  extracted_volume_count : Int
  extracted_light_count : Int
}
pub fn VolumetricFogPluginState::VolumetricFogPluginState() -> Self
pub fn VolumetricFogPluginState::fog_assets(Self) -> FogAssets?
pub impl @app.Resource for VolumetricFogPluginState

type VolumetricFogUniform

pub struct VolumetricFogUniformBuffer {
  // private fields
}
pub fn VolumetricFogUniformBuffer::VolumetricFogUniformBuffer() -> Self
pub fn VolumetricFogUniformBuffer::bytes(Self) -> Bytes
pub fn VolumetricFogUniformBuffer::clear(Self) -> Unit
pub fn VolumetricFogUniformBuffer::count(Self) -> Int
pub impl @app.Resource for VolumetricFogUniformBuffer

// Type aliases
pub using @light {type FogVolume}

pub using @light {type VolumetricFog}

// Traits

